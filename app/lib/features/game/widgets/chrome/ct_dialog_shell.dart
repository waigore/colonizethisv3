// Re-export shim. Source of truth moved to `app/lib/widgets/ct_dialog_shell.dart`
// so shared chrome lives in the widgets layer (features -> widgets), reversing
// the previously inverted dependency direction (issue #3594 item 5).
export '../../../../widgets/ct_dialog_shell.dart';
