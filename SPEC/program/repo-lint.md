# Repo lint (unified CLI)

**SPEC/program** — Single entrypoint for **repository-wide** convention checks that are not `dart analyze` / `custom_lint` (those stay IDE- and analyzer-driven).

## Policy: no violation allowlists (repo lint)

**Repo lint** means manifest rules (`repo.*` in `tool/ct_repo_lint_manifest.yaml`) executed by `dart run tool/ct_repo_lint.dart`.

**Forbidden:** Keyed waiver data (grandfather YAML, per-path or per-symbol caps) whose purpose is to let in-scope code violate a rule’s threshold or predicate while CI still passes.

**Allowed:** Scope-only wiring in `tool/ct_repo_lint_scan_contract.dart`, checker path predicates, and env such as `CT_REPO_LINT_INCLUDE_APP` that exclude whole paths—not waivers inside an analyzed file.

**Do not add new violation allowlists** for any `repo.*` rule.

**Implementation status (GitHub #1912, checker slice):** `repo.function_size`, `repo.control_flow_nesting_depth`, and `repo.part_unit_size` enforce universal thresholds only; they do **not** read keyed waiver YAML or other per-symbol / per-file exemption tables. Per-rule contracts: `SPEC/program/function-size.md`, `SPEC/program/control-flow-nesting-depth.md`, `SPEC/program/part-unit-size.md`.

**Implementation status (GitHub #1912, checker slice):** `repo.disallowed_ast_patterns` does not use per-file suppression allowlists for sea-zone rules; in-file suppressions follow the same `ignore` / `ignore_for_file` contract as other disallowed-AST rules. The debug-console logic boundary rule uses YAML `match.kind` **`scoped_package_import_contract`** (closed URI set per scope), not the legacy identifier `package_import_allowlist`.

**Implementation status (GitHub #1912, dart file size):** `repo.dart_file_non_comment_line_size` enforces a universal **1000** non-comment-line cap for scanned hand-written Dart; it skips only **generated whole paths** (suffixes `.g.dart`, `.freezed.dart`, `.mocks.dart`, `.gen.dart`, and generated Flutter l10n path prefixes under `app/lib/l10n/gen/`). Per-rule contract: `SPEC/program/dart-file-non-comment-line-size.md`.

**Implementation status (GitHub #1912 / #4352 / #4450 / #4582 / #4606, game widgets and core services file size):** `repo.game_widgets_file_size` enforces a universal **250** physical-line cap for every `.dart` file under `app/lib/features/game/widgets/**` with no keyed waivers (wave-21 #4606, down from wave-20 **260**). Per-rule contract: `SPEC/program/game-widgets-file-size.md`. Sibling `repo.app_catalog_widgets_file_size` enforces the same **250** cap under `app/lib/widgets/**` (Refs #4352 AC4, #4582, #4606; `SPEC/program/app-catalog-widgets-file-size.md`). Wave-16 `repo.app_core_services_file_size` enforces the same **250** cap under `app/lib/core/services/**` with an empty grandfather (Refs #4450, #4582, #4606; `SPEC/program/app-core-services-file-size.md`). Wave-17 `repo.app_turn_resolution_file_size` enforces the same **250** cap under `app/lib/features/game/turn_resolution/**` with an empty grandfather (Refs #4512, #4582, #4606; `SPEC/program/app-turn-resolution-file-size.md`).

**Implementation status (GitHub #1912 / #4626, tech id literals):** `repo.tech_id_constants` treats the string values of every `const String kTechId*` in `packages/colonizethis_data/lib/src/tech_ids.dart` as the canonical tech-id set (replacing the prior `tech_catalog.dart` map-key scrape). `tech_catalog.dart` uses those same constants for map keys and `TechDefinition` ids so the catalog stays aligned with the checker. Top-level map keys in `packages/colonizethis_data/lib/src/tech_extraction_caps*.dart` that match catalog tech ids are in scope (they are otherwise skipped as `TopLevelVariableDeclaration`). Pin: `test/check_tech_id_constants_test.dart`.

**Implementation status (GitHub #1912, custom exceptions):** `repo.custom_exceptions` (`tool/check_custom_exceptions.dart` + `packages/colonizethis_exception_lint`) enforces the generic-throw ban on in-scope domain files only; it does **not** read keyed waiver YAML or per-symbol / per-file exemption tables. Whole-tree scope follows `collectRepoLintDomainDartFiles` / `shouldEnforceDomainExceptions` (generated suffixes and test-path skips are scope wiring, not violation waivers). Per-rule contract: `SPEC/program/exception-enforcement.md`.

**Implementation status (GitHub #1912, regression harness):** `repo.dart_file_non_comment_line_size`, `repo.game_widgets_file_size`, and `repo.repeated_magic_numbers` do not read keyed waiver YAML; repository tests place a decoy waiver-shaped file under a temporary workspace `tool/` tree and assert threshold violations still fail the run (`test/check_dart_file_non_comment_line_size_test.dart`, `test/check_game_widgets_file_size_test.dart`, `test/check_repeated_magic_numbers_test.dart`).

**Implementation status (GitHub #2288, logic test file size):** `repo.logic_test_file_size` enforces the **400 physical-line** cap across every `.dart` file under `packages/colonizethis_logic/test/**` on full-tree runs and narrows to changed files when `ct_repo_lint` is invoked with an incremental baseline (`CT_REPO_LINT_BASE_SHA` in CI). The earlier #2216 deferral that skipped the rule on `incrementalCsv == null` has been removed because the `test/**` debt is cleared. The repository test `test/check_logic_test_file_size_full_tree_test.dart` asserts the full-tree scan stays green on the current repo so future regrowth fails CI.

### Acceptance criteria (policy)

- Given a `repo.*` rule implementation, when a maintainer audits it for waiver data, then the maintainer finds no keyed tables loaded solely to raise effective limits for specific in-scope symbols or files.
- Given `SPEC/program/repo-lint.md`, when a contributor adds or changes a manifest rule, then the contributor does not introduce a new violation-allowlist mechanism.
- Given a raw catalog tech-id string used as a map key under `packages/colonizethis_data/lib/src/tech_extraction_caps*.dart`, when `dart run tool/ct_repo_lint.dart` runs `repo.tech_id_constants`, then the checker fails naming that literal.

## Source of truth

| Artifact | Role |
|----------|------|
| `tool/ct_repo_lint_manifest.yaml` | Lists **rules** with stable **`rule_id`**, **group**, human title, SPEC path, and how to invoke the checker |
| `tool/ct_repo_lint.dart` | Orchestrator: loads manifest, runs rules in order, stops on first failure |
| `tool/ct_repo_lint_lib.dart` | Parse/execute helpers (also covered by `test/ct_repo_lint_test.dart`) |
| `tool/ct_repo_lint_map_dispatch.dart` | In-process dispatch for map/`colonizethis_map` manifest rules (keeps `ct_repo_lint_lib.dart` under the 1000-NCL ceiling; Refs #4022) |
| `tool/ct_repo_lint_world_dispatch.dart` | In-process dispatch for `repo.world_*` manifest rules (keeps `ct_repo_lint_lib.dart` under the 1000-NCL ceiling; Refs #4515) |
| `tool/ct_repo_lint_scan_contract.dart` | Shared roots, skip helpers, canonical tile-key collectors, app UI path gate, PR path split — see `test/ct_repo_lint_scan_contract_test.dart` |
| `tool/disallowed_ast_pattern_rules.dart` | Rule model + YAML parse (`parseDisallowedAstRulesFromYaml`) from `tool/disallowed_ast_patterns.yaml` (`SPEC/program/disallowed-ast-patterns.md`); consumed by `tool/check_disallowed_ast_patterns.dart` |
| `tool/check_disallowed_ast_patterns.dart` | Config-driven AST visitor and scan CLI; rule `repo.disallowed_ast_patterns`; also invoked in-process for `repo.debug_console_logic_contract_boundary` |
| `tool/check_repeated_magic_numbers.dart` | Repeated **hash/LCG-style** integer literals (`SPEC/program/repeated-magic-numbers.md`); rule `repo.repeated_magic_numbers` |
| `tool/control_flow_nesting_depth_scan.dart` | Control-flow nesting visitors (`SPEC/program/control-flow-nesting-depth.md`) |
| `tool/check_control_flow_nesting_depth.dart` | Control-flow nesting depth scan + CLI; rule `repo.control_flow_nesting_depth` |
| `tool/check_function_size.dart` | Function measured-line threshold (`SPEC/program/function-size.md`); rule `repo.function_size` |
| `tool/check_part_unit_size.dart` | Dart `part` fragment physical line limit (`SPEC/program/part-unit-size.md`); rule `repo.part_unit_size` |
| `tool/check_debug_console_logic_contract_boundary.dart` | AST-enforced scoped package import contract for debug-console → logic boundary; rule `repo.debug_console_logic_contract_boundary` |
| `tool/check_debug_console_shared_helpers.dart` | Positive invariants for shared debug-console parse/execute helpers (`SPEC/program/debug-console-internals.md`); rule `repo.debug_console_shared_helpers` |
| `tool/check_debug_console_lib_file_size.dart` | Physical-line gate: every non-generated `.dart` file under `packages/colonizethis_debug_console/lib/**` must stay at or below **270** physical lines (empty grandfather); rule `repo.debug_console_lib_file_size` (Refs #4433) |
| `tool/check_debug_console_test_file_size.dart` | Physical-line gate: every `.dart` file under `packages/colonizethis_debug_console/test/**` must stay at or below **400** physical lines (empty grandfather); rule `repo.debug_console_test_file_size` (Refs #4433) |
| `tool/check_app_ui_chrome_lib_file_size.dart` | Physical-line gate: every non-generated `.dart` file under `packages/colonizethis_app_ui_chrome/lib/**` must stay at or below **300** physical lines (empty grandfather); rule `repo.app_ui_chrome_lib_file_size` (Refs #4480) |
| `tool/check_app_ui_chrome_test_file_size.dart` | Physical-line gate: every `.dart` file under `packages/colonizethis_app_ui_chrome/test/**` must stay at or below **400** physical lines (empty grandfather); rule `repo.app_ui_chrome_test_file_size` (Refs #4480) |
| `tool/check_app_debug_handler_guard_helpers.dart` | Forbid inline duplicated guard string literals in `packages/colonizethis_app_debug/lib/src/app_event_handler_debug_*.dart`; handlers must use the shared guard helpers in `debug_command_helpers.dart`; spawn handlers must not contain a raw `25` clamp literal and must use `boundDebugSpawnCount` (`SPEC/program/debug-console-internals.md`); rule `repo.app_debug_handler_guard_helpers` |
| `tool/check_app_debug_lib_file_size.dart` | Physical-line gate: every non-generated `.dart` file under `packages/colonizethis_app_debug/lib/**` must stay at or below **200** physical lines (empty grandfather); rule `repo.app_debug_lib_file_size` (Refs #4484) |
| `tool/check_app_debug_test_file_size.dart` | Physical-line gate: every `.dart` file under `packages/colonizethis_app_debug/test/**` must stay at or below **400** physical lines (empty grandfather); rule `repo.app_debug_test_file_size` (Refs #4484) |
| `tool/check_app_asset_image_decode_dedup.dart` | AST scan over `app/lib/**` enforcing the shared Flame asset-image decode helper (Refs #3699): the `ui.decodeImageFromList(...)` PNG-decode idiom may appear **only** in the canonical helper file `app/lib/features/game/flame/caches/asset_image_cache.dart` (which exposes `decodeImageAsset(...)`); every icon cache and tileset must route decode through that helper instead of re-inlining `rootBundle.load` -> `Completer` -> `decodeImageFromList`. Additionally, every concrete icon cache (a public class declared in a `*_icon_cache.dart` file whose name ends with `IconCache`) must declare `extends AssetImageCache` so the load lifecycle / `_isLoading`/`_isLoaded` flags / structured logging / decode fan-out stay single-sourced in the base; the `AssetImageCache` base file is exempt and tilesets adopt only the decode helper (not the cache base). Generated suffixes, `_test.dart` / `/test/` paths, and fixture dirs are skipped; matches use AST method-invocation / extends-clause checks so comments are ignored; rule `repo.app_asset_image_decode_dedup` |
| `tool/check_app_event_handler_scope_logic_boundary.dart` | Enforce that `app/lib/core/services/app_event_handler/app_event_handler_scope.dart` has no direct import from `app/lib/features/game/logic/**`; rule `repo.app_event_handler_scope_logic_boundary` |
| `tool/check_app_no_part_directives.dart` | Forbid new Dart `part` / `part of` directives under `app/lib/**` outside a shrink-only grandfather allowlist while wave-9 de-part slices land (mirrors orders/turn/setup no-part gates; Refs #4117) — see `SPEC/program/app-no-part-directives.md`; rule `repo.app_no_part_directives` |
| `tool/check_app_lib_no_src_imports.dart` | Forbid `package:colonizethis_<domain>/src/` import directives under `app/lib/**`; UI must consume promoted symbols through domain barrels or narrow logic contract entrypoints (`turn_time_api.dart`, `civilian_intel_api.dart`, etc.); rule `repo.app_lib_no_src_imports` (Refs #4269) |
| `tool/check_subscription_tracker.dart` | Enforce that `app/lib/features/**/*.dart` does not store subscriptions in raw `StreamSubscription` lists; use `SubscriptionTracker` for shared-lifecycle subscription cleanup; rule `repo.subscription_tracker` |
| `tool/check_app_lib_no_suggest_work_orders.sh` | Forbid `suggestWorkOrders(` call sites under `app/lib/**` (per-unit UI uses `getAvailableWorkTargetsForUnit` / `getValidWorkOrderTileKeysWithVisibility`; Refs #2133); rule `repo.app_lib_no_broad_suggest_work_orders` |
| `tool/check_app_lib_player_by_id_lookup.dart` | Forbid linear `game.players` **id** lookups under `app/lib/**` (`.players.where((p) => p.id ==` / `firstWhere`, and `for (final p in game.players)` whose body compares `p.id`). Use `Game.playerById`. Capital-predicate walks (`p.capitalProvinceId` / `p.capitalTile`) and full-list transforms (`{for (final p in game.players) p.id: p.isHuman}`) are not flagged (Refs #2575, #4534); rule `repo.app_lib_player_by_id_lookup` |
| `tool/check_app_lib_unit_lookup_sot.dart` | Forbid dual-region unit-list concatenation (`...oldWorld.units, ...newWorld.units`) and dual-region `unit.id` walks under `app/lib/**`. Use `WorldState.tryGetUnitById` / `allUnitsById`. Single-region arguments to named functions (`civilianUnitsInRegionForOwners(worldState.oldWorld.units, …)`) are not flagged (Refs #4534); rule `repo.app_lib_unit_lookup_sot` |
| `tool/check_app_chrome_popover_sot.dart` | OverlayEntry together with `DismissIntent` and `dialogScrim` under `app/lib/**` may appear only in `app/lib/features/game/widgets/shell/chrome_anchored_popover.dart`. Widget tests are out of scope. `dart run tool/ct_repo_lint.dart` runs the rule in-process (no extra `quality.yml` job). Refs #4582; rule `repo.app_chrome_popover_sot` |
| `tool/check_app_province_inline_action_state.dart` | Under `app/lib/features/game/**`, forbid isomorphic civilian inline-action record fields (`hasEngineerUnits`, `hasExplorerUnits`, `hasRailBuilderUnits`, `hasMerchantUnits`) and 3-field records using `hasBuilderUnits` (upgrade-town 4-field record is allowed). Pin `province_sea_zone_detail_overlay_widget.dart` and `province_sea_zone_detail_overlay_province_content.dart` at ≤280 physical lines each (Refs #4559); rule `repo.app_province_inline_action_state` |
| `tool/check_flutter_action_pins.dart` | Enforce that every `.github/workflows/*.{yml,yaml}` step using `subosito/flutter-action@...` defines `with.flutter-version` with semantic version value **>= `3.41.9`**; rule `repo.flutter_action_pins` |
| `tool/check_workspace_outdated_resolvable.dart` | Enforce that the #2073 outdated audit targets (repo root + every workspace member using `sdk: flutter`) are at `current == resolvable`; rule `repo.workspace_outdated_resolvable` |
| `tool/check_workspace_outdated_latest_direct.dart` | Enforce that audited direct dependency rows are at `Latest` whenever `resolvable == latest` (repo root + every workspace member using `sdk: flutter`); rule `repo.workspace_outdated_latest_direct` |
| `tool/check_no_flame_in_widgets.dart` | Disallow direct `package:flame/*` **import** and **export** lines (single- or double-quoted URI) under `app/lib/widgets/**`; rule `repo.no_flame_in_widgets` |
| `tool/check_no_screen_in_game_widgets.dart` | Disallow `*_screen.dart` files under `app/lib/features/game/widgets/**`; rule `repo.no_screen_in_game_widgets` |
| `tool/check_game_widgets_file_size.dart` | Enforce `app/lib/features/game/widgets/**` Dart files at **250 physical lines or fewer** — see `SPEC/program/game-widgets-file-size.md`; rule `repo.game_widgets_file_size` (Refs #4352 Slice B, #4582, #4606) |
| `tool/check_app_catalog_widgets_file_size.dart` | Enforce `app/lib/widgets/**` hand-written Dart files at **250 physical lines or fewer** — see `SPEC/program/app-catalog-widgets-file-size.md`; rule `repo.app_catalog_widgets_file_size` (Refs #4352 Slice B / AC4, #4582, #4606) |
| `tool/check_app_core_services_file_size.dart` | Enforce `app/lib/core/services/**` hand-written Dart files at **250 physical lines or fewer** — see `SPEC/program/app-core-services-file-size.md`; rule `repo.app_core_services_file_size` (Refs #4450 Slice A / AC3, #4582, #4606) |
| `tool/check_app_turn_resolution_file_size.dart` | Enforce `app/lib/features/game/turn_resolution/**` hand-written Dart files at **250 physical lines or fewer** — see `SPEC/program/app-turn-resolution-file-size.md`; rule `repo.app_turn_resolution_file_size` (Refs #4512 Slice B / AC6, #4582, #4606) |
| `tool/check_logic_test_file_size.dart` | Enforce `packages/colonizethis_logic/test/**` Dart files at **400 physical lines or fewer** (physical line count); rule `repo.logic_test_file_size` runs **full-tree** in `ct_repo_lint` and narrows to changed files when an incremental baseline is supplied (`CT_REPO_LINT_BASE_SHA` in CI). Originally PR-incremental only against the #2216 debt; transitioned to full-tree enforcement once the residual `test/**` violations were cleared (GitHub #2288). The dedicated `quality.yml` step for changed logic tests remains as a fast early signal on PRs. |
| `tool/check_logic_test_tree_loc.dart` | Enforce total physical LOC under `packages/colonizethis_logic/test/**` at **≤5,800** (post-#4090 orphan purge ratchet); rule `repo.logic_test_tree_loc`. |
| `tool/check_logic_test_orphan_basenames.dart` | Forbid reintroduction of the thirteen purged domain collision basenames under `packages/colonizethis_logic/test/**` (Refs #4090 AC3/AC5); rule `repo.logic_test_orphan_basenames`. |
| `tool/check_domain_package_test_file_size.dart` | Enforce **400 physical lines or fewer** across every `.dart` file under `packages/colonizethis_<domain>/test/**` for the eight split logic-domain packages (`world`, `combat`, `economy`, `diplomacy`, `setup`, `orders`, `turn`, `ai_contracts`), excluding `packages/colonizethis_turn/test/support/**` (governed by `repo.turn_test_support_loc` / `repo.turn_test_file_size`); rule `repo.domain_package_test_file_size` (Refs #3290). Repository test `test/check_domain_package_test_file_size_test.dart` asserts the full-tree scan stays green on `dev`. |
| `tool/check_dart_file_non_comment_line_size.dart` | Enforce repository-wide Dart files at **1000 non-comment lines or fewer** (fail when strictly greater), excluding generated suffixes and generated l10n path prefixes — see `SPEC/program/dart-file-non-comment-line-size.md`; rule `repo.dart_file_non_comment_line_size` |
| `tool/check_long_string_switches.dart` | Warn/fail on large string-literal switch statements/expressions (warn ≥20, fail ≥50) via manifest rule `repo.dart_long_string_switches`; uses shared repo-wide collector `collectRepoLintRepoWideDartFiles` — see `SPEC/program/show-tech-tool.md` |
| `tool/check_land_province_bucket_keys.dart` | For guarded explore/fog/news paths, disallow local-only land-province tile-bucket lookups (`tileKeysByRegionAndProvince[region]?[localId]`); require canonical full-id buckets only; rule `repo.land_province_bucket_keys` |
| `tool/check_orders_dedup_map_clones.dart` | Forbid raw map clones anywhere under `packages/colonizethis_orders/lib/src/orders/**`: stockpile-quantities clones (`Map<String, int>.from(<stockpile>.quantities)`) must delegate to `Stockpile.copyQuantities()`, and unit-by-id clones (`Map<String, Unit>.from(...)`) must delegate to the canonical `copyUnitsById(...)` helper; comment lines are ignored (Refs #3404); rule `repo.orders_dedup_map_clones` |
| `tool/check_orders_dedup_owned_tile_graph.dart` | Forbid private owned-land graph duplicates (`_ownedLandTileKeys`, `_extensionDistancesOverOwnedLand`, `_cardinalNeighborTileKeys`, duplicate `isTileAdjacentToConnectedSet`) outside `owned_tile_graph.dart`; require `connectivity_dev_snapshot.dart` to import and delegate to that module (Refs #4317); rule `repo.orders_dedup_owned_tile_graph` |
| `tool/check_combat_rng_factories.dart` | Forbid direct `Random(...)` / `DeterministicRng(...)` construction anywhere under `packages/colonizethis_combat/lib/src/**`, except the seam module `combat/combat_rng.dart` and the LCG implementation `combat/deterministic_rng.dart`; every combat RNG must route through a named `combat_rng.dart` factory (`preCombatBindingRng`, `battleAssignmentRng`, `quickBattleRng`, `probabilisticEngagementRng`, `navalCombatRng`) so seed recipes have one source of truth per `SPEC/program/combat-resolution.md` §3; comment lines and `Random` type annotations (no `(`) are ignored (Refs #3448); rule `repo.combat_rng_factories` |
| `tool/check_combat_no_raw_copies_in_resolution.dart` | Forbid raw ship-list clones `List.from(...)` / `List<ShipInstance>.from(...)` in `packages/colonizethis_combat/lib/src/combat/naval_combat_resolver.dart`; naval ship-list copies must delegate to the canonical `copyNavalShips(...)` helper. `List<Fleet>.from(...)` and other non-ship clones are out of scope; comment lines are ignored (Refs #3448); rule `repo.combat_no_raw_copies_in_resolution` |
| `tool/check_map_grid_ops_central.dart` | Keep tile-grid deep-copy logic centralized in `packages/colonizethis_map/lib/**`: forbid any reference to the removed `copyTileMapGrid(` helper and any inline row-major deep copy `<g>.map((row) => row.toList()).toList()` outside the canonical helper file `packages/colonizethis_map/lib/src/tile_map_grid.dart`. Every defensive grid copy in the land-seed, lakes-province, and join-sea generation passes must route through `TileMapGrid.copy(...)` so the copy site has one source of truth; the canonical file is exempt and comment lines are ignored (Refs #3459); rule `repo.map_grid_ops_central` |
| `tool/check_map_grid_cell_iteration_central.dart` | Keep tile-grid cell traversal centralized in `packages/colonizethis_map/lib/**`: forbid hand-rolled nested `for (var y …) { for (var x …) }` tile walks (a `for (<decl> y …)` header immediately followed by a `for (<decl> x …)` header) so iteration order — which seeded generation depends on for bit-for-bit determinism — has one definition in `TileMapGrid.forEachIndex(...)` / `TileMapGrid.forEachCell(...)`. Whole-file exemptions cover the canonical API home `src/tile_map_grid.dart`, the graph BFS/ocean modules `src/gen/tile_map_grid_graph_connectivity.dart` and `src/gen/tile_map_grid_graph_ocean.dart`, and the two-pass distance transform `src/gen/tile_map_manhattan_distance_transform.dart`. A genuine per-loop exception (e.g. a `sync*` generator whose body must `yield`, or a bordered edge-skipping walk) carries an inline `ct-lint-allow: nested-grid-walk` marker on the outer loop header line or the comment line directly above it; comment lines are otherwise ignored (Refs #3574, #4561); rule `repo.map_grid_cell_iteration_central` |
| `tool/check_map_gen_stage_protocol.dart` | Require the shared `MapGenStage` contract in `packages/colonizethis_map/lib/src/gen/map_gen_stage.dart` and `implements MapGenStage` (or the uniform `MapGenPass`) on each bound generator service family (`TileMapGenLandSeeds`, `TileMapGenLakesProvinces`, `ContinentJoinPass`, `TerrainJitterPass`, `SeaZoneSubdividePass`, `TileMapGenTerrainResource`) so pass orchestration shares a uniform params + grid-in/grid-out protocol, and require **at least 4** bound families to adopt the uniform `MapGenPass` entry point (raised from 3 after the former join-sea family was split into three standalone `MapGenPass` passes, removing the last `MapGenStage`-only exemption) (Refs #3459, #3574, #3588); rule `repo.map_gen_stage_protocol` |
| `tool/check_map_no_partfile_classes.dart` | Two map structure gates share this script. `repo.map_no_partfile_classes` forbids any `part of` file under `packages/colonizethis_map/lib/src/**` from declaring a top-level non-private class. `repo.map_gen_no_new_partfiles` forbids **any** `part` / `part of` directive under the generation layer (`lib/src/gen/**`) and view layer (`lib/src/view/**`) with an **empty** allowlist after #4022 promoted `tile_map_generator_types.dart` to a standalone library (closing the temporary #3588 exception); rules `repo.map_no_partfile_classes`, `repo.map_gen_no_new_partfiles` |
| `tool/check_map_lib_file_size.dart` | Enforce that every `.dart` file under the colonizethis_map generation layer (`lib/src/gen/**`), view layer (`lib/src/view/**`), and render layer (`lib/src/render/**`) is **500 non-comment lines or fewer** (fail when strictly greater), reusing `countNonCommentLinesFromSource`; the gate also fails when any scanned root is missing so it cannot silently rot if those layers move. Complements the repository-wide `repo.dart_file_non_comment_line_size` cap so the #3588 / #3846 part-file decomposition keeps each pass/concern small (Refs #3588, #3846); rule `repo.map_lib_file_size` |
| `tool/check_map_lib_file_size_headroom.dart` | Wave-7 headroom ratchet: every `.dart` file under the same gen/view/render scan roots as `repo.map_lib_file_size` must be **250 non-comment lines or fewer** unless listed in the manifest grandfather baseline (shrink-only allowlist). Complements the 500-NCL hard cap without lowering it (Refs #4112, #4297, #4371, #4561); rule `repo.map_lib_file_size_headroom` |
| `tool/check_data_lib_file_size.dart` | Enforce that every hand-written `.dart` file under `packages/colonizethis_data/lib/src/**` is **250 physical lines or fewer** (fail when strictly greater); skips generated suffixes (`.g.dart`, `.freezed.dart`, `.mocks.dart`, `.gen.dart`). Complements the repository-wide 1000 NCL cap so wave-7 topic splits stay small (Refs #4072, #4292, #4412, #4626); rule `repo.data_lib_file_size` |
| `tool/check_data_no_part_directives.dart` | Forbid new Dart `part` / `part of` directives under `packages/colonizethis_data/lib/**` outside a shrink-only grandfather allowlist while wave-4 de-part slices land (mirrors app/orders/world no-part gates; Refs #4121) — see `SPEC/program/data-no-part-directives.md`; rule `repo.data_no_part_directives` |
| `tool/check_data_test_file_size.dart` | PR-blocking physical-line gate: every `.dart` file under `packages/colonizethis_data/test/**` must stay at or below **250** physical lines (empty shrink-only allowlist; mirrors `repo.economy_test_file_size`; Refs #4121, #4292, #4412, #4626) — see `SPEC/program/data-test-file-size.md`; rule `repo.data_test_file_size` |
| `tool/check_data_victory_config_registry_parity.dart` | Enforce exact set parity between every `const int` / `const double` `k*` in `packages/colonizethis_data/lib/src/ai_victory_config*.dart` and every `victoryConfigIntParam` / `victoryConfigDoubleParam` name string across `ai_parameter_victory_config_params*.dart`. Structured `Map` / `String` constants stay out of scope (Refs #4072); rule `repo.data_victory_config_registry_parity` |
| `tool/check_map_public_barrel_surface.dart` | Keep the public `colonizethis_map` barrel `packages/colonizethis_map/lib/colonizethis_map.dart` narrowed to intentional entry points: scan the barrel **and transitive `export` closure** (barrel `export` targets, recursively — not a full `lib/src/gen|view|render/**` walk) and forbid re-export of internal-only generation primitives (`src/gen/grid_voronoi.dart`, `src/gen/topology_inference.dart`, `src/gen/tile_map_grid_graph.dart`) and any `*_test_api.dart` module (including `src/gen/tile_map_generator_lakes_test_api.dart`). Same-package tests import these from `src/` directly; comment lines and `import` directives are ignored (Refs #3459 AC4, #4561); rule `repo.map_public_barrel_surface` |
| `tool/check_map_region_data_access_central.dart` | Keep old/new-world region data selection centralized in `packages/colonizethis_map/lib/**`: forbid any inline `.oldWorld.provinces`, `.oldWorld.units`, `.newWorld.provinces`, or `.newWorld.units` region branch outside the canonical helper file `packages/colonizethis_map/lib/src/region_data_access.dart`. Every region-scoped provinces/units read in the view-building (orchestration, cell/unit) and ownership-overlay passes must resolve the region via `regionDataForMapRegionId(world, regionId)` so the old/new selection has one source of truth; the canonical file is exempt and comment lines are ignored (Refs #3459 AC3); rule `repo.map_region_data_access_central` |
| `tool/check_map_region_dispatch_central.dart` | Keep old/new-world region *dispatch* centralized in `packages/colonizethis_map/lib/**`: forbid any inline `==` / `!=` comparison against `kRegionOldWorld` or `kRegionNewWorld` outside the canonical dispatch file `packages/colonizethis_map/lib/src/map_region_dispatch.dart`. Colour, capital-scope, port-tile bucketing, and view-data assembly paths must resolve old/new branching via `selectByMapRegionId(...)` / `selectByMapRegionIdOrNull(...)` so dispatch has one source of truth. Using the constants as map keys, list elements, named arguments, or string-interpolation operands is unaffected; the canonical file is exempt and comment lines are ignored (Refs #3574); rule `repo.map_region_dispatch_central` |
| `tool/check_map_gen_no_image_import.dart` | Enforce the `colonizethis_map` generation→render layer boundary in `packages/colonizethis_map/lib/**`: forbid any `import` / `export` of `package:image` outside the render-layer directory `packages/colonizethis_map/lib/src/render/`. Every Dart file under `lib/src/render/` is a PNG-encoding render module permitted to depend on `package:image`; generation passes and view-model builders stay image-free so internal layering (gen → view inputs → render) is one-directional (slice-5 render relocation complete). A same-prefix package such as `package:image_picker/...` does not match; comment lines are ignored (Refs #3574); rule `repo.map_gen_no_image_import` |
| `tool/check_combat_dedup_precombat_indexes.dart` | Require both combat-phase consumers `combat/conflict_detection.dart` and `combat/unopposed_province_capture.dart` to keep referencing `PreCombatMovementIndex.build` and `greatPowerArmyMoves`, so the deduplicated pre-combat movement/province scan stays delegated to the shared `pre_combat_index.dart` index instead of being re-inlined (Refs #3448); rule `repo.combat_dedup_precombat_indexes` |
| `tool/check_combat_documented_keep_copies.dart` | Companion to `repo.combat_no_raw_copies_in_resolution`: for combat-resolution copies the #3448 copy-disposition table marks **keep** (`List<Player>.from(...)` in `military_attack_economy.dart`, `copyGroups` `List<String>.from(...)` in `quick_battle_resolver_engine_groups.dart`, `copyWith()).toList()` in `combat_resolver_probabilistic.dart`, `defenderUnitIds.toList()` in `combat_resolver.dart`), require the file to carry an inline `copy-disposition` rationale marker whenever the keep-copy expression is present, so semantically required working-state copies stay documented rather than silently flipped into undocumented clones (Refs #3448); rule `repo.combat_documented_keep_copies` |
| `tool/check_setup_dedup_init_pipeline_retry.dart` | Forbid re-inlining the init map+setup retry loop under `packages/colonizethis_setup/lib/src/setup/**`: any inline per-attempt seed bump (`* 100003`) must use `kInitPipelineSeedBump` via `runInitPipelineWithRetries`, and the retriable-topology-code predicate (`code == 'faction_component_bin_pack_failed'`) must appear at most once (delegate to `isRetriableInitTopologyCode`); comment lines and throw sites (`code: '...'`) are ignored (Refs #3449); rule `repo.setup_dedup_init_pipeline_retry` |
| `tool/check_setup_dedup_gp_ow_tile_scans.dart` | Forbid re-inlining the Great Power Old World tile-scan helpers under `packages/colonizethis_setup/lib/src/setup/**` outside the shared `gp_old_world_tile_scan.dart` module: any reappearance of the duplicated private clones (`_ownerByLocalProvinceId`, `_owTileKey`, `_isGpId`, `_collectEligibleTilesSorted`) must instead delegate to the canonical public helpers (`gpOwnerByLocalProvinceId`, `gpOwTileKey`, `isGpOwner`, `visitGpOwLandTiles`, `collectGpOwEligibleTilesSorted`); the shared module is exempt and comment lines are ignored (Refs #3449); rule `repo.setup_dedup_gp_ow_tile_scans` |
| `tool/check_setup_dedup_gp_ids_from_players.dart` | Forbid re-inlining the slot-ordered Great Power id projection over a player collection (`players.map((p) => p.id)`) anywhere under `packages/colonizethis_setup/lib/src/setup/**` outside the shared `gp_old_world_tile_scan.dart` module: GP id lists/sets must call the canonical `gpIdsSortedFromPlayers(game)` helper (and derive the membership set via `.toSet()`) so player slot order (gp1, gp2, … — never lex-sorted) has one source of truth across the resource/terrain redistribution concerns; the shared module is exempt and comment lines are ignored (Refs #3449); rule `repo.setup_dedup_gp_ids_from_players` |
| `tool/check_setup_prefixed_province_id_normalizer.dart` | Forbid manual `ProvinceId.isPrefixed(...) ? ...` normalization ternaries under `packages/colonizethis_setup/{lib/src/setup,test}/**`; canonical prefixed keys must use `ProvinceId.prefixedFrom(regionId, id)`, and local-id fallback normalization must use `ProvinceId.localFromMaybePrefixed(id)`. Full-line comments are ignored (Refs #4054); rule `repo.setup_prefixed_province_id_normalizer` |
| `tool/check_setup_per_pass_nw_owner_map.dart` | Require advanced-start world knowledge / colonization to build per-pass NW owner maps via `nwOwnerByLocalProvinceId` / `nwTribeOwnerByLocalProvinceId` in `advanced_start_nw_owner_lookup.dart`; forbid reintroduced `_ownerIdForLocalProvince` / `_tribeOwnerForLocalProvince` and linear `prefixedFrom`+`ownerId` scans in those bootstrap files (Refs #4054); rule `repo.setup_per_pass_nw_owner_map` |
| `tool/check_setup_dedup_advanced_start_selection.dart` | Forbid re-inlined minor-buyer `i % game.players.length` round-robin and `(n * fraction).ceil()`+`.take(...)` fraction selection under `packages/colonizethis_setup/lib/src/setup/**` outside `advanced_start_selection.dart`; callers must use `minorBuyerIdRoundRobin` / `selectByFractionCeil` (Refs #4054); rule `repo.setup_dedup_advanced_start_selection` |
| `tool/check_setup_dedup_unit_spawn.dart` | Forbid a second capital unit-spawn / home-fleet merge scaffold under `packages/colonizethis_setup/lib/src/setup/**` outside `setup_unit_spawn.dart`: private clones (`_spawnCivilianUnitsOfType`, `_addStartingRegimentsForPlayer`, `_spawnRegimentsForPlayer`, `_mergeHomeFleetShipsForPlayer`), `mintShipInstances(` call sites, and `tileKey: capitalTileKey` Unit appends must use `spawnCivilianUnitsOfType` / `spawnRegimentsAtCapital` / `mergeHomeFleetShips` / `prepareHomeFleetMergeScratch`; comment lines ignored (Refs #4054); rule `repo.setup_dedup_unit_spawn` |
| `tool/check_setup_naming_lookup.dart` | Forbid re-inlined empty `MinorNationNaming(id: '', displayName: '')` / `TribeNaming(id: '', displayName: '', provinceNamePool: [])` sentinels under `packages/colonizethis_setup/lib/src/setup/**` outside `setup_naming_lookup.dart`; callers must use `resolvedMinorNaming` / `resolvedTribeNaming`; comment lines ignored (Refs #4054); rule `repo.setup_naming_lookup` |
| `tool/check_setup_no_world_src_imports.dart` | Forbid `import`/`export` of `package:colonizethis_world/src/**` anywhere under `packages/colonizethis_setup/{lib,test}/**` (allowlist empty): the setup-facing world surface (graph traversal, capital reassignment, town/capital tile strip, validation exceptions) is published from the `colonizethis_world` barrel and must be consumed through `package:colonizethis_world/colonizethis_world.dart`; full-line comments ignored (Refs #4054); rule `repo.setup_no_world_src_imports` |
| `tool/check_setup_dedup_grid_bfs_coord_keys.dart` | Forbid re-inlining a standalone grid breadth-first search or a raw 2D coordinate key under `packages/colonizethis_setup/lib/**` outside `grid_bfs.dart`: an AST scan flags inline cardinal-delta integer-pair list literals (`[1,0]`, `[-1,0]`, `[0,1]`, `[0,-1]`) and pure `'$a\|$b'` string interpolations whose both operands are coordinate-like (`x`/`y`/`nx`/`ny`/`cx`/`cy`/`sx`/`sy` or a `.x`/`.y` property; id joins such as `'$provinceId\|$seaZoneId'` are not coordinate-like and are not flagged). Setup grid BFS + coord keys must route through the canonical `grid_bfs.dart` helpers (`bfsGridParents`, `bfsGridDistances`, `gridCoordKey`, `kGridNeighborsCardinal4`); `grid_bfs.dart` is exempt as the source of truth (Refs #3740); rule `repo.setup_dedup_grid_bfs_coord_keys` |
| `tool/check_setup_dedup_topology_adjacency.dart` | Forbid re-inlining the topology/tile adjacency helpers under `packages/colonizethis_setup/lib/src/setup/**` outside the shared `setup_topology_adjacency.dart` module: any reappearance of the duplicated private clones (`_provinceNodeIds`, `_topologyNodeById`, `_seaZonesAdjacentToProvince`, `_provinceSeaZones`, `_anyCardinalNeighborCell`, `_isTileAdjacentToSeaZone`, `_tileKeyAdjacentToProvinceSeaZone`) is a violation. Pure topology concerns must use the canonical cached `colonizethis_world` helpers (`provinceNodeIds`, `seaZonesAdjacentToProvince`); the tile-grid concerns must use the shared `setup_topology_adjacency.dart` helpers (`anyCardinalNeighborCell`, `tileAdjacentToSeaZone`, `tileAdjacentToAnySeaZone`, `tileKeyAdjacentToAnySeaZone`); the shared module is exempt and comment lines are ignored (Refs #3740, #3840); rule `repo.setup_dedup_topology_adjacency` |
| `tool/check_setup_test_no_duplicate_scaffolding.dart` | Forbid reimplementing production scaffolding helpers anywhere under `packages/colonizethis_setup/test/**`: a province–province neighbours builder cloned from `provinceNeighboursFromTopology` (recognised by scanning `topology` `.edges` while filtering `TopologyNodeType.province` to build a `Map<String, Set<String>>` adjacency) and a connected-components routine cloned from `connectedComponentsInSubset` (recognised by a `<Set<String>>[]` accumulator paired with a `visited` set) are violations. Setup tests must reuse the production helpers (`provinceNeighboursFromTopology` re-exported via the `colonizethis_logic` barrel, and `connectedComponentsInSubset` from `colonizethis_world`); full-line comments are ignored. Mirrors the sibling `repo.*_test_no_duplicate_*` gates (Refs #3740); rule `repo.setup_test_no_duplicate_scaffolding` |
| `tool/check_setup_test_default_init_options.dart` | Forbid the repeated `InitGameOptions(cellSize: 8, renderPng: false)` literal under `packages/colonizethis_setup/test/**` when `defaultInitOptions` exists in `init_game_orchestrator_test_support.dart`; the test-support file that defines `defaultInitOptions` is exempt; full-line comments are ignored; overrides that change non-default fields (e.g. `renderPng: true`) are allowed (Refs #3840); rule `repo.setup_test_default_init_options` |
| `tool/check_setup_dedup_faction_capital_collect.dart` | Forbid triple-faction capital-map collection loops (`for (final p in game.players)` + `for (final m in game.minorNations)` + `for (final t in game.tribes)` with capital-map markers) anywhere under `packages/colonizethis_setup/lib/src/setup/**` outside `faction_setup_helpers.dart`; callers must use `collectCapitalMapsByOwner` / `forEachSetupFaction` (Refs #3840); rule `repo.setup_dedup_faction_capital_collect` |
| `tool/check_setup_dedup_raise_road_at_least.dart` | Forbid re-inlining the raise-road-at-least body (`raiseRoadAtLeast` / `_raiseRoadAtLeast` / `_setRoadLevelMax` with `roadLevel` compare + `setRoadLevel`) anywhere under `packages/colonizethis_setup/lib/src/setup/**` outside `setup_road_wiring.dart`; call sites must use the shared helper (Refs #4020); rule `repo.setup_dedup_raise_road_at_least` |
| `tool/check_setup_dedup_road_tile_key_maps.dart` | Forbid private coord→tile / owned-tile-key clones (`_coordToTileKey`, `_allowedTileKeysForFaction`, `_bfsParentsFromCapital`, `_buildCoordToTileKeyByRegion`, `_addCoordMappingIfPresent`) and re-bodies of the public helpers outside `setup_road_wiring.dart` (Refs #4020); rule `repo.setup_dedup_road_tile_key_maps` |
| `tool/check_setup_dedup_seaboard_nearest_tile.dart` | Forbid a second nearest-seaboard nested full-grid + `tileAdjacentToSeaZone` Manhattan scan (`_nearestCoastalTileInProvinceForSeaZone`, `_closestSeaboardTileInProvince`, or equivalent) outside `setup_road_wiring.dart`; call sites must use `nearestSeaboardTileInProvinceForSeaZone` (Refs #4020); rule `repo.setup_dedup_seaboard_nearest_tile` |
| `tool/check_setup_dedup_full_grid_tile_scans.dart` | Forbid nested full-grid `for (var y = 0; y < *.height` + `for (var x = 0; x < *.width` walks under `packages/colonizethis_setup/lib/src/setup/**` outside `tile_cell_scan.dart`; production full-grid consumers must use `forEachTileCell` / `forEachProvinceCell` / `visitGpOwLandTiles` (edge scans without a nested width loop remain allowed); comment lines are ignored (Refs #4029); rule `repo.setup_dedup_full_grid_tile_scans` |
| `tool/check_setup_dedup_advanced_start_flood_fill.dart` | Forbid a second hand-rolled province `visited`/`queue`/`head` BFS (or `_enqueueUnvisitedNeighbors`) in `advanced_start_bootstrap_*.dart` outside `advanced_start_nw_topology.dart`; colonization and other bootstrap callers must use `advancedStartFloodFillProvinces` (Refs #4029); rule `repo.setup_dedup_advanced_start_flood_fill` |
| `tool/check_setup_dedup_ownership_paint.dart` | Forbid re-inlined ownership paint scaffolding under `game_setup_ownership*.dart` outside `game_setup_ownership_paint.dart`: direct `assignTerritoriesLockedOnLandmass` / `assignTerritoriesByBfsGrowth` wiring or `_lockedGrowthOrder` clones; call sites must use `paintLandmass` / `_paintLandmass` (`locked`/`bfs`); locked assigner and BFS growth engines remain separate allowlisted backends (Refs #4029, #4086); rule `repo.setup_dedup_ownership_paint` |
| `tool/check_setup_helpers_first_class_libraries.dart` | Require `game_setup_helpers_{towns,naming,bootstrap}.dart` to remain first-class libraries (no `part of`) and `game_setup_helpers.dart` to re-export them without `part` directives (Refs #4029); rule `repo.setup_helpers_first_class_libraries` |
| `tool/check_setup_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_setup/lib/**` so ownership / create / capital / redistribution / locked-assigner (and naming / init orchestrator topic libs) stay proper libraries with explicit imports (mirrors orders/turn/diplomacy no-part gates; Refs #4086) — see `SPEC/program/setup-no-part-directives.md`; rule `repo.setup_no_part_directives` |
| `tool/check_colonizethis_setup_lib_file_size.dart` | Ratchet ceiling for `packages/colonizethis_setup/lib/**` physical lines per file (wave-8 ≤250; shrink-only grandfather allowlist during transition; generated suffixes excluded); rule `repo.colonizethis_setup_lib_file_size` (Refs #4273, #4349, #4624) |
| `tool/check_colonizethis_setup_test_file_size.dart` | Ratchet ceiling for `packages/colonizethis_setup/test/**` physical lines per file excluding `test/setup/support/` (wave-8 ≤250; empty shrink-only grandfather); rule `repo.colonizethis_setup_test_file_size` (Refs #4624) |
| `tool/check_colonizethis_setup_test_mirrors_lib.dart` | Forbid loose Dart sources directly at the `packages/colonizethis_setup/test/` root: setup production code lives under `lib/src/setup/`, so the testing rule (`colonizethis-testing.mdc`) requires `test/` to mirror `lib/`. Every setup test (and its co-located support helper) must live in a mirroring subdirectory such as `test/setup/` (with optional `test/setup/plains/`, `test/setup/locked_assigner/` subtrees) rather than flat in `test/`. A file is flagged when the path remainder after the `packages/colonizethis_setup/test/` prefix contains no further separator; nested files such as `test/setup/validation_exceptions_test.dart` are compliant. Mirrors the sibling orders/combat layout gates; rule `repo.colonizethis_setup_test_mirrors_lib` (Refs #4273) |
| `tool/check_colonizethis_setup_scenario_table_runner.dart` | Prefer-scenario-tables gate for `packages/colonizethis_setup/test/**`: flags long imperative `test('…') { … }` bodies (heuristic: opening `{` on the same line as `test(` with no surrounding `for (final scenario` within a short look-behind window) outside `setupPreferScenarioTablesAllowlist`. Wave-7 slice C (#4349) sets baseline allow-all **off**; remaining legitimately imperative suites stay on the explicit allowlist after densifying the ≥300-line suites into `support/*_scenarios.dart`; mirrors `repo.orders_test_prefer_scenario_tables`; rule `repo.colonizethis_setup_prefer_scenario_tables` (Refs #4273, #4349) |
| `tool/check_colonizethis_setup_test_support_loc.dart` | Ratchet for Dart under `packages/colonizethis_setup/test/setup/support/`: tree physical LOC ceiling **4100** (wave-7 slice D post-split measured 3996 + headroom; wave-6 was **2700**; slice C densify moved suite bodies into support so the tree total cannot return to 2700) and per-file ceiling **250** (fail at ≥250); fail on net tree regrowth or a fat scenario module; rule `repo.colonizethis_setup_test_support_loc` (Refs #4273, #4349, #4624) |
| `tool/check_setup_test_use_shared_fixtures.dart` | Forbid re-inlined empty `Game(` / `WorldState(` shells and full `GameSetupConfig(` rebuilds under `packages/colonizethis_setup/test/**`; empty/minimal shells must use `package:colonizethis_test/game_test_fixtures.dart` `TestFixtures` (or a thin support delegate), and config variants must use `configWithOverrides` / `lockedFullInitConfig` in `init_game_orchestrator_test_support.dart` (that support file alone may construct `GameSetupConfig`); full-line comments ignored (Refs #4029). Additionally forbid running `runInitGame(config: X, options: defaultInitOptions)` with an identical config expression in two or more distinct test bodies of one file — such assert-only repeats must reuse the memoized `sharedInitGameResult(config)` harness; determinism double runs inside a single test body, calls with custom `InitGameOptions` or extra arguments (for example `generateRegion:`), and the support file itself are structurally exempt (Refs #4054); rule `repo.setup_test_use_shared_fixtures` |
| `tool/check_orders_file_size.dart` | Enforce the two orders hot files `packages/colonizethis_orders/lib/src/orders/orders_application.dart` and `.../orders_application_completed_work.dart` at **1000 non-comment lines or fewer** each (fail when strictly greater), reusing `countNonCommentLinesFromSource`; the gate also fails when either named file is missing so it cannot silently rot if the orders module moves. Complements the repository-wide `repo.dart_file_non_comment_line_size` cap with an orders-specific message so the #3404 dedup/abstraction refactors keep these files bounded (Refs #3404); rule `repo.orders_file_size` |
| `tool/check_orders_test_no_duplicate_fixtures.dart` | Forbid re-introducing a local copy of the shared game/world test fixtures anywhere under `packages/colonizethis_orders/test/**`: any file named `test_fixtures.dart` or any test file that redeclares a top-level `TestFixtures` class is a violation. Orders tests must import the canonical `package:colonizethis_test/game_test_fixtures.dart` factories (single source of truth) rather than duplicating them; references to the shared `TestFixtures` symbol are unaffected. Complements the testing-rule shared-fixtures policy (Refs #3714); rule `repo.orders_test_no_duplicate_fixtures` |
| `tool/check_world_test_no_duplicate_fixtures.dart` | Forbid re-introducing a local copy of the shared game/world test fixtures under `packages/colonizethis_world/test/**`. A violation is any test file named `test_fixtures.dart` or any test file that redeclares a top-level `TestFixtures` class. World tests MUST import the shared `TestFixtures` from `package:colonizethis_test/game_test_fixtures.dart` (Refs #3843); rule `repo.world_test_no_duplicate_fixtures` |
| `tool/check_turn_test_no_duplicate_fixtures.dart` | Forbid re-introducing a local copy of the shared game/world test fixtures under `packages/colonizethis_turn/test/**`. A violation is any test file named `test_fixtures.dart` or any test file that redeclares a top-level `TestFixtures` class. Turn tests MUST import the shared `TestFixtures` from `package:colonizethis_test/game_test_fixtures.dart` (Refs #3842); rule `repo.turn_test_no_duplicate_fixtures` |
| `tool/check_turn_world_market_test_support.dart` | World-market tests under `packages/colonizethis_turn/test/turn/` that invoke `worldMarketTurnPhaseHandler` directly MUST import `test/support/world_market_test_support.dart` and call a shared runner (`runWorldMarketPhase` / `runWorldMarketPhasePipeline` / `From` variants, or wrappers `runWorldMarketFrrCreditPhase` / `runTreasuryClampPhase` / `runWorldMarketTradePhase`). Phase-handler suites (`*_phase_*_test.dart`) must use a runner even when they never spell the handler symbol. Allowlisted pure helper unit suites: `world_market_phase_orders_helpers_test.dart`, `world_market_phase_completed_trade_pairs_test.dart`, `world_market_phase_deals_subsidy_test.dart` (Refs #3876 / #4039); rule `repo.turn_world_market_test_support` |
| `tool/check_turn_test_resolve_complete.dart` | Forbid nested `requireTurnResolutionComplete(resolveTurnForGame(...))` and direct `resolveTurnForGame(` under `packages/colonizethis_turn/test/**` outside the harness and a short pending-result allowlist; success paths MUST use `resolveTurnComplete` from `test/support/turn_resolver_test_harness.dart` (Refs #4039); rule `repo.turn_test_resolve_complete` |
| `tool/check_turn_test_fixture_location.dart` | Forbid `*_fixtures.dart` / `*_test_support.dart` under `packages/colonizethis_turn/test/**` outside `test/support/` so fixture modules stay co-located with harnesses (Refs #4039); rule `repo.turn_test_fixture_location` |
| `tool/check_turn_test_support_loc.dart` | Ratchet ceiling for all Dart under `packages/colonizethis_turn/test/support/` (physical LOC; wave-6 post-densify ceiling **5700**); fail on net regrowth beyond the ceiling (Refs #4039, #4113, #4168, #4252); rule `repo.turn_test_support_loc` |
| `tool/check_turn_test_support_region_literals.dart` | Forbid bare `'oldWorld'` / `'newWorld'` string literals under `packages/colonizethis_turn/test/support/**` (use `kRegionOldWorld` / `kRegionNewWorld`); rule `repo.turn_test_support_region_literals` (Refs #4168) |
| `tool/check_turn_lib_file_size.dart` | Ratchet ceiling for `packages/colonizethis_turn/lib/**` physical lines per file (wave-8 ≤250; empty shrink-only grandfather; generated suffixes excluded); rule `repo.turn_lib_file_size` (Refs #4113, #4342, #4583) |
| `tool/check_turn_test_file_size.dart` | Ratchet ceiling for `packages/colonizethis_turn/test/**` physical lines per file excluding `test/support/` (wave-8 ≤300; support tree governed by `repo.turn_test_support_loc`); rule `repo.turn_test_file_size` (Refs #4113, #4583) |
| `tool/check_turn_test_no_src_imports.dart` | Forbid `package:colonizethis_turn/src/` imports under `packages/colonizethis_turn/test/**`; tests must use `colonizethis_turn.dart` or `colonizethis_turn_testing.dart` (Refs #4252); rule `repo.turn_test_no_src_imports` |
| `tool/check_turn_test_integration_inline_game_ctor.dart` | Forbid inline `Game(` constructor calls under `packages/colonizethis_turn/test/support/integration/**`; `_allowlistedRelativePaths` is empty after wave 8; use harness builders (`turnTestOwGame`, `turnTestOwNwCrossRegionGame`, `adjacentOwP1P2Game`, …); rule `repo.turn_test_integration_inline_game_ctor` (Refs #4252, #4168, #4583) |
| `tool/check_world_lib_file_size.dart` | Ratchet ceiling for `packages/colonizethis_world/lib/**` physical lines per file (wave-8 ≤250 except sealed `GameEvent` in `lib/src/game_events.dart` at ≤400; empty shrink-only grandfather; generated suffixes excluded); rule `repo.colonizethis_world_lib_file_size` (Refs #4125, #4330, #4515, #4611) |
| `tool/check_world_test_file_size.dart` | Ratchet ceiling for `packages/colonizethis_world/test/**` physical lines per file excluding `test/world_test_support/` (wave-8 ≤250; empty shrink-only grandfather after densify); rule `repo.colonizethis_world_test_file_size` (Refs #4125, #4330, #4515, #4611) |
| `tool/check_world_test_support_file_size.dart` | Ratchet ceiling for `packages/colonizethis_world/test/world_test_support/**` physical lines per file (wave-8 ≤250; empty grandfather after densify); rule `repo.world_test_support_file_size` (Refs #4515, #4611) |
| `tool/check_world_test_mirrors_lib.dart` | Keep the `packages/colonizethis_world/test/**` tree mirrored to `lib/src/` per the testing rule (`test/` mirrors `lib/`): any Dart source that sits **directly** at the `test/` root is a violation — both `*_test.dart` files and co-located support helpers must live in a subdirectory such as `test/world/`, `test/trace/`, or `test/world_test_support/`. Complements the testing-rule policy (Refs #4330); rule `repo.world_test_mirrors_lib` |
| `tool/check_ai_test_no_duplicate_scaffolding.dart` | Keep `packages/colonizethis_ai/test/**` scaffolding coherent. Three regressions are violations: (1) any `_test.dart` living inside `test/support/` (the scaffolding directory holds helpers only; test files belong in a sibling such as `test/support_test/`); (2) any file named `test_fixtures.dart` (a confusing shadow of the shared `colonizethis_test` `TestFixtures` — AI-specific fixtures must use a disambiguated name such as `ai_planner_fixtures.dart`); and (3) any AI test file that redeclares a top-level `TestFixtures` class instead of importing `package:colonizethis_test/game_test_fixtures.dart`. References to the shared `TestFixtures` symbol are unaffected. Complements the testing-rule shared-fixtures/`test/`-mirrors-`lib/` policy (Refs #3717); rule `repo.ai_test_no_duplicate_scaffolding` |
| `tool/check_ai_expand_peace_test_shared_fixtures.dart` | Forbid local expand-peace / Phase-8 / Phase-11 fixture redeclarations: (1) `expand_phase_planner_*peace*_test.dart` must not redeclare `_ownSnapshot` / `_criticalGame` / `_distractionGame` / `_zeroRegimentGame` / `_peerGame` / `_ownVsPartnerGame`; (2) documented ownSnapshot adopters (`expand_phase_planner_focus_minor_target_*_cases.dart`, `expand_phase_planner_stalled_minor_or_gp_blocker_pivot_test.dart`, `observer_goal_phase_survival_great_power_peace_targets_test.dart`) must not redeclare `_ownSnapshot`; (3) `expand_phase_peace_matrix_sole_gp_blocker_guard_cases.dart` / `expand_phase_peace_matrix_sole_gp_blocker_fire_cases.dart` must not redeclare `_ownVsPartnerGame`; (4) Phase-11 adopters must not redeclare `_gameOf` (`expand_phase_planner_default_start_and_near_quota_peace_test.dart`), `_ownGame` (stalled-futile / tribe-distraction cases + default-start futile-minor pin), `_tribeDistractionGame`, `_pivotGame`, `_survivalGame`, or `_buildGame` (`expand_phase_peace_matrix_*_cases.dart`). Callers MUST import shared builders from `packages/colonizethis_ai/test/support/expand_phase_peace_test_support.dart`. The gate also fails when that shared support file is missing (Refs #3967 / #3997 / #4239); rule `repo.ai_expand_peace_test_shared_fixtures` |
| `tool/check_ai_orchestrator_test_shared_fixtures.dart` | Forbid local OW-quota province-list redeclarations in `packages/colonizethis_ai/test/planning/domain_planner_orchestrator_*_test.dart`. A violation is any `const List<String> _gp1OwProvincesBelowQuota` or `const List<String> _gp1OwProvincesAtQuota` declaration inside those pins. Pins MUST import `kGp1OwProvincesBelowQuota` / `kGp1OwProvincesAtQuota` from `packages/colonizethis_ai/test/support/domain_planner_orchestrator_test_support.dart`. The gate also fails when that shared support file is missing. Phase-variant lists (near-quota / develop-only / custom `_gp1OwProvinces`) remain file-local when they are not the canonical below-/at-quota sets. Additionally, pins that call paired Game builders MUST NOT redeclare local snapshot helpers: `buildOrchestratorExpandMinorWarScenarioGame` / `buildOrchestratorPendingCostTradeScenarioGame` → no `_expandSnapshot` (use `buildOrchestratorExpandMinorWarAtWarSnapshot`); `buildOrchestratorGp1TribeNwScenarioGame` → no `_expandSnapshot` / `_colonialSnapshot` (use `buildOrchestratorExpandNwTribeTargetSnapshot` / `buildOrchestratorColonialNwTribeTargetSnapshot`); `buildOrchestratorExpandGpOnlyBlockerScenarioGame` → no `_expandSnapshot` / `_developSnapshot` (use `buildOrchestratorExpandGpOnlyBlockerSnapshot` / `buildOrchestratorDevelopGpOnlyBlockerSnapshot`); `buildOrchestratorExpandAdjacentMinorScenarioGame` → no `_expandSnapshot` / `_developSnapshot` (use `buildOrchestratorExpandAdjacentMinorSnapshot` / `buildOrchestratorDevelopAdjacentMinorSnapshot`); `buildOrchestratorDevelopGpOwnedNwScenarioGame` → no `_developSnapshot` (use `buildOrchestratorDevelopNoColonialTargetsSnapshot`); `buildOrchestratorColonialLiteDeclareWarScenarioGame` → no `_colonialSnapshot` / `_colonialLiteSnapshot` (use `buildOrchestratorColonialNwTribeTargetSnapshot` / `buildOrchestratorExpandNwTribeTargetSnapshot`). Documented diplomatic-scoring COLONIAL tribe adopters (`diplomatic_candidate_scoring_intervention_tribe_tolerance_test.dart`, `diplomatic_candidate_scoring_personality_colonial_divergence_test.dart`) MUST NOT redeclare `_gp1OwProvincesAtQuota` or a brace-bodied `_colonialSnapshot` (use `kGp1OwProvincesAtQuota` / `buildOrchestratorColonialNwTribeTargetSnapshot`) (Refs #3941 / #3997); rule `repo.ai_orchestrator_test_shared_fixtures` |
| `tool/check_ai_colonial_acquisition_test_shared_fixtures.dart` | Forbid local acquisition Game / DiplomacyRelation helper redeclarations in `packages/colonizethis_ai/test/planning/colonial_phase_planner_acquisition_*_test.dart`. A violation is any `Game _acquisitionGame` / `_purchaseLandGame` / `_declareWarGame` / `_bothValidGame` or `DiplomacyRelation _friendly` / `_peaceFriendly` / `_atWar` declaration inside those pins. Pins MUST import `buildColonialAcquisitionGame` / `colonialAcquisitionFriendly` (and siblings) from `packages/colonizethis_ai/test/support/colonial_acquisition_test_support.dart`. The gate also fails when that shared support file is missing (Refs #3972); rule `repo.ai_colonial_acquisition_test_shared_fixtures` |
| `tool/check_ai_expand_peace_pin_suite_size.dart` | Forbid oversize EXPAND-peace contracts: under `packages/colonizethis_ai/test/planning/`, any `expand_phase_planner_*peace*_test.dart` with more than **300** physical lines that does not import a sibling `*_cases.dart` is a violation (Refs #3977, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4365 Slice B, #4602 Slice D); rule `repo.ai_expand_peace_pin_cases_required` |
| `tool/check_ai_colonial_phase_dispatch_test_shared_fixtures.dart` | Forbid local `_colonialLiteGame` / `_colonialGame` / `_expandGame` / `_developGame` / `_expandSnapshot` / `_colonialLiteSnapshot` / `_colonialSnapshot` / `_developSnapshot` redeclarations in `packages/colonizethis_ai/test/planning/phase_planner_dispatch_test.dart`. Pins MUST import `buildPhasePlannerDispatchColonialLiteGame` / `buildPhasePlannerDispatchColonialGame` / `buildPhasePlannerDispatchExpandGame` / `buildPhasePlannerDispatchDevelopGame` / `buildPhasePlannerDispatchExpandSnapshot` / `buildPhasePlannerDispatchColonialLiteSnapshot` / `buildPhasePlannerDispatchColonialSnapshot` / `buildPhasePlannerDispatchDevelopSnapshot` from `packages/colonizethis_ai/test/support/colonial_phase_planner_test_support.dart`. The gate also fails when that shared support file is missing (Refs #3977 / #3997); rule `repo.ai_colonial_phase_test_shared_fixtures` |
| `tool/check_ai_phase_planner_nw_suppression_test_shared_fixtures.dart` | Forbid local `_expandGame` / `_expandSnapshot` / `_developGame` / `_developSnapshot` redeclarations in `expand_phase_planner_nw_suppression_test.dart` and `develop_phase_planner_nw_suppression_test.dart`. Pins MUST import `buildExpandPhaseNwSuppressionGame` / `buildExpandPhaseNwSuppressionSnapshot` / `buildDevelopPhaseNwSuppressionGame` / `buildDevelopPhaseNwSuppressionSnapshot` from `packages/colonizethis_ai/test/support/phase_planner_nw_suppression_test_support.dart`. The gate also fails when that shared support file is missing (Refs #3997); rule `repo.ai_phase_planner_nw_suppression_test_shared_fixtures` |
| `tool/check_ai_lock_recovery_seller_test_shared_fixtures.dart` | Forbid local `_lockRecoverySellerGame` redeclarations in `cast_iron_labour_gate_test.dart` and `domain_planner_orchestrator_h8_feedstock_civilian_work_test.dart`. Pins MUST import `buildCastIronLabourLockRecoverySellerGame` / `buildH8FeedstockLockRecoverySellerGame` from `packages/colonizethis_ai/test/support/lock_recovery_seller_test_support.dart`. The gate also fails when that shared support file is missing (Refs #3997); rule `repo.ai_lock_recovery_seller_test_shared_fixtures` |
| `tool/check_ai_treasury_satellite_test_shared_fixtures.dart` | Forbid local `_gameWithStockpile` in `treasury_planner_forecasting_test.dart` and local `_game` in boycott / supplier castIron / trade-deal / offerable-fabric treasury satellite pins. Forecasting MUST import `treasuryPlannerTestGameWithStockpile` from `treasury_planner_main_support.dart`; the other satellites MUST import named factories from `treasury_planner_satellite_support.dart`. The gate also fails when either shared support file is missing (Refs #4104); rule `repo.ai_treasury_satellite_test_shared_fixtures` |
| `tool/check_ai_recruitment_planner_test_shared_fixtures.dart` | Forbid local `_gameWith` in `recruitment_planner_test.dart` and `recruitment_planner_paper_ledger_test.dart`. Pins MUST import `recruitmentPlannerTestGameWith` from `recruitment_planner_test_support.dart`. The gate also fails when that shared support file is missing (Refs #4104); rule `repo.ai_recruitment_planner_test_shared_fixtures` |
| `tool/check_ai_phase_planner_diplomacy_filter_test_shared_fixtures.dart` | Forbid local `_buildGame` in `phase_planner_diplomacy_ow_bonus_scaling_test.dart` and `phase_planner_diplomacy_declare_war_nw_suppression_test.dart`. Pins MUST import `buildPhasePlannerDiplomacyOwBonusScalingGame` / `buildPhasePlannerDiplomacyNwSuppressionGame` from `phase_planner_diplomacy_filter_test_support.dart`. The gate also fails when that shared support file is missing (Refs #4104); rule `repo.ai_phase_planner_diplomacy_filter_test_shared_fixtures` |
| `tool/check_ai_s7d_support_suite_size.dart` | Soft size gate for post-split S7D support modules: under `packages/colonizethis_ai/test/support/s7d/`, any `*.dart` with more than **300** physical lines is a violation (Refs #3997, #4079, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice A, #4365 Slice B, #4602 Slice E); rule `repo.ai_s7d_support_suite_size` |
| `tool/check_ai_observer_suite_size.dart` | Physical-line gate for AI observer/diagnostic suites: under `packages/colonizethis_ai/test/observer/`, any `*.dart` with more than **300** physical lines is a violation (Refs #4530 Slice C, #4602 Slice E); rule `repo.ai_observer_suite_size` |
| `tool/check_ai_test_support_file_size.dart` | Physical-line gate for AI test support modules: under `packages/colonizethis_ai/test/support/` (excluding `s7d/`, which is gated separately), any `*.dart` with more than **300** physical lines is a violation; empty shrink-only grandfather (Refs #4291, #4602 Slice E); rule `repo.ai_test_support_file_size` |
| `tool/check_ai_colonial_military_naval_pin_suite_size.dart` | Forbid oversize COLONIAL military / naval / lite-naval contracts: `colonial_phase_planner_military_test.dart`, `colonial_phase_planner_naval_test.dart`, and `colonial_phase_planner_colonial_lite_naval_test.dart` with more than **300** physical lines that do not import a sibling `*_cases.dart` are violations (Refs #3977, #3997, #4365 Slice C, #4602 Slice D); rule `repo.ai_colonial_military_naval_pin_cases_required` |
| `tool/check_ai_planning_cases_suite_size.dart` | Forbid oversize AI planning case libraries: under `packages/colonizethis_ai/test/planning/`, any `*_cases.dart` with more than **300** physical lines that is not registered in `aiPlanningCasesSuiteSizeAllowlist` (path → issue Ref) is a violation. Prefer topic-splits over allowlist entries (Refs #3997, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4365 Slice B, #4602 Slice D); rule `repo.ai_planning_cases_suite_size` |
| `tool/check_ai_test_file_size.dart` | Physical-line gate for ungated AI planning / support_test Dart: under `packages/colonizethis_ai/test/planning/` and `test/support_test/`, any `*.dart` with more than **300** physical lines is a violation (Refs #4602 Slice D); rule `repo.ai_test_file_size` |
| `tool/check_ai_residual_fat_pin_suite_size.dart` | Forbid oversize residual fat-pin contracts: gated basenames under `packages/colonizethis_ai/test/planning/` and `packages/colonizethis_ai/test/support_test/` (Phase-8/9 set plus Phase-10 Slice C densify: stalled-peace, recruitment, colonial naval scoring, army conquest prep, acquisition declare-war, orchestrator NW-work suppression, expand declare-war/military, growth-stage; Phase-11 Slice C densify: diplomacy-filter, gp-blocker-focus peace; Phase-12 Slice D densify: colonial-lite overtures, peace collectors, mutual-exhausted peace, phase-priority weights, treasury forecasting, colonial civilian, naval plans/peace targets/wiring, colonial planner; full set in `residualFatPinSuiteGatedBasenames`) with more than **300** physical lines that do not import a sibling `*_cases.dart` are violations (Refs #3997, #4079, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4365 Slice B, #4602 Slice D); rule `repo.ai_residual_fat_pin_cases_required` |
| `tool/check_ai_economy_regiment_build_test_shared_fixtures.dart` | Forbid local economy regiment-build Game factory redeclarations in `packages/colonizethis_ai/test/planning/economy_planner_regiment_build*_test.dart` and `*_cases.dart` (the shared `*_support.dart` is exempt). A violation is any `Game _regimentRebuildProductionGame` / `_castIronImprovementInputGame` / `_supplierCastIronSourceGame` / `_castIronFeedstockCoavailabilityGame` / `_castIronStagingNoFabricGateGame` / `_castIronLabourPeasantRecruitFabricStagingGame` declaration inside those pins. Pins MUST import the public factories from `packages/colonizethis_ai/test/planning/economy_planner_regiment_build_input_support.dart`. The gate also fails when that shared support file is missing (Refs #3972); rule `repo.ai_economy_regiment_build_test_shared_fixtures` |
| `tool/check_ai_test_no_matrix_part_shards.dart` | Forbid planning matrix part shards that have already grown a sibling support library. Under `packages/colonizethis_ai/test/planning/`, any `*_partN_test.dart` with `N ≥ 2` is a violation when a same-directory `{stem}_support.dart` exists for that stem (e.g. `foo_matrix_part2_test.dart` next to `foo_matrix_support.dart`). Suites mid-migration without a support file are not flagged. Consolidated suites keep one `*_test.dart` contract plus support / case-table libraries (Refs #3941); rule `repo.ai_test_no_matrix_part_shards` |
| `tool/check_ai_test_mirrors_lib.dart` | Forbid `*_test.dart` sources directly at the `packages/colonizethis_ai/test/` root: AI production code lives under `lib/src/{perception,planning,social,tactical,util}/`, so the testing rule (`colonizethis-testing.mdc`) requires `test/` to mirror `lib/`. Every AI unit test must live in a mirroring subdirectory such as `test/planning/` rather than flat in `test/`. A `*_test.dart` is flagged when the path remainder after the `packages/colonizethis_ai/test/` prefix contains no further separator; nested files such as `test/planning/war_desire_score_test.dart` are compliant. Non-`*_test.dart` scaffolding helpers at the root are out of scope here (governed by `repo.ai_test_no_duplicate_scaffolding`). A documented allowlist (`aiTestMirrorsLibAllowlist`) permits only the three package-level surface tests (`ai_config_test.dart`, `ai_di_export_test.dart`, `seed_bundle_test.dart`). Cross-cutting seed-42 / connectivity / full-AI observer campaigns live under `test/observer/` (Refs #4530). Mirrors the sibling layout gates `repo.orders_test_mirrors_lib` / `repo.combat_test_mirrors_lib` (Refs #3717); rule `repo.ai_test_mirrors_lib` |
| `tool/check_ai_test_seed42_campaign_harness.dart` | Forbid re-inlining the seed-42 Full-AI observer campaign loop in new `packages/colonizethis_ai/test/**/*_test.dart` files. A file is a violation when it contains the seed-42 campaign init literal `GameSetupConfig(seed: 42)` (whitespace tolerant) **and** the trusted-order turn-resolution call `validateOrdersAndResolveTurnFromTrustedOrders`, **and** does not delegate to the shared `runSeed42ObserverCampaign` harness (`packages/colonizethis_ai/test/support/seed42_observer_campaign.dart`). New seed-42 observer campaigns MUST drive the `runInitGame(seed: 42)` → faithful Full-AI handoff → per-turn `generateOrdersForGameFullAI` → `mergeOrderLists` → trusted-order resolve loop through that harness. A documented allowlist (`seed42CampaignHarnessAllowlist`) grandfathers the pre-harness inline campaigns (diagnostics, perf budget pins, and regression suites) that are migrated incrementally as a follow-up phase; the harness support file itself is exempt because it uses the `seed:` parameter rather than the `42` literal. Locks in the #3749 step-2 harness so the inline-loop count cannot grow (Refs #3749); rule `repo.ai_test_seed42_campaign_harness` |
| `tool/check_ai_test_soft_weight_contract.dart` | Keep the Phase-3 soft-weight wiring tests consolidated. The reserved filename suffix `*soft_weight_wiring_test.dart` is restricted to the **parameterized** soft-weight *contract* tests under `packages/colonizethis_ai/test/**`: any file carrying that suffix outside the documented allowlist (`softWeightContractAllowlist` — `phase_planner_colonial_pressure_scaled_bonus_soft_weight_wiring_test.dart` for the shared `scaleWeightedBonus` scaled-magnitude contract and `phase_planner_threshold_cap_soft_weight_wiring_test.dart` for the colonial-pressure threshold-cap contract) is a violation. New soft-weight wiring assertions for a `scaleWeightedBonus`-shaped or threshold-cap helper MUST extend one of those parameterized contract files (add a helper-table row), not spawn a new per-helper clone; behavioural integration pins that observe wiring through `pickBuildOrder` / `evaluateStrategicGoalScores` / `computeDiplomaticCandidateScores` deliberately use other suffixes (`*_cargo_bonus_test.dart`, `*_colonial_pressure_test.dart`, `*_nw_suppression_test.dart`, `*_ow_bonus_scaling_test.dart`) and are out of scope. Locks in the #3749 step-3 consolidation so the per-helper wiring-suite count cannot grow (Refs #3749); rule `repo.ai_test_soft_weight_contract` |
| `tool/check_ai_expand_peace_decider_registry.dart` | Lock in the #3749 step-5 EXPAND-regime peace **decider registry** so peace targets stay composed from ordered data, not hand-unrolled `yield*` chains. The canonical host `packages/colonizethis_ai/lib/src/planning/observer_goal_phase_peace_aggregators.dart` must (1) declare the `typedef ExpandPeaceDecider`, (2) declare both ordered registry constants `List<ExpandPeaceDecider> kSurvivalGreatPowerPeaceDeciders` and `List<ExpandPeaceDecider> kExpandRatchetGreatPowerPeaceDeciders`, and (3) fold each registry via `for (final decider in <registry>)`. Any `yield* <name>(...)` in the host whose target is not the registry loop variable `decider` is a violation (a re-inlined decider chain bypassing the registry). Line comments (`//` / `///`) are stripped before matching so doc prose mentioning `yield*` or the registry symbols is never flagged, and a missing host file fails the gate so the abstraction cannot silently move. Mirrors the sibling `repo.ai_test_*` #3749 gates (Refs #3749); rule `repo.ai_expand_peace_decider_registry` |
| `tool/check_orders_test_mirrors_lib.dart` | Forbid loose Dart sources directly at the `packages/colonizethis_orders/test/` root: orders production code lives under `lib/src/orders/`, so the testing rule (`colonizethis-testing.mdc`) requires `test/` to mirror `lib/`. Every orders test (and its co-located `*_test_support.dart` helper) must live in a mirroring subdirectory such as `test/orders/` (with `validators/`, `validators/diplomatic/`, `work_handlers/` subtrees) rather than flat in `test/`. A file is flagged when the path remainder after the `packages/colonizethis_orders/test/` prefix contains no further separator; nested files such as `test/orders/order_merge_part1_test.dart` are compliant. Mirrors the sibling combat layout gate `repo.combat_test_mirrors_lib` (Refs #3714); rule `repo.orders_test_mirrors_lib` |
| `tool/check_orders_test_preserved_descriptions.dart` | Ensures every single-line `test('…')` / `testWidgets('…')` description in `packages/colonizethis_orders/test/DESCRIPTION_BASELINE.txt` still appears in the orders test tree (including `label:` pins under `test/orders/support/`) after wave-3 runner consolidation (Refs #3949); rule `repo.orders_test_preserved_descriptions` |
| `tool/check_orders_test_support_layout.dart` | Non-`*_test.dart` Dart under `packages/colonizethis_orders/test/orders/` must live under `test/orders/support/` (wave-3 support consolidation; Refs #3949); rule `repo.orders_test_support_layout` |
| `tool/check_orders_test_support_loc.dart` | Physical-line ceilings for `packages/colonizethis_orders/test/orders/support/**/*.dart` (wave-5 support AC ≤13,950) and package `test/**/*.dart` (wave-9 package AC ≤15,800 after counsel ranking densify; Refs #4109, #4246, #4262, #4281, #4282, #4317, #4307, #4508); rule `repo.orders_test_support_loc` |
| `tool/check_orders_test_shorthand_file_size.dart` | Physical-line ceiling (**180**) for every `*expectation_shorthand.dart` under `packages/colonizethis_orders/test/orders/support/**` (Refs #4109 wave 5); rule `repo.orders_test_shorthand_file_size` |
| `tool/check_orders_lib_source_file_size.dart` | Ratchet ceiling for `packages/colonizethis_orders/lib/**` physical lines per file (wave-10 ≤250; empty shrink-only grandfather; generated suffixes excluded); rule `repo.orders_lib_source_file_size` (Refs #4246, #4317, #4508, #4587) |
| `tool/check_app_flame_lib_physical_file_size.dart` | Ratchet ceiling for `app/lib/features/game/flame/**` physical lines per file (wave-21 ≤250; shrink-only grandfather allowlist empty after slice C; generated suffixes excluded); rule `repo.app_flame_lib_physical_file_size` (Refs #4352, #4582, #4606) |
| `tool/check_app_features_screens_lib_physical_file_size.dart` | Ratchet ceiling for `app/lib/features/game/screens/**` physical lines per file (wave-21 ≤250; empty shrink-only grandfather; generated suffixes excluded); rule `repo.app_features_screens_lib_physical_file_size` (Refs #4305 Slice B, #4352, #4582, #4606) |
| `tool/check_app_features_shell_lib_physical_file_size.dart` | Ratchet ceiling for `app/lib/features/shell/**` physical lines per file (wave-21 ≤250; empty shrink-only grandfather; generated suffixes excluded); rule `repo.app_features_shell_lib_physical_file_size` (Refs #4606) |
| `tool/check_orders_test_support_raw_game.dart` | Ceiling on `return Game(` / `=> Game(` under `test/orders/support/**` excluding `common/` (wave-4 migration ratchet; Refs #3971); rule `repo.orders_test_support_raw_game` |
| `tool/check_orders_scenario_table_runner.dart` | Prefer scenario-table runners for new long imperative `test()` bodies under `packages/colonizethis_orders/test/**`; after wave-3 slice 13, baseline allow-all is off and remaining imperative suites are listed in `ordersPreferScenarioTablesAllowlist` / `REFACTOR_TRACE.md` (Refs #3949); rule `repo.orders_test_prefer_scenario_tables` |
| `tool/check_combat_test_no_duplicate_fixtures.dart` | Forbid re-introducing a local copy of the shared game/world test fixtures anywhere under `packages/colonizethis_combat/test/**`: any file named `test_fixtures.dart` (the deleted byte-identical-modulo-doc-comment subset of `package:colonizethis_test/game_test_fixtures.dart`) or any test file that redeclares a top-level `TestFixtures` class (optionally `abstract`/`final`/`base`/`sealed`/`interface`/`mixin`) is a violation. Combat tests must import the canonical `package:colonizethis_test/game_test_fixtures.dart` factories (single source of truth) rather than duplicating them; references to the shared `TestFixtures` symbol are unaffected, and classes whose names merely contain `TestFixtures` (e.g. `MyTestFixturesBuilder`) are not flagged. Complements the testing-rule shared-fixtures policy (Refs #3716); rule `repo.combat_test_no_duplicate_fixtures` |
| `tool/check_map_test_no_duplicate_view_fixtures.dart` | Forbid re-inlining the dual-region `buildInitGameMapViewData` test scaffolding under `packages/colonizethis_map/test/init_game_map_view_builder_*`: a file in scope is a violation when it renders a view (`buildInitGameMapViewData(`) **and** still constructs the dual-region scaffolding inline — an inline `Game(` (`\bGame\(`, so `minimalGame(` is not matched) or `MapTopology(` (`\bMapTopology\(`, so `regionTopology(` / `singleProvinceAndSeaTopology(` are not matched). View-builder tests MUST build their `owMap`/`nwMap`/`owTopology`/`nwTopology`/`Game` setup through the shared `test/support/init_game_map_view_fixtures.dart` builders (`minimalGame`, `regionTopology`, `singleProvinceAndSeaTopology`, `mapTileGrid`); full-line comments are stripped so prose mentioning the constructors is not flagged, and the shared support file plus other map suites (e.g. `tile_map_visualization_test_fixtures.dart`, generator tests with bespoke grids) are out of scope (Refs #3746); rule `repo.map_test_no_duplicate_view_fixtures` |
| `tool/check_map_test_view_builder_file_count.dart` | Hard cap of **4** files matching `packages/colonizethis_map/test/init_game_map_view_builder_*_test.dart` after the #3846 view-builder consolidation; no documentation escape hatch (Refs #3846). After #4022 AC5, the former markers mega-suite is split (`*_markers_test.dart` vs `*_town_civilian_markers_test.dart`) by folding visibility into `*_region_cells_test.dart` so the file count stays ≤4; rule `repo.map_test_view_builder_file_count` |
| `tool/check_map_test_shared_topology_fixtures.dart` | Forbid inline `\bTopologyNode\(` in render/visualizer tests (`test/tile_map_visualization_*`, `test/game_world_state_map_visualizer_test.dart`, `test/multi_region_map_rendering_test.dart`) unless allowlisted: hardcoded exempt files (e.g. `tile_map_topology_validation_test.dart`), `*_validation*_test.dart` basename glob, or a per-site `// map-topology-fixture-exempt: <reason>` marker on the line immediately preceding the inline constructor; `test/support/**` and `test/*_fixtures.dart` are out of scope (Refs #3846); rule `repo.map_test_shared_topology_fixtures` |
| `tool/check_map_test_run_generation_harness.dart` | Forbid inline `\bTileMapGenerator\s*\(` under `packages/colonizethis_map/test/tile_map_generator_*_test.dart`, `tile_map_generation_*`, `tile_map_pass4_*`, `tile_map_forest_*`, and `tile_map_terrain_*` suites unless the preceding line carries `// map-generation-harness-exempt: <reason>` (constructor/DI probes). Integration generates must use `runTileMapGeneration` from `test/support/tile_map_gen_fixtures.dart`; that support file is out of scope (Refs #4022); rule `repo.map_test_run_generation_harness` |
| `tool/check_map_test_minimal_game_shared.dart` | Forbid inline `\bGame\(` in the scoped colour/capital/region-input/format/port suites (`faction_ownership_color_test.dart`, `tile_map_capital_markers_test.dart`, `region_map_view_inputs_test.dart`, `map_format_util_test.dart`, `port_icon_placement_test.dart`); those suites must use `minimalGame` from `test/support/init_game_map_view_fixtures.dart`. Full-line comments are stripped; `minimalGame(` is not matched (Refs #4022); rule `repo.map_test_minimal_game_shared` |
| `tool/check_map_test_file_size.dart` | Physical-line ceiling **250** for every `packages/colonizethis_map/test/**` Dart file unless manifest-baseline grandfathered (Refs #4112, #4561); rule `repo.map_test_file_size` |
| `tool/check_map_render_legend_layout_dedup.dart` | Forbid inline `legendPadding * 2 + legendLines * legendLineHeight` outside `tile_map_visualization_legend_layout.dart` (canonical `legendHeightForLineCount` owner); full-line comments stripped (Refs #4112); rule `repo.map_render_legend_layout_dedup` |
| `tool/check_map_tile_marker_sort_sot.dart` | Forbid inlined y then x then `tileKey` sorts under `packages/colonizethis_map/lib/src/view/**` outside `init_game_map_view_tile_marker_sort.dart` (Refs #4654); rule `repo.map_tile_marker_sort_sot` |
| `tool/check_combat_test_mirrors_lib.dart` | Keep the `packages/colonizethis_combat/test/**` tree mirrored to `lib/` per the testing rule (`test/` mirrors `lib/`): any Dart source that sits **directly** at the `test/` root (its repo-relative path after `packages/colonizethis_combat/test/` contains no further `/`) is a violation — both `*_test.dart` files and co-located support helpers must live in a subdirectory such as `test/combat/` that mirrors `lib/src/combat/`. Files already nested under a subdirectory (e.g. `test/combat/combat_rng_test.dart`) are compliant; generated suffixes and fixture trees are skipped via the shared scan contract. Complements the testing-rule `test/`-mirrors-`lib/` policy (Refs #3716); rule `repo.combat_test_mirrors_lib` |
| `tool/check_combat_test_no_local_support.dart` | Forbid package-local `*_test_support.dart` files under `packages/colonizethis_combat/test/**`: canonical home is `colonizethis_combat_test_support` (Refs #3865); rule `repo.combat_test_no_local_support` |
| `tool/check_combat_test_core_fixtures_shared.dart` | Forbid inline `Game(` construction under `packages/colonizethis_combat/test/**`; use shared builders from `colonizethis_combat_test_support` (Refs #3865); rule `repo.combat_test_core_fixtures_shared` |
| `tool/check_combat_test_duplicate_descriptions.dart` | Preserve unique `test('…')` descriptions during combat table merges — fail when the same description appears in more than one file under `packages/colonizethis_combat/test/**` (Refs #3865); rule `repo.combat_test_duplicate_descriptions` |
| `tool/check_orders_no_cross_package_src_imports.dart` | Forbid `colonizethis_orders/lib/**` from deep-importing another colonizethis package's private `lib/src/**` tree: any `import 'package:colonizethis_<pkg>/src/...'` directive where `<pkg>` is not `orders` is a violation; cross-package symbols must be consumed through the owning package's public barrel `package:colonizethis_<pkg>/colonizethis_<pkg>.dart`. Self-imports of `package:colonizethis_orders/src/...` and relative imports are out of scope; generated suffixes (`*.g.dart`, `*.freezed.dart`, `*.mocks.dart`) are skipped. Stronger than `repo.domain_package_barrel_import` (which only blocks deep imports of barrel-*published* files): this rule blocks **all** sibling `src/` imports so the orders package never reaches into another package's internals (Refs #3543); rule `repo.orders_no_cross_package_src_imports` — see `SPEC/program/logic-package-barrel-contracts.md` |
| `tool/check_world_test_no_upstream_domain_deps.dart` | Forbid `package:colonizethis_logic/`, `package:colonizethis_turn/`, or `package:colonizethis_orders/` imports under `packages/colonizethis_world/test/**` (including `world_test_support/`) and forbid those packages as keys in `packages/colonizethis_world/pubspec.yaml`; complements `repo.world_no_logic_deps` (lib-only) (Refs #4515); rule `repo.world_test_no_upstream_domain_deps` — see `SPEC/program/world-test-no-upstream-domain-deps.md` |
| `tool/check_world_no_circular_imports.dart` | Build the file-level `import` / `export` graph among `packages/colonizethis_world/lib/**` hand-written Dart files — resolving relative imports and `package:colonizethis_world/...` self-imports to files inside that tree, ignoring `dart:` / other-package URIs, `part` / `part of` directives, and generated suffixes (`*.g.dart`, `*.freezed.dart`, `*.mocks.dart`) — and fail when any directed cycle exists. Complements `repo.world_no_logic_deps` (cross-package logic deps) and `repo.domain_package_import_dag` (cross-package direction) by catching intra-package cycles such as the former `province_lookup.dart` ↔ `province_owner_cache.dart` and `naval.dart` ↔ `topology_helpers.dart` two-node cycles, which prevented either file from being understood or unit-tested in isolation. The remediation is to extract the shared symbol into a third file imported by both sides; the checker reports one offending cycle as an ordered file chain (Refs #3544); rule `repo.world_no_circular_imports` |
| `tool/check_world_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_world/lib/**` so fog, capital/GP fall, and army-migration sub-files stay proper libraries with explicit imports (mirrors turn/diplomacy/orders no-part gates; Refs #3968); rule `repo.world_no_part_directives` |
| `tool/check_combat_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_combat/lib/**` so land-resolver sub-files stay proper libraries with explicit imports (mirrors turn/orders/world no-part gates; Refs #3983); rule `repo.combat_no_part_directives` |
| `tool/check_combat_test_scenario_harness.dart` | Forbid bare `for (final scenario in …)` loops under `packages/colonizethis_combat/test/**/*_test.dart`; scenario tables must register via `runLabeledScenarios` / `runLabeledScenarioGroup` from `colonizethis_combat_test_support` (Refs #3983); rule `repo.combat_test_scenario_harness` |
| `tool/check_economy_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_economy/lib/**` so DealMatcher matching/indexing sub-files stay proper libraries with explicit imports (mirrors world/turn/diplomacy/orders no-part gates; Refs #3979); rule `repo.economy_no_part_directives` |
| `tool/check_models_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_models/lib/**` so orders/world_state/world_market/app_events sub-files stay proper libraries with explicit imports (mirrors economy/world/turn no-part gates; Refs #4068); rule `repo.models_no_part_directives` |
| `tool/check_ai_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_ai/lib/**` so planning/perception sub-files stay proper libraries with explicit imports (mirrors economy/world/turn/models no-part gates; Refs #4079); rule `repo.ai_no_part_directives`. Also hosts the shared parameterized scanner used by `repo.ai_contracts_no_part_directives` (Refs #4084). |
| `tool/check_ai_contracts_no_part_directives.dart` | Forbid Dart `part` / `part of` directives under `packages/colonizethis_ai_contracts/lib/**` so Full AI civilian selection / heuristics / localization sub-files stay proper libraries with explicit imports (mirrors `repo.ai_no_part_directives`; Refs #4084) — see `SPEC/program/ai-contracts-no-part-directives.md`; rule `repo.ai_contracts_no_part_directives` |
| `tool/check_ai_contracts_h8_test_shared_fixtures.dart` | Forbid local `_flaggedSellerGame` redeclarations in the three H8 seller feedstock acquisition-target pins and local `_supplierGame` redeclarations in the OW feedstock prospect localization / mineral-eligibility pins under `packages/colonizethis_ai_contracts/test/`. Pins MUST import `flaggedSellerGame` / `supplierGame` from `test/support/h8_flagged_seller_game.dart` and `test/support/h8_supplier_prospect_game.dart`. The gate also fails when either shared support file is missing (Refs #4084); rule `repo.ai_contracts_h8_test_shared_fixtures` |
| `tool/check_ai_contracts_source_file_size.dart` | Ratchet ceiling for `packages/colonizethis_ai_contracts/lib/**` physical lines per file (wave-2 ≤**260**; empty shrink-only grandfather; generated suffixes excluded); stricter than the shared 500-line `repo.domain_package_source_file_size` cap; rule `repo.ai_contracts_source_file_size` (Refs #4368) |
| `tool/check_ai_contracts_test_file_size.dart` | Ratchet ceiling for `packages/colonizethis_ai_contracts/test/**` physical lines per file (wave-2 ≤**280**; empty shrink-only grandfather); stricter than `repo.domain_package_test_file_size` (400); rule `repo.ai_contracts_test_file_size` (Refs #4368) |
| `tool/check_ai_source_file_size.dart` | Enforce `packages/colonizethis_ai/lib/src/**` Dart files at **250 physical lines or fewer**, with an optional shrink-only grandfather allowlist that fails when entries are missing or now under-cap (mirrors save/data size gates; headroom ratchet after #4104 Slice B, #4239 Slice A, #4310 Slice B, #4365 Slice A, and #4602 Slice A near-cap splits; Refs #4079 Slice C, #4104 Slice B, #4239 Slice A, #4310, #4365, #4602) — see `SPEC/program/ai-source-file-size.md`; rule `repo.ai_source_file_size` |
| `tool/check_models_test_file_size.dart` | Fail when any `.dart` file under `packages/colonizethis_models/test/**` exceeds 250 physical lines (wave-4 ratchet; Refs #4068 Slice D, #4571); rule `repo.models_test_file_size` |
| `tool/check_models_lib_physical_file_size.dart` | Fail when any non-generated `.dart` file under `packages/colonizethis_models/lib/**` exceeds 250 physical lines (wave-4 ratchet 300→250; empty shrink-only grandfather; Refs #4334, #4571); rule `repo.models_lib_physical_file_size` |
| `tool/check_models_test_mirrors_lib.dart` | Keep the `packages/colonizethis_models/test/**` tree mirrored to `lib/` per the testing rule (`test/` mirrors `lib/`): any Dart source that sits **directly** at the `test/` root is a violation — tests must live in subdirectories such as `test/src/`, `test/app_events/`, or `test/diplomacy/` that mirror `lib/src/` (Refs #4334); rule `repo.models_test_mirrors_lib` |
| `tool/check_world_topology_identity_home.dart` | Require topology identity helpers (`indexTopologyNodesByRegion`, `provinceTopologyNodeId`, `provinceIdsAdjacentToSeaZone`, `regionIdForSeaZone`) to be defined in `packages/colonizethis_world/lib/src/world/topology_identity.dart` and forbid re-declaring them as top-level functions in `naval.dart` (re-export allowed; Refs #3968); rule `repo.world_topology_identity_home` |
| `tool/check_world_test_support_builders_used.dart` | Require named world test-support builders (`spyRevealFogGame`, `ownershipTransferVisibilityGame`, `coastalSeaVisibilityGame`, `capitalLossGame`, `gpCapitalLossGame`, `minorCapitalLossGame`, `gpCapitalReassignmentGame`, `factionCapitalReassignmentGame`, `ordersPhaseGame`, `topologyGraph`) to have ≥1 call site under `packages/colonizethis_world/test/**` outside `world_test_support/` so scenario factories cannot rot unused (Refs #3968 / #4038); rule `repo.world_test_support_builders_used` |
| `tool/check_world_fog_connectivity_no_owner_map_rebuild.dart` | Forbid fog/connectivity world lib files from calling `ownerByProvinceIdMap(...)`; ownership must use `ProvinceOwnerCache.of` (Refs #3978); rule `repo.world_fog_connectivity_no_owner_map_rebuild` — see `SPEC/program/world-fog-connectivity-ownership-sot.md` |
| `tool/check_world_test_no_raw_game_worldstate.dart` | Forbid fog/capital/connectivity/ownership/player_view/travers world tests outside `world_test_support/` from inlining `Game(...)` / `WorldState(...)`; use shared builders or `TestFixtures` (Refs #3978, #4038); rule `repo.world_test_no_raw_game_worldstate` — see `SPEC/program/world-test-no-raw-game-worldstate.md` |
| `tool/check_logic_dual_region_province_field_access.dart` | Caps direct `oldWorld/newWorld` `provinces` / `units` references in `packages/colonizethis_world/lib/src/**` outside canonical lookup files (`province_lookup.dart`, `province_lookup_extension.dart`, `province_lookup_indexes.dart`, `unit_lookup.dart`) (GitHub #2071; post-split scan root Refs #3290; wave-5 split Refs #4125); rule `repo.logic_dual_region_province_field_access` — see `SPEC/program/logic-dual-region-province-access.md` |
| `tool/check_world_no_duplicate_extension_helper.dart` | Forbid a top-level helper in `packages/colonizethis_world/lib/**` from re-implementing a same-named `WorldState` extension method instead of delegating to it. AST-based: any top-level function whose name is in the watched set (initially `allProvinces`) must have a thin delegating body — either `=> <target>.<name>()` or a single `{ return <target>.<name>(); }` calling the same-named no-argument extension method — otherwise it is flagged as a parallel definition. Closes the gap that let the duplicate `allProvinces(WorldState)` free function and `WorldState.allProvinces()` extension drift apart (Refs #3710); rule `repo.world_no_duplicate_extension_helper` |
| `tool/check_world_lib_unit_lookup_sot.dart` | Forbid non-comment `allUnitsFromWorld(` call sites under `packages/colonizethis_world/lib/src/**` except the canonical `unit_lookup.dart` facade; world `lib/src` must iterate `WorldState.allUnitsById.values`. Other packages that still call `allUnitsFromWorld` are out of scope (Refs #4611); rule `repo.world_lib_unit_lookup_sot` |

| `tool/check_logic_units_by_id_rebuild.dart` | Forbid non-canonical `unitsByIdFromWorld(` call sites across the split domain package source trees (`world`, `combat`, `economy`, `diplomacy`, `setup`, `orders`, `turn`, `ai_contracts` `lib/src/**`, plus the thin `colonizethis_logic/lib/src`), excluding the canonical `packages/colonizethis_world/lib/src/world/unit_lookup.dart`; consumers MUST use the cached `WorldState.allUnitsById` getter so the per-`WorldState` unit index is reused on the order-suggestion / turn-resolution hot path (GitHub #2836 AC 2; post-split scan roots Refs #3290); rule `repo.logic_units_by_id_rebuild` |
| `tool/check_logic_dead_files.dart` | Unreferenced `lib/src` scan for `colonizethis_logic`; rule `repo.logic_dead_files` — see subsection below |
| `tool/check_logic_no_map_deps.dart` | Forbid production imports/exports of `package:colonizethis_map/...` anywhere under `packages/colonizethis_logic/lib/**`: the map package is a **dev-only** dependency of `colonizethis_logic` (logic `lib/` has zero map imports; only two integration tests reference the map barrel), so the runtime dependency direction must not point logic → map. Comment lines and generated suffixes (`*.g.dart`, `*.freezed.dart`, `*.mocks.dart`) are ignored; same-prefix package names (e.g. `colonizethis_mapper`) are not matched because the directive must contain `package:colonizethis_map/`. Test code under `packages/colonizethis_logic/test/**` is out of scope (Refs #3574, slice 6); rule `repo.logic_no_map_deps` |
| `tool/check_domain_package_dead_files.dart` | Workspace-wide unreferenced `lib/src` scan for the eight split logic-domain packages (`world`, `combat`, `economy`, `diplomacy`, `setup`, `orders`, `turn`, `ai_contracts`); rule `repo.domain_package_dead_files` (Refs #3290) — see subsection below |
| `tool/check_logic_dedup_logger.dart` | Forbid bare anonymous `final _log = packageLogger();` declarations across the eight split logic-domain package source trees (`world`, `combat`, `economy`, `diplomacy`, `setup`, `orders`, `turn`, `ai_contracts` `lib/src`) plus the thin `colonizethis_logic/lib/src` core (Refs #2391 Pattern 1; post-split scan roots Refs #3290); files MUST consume their package's shared package logger (for example `logicLog` from `package:colonizethis_logic/package_logger.dart`); rule `repo.logic_dedup_logger` |
| `tool/check_domain_package_logger_dedup.dart` | Forbid copy-pasted `packageLogger()` factory bodies across every `packages/<pkg>/lib/package_logger.dart`: a violation is any inline `CtLogger(` construction (the duplicated prefix-composition body). Each per-package `packageLogger([subPrefix])` wrapper MUST delegate to the shared `domainPackageLogger(kPackageLogPrefix, subPrefix)` factory exported from `package:colonizethis_logger/colonizethis_logger.dart`; the single canonical `CtLogger(` construction lives only in `packages/colonizethis_logger/lib/src/package_domain_logger.dart` (not scanned). `export '...' show CtLogger;` and the `CtLogger packageLogger(...)` return-type annotation are not flagged (neither places `(` immediately after the `CtLogger` token) (Refs #3393 Phase 2); rule `repo.domain_package_logger_dedup` |
| `tool/check_ai_api_narrow_surface.dart` | Forbid barrel-bypassing deep `src/` exports in `packages/colonizethis_logic/lib/ai_api.dart`: every `export 'package:colonizethis_<domain>/src/<file>'` directive is a violation when the owning domain barrel (`package:colonizethis_<domain>/colonizethis_<domain>.dart`) already re-exports `<file>` in its transitive `export` closure; AI-contract symbols must route through the domain barrel (`show ...`) in that case. Deep exports of files the barrel does not publish are allowed (no barrel alternative exists); the predicate reads only live barrel contents (no keyed waiver data). See `SPEC/program/logic-package-barrel-contracts.md` (Refs #3393 Phase 3); rule `repo.ai_api_narrow_surface` |
| `tool/check_domain_package_barrel_import.dart` | Forbid barrel-bypassing deep `src/` **imports** between split domain packages on migrated boundaries: for each enforced consumer→target pair, an `import 'package:colonizethis_<target>/src/<file>'` directive in the consumer package's `lib/**` is a violation when `<file>` is in the target barrel's transitive `export` closure (granularity is per file, matching the "files the barrel does not publish at all" carve-out). Consumers MUST import `package:colonizethis_<target>/colonizethis_<target>.dart` instead. Deep imports of files the barrel omits remain allowed; a `show`/`hide`-restricted re-export is treated as not fully publishing the file, so deep imports of partially-exported files (e.g. `world/fog_resolution.dart`) also remain allowed; deliberate narrow deep `export` re-exports are out of scope (only `import` directives are scanned); generated files are skipped. The enforced boundary set covers `turn → {combat, diplomacy, economy, orders, world}`, `orders → {diplomacy, economy, world}`, `combat → {world}`, `economy → {world}`, `logic → {economy, orders, world}`, `diplomacy → {economy, world}`, and `setup → {diplomacy, world}`, growing as later Phase 1 slices migrate more pairs (the `logic`/`diplomacy` → `economy` pairs were added when the world-market admission/deal-matcher/bid-cap re-exports were routed through the economy barrel, Refs #3615 Cluster 5). See `SPEC/program/logic-package-barrel-contracts.md` (Refs #3393 Phase 1); rule `repo.domain_package_barrel_import` |
| `tool/check_ai_dedup_gp_wars_filter.dart` | Forbid inline GP-wars filters under `packages/colonizethis_ai/lib/**` outside the canonical helper file `src/planning/planning_helpers.dart`, in either the comprehension form (`for (final … in <expr>.atWarWith) … playerById(…) != null`) or the functional form (`<expr>.atWarWith.any/where((id) => playerById(id) != null)`, matched within the same statement so non-`playerById` `atWarWith.any(...)` predicates and later unrelated `playerById` tests are not flagged); planner/filter code MUST call the shared `gpFactionIdsAtWarWith(game, snapshot)` (id list/length) or `isAtWarWithAnyGreatPower(game, snapshot)` (boolean presence) helper. Also forbids the inline **non-GP peace-collector** comprehension (`for (final … in <expr>.atWarWith) if (playerById(…) == null) <id>,`), anchored on the collected comprehension element so the `playerById(…) == null) continue;` skip loop is not flagged; planner code MUST call the shared `nonGreatPowerAtWarPeaceTargetsWhere(game: …, snapshot: …)` collector instead (Refs #3278, #3717, #3749); rule `repo.ai_dedup_gp_wars_filter` |
| `tool/check_ai_dedup_minor_tribe_membership.dart` | Forbid inline minor/tribe roster-membership predicates under `packages/colonizethis_ai/lib/**` outside the canonical helper file `src/util/faction_query.dart`: a violation is any `(minorNations\|tribes).any((param) => param.id == …)` call — the roster-membership form. Planner/scoring code MUST call the shared `isMinorFaction(game, id)`, `isTribeFaction(game, id)`, or `isMinorOrTribeFaction(game, id)` helper. Roster scans whose predicate is **not** a `param.id ==` equality test (e.g. `minorNations.any((m) => ownerCache.ownsAnyInRegion(m.id, kRegionOldWorld))` or `minorNations.any((m) => _minorOwnsOldWorldProvinces(game, m.id))`) are not flagged (Refs #3717); rule `repo.ai_dedup_minor_tribe_membership` |
| `tool/check_diplomacy_no_duplicate_overture_clear.dart` | Forbid re-inlining the overture pair-clearing filter under `packages/colonizethis_diplomacy/lib/**` outside the canonical helper file `src/diplomacy/diplomacy_shared_helpers.dart`: a violation is any `.where(...)` predicate that **negates a GP/target pair match** (the closure body contains `!(` together with both `.gpId` and `.targetId`), the shape used by the old intervention do-nothing and war-termination clears. Overture clearing between a GP and a faction MUST route through `clearOverturesBetweenGpAndFaction(game, gpId, factionId, {bidirectional})`. Positive overture lookups (`firstWhere`/`indexWhere` equality without `!(`), single-faction teardown filters (`!=` on one id, as in faction absorption), and pair-key set-membership diffs do not match this signature (Refs #3562 AC7); rule `repo.diplomacy_no_duplicate_overture_clear` |
| `tool/check_diplomacy_test_no_duplicate_fixtures.dart` | Forbid re-introducing a local copy of the shared game/world test fixtures under `packages/colonizethis_diplomacy/test/**`. A violation is any test file named `test_fixtures.dart` (the deleted byte-identical subset of `package:colonizethis_test/game_test_fixtures.dart`) or any test file that redeclares a top-level `TestFixtures` class (optionally `abstract`/`final`/`base`/`sealed`/`interface`/`mixin`). Diplomacy tests MUST import the shared `TestFixtures` from `package:colonizethis_test/game_test_fixtures.dart`; references to that shared symbol are untouched, and classes whose names merely contain `TestFixtures` (e.g. `MyTestFixturesBuilder`) are not flagged (Refs #3715); rule `repo.diplomacy_test_no_duplicate_fixtures` |
| `tool/check_diplomacy_test_mirrors_lib.dart` | Keep the `packages/colonizethis_diplomacy/test/**` tree mirrored to `lib/` per the testing rule (`test/` mirrors `lib/`): any Dart source that sits **directly** at the `test/` root (its repo-relative path after `packages/colonizethis_diplomacy/test/` contains no further `/`) is a violation — both `*_test.dart` files and co-located support helpers must live in a subdirectory such as `test/diplomacy/` or `test/dossier/` that mirrors `lib/src/`. Files already nested under a subdirectory (e.g. `test/diplomacy/diplomacy_ftp_resolver_test.dart`) are compliant; generated suffixes and fixture trees are skipped via the shared scan contract. Complements the testing-rule `test/`-mirrors-`lib/` policy (Refs #3715); rule `repo.diplomacy_test_mirrors_lib` |
| `tool/check_diplomacy_phase_types_value_equality.dart` | Forbid hand-written `operator ==` declarations in the Diplomacy-phase value-type files under `packages/colonizethis_diplomacy/lib/src/diplomacy/phase_types/**` (the Offer/Decision/Prompt/Pending pairs). Each value type MUST derive structural equality from the shared `ValueEquality` mixin (canonical source `phase_types/value_equality.dart`) by mixing it in and exposing its identifying fields via `equalityFields`, instead of re-implementing the identical `operator ==` + `hashCode` boilerplate per class. The canonical `value_equality.dart` mixin file (which legitimately declares `operator ==`) is exempt, and non-`.dart` files are ignored (Refs #3715); rule `repo.diplomacy_phase_types_value_equality` |
| `tool/check_diplomacy_event_logging_unified.dart` | Forbid raw `appendDiplomaticEvent(` call sites under `packages/colonizethis_diplomacy/lib/**` resolver modules outside the canonical `src/diplomacy/diplomacy_event_logging.dart` definition file; resolver paths MUST append operator-logged history via `logDiplomaticEvent` (Refs #3825); rule `repo.diplomacy_event_logging_unified` |
| `tool/check_diplomacy_treasury_debit_shared.dart` | Forbid inline `.treasury -` debit patterns under `packages/colonizethis_diplomacy/lib/**` outside `diplomacy_shared_helpers.dart` (`debitPlayerTreasury` is the sanctioned helper) (Refs #3825); rule `repo.diplomacy_treasury_debit_shared` |
| `tool/check_diplomacy_no_loop_upsert_relation.dart` | Forbid `upsertRelation(` inside `for`/`while` bodies and more than one standalone `upsertRelation(` call in the same function body under `packages/colonizethis_diplomacy/lib/**` (definition file exempt); batched relation mutations MUST use `RelationUpsertIndex` (Refs #3825, #3837); rule `repo.diplomacy_no_loop_upsert_relation` |
| `tool/check_diplomacy_no_list_relation_wrappers.dart` | Forbid deleted list-returning wrappers `setWarStateForPair` / `applyPeaceForPair` / `applyGrantAidModifier` / `applySubsidyBoost` under `packages/colonizethis_diplomacy/lib/**`; use updater factories + `RelationUpsertIndex` (Refs #4037); rule `repo.diplomacy_no_list_relation_wrappers` |
| `tool/check_diplomacy_test_duplicate_descriptions.dart` | Fail when the same single-line `test('...')` / `testWidgets('...')` description appears in more than one file under `packages/colonizethis_diplomacy/test/**` (Refs #3825); rule `repo.diplomacy_test_duplicate_descriptions` |
| `tool/check_diplomacy_test_inline_game_ctor.dart` | Forbid inline `Game(` constructor calls under **all** `packages/colonizethis_diplomacy/test/**` Dart files (exceptions-only allowlist; empty after wave-3 fixture migration). Suites MUST build games through shared helpers in `package:colonizethis_diplomacy_test_support` (Refs #3825, #3837, #4028); rule `repo.diplomacy_test_inline_game_ctor` |
| `tool/check_diplomacy_relation_upsert_commit.dart` | Forbid raw `diplomacyRelations: relationsIndex.toList()` under `packages/colonizethis_diplomacy/lib/**` outside `diplomacy_relation_upsert.dart`; commits MUST use `committedRelations` / `withCommittedRelations` (Refs #4028); rule `repo.diplomacy_relation_upsert_commit` |
| `tool/check_diplomacy_test_support_loc.dart` | Ratchet ceiling for `packages/colonizethis_diplomacy_test_support/lib/**` physical Dart LOC (≤536 after wave-5 slice E fixture relocation; was ≤882, ≤1084, ≤2324 after slice B, ≤2488 after wave-4); rule `repo.diplomacy_test_support_loc` (Refs #4028, #4037, #4130) |
| `tool/check_diplomacy_test_no_src_imports.dart` | Forbid `package:colonizethis_diplomacy/src/` imports under `packages/colonizethis_diplomacy/test/**` except the allowlisted `diplomacy_phase_types_split_test.dart` deep-import gate; tests MUST use the public barrel or `colonizethis_diplomacy_test_support` (Refs #3837); rule `repo.diplomacy_test_no_src_imports` |
| `tool/check_diplomacy_test_file_size.dart` | Fail when any `.dart` file under `packages/colonizethis_diplomacy/test/**` exceeds 250 physical lines; rule `repo.diplomacy_test_file_size` (Refs #4574) |
| `tool/check_diplomacy_lib_file_size.dart` | Ratchet ceiling for `packages/colonizethis_diplomacy/lib/**` physical lines per file (wave-7 ≤250; shrink-only grandfather allowlist empty; generated suffixes excluded); rule `repo.colonizethis_diplomacy_lib_file_size` (Refs #4130, #4341, #4574) |
| `tool/check_diplomacy_test_support_file_size.dart` | Ratchet ceiling for `packages/colonizethis_diplomacy_test_support/lib/**` physical lines per file (wave-5 ≤400; shrink-only grandfather allowlist empty after slice E densify; generated suffixes excluded); rule `repo.colonizethis_diplomacy_test_support_file_size` (Refs #4130) |
| `tool/check_diplomacy_test_support_package_only.dart` | Forbid package-local fixture modules under `packages/colonizethis_diplomacy/test/support/**` and new `*_test_support.dart` shims under `packages/colonizethis_diplomacy/test/**`; shared fixtures MUST live in `colonizethis_diplomacy_test_support` (Refs #3837); rule `repo.diplomacy_test_support_package_only` |
| `tool/check_ai_dedup_weight_scale_clamp.dart` | Forbid the inline weight-scale clamp idiom (`<= 0.0) { return 0; }` guard + `final clamped = X > 1.0 ? 1.0 : X;` + `return (Y * clamped).round();`) under `packages/colonizethis_ai/lib/**` outside the canonical helper file `src/planning/planning_helpers.dart`; filter/scoring code MUST call the shared `scaleWeightedBonus(weight, baseConstant)` helper (Refs #3278); rule `repo.ai_dedup_weight_scale_clamp` |
| `tool/check_ai_dedup_colonial_pressure_scale.dart` | Forbid inline colonial-pressure null-fallback ternaries (`… ? 1.0 : 0.0`) under `packages/colonizethis_ai/lib/**` outside `src/planning/planning_helpers.dart`; planner code MUST call the shared `colonialPressureScaleFromWeight` helper (Refs #3822); rule `repo.ai_dedup_colonial_pressure_scale` |
| `tool/check_ai_dedup_commodity_ids.dart` | Forbid direct `CommodityCatalog.fabric.id`, `CommodityCatalog.castIron.id`, and `CommodityCatalog.lumber.id` references under `packages/colonizethis_ai/lib/**` outside `src/planning/ai_commodity_ids.dart`; planner code MUST use `kAiCommodityIds` (Refs #3822); rule `repo.ai_dedup_commodity_ids` |
| `tool/check_ai_dedup_list_equals.dart` | Forbid local `_listEquals` / `_colonialListEquals` definitions under `packages/colonizethis_ai/lib/**` outside `src/planning/planning_helpers.dart`; phase planner value types MUST call `planningListEquals` (Refs #3822); rule `repo.ai_dedup_list_equals` |
| `tool/check_ai_no_repeated_production_recipes_catalog_scan.dart` | Forbid member access `ProductionRecipesCatalog.all` inside `packages/colonizethis_ai/lib/src/planning/treasury_planner.dart`; the treasury-planner hot path MUST resolve recipes by output commodity through the O(1) index `ProductionRecipesCatalog.producing(commodityId)` (backed by `byOutputCommodityId`) instead of O(recipes) full-catalog scans, protecting the 15 000 ms next-turn budget (`SPEC/program/turn-resolution.md`). `ProductionRecipesCatalog.all` remains allowed in other AI files such as `economy_planner.dart` (Refs #3288); rule `repo.ai_no_repeated_production_recipes_catalog_scan` |
| `tool/check_ai_no_repeated_game_players_scan.dart` | Forbid member access `game.players` inside `packages/colonizethis_ai/lib/src/planning/treasury_planner.dart`; the treasury-planner hot path MUST resolve single players through the O(1) lookup `game.playerById(playerId)` and compute per-turn aggregates (treasury totals, trade-eligible players, offer-clearing capacity) in one precomputed pass instead of repeated O(players) `game.players` scans, protecting the 15 000 ms next-turn budget (`SPEC/program/turn-resolution.md`). `game.players` iteration remains allowed in other AI files such as `economy_planner.dart`, `full_ai_planner.dart`, and the lock-recovery rotation in `treasury_lock_recovery.dart` (Refs #3288); rule `repo.ai_no_repeated_game_players_scan` |
| `tool/check_economy_cost_check_shared_helper.dart` | Require the two economy cost-check consumers `packages/colonizethis_economy/lib/src/economy/worker_action_cost.dart` and `.../build_cost.dart` to keep calling the shared `checkPreconditionsInOrder` helper (defined in `.../cost_check.dart`) so the canonical affordability priority order (tech → workers → treasury → materials) and failure-reason vocabulary are not re-inlined / duplicated across the recruit-worker (`canAffordRecruitWorker`) and build-unit (`_resolveBuildPlanForCatalog`) paths. Fails when either consumer drops the helper call or when `cost_check.dart` no longer defines the helper; the canonical helper file is exempt from the consumer requirement (Refs #3517 Cluster 2); rule `repo.economy_cost_check_shared_helper` |
| `tool/check_economy_force_feeding_shared_helper.dart` | Require `previewForceFeeding` (`force_feeding_readiness.dart`) and `allocateConsumption` (`economy_consumption_allocation.dart`) to call shared `allocateMilitaryNavyFood`; fail when `force_feeding_readiness.dart` calls `consumeMilitaryFood` / `consumeNavyFood` or inlines `RegimentEconomyCatalog` (Refs #4631); rule `repo.economy_force_feeding_shared_helper` |
| `tool/check_economy_grain_bonus_shared_helper.dart` | Require `computeExtraction`, `developmentExtractionProjectionForRegion`, and `computeProvinceExtractionSnapshots` to call `capitalTileGrainBonusForPlayer`; fail non-comment `capitalTileGrainBonusPerTurn` adds outside that helper (Refs #4631); rule `repo.economy_grain_bonus_shared_helper` |
| `tool/check_economy_world_market_admission_shared.dart` | Require the two world-market consumers `packages/colonizethis_economy/lib/src/economy/world_market/trade_order_validator.dart` and `.../trade_order_suggester.dart` to keep importing the shared `trade_order_admission.dart` module so the rule-2 (tradeable), rule-3 (mutual exclusion) and rule-4 (submission-order bid admission) contracts are not re-inlined across the validator and suggester. Fails when either consumer drops the import or when the module no longer defines `isWorldMarketTradeableCommodity`, `commoditiesWithBidAndOffer`, or `admittedBidCommodityIdsInSubmissionOrder`; the canonical module is exempt from the consumer requirement (Refs #3615 Cluster 1); rule `repo.economy_world_market_admission_shared` |
| `tool/check_economy_dedup_port_tile_keys.dart` | Forbid re-inlining the port-seaboard tile-key comprehension `portsByProvinceSeaboard.values.toSet()` anywhere under `packages/colonizethis_economy/lib/**` outside the shared `src/economy/game_lookup_helpers.dart` module: callers must use `collectPortTileKeys(game)` so the per-seaboard port tile-key set has one source of truth; the shared module is exempt and comment lines are ignored (Refs #3615 Cluster 3, #3396 Cluster 5); rule `repo.economy_dedup_port_tile_keys` |
| `tool/check_economy_test_duplicate_descriptions.dart` | Forbid the **same** single-line `test('...')` / `testWidgets('...')` description string from appearing in **more than one** file under `packages/colonizethis_economy/test/`, locking in the #3661 dedup pass so a copy-pasted scenario cannot re-spread across slice files (each numeric contract keeps a single source of truth). Within-file repetition across groups is allowed, comment lines are ignored, and Dart adjacent-string (multi-line) descriptions are intentionally not matched (Refs #3661); rule `repo.economy_test_duplicate_descriptions` |
| `tool/check_economy_test_no_duplicate_fixtures.dart` | Forbid re-introducing a local copy of the shared game/world test fixtures under `packages/colonizethis_economy/test/**`. A violation is any test file named `test_fixtures.dart` or any test file that redeclares a top-level `TestFixtures` class. Economy tests MUST import the shared `TestFixtures` from `package:colonizethis_test/game_test_fixtures.dart` (Refs #3823); rule `repo.economy_test_no_duplicate_fixtures` |
| `tool/check_economy_test_no_local_world_market_support.dart` | Forbid any `*_test_support.dart` basename under `packages/colonizethis_economy/test/**`; canonical home is `colonizethis_economy_test_support` (Refs #3823, #3831, #4631); rule `repo.economy_test_no_local_world_market_support` |
| `tool/check_economy_gp_treasury_rollup_shared.dart` | Require `first_right_credit_records.dart` and `purchased_tile_riches_records.dart` to compose shared `GpTreasuryCreditRollup` beside `gp_treasury_credit_accumulator.dart` (Refs #3831; phase-8 type siblings Refs #4299); rule `repo.economy_gp_treasury_rollup_shared` |
| `tool/check_economy_test_no_local_stockpile_game_builder.dart` | Forbid local `_buildGame` / `buildGame` / `_gameWith*` helpers under `packages/colonizethis_economy/test/**`; use shared builders from test_support (Refs #3831, #3856); rule `repo.economy_test_no_local_stockpile_game_builder` |
| `tool/check_economy_bid_treasury_spend_shared.dart` | Require `trade_order_validator.dart` to import the `treasury_bid_budget.dart` barrel and call the shared `bidTreasurySpendForOrder` helper (defined in the phase-7 sibling `treasury_bid_spend.dart`) so per-bid treasury spend is not re-inlined (Refs #3823, #4049); rule `repo.economy_bid_treasury_spend_shared` |
| `tool/check_economy_test_file_size.dart` | PR-blocking structural check: files under `packages/colonizethis_economy/test/**` must stay at or below **250** physical lines (mirrors `repo.logic_test_file_size`; Refs #3823, #4550, #4631); rule `repo.economy_test_file_size` |
| `tool/check_economy_test_support_loc.dart` | Ratchet ceiling for `packages/colonizethis_economy_test_support/lib/**` physical Dart LOC (#4410 ≤6590, measured 6540 post leftover-merge; #4108 was ≤7150); rule `repo.economy_test_support_loc` (Refs #4014, #4049, #4108, #4410) |
| `tool/check_economy_test_support_file_size.dart` | Ratchet ceiling for `packages/colonizethis_economy_test_support/lib/**` physical lines per file (#4108 ≤220, chosen just above the post-split largest file `deal_matcher_priority_scenarios.dart` ≈215); generated suffixes excluded; rule `repo.economy_test_support_file_size` (Refs #4108) |
| `tool/check_economy_test_support_no_expectations_modules.dart` | Forbid any `*_expectations.dart` under `packages/colonizethis_economy_test_support/lib/**` after the leftover pin-type merge; rule `repo.economy_test_support_no_expectations_modules` (Refs #4410) |
| `tool/check_combat_test_support_loc.dart` | Ratchet ceiling for `packages/colonizethis_combat_test_support/lib/**` physical Dart LOC (#4633 ≤6997 measured post-format; never above 7100; #4196 was ≤7250); rule `repo.combat_test_support_loc` (Refs #4196, #4633) |
| `tool/check_combat_test_support_file_size.dart` | Ratchet ceiling for `packages/colonizethis_combat_test_support/lib/**` physical lines per file (#4633 ≤200; #4196 was ≤220); generated suffixes excluded; rule `repo.combat_test_support_file_size` (Refs #4196, #4633) |
| `tool/check_combat_test_support_no_logic_barrel.dart` | Forbid `package:colonizethis_logic/` imports under `packages/colonizethis_combat_test_support/lib/**`; combat APIs come from the combat barrel, `QuickBattleInput` from models, `kWorkTargetBuildRoad` from orders (Refs #4633); rule `repo.combat_test_support_no_logic_barrel` |
| `tool/check_combat_lib_file_size.dart` | Ratchet ceiling for `packages/colonizethis_combat/lib/**` physical lines per file (phase-5 ≤250 after #4545 near-cap splits; phase-4 was ≤280); stricter than the shared 550-line `repo.domain_package_source_file_size` cap; generated suffixes excluded; shrink-only grandfather allowlist during transition; rule `repo.combat_lib_file_size` (Refs #4284, #4545) |
| `tool/check_economy_source_file_size.dart` | Ratchet ceiling for `packages/colonizethis_economy/lib/**` physical lines per file (phase-9 ≤250 after #4550 near-cap splits; phase-8 was ≤260) so kitchen-sink modules cannot silently re-grow; stricter than the shared 550-line `repo.domain_package_source_file_size` cap; generated suffixes excluded; rule `repo.economy_source_file_size` (Refs #4049, #4299, #4550) |
| `tool/check_economy_test_mirrors_lib.dart` | Keep the `packages/colonizethis_economy/test/**` tree mirrored to `lib/src/economy/` per the testing rule (`test/` mirrors `lib/`): any Dart source that sits **directly** at the `test/` root is a violation — both `*_test.dart` files and co-located support helpers must live in a subdirectory such as `test/economy/` or `test/economy/world_market/` that mirrors `lib/src/economy/`. Complements the testing-rule policy (Refs #4299); rule `repo.economy_test_mirrors_lib` |
| `tool/check_save_source_file_size.dart` | PR-blocking physical-line gate: every `.dart` file under `packages/colonizethis_save/lib/src/**` must stay at or below **400** physical lines (empty shrink-only allowlist); keeps the #4077 `GameSaveAdapter` concern splits from re-merging; rule `repo.save_source_file_size` (Refs #4077) |
| `tool/check_save_test_file_size.dart` | PR-blocking physical-line gate: every `.dart` file under `packages/colonizethis_save/test/**` must stay at or below **400** physical lines (empty shrink-only allowlist; mirrors `repo.economy_test_file_size` / `repo.logic_test_file_size`); rule `repo.save_test_file_size` (Refs #4077) |
| `tool/check_colonizethis_test_lib_file_size.dart` | PR-blocking physical-line gate: every `.dart` file under `packages/colonizethis_test/lib/**` must stay at or below **250** physical lines (empty shrink-only allowlist); rule `repo.colonizethis_test_lib_file_size` (Refs #4547) |
| `tool/check_colonizethis_test_test_file_size.dart` | PR-blocking physical-line gate: every `.dart` file under `packages/colonizethis_test/test/**` must stay at or below **250** physical lines (empty shrink-only allowlist); rule `repo.colonizethis_test_test_file_size` (Refs #4547) |
| `tool/check_economy_test_no_local_run_scenarios.dart` | Forbid local `void Function() run` scenario shells under `packages/colonizethis_economy/test/**`; pins live in `colonizethis_economy_test_support` (Refs #4014); rule `repo.economy_test_no_local_run_scenarios` |
| `tool/check_app_test_file_size.dart` | PR-blocking physical-line gate: `app/test/**/*.dart` must stay at or below **400** lines; `appTestFileSizeAllowlistForTests` is **empty** after wave-15 Slice D densify (stale/missing entries still fail; Refs #4013, #4021, #4305, #4352); rule `repo.app_test_file_size`. Distinct from repo-wide `repo.dart_file_non_comment_line_size` (1000 **NCL**). |
| `tool/check_app_e2e_support_lib_file_size.dart` | PR-blocking physical-line gate: `packages/colonizethis_app_e2e_support/lib/**/*.dart` must stay at or below **250** lines; oversized files live on a **shrink-only** allowlist (`appE2eSupportLibFileSizeAllowlistForTests`) — missing or now-under-cap entries fail as stale (Refs #4075, #4195, #4344, #4598); rule `repo.app_e2e_support_lib_file_size`. |
| `tool/check_app_e2e_support_test_file_size.dart` | PR-blocking physical-line gate: `packages/colonizethis_app_e2e_support/test/**/*.dart` must stay at or below **300** lines; `appE2eSupportTestFileSizeAllowlistForTests` is **empty** after wave-4 Slice C densify (stale/missing entries still fail; Refs #4075, #4195, #4344, #4598); rule `repo.app_e2e_support_test_file_size`. Harness modules under `test/support/` should prefer ≤300 physical lines. |
| `tool/check_app_e2e_support_no_duplicate_hosts.dart` | AST gate: pin files directly under `packages/colonizethis_app_e2e_support/test/*.dart` must not re-declare `_wrap` / `_pumpScaffold` / `_pumpDialog` / `_pumpEmpty` / `_pumpHost` / `_pumpAndResolveRoot`; `test/support/` is exempt (Refs #4598); rule `repo.app_e2e_support_no_duplicate_hosts`. |
| `tool/check_app_e2e_support_no_part_directives.dart` | Forbids Dart `part` / `part of` under `packages/colonizethis_app_e2e_support/{lib,test}/**` via package-root prefix on the shared no-part scanner (Refs #4344); rule `repo.app_e2e_support_no_part_directives`. |
| `tool/check_app_test_support_file_size.dart` | PR-blocking physical-line gate: `app/test/support/**/*.dart` must stay at or below **600** lines; oversized files live on a **shrink-only** allowlist (`appTestSupportFileSizeAllowlistForTests`) — missing or now-under-cap entries fail as stale (Refs #4048); rule `repo.app_test_support_file_size`. Distinct from `repo.app_test_file_size` (400) and from `repo.ai_s7d_support_suite_size` (600, no allowlist). |
| `tool/check_app_test_support_loc.dart` | Ratchet ceiling for all Dart under `app/test/support/` (physical LOC; post-#4117 slice F ceiling **0** — support tree holds JSON fixtures only); fail on net regrowth (Refs #4021, #4035, #4048, #4058, #4117); rule `repo.app_test_support_loc` |
| `tool/check_app_test_no_duplicate_dark_token_asserts.dart` | AST gate: forbid private top-level `_expectMuted*` / `_expectEditorialMonocle*` helpers outside `app/test/editorial_monocle_dark_token_assertions.dart` — suites must call shared APIs there (Refs #4013); rule `repo.app_test_no_duplicate_dark_token_asserts` |
| `tool/check_app_test_no_duplicate_scaffolding.dart` | Full-tree AST gate: forbid reintroduced min-viewport / Widgetbook use-case / trade-screen host / units-panel `Game(` scaffolding, plus naval-part `_pumpNaval`/`pumpNaval` clones, military-suite `_pumpMilitary`/`pumpMilitary` clones, technology-suite `_pumpPanel`/`pumpPanel` clones, `Scaffold(body: Center(...))` dialog-host clones in governed `*_320dp_min_viewport_test.dart` (use `pumpDialogs320At`; showDialog-launcher hosts with `Builder` are not matched), inline `MaterialApp` hosts in panel/dialog/shell families listed in the checker, and private `_gameWithCapital` / `_gameWithPlayer` / `_gameWith` / `_buildGame` factories in `app_event_handler_debug_*_test.dart` (use `debug_handler_test_fixtures.dart`) (Refs #3730, #4035, #4048, #4058); rule `repo.app_test_no_duplicate_scaffolding` |
| `tool/check_app_test_no_duplicate_shortcut_fixtures.dart` | AST gate over `app/test/province_*shortcut_host_*_test.dart` excluding `province_shortcut_host_emit_test_support_test.dart`: a governed file that re-declares `MapTopology(` or `TileMapResult(` as a top-level or class field fails. Fixture SoT is `province_shortcut_host_emit_test_support.dart` / `province_shortcut_host_emit_fixtures.dart` (not flagged). Non-host `province_*shortcut_test.dart` files without `_host_` are out of scope (Refs #4450 AC8–AC9); rule `repo.app_test_no_duplicate_shortcut_fixtures` |
| `tool/check_app_test_no_duplicate_shortcut_golden_game_service.dart` | AST gate over the six `app/test/province_{purchase_land,build_railroad,build_road,build_fort,build_port,build_improvement}_shortcut_host_goldens_test.dart` files: a class that `extends GameService` fails. SoT is `province_shortcut_host_golden_game_service.dart`. Consulate goldens are out of scope (Refs #4606 AC1–AC3); rule `repo.app_test_no_duplicate_shortcut_golden_game_service` |
| `tool/check_app_test_no_duplicate_golden_host.dart` | Full-tree gate: forbid duplicated golden-host scaffold clones outside `golden_capture_harness.dart` (Refs #3847, #4035); rule `repo.app_test_no_duplicate_golden_host` |
| `tool/check_app_test_no_duplicate_diplomacy_host.dart` | Full-tree gate: forbid redeclared diplomacy/civilian bus-dialog hosts outside `diplomacy_panel_test_support.dart` (Refs #3847, #4035); rule `repo.app_test_no_duplicate_diplomacy_host` |
| `tool/check_app_test_no_duplicate_yarn_bundle.dart` | Full-tree gate: forbid duplicated Yarn `AssetBundle` fakes outside the approved dialogue test support module (Refs #3847, #4035); rule `repo.app_test_no_duplicate_yarn_bundle` |
| `tool/check_app_test_no_partn_double_suffix.dart` | Full-tree gate: forbid `*_test_partN_test.dart` double-suffix split fragments; require `<family>_partN_test.dart` (Refs #3730, #4035); rule `repo.app_test_no_partn_double_suffix` |
| `tool/check_economy_bid_quantity_cap_shared.dart` | Require `trade_order_suggester.dart` to delegate bid quantity capping to `capBidQuantityForBudgets` (phase-7 home `treasury_bid_caps.dart`, re-exported via the `treasury_bid_budget.dart` barrel); forbid inline `remainingTreasuryBudget ~/ unitPrice` elsewhere under economy lib (Refs #3836, #4049); rule `repo.economy_bid_quantity_cap_shared` |
| `tool/check_economy_test_no_src_imports.dart` | Forbid `package:colonizethis_economy/src/` imports under `packages/colonizethis_economy/test/**`; tests must use the public barrel (Refs #3836); rule `repo.economy_test_no_src_imports` |
| `tool/check_economy_test_core_fixtures_shared.dart` | Forbid inline `Game(` construction under `packages/colonizethis_economy/test/**` and `packages/colonizethis_economy_test_support/lib/**` except `lib/src/fixture_builders/**`; use shared builders from `colonizethis_economy_test_support` (Refs #3836, #3856, #4108); rule `repo.economy_test_core_fixtures_shared` |
| `tool/check_economy_deal_matcher_treasury_shared.dart` | Require `deal_matcher_session.dart` to call `maxAffordableBidQuantity` / `decrementTreasuryForFill` from the `treasury_bid_budget.dart` barrel (phase-7 home `treasury_bid_caps.dart`); forbid `_maxAffordableQuantity` / `_decrementTreasury` outside the shared module (Refs #3856 / #3979, #4049); rule `repo.economy_deal_matcher_treasury_budget_shared` |
| `tool/check_app_no_duplicate_helpers.dart` | AST scan that pins **canonical helper symbols** extracted for #2180 (e.g. `eraRoman`, `techCategoryLabelL10n`, `commodityDisplayName`, the `trainDialog*` set) to one canonical file under `app/lib/**` and forbids reappearance of removed helpers — the previous private duplicates (`_eraRoman`, `_categoryLabel`, `_categoryLabelL10n`, `_commodityDisplayName`) and the deleted public wrapper `unitsPanelRegionLabel` (replaced by `regionDisplayLabel`, #3279) — anywhere as top-level functions or class methods (GitHub #2180, #3279); rule `repo.app_no_duplicate_helpers` |
| `tool/check_app_test_no_debug_init.dart` | AST scan that fails when any `app/test/**/*.dart` file invokes the expensive debug map generator `getDebugInitGameResult()` (runs `runInitGame(GameSetupConfig.defaultConfig)`, ~7-11s per call paid once per `flutter test` isolate) outside an in-script allowlist. The allowlist is the migration backlog: every `app/test/**` file that still calls the helper; it must only shrink as panel suites move to the shared lightweight fixtures in `app/test/support/panel_test_fixtures.dart` or a committed serialized fixture, and new entries require justification for a genuine generated-map/topology dependency. Comment-only mentions of the symbol are ignored (AST method-invocation match). The shrink-only invariant is enforced: a **stale allowlist entry** (file missing, or migrated so it no longer invokes the helper) also fails the check and must be removed (Refs #3656); rule `repo.app_test_no_debug_init` |
| `tool/check_app_region_string_literals.dart` | Scan every `app/lib/**/*.dart` source (excluding generated suffixes and `_test.dart` / `/test/` paths) and fail when any line outside `//` / `///` comments contains a bare canonical-region string literal exactly equal to `'oldWorld'` / `'newWorld'` (single- or double-quoted). The constants `kRegionOldWorld` / `kRegionNewWorld` (`colonizethis_world`, re-exported via `package:colonizethis_logic/colonizethis_logic.dart`) are the single source of truth, decoupling the app from the magic strings. The backreferenced quote and anchored content keep prefixed/composite ids (`'oldWorld&#124;p1'`, `'$kRegionOldWorld&#124;'`) and identifiers (`region_oldWorld`) out of scope; an empty whole-file allowlist exists for any future sanctioned literal (Refs #3658); rule `repo.app_region_string_literals` |
| `tool/check_app_shell_panel_dedup.dart` | Keep the observe-mode shell guard centralized in one canonical helper: scan every `app/lib/**` Dart source (excluding tests and generated suffixes) and fail when any file other than the guard's declaration site (`app/lib/features/game/widgets/shell/shell_player_context.dart`) and the canonical helper `app/lib/features/game/widgets/shell/shell_player_guarded_body.dart` (which exposes `observeNotDefinedSentinel`) calls `shellPanelsNotDefined(...)` directly. The full-screen feature bodies (trade / technology / production / diplomacy) and the military/naval unit-panel sheets previously each re-derived the same `shellPanelsNotDefined(...)` + `ObserveModeNotDefinedPanel` branch; this gate prevents that duplicated guard from creeping back into individual surfaces. Line comments (`//`, `///`) are ignored; matches use `\bshellPanelsNotDefined\s*\(` (Refs #3546); rule `repo.app_no_shell_panel_duplication` |
| `tool/check_app_widgetbook_file_naming.dart` | Keep the Widgetbook catalog (`app/lib/widgetbook/catalog.dart` library) split by **UI domain** rather than by an arbitrary size index: scan every `*.dart` file under `app/lib/widgetbook/**` and fail when any basename matches the numbered fragment pattern `catalog_part<N>.dart` (case-insensitive on the `part` token). The catalog `part` files were previously named `catalog_part1.dart` … `catalog_part9.dart`, split only to stay under the `repo.part_unit_size` cap; new parts must use descriptive domain names (e.g. `catalog_panels.dart`, `catalog_dialogs.dart`, `catalog_primitives.dart`) and a single oversized domain is split into clearly-named sibling parts instead of a numbered fragment. When `app/lib/widgetbook/` is absent (partial checkout) the rule passes. The matcher requires the basename to be exactly `catalog_part<digits>.dart`, so descriptive names containing the substring `part` (e.g. `catalog_partner_panels.dart`) are not flagged (Refs #3546); rule `repo.app_widgetbook_file_naming` |
| `tool/check_app_panel_screen_id.dart` | AST scan that requires every **game-bearing panel** under `app/lib/features/game/widgets/**` — a public class whose name ends with `Panel` and whose constructor declares a `game` parameter — to declare `static const screenId` (initialized from a `UiScreenIds.*` constant) so the surface is grep-able and traceable to `SPEC/ui/screen-registry.md` (per `colonizethis-ui-documentation.mdc` § Code binding). Panels without a `game` parameter (`PauseMenuPanel`, `ObserveModeNotDefinedPanel`) and sub-panels/slots outside that directory (`VictoryPanel`, `GameMapProvinceDetailSidePanel`, private `_*Panel` helpers) are out of scope (Refs #3279); rule `repo.app_panel_screen_id` |
| `tool/check_app_game_panel_mixin.dart` | AST scan that requires every **fully-shaped game panel** under `app/lib/features/game/widgets/**` — a public class whose name ends with `Panel` and whose primary (unnamed) constructor declares **all three** of the `game`, `bus`, and `readOnly` parameters — to adopt `with GamePanelMixin` (declared in `app/lib/features/game/widgets/panels/game_panel_contract.dart`) so the shared contract getters and the `gamePanelConfig` bundle are part of its type. Panels that omit any of those parameters — `ProductionPanel` / `TechnologyPanel` (take a `Player`, no `bus`/`readOnly`) and `PauseMenuPanel` / `ObserveModeNotDefinedPanel` (no `game`) — are out of scope; `screenId` stays a `static const` enforced by the sibling `repo.app_panel_screen_id` rule (Refs #3279); rule `repo.app_game_panel_mixin` |
| `tool/check_chrome_button_base.dart` | AST scan that requires every chrome **text button** under `app/lib/widgets/**` to share the canonical hover/disabled chrome base. The Dart analyzer guarantees a `StatefulWidget` is backed by a `State<...>` subclass, so the gate is enforced on the backing state: any `State<X>` whose `X` is a public class name ending in `TextButton` must declare `with CtHoverButtonStateMixin` (declared in `app/lib/widgets/ct_hover_button.dart`). This prevents a new `*TextButton` from re-implementing the hover state machine and disabled / interactive / semantics / tooltip wrapping instead of reusing `buildHoverButton(...)` (Refs #3594, #4035); rule `repo.chrome_button_base` |
| `tool/check_app_no_feature_chrome_imports.dart` | Fail when `app/lib/features/game/widgets/chrome/` exists or when any scanned Dart under `app/`, `packages/`, `widgetbook_host/`, `test/`, or `tool/` imports/exports `package:colonizethis_app/features/game/widgets/chrome/...` or a relative `.../chrome/ct_*.dart` path. Shared Ct-* chrome must resolve through `package:colonizethis_app/widgets/...` (Refs #4035 AC2); rule `repo.app_no_feature_chrome_imports` |
| `tool/check_app_split_dialog_base.dart` | AST scan that requires every in-game **split dialog** under `app/lib/features/game/widgets/**` — a public class whose name starts with `Split` and ends with `Dialog` (e.g. `SplitArmyDialog`, `SplitFleetDialog`), excluding the `SplitEntityDialog` base itself — to declare `extends SplitEntityDialog` (declared in `app/lib/features/game/widgets/unit_orders/split_entity_dialog.dart`). This prevents a new `Split*Dialog` from re-implementing the shared `CtDialogShell(520 x 550)` + padded title + `CtTransferList` scaffold and the split Confirm rule instead of returning `buildSplitDialogScaffold(...)` from `build` (Refs #3594); rule `repo.split_dialog_base` |
| `tool/check_app_train_dialog_base.dart` | AST scan that requires every in-game **train-at-capital dialog** under `app/lib/features/game/widgets/**` — a public class whose name starts with `Train` and ends with `Dialog` (e.g. `TrainCiviliansDialog`, `TrainMilitaryDialog`, `TrainNavalDialog`), excluding the `TrainDialogBase` base itself — to declare `extends TrainDialogBase` (declared in `app/lib/features/game/widgets/train/train_dialog_base.dart`). This prevents a new `Train*Dialog` from re-implementing the shared `PopScope` + `CtDialogShell` wrapper, no-capital message, header/body composition, stepper count state, tech-lock resolution, and order materialization that the `TrainDialogBaseState` already centralizes; unit-type-specific cost modelling stays in each subclass via `buildBody(...)` / `canAffordIncrement(...)`. Non-dialog `Train*` chrome widgets (e.g. `TrainDialogHeader`) and private `_Train*Dialog` helpers are out of scope (Refs #3594); rule `repo.train_dialog_base` |
| `tool/check_app_train_dialog_commodity_cost_dedup.dart` | AST scan over the two **commodity-cost** train dialogs (`app/lib/features/game/widgets/train/train_military_dialog.dart`, `train_naval_dialog.dart`) requiring each to declare a state class `extends CommodityCostTrainDialogState` (declared in `app/lib/features/game/widgets/train/train_commodity_cost_dialog_base.dart`) and to **not** re-declare the shared cost machinery — members `_totalTreasuryCost` / `_totalPeasantCost` / `_totalCommodityCosts` / `_remainingTreasury` / `_remainingPeasants` / `_remainingCommodity` / `_deficitHint` / `canAffordIncrement`, or a private `_*ResourceBar` / `_*Row` widget. This keeps the treasury + peasant + commodity cost model, deficit hint, resource bar, and unit-row rendering single-sourced in the base; each dialog supplies only its catalog adapter (`commodityCostEntries`), `resourceBarCommodityIds`, tech map, title, and committed-orders event. `TrainCiviliansDialog` (treasury + paper model, entry-style bar) is out of scope (Refs #3686); rule `repo.app_train_dialog_commodity_cost_shared` |
| `tool/check_logic_work_target_switch.dart` | AST scan that forbids enumerated `switch` dispatch on three or more `kWorkTarget*` constants in `packages/colonizethis_orders/lib/src/orders/work_handlers/` and `packages/colonizethis_orders/lib/src/orders/order_visibility.dart` so new work-target branches extend the canonical `workOrderHandlersByTarget` / `_workOrderVisibilityByTarget` registry maps instead (Refs #2560); rule `repo.logic_work_target_switch` |
| `tool/check_screen_registry_active_paths.dart` | Parse `SPEC/ui/screen-registry.md` `## Registry` table; for every row whose `Status` column is `active`, require the Implementation column to be a single backticked `app/lib/...dart` path resolving to an existing file on disk (Refs #2801); rule `repo.screen_registry_active_paths` — `draft` rows are ignored so spec/widget work can land without immediate code presence |
| `tool/check_dialogue_yarn_variable_bindings.dart` | Cross-reference each dialogue overlay under `app/lib/features/game/widgets/dialogue/**` against the Yarn assets it loads via `kDialogue*Asset` constants; fail when a `{$var}` interpolation referenced by an asset is not bound with a matching `$`-prefixed `setVariable(r'$var', …)` in the loading overlay (Jenny resolves interpolation at parse time and stores variables `$`-prefixed); rule `repo.dialogue_yarn_variable_bindings` (Refs #3463) |
| `tool/check_dialogue_blocking_combined_step.dart` | AST scan over `app/lib/features/game/widgets/dialogue/**/*.dart` enforcing the collapsed single-step blocking-dialogue contract in three checks. **(1) Adoption:** any file that constructs `CtDialogueView(` must also construct `CtDialogueLineChoiceBody(`, so overlays delegate Yarn line/choice rendering to the shared body instead of re-implementing mutually-exclusive `currentLine` / `currentChoice` branches. **(2) No bespoke wiring:** overlay files must not invoke or tear off `advanceLine` / `selectOption` / `confirmCombinedLineOption` on a `CtDialogueView` receiver — only `ct_dialogue_line_choice_body.dart` may. **(3) Golden registry parity:** the set of `*_overlay.dart` files that construct `CtDialogueView(` must equal the set of dialogue overlays imported by `app/test/dialogue_combined_line_choice_goldens_test.dart` (symmetric-difference fail: missing golden for a new overlay, or stale golden import for a removed overlay). Canonical files (`ct_dialogue_view.dart`, `ct_dialogue_line_choice_body.dart`) and the non-Jenny blocking diplomacy overlay (`call_to_arms_dialogue_overlay.dart`, which constructs no view) are exempt; rule `repo.dialogue_blocking_combined_step` (Refs #3628) |
| `tool/check_app_editorial_monocle_colors.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` and `app/lib/widgets/**/*.dart` resolves colors through `EditorialMonoclePalette` tokens (or `CtGradients`) rather than the banned Material color literals `Colors.{black,white,red,redAccent,green,greenAccent,grey,gray}` (with any opacity / grade suffix such as `black54`, `white70`, `redAccent100`) or raw `const Color(0x...)` hex literals — see `SPEC/ui/pixel-art-ui-catalog.md` § Hard-coded color ban (#2914 §S4 + §G1); whole-file scope-only allowlist for Flame canvas renderers (`region_map_component_render_*`, `game_region_minimap.dart`), the pixel-art palette table (`resource_icon_disc_palette.dart`), and `app/lib/widgets/` canvas-compositing files where the color literal is an alpha / blend argument rather than a theme palette reference (`app/lib/widgets/main_menu.dart` and its split-out part file `app/lib/widgets/main_menu_buttons.dart` hover `ColorFilter.mode` darken composite (collage painters live in `colonizethis_app_ui_chrome`, Refs #3942)). Shared Ct-* chrome lives under `app/lib/widgets/` and is **not** directory-exempt (Refs #4035); rule `repo.app_editorial_monocle_colors` |
| `tool/check_app_textstyle_fontsize_fallback.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` and `app/lib/widgets/**/*.dart` sizes themed-text fallbacks to the canonical `TextTheme` slot: for every `textTheme.<slot> ?? const TextStyle(fontSize: N)` idiom whose `<slot>` is in the canonical M3 dark slot→size table, the literal `N` must equal that slot's size (see `SPEC/ui/pixel-art-ui-catalog.md` and #2914 §S7). The pattern tolerates a line break between the slot read and the `?? const TextStyle(...)` fallback; matches whose slot-token line begins with `//` (line comment / `///` dartdoc) are skipped, and fallbacks reading a slot outside the table are not enforced. Canonical slots: `headlineMedium`=28, `headlineSmall`=24, `titleLarge`=22, `titleMedium`=16, `titleSmall`=14, `bodyLarge`=16, `bodyMedium`=14, `bodySmall`=12, `labelLarge`=14, `labelMedium`=12, `labelSmall`=11. The checker uses only generated-suffix and test-path scope skips; it does not load keyed waiver data; rule `repo.app_textstyle_fontsize_fallback` |
| `tool/check_app_no_material_iconbutton.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct Material `IconButton(` chrome (including `IconButton.outlined`, `IconButton.filled`, `IconButton.filledTonal`) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material IconButton ban (#2914 Phase 2 §G2). Glyph-only tap affordances must resolve through the `CtIconAction` catalog primitive; back-affordance chrome through `CtBackButton`; heavier label / icon chrome through `CtNinePatchButton`. Whole-file scope-only allowlist for dev-tooling screens (`debug_console_overlay_panel.dart` SYS20001, `debug_log_viewer_screen.dart` SYS10001) Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2); rule `repo.app_no_material_iconbutton` |
| `tool/check_app_no_material_alertdialog.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct Material `AlertDialog(` chrome (including the `AlertDialog.adaptive` named constructor) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton). Dialog popups (confirm dialogs, alert popups, single-action prompts, destructive-confirmation sub-dialogs) must resolve through `CtDialogShell` (single dialog frame) or `CtFullScreenDialogueShell` (scrim + dialog scaffold) with the `dialogScrim` token providing the barrier color. Whole-file scope-only allowlist mirrors the sibling IconButton rule's contract (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_alertdialog` |
| `tool/check_app_no_material_textbutton.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct Material `TextButton(` chrome (including the `TextButton.icon` and `TextButton.tonalIcon` named constructors) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog). Click affordances must resolve through `CtNinePatchButton` (canonical brass-bracket catalog button) or `CtDangerTextButton` (small destructive text-styled affordance per `SPEC/ui/production-panel.md` § Labour Controls). The static `TextButton.styleFrom(` `ButtonStyle` factory is not flagged (it is a theme helper, not a widget construction). The leading word boundary in the matcher keeps catalog widgets that contain `TextButton` as a suffix — notably `CtDangerTextButton` — out of scope. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_textbutton` |
| `tool/check_app_no_material_scaffold.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct Material `Scaffold(` chrome — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton). Screen-level chrome must resolve through `CtGameFeatureScreenShell` for game-bound feature screens (with the optional `backgroundColor` parameter for per-screen mockup backgrounds such as `EditorialMonoclePalette.bg`) or `CtScreenShell` for shell-bound utility screens; both Ct-* shells own the editorial-monocle background, `SafeArea`, and top-bar / panel chrome. The leading word boundary in the matcher keeps `ScaffoldMessenger(`, `ScaffoldState(`, and any user-defined identifier ending in `Scaffold` (e.g. `MyScaffold`) out of scope; static member accesses such as `Scaffold.of(context)` and `Scaffold.maybeOf(context)` are also out of scope because the regex requires an opening parenthesis immediately after the `Scaffold` token. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog / TextButton rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_scaffold` |
| `tool/check_app_no_material_filterchip.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct Material `FilterChip(` chrome (including the `FilterChip.elevated` named constructor) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold). Single-select toggle chips and multi-select pickers must resolve through `CtChoiceChip` (compose multi-select pickers as a row of `CtChoiceChip` widgets). The leading word boundary in the matcher keeps any user-defined identifier ending in `FilterChip` (e.g. `MyFilterChip`) out of scope. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog / TextButton / Scaffold rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_filterchip` |
| `tool/check_app_no_material_switchlisttile.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct Material `SwitchListTile(` chrome (including the `SwitchListTile.adaptive` named constructor) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold). The combined label + switch row composition must resolve by laying out `CtSectionLabel` (or body text in the active theme `TextTheme` slot) alongside `CtToggleSwitch`, so toggle row chrome avoids the Material `ListTile` row chrome banned alongside `SwitchListTile`. The leading word boundary in the matcher keeps any user-defined identifier ending in `SwitchListTile` (e.g. `MySwitchListTile`) out of scope. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog / TextButton / Scaffold / FilterChip rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_switchlisttile` |
| `tool/check_app_no_material_listtile.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the bare Material `ListTile(` widget — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile / Chip). Per-entity unit / army / fleet rows must resolve through the `UnitsEntityActionRow` composite (`SPEC/ui/components/units-entity-action-row.md`), commodity rows through `CtResourceCell`, and other tappable list rows through an `InkWell` / `GestureDetector` over palette-token chrome. The leading word boundary in the matcher keeps the sibling list-tile families `SwitchListTile(` (covered by `repo.app_no_material_switchlisttile`), `CheckboxListTile(`, and `RadioListTile(`, plus any user-defined identifier ending in `ListTile` (e.g. `MyListTile`), out of scope because their `ListTile` token is preceded by a word character. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile / Chip rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_listtile` |
| `tool/check_app_no_material_button.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material raised / outlined button families `ElevatedButton(`, `FilledButton(`, `OutlinedButton(` (including their `.icon`, `.tonal`, and `.tonalIcon` named constructors) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile / Chip / ListTile to cover the remaining Material button families). Click affordances must resolve through `CtNinePatchButton` (canonical brass-bracket catalog button; pass `dangerVariant: true` for destructive actions). The sibling `repo.app_no_material_textbutton` rule covers `TextButton`, so this rule completes the Material-button ban family without overlapping it. The leading word boundary keeps any user-defined identifier ending in a banned button name out of scope, and the explicit named-constructor enumeration keeps the static `*.styleFrom(` `ButtonStyle` factories (theme helpers, not widget construction) and theme types such as `ElevatedButtonTheme(` out of scope. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile / Chip / ListTile rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_button` |
| `tool/check_app_no_material_chip.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the un-prefixed Material `Chip(` widget — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile). Static chip-style labels (e.g. legend swatches, category badges) must compose palette-token-resolved primitives (`Container` + `Border.all` resolving from `EditorialMonoclePalette`); interactive toggle chips must resolve through `CtChoiceChip` (the sibling `repo.app_no_material_filterchip` rule covers `FilterChip`; prefixed chip families `ChoiceChip(`, `ActionChip(`, `InputChip(`, `RawChip(`, `MaterialChip(` are intentionally out of scope of this rule because the negative lookbehind `(?<![A-Za-z0-9_])` requires the `Chip` token to be preceded by a non-word character so any prefixed `*Chip(` identifier is not matched). User-defined identifiers ending in `Chip` (e.g. `_LegendChip(`, `MyChip(`, `CtChip(`) are likewise not flagged. Whole-file scope-only allowlist mirrors the sibling IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)) so the Material-widget ban family stays scope-uniform across rules; rule `repo.app_no_material_chip` |
| `tool/check_app_no_material_card.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material `Card(` widget (including the `Card.filled` and `Card.outlined` named constructors) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). In-screen sections must resolve through `CtPanel` (the horizontally-banded editorial-monocle section surface). The leading word boundary in the matcher keeps user-defined identifiers ending in `Card` (e.g. `ResourceCard`) and theme types such as `CardTheme(` out of scope. Whole-file scope-only allowlist mirrors the sibling Material-widget ban rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)); rule `repo.app_no_material_card` |
| `tool/check_app_no_material_dialog.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material `Dialog(` widget (including the `Dialog.fullscreen` named constructor) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). Popups must resolve through `CtDialogShell` (single dialog frame) or `CtFullScreenDialogueShell` (scrim + dialog scaffold). The sibling `repo.app_no_material_alertdialog` rule covers `AlertDialog`; the leading word boundary keeps `AlertDialog(`, `SimpleDialog(`, the `showDialog(` / `showGeneralDialog(` plumbing helpers, the catalog widget `CtDialogShell(`, and types such as `DialogRoute(` / `DialogTheme(` out of scope. Whole-file scope-only allowlist mirrors the sibling Material-widget ban rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2) — notably `CtDialogShell` itself, which composes the Material `Dialog` internally); rule `repo.app_no_material_dialog` |
| `tool/check_app_no_material_choicechip.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material `ChoiceChip(` widget (including the `ChoiceChip.elevated` named constructor) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). Single-select toggle chips must resolve through `CtChoiceChip`; the sibling `repo.app_no_material_filterchip` and `repo.app_no_material_chip` rules cover `FilterChip` and the un-prefixed `Chip`. The leading word boundary keeps the catalog widget `CtChoiceChip(` and any user-defined identifier ending in `ChoiceChip` out of scope. Whole-file scope-only allowlist mirrors the sibling Material-widget ban rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)); rule `repo.app_no_material_choicechip` |
| `tool/check_app_no_material_slider.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material `Slider(` widget (including the `Slider.adaptive` named constructor) — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). Continuous and discrete (`divisions`) value controls must resolve through `CtSlider`. The leading word boundary keeps the catalog widget `CtSlider(`, the separate Material `RangeSlider(`, any user-defined identifier ending in `Slider`, and `SliderTheme(` out of scope. Whole-file scope-only allowlist mirrors the sibling Material-widget ban rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)); rule `repo.app_no_material_slider` |
| `tool/check_app_no_material_dropdownbutton.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material `DropdownButton(` widget, including the generic-typed form `DropdownButton<T>(` — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). Select controls must resolve through `CtDropdown` (compact flat trigger + modal picker inside `CtDialogShell`). The matcher accepts an optional `<...>` type-argument segment before the parenthesis; the leading word boundary plus the immediate generic / `\s*\(` requirement keeps the sibling Material types `DropdownButtonFormField(` and `DropdownButtonHideUnderline(`, and any user-defined identifier ending in `DropdownButton`, out of scope. Whole-file scope-only allowlist mirrors the sibling Material-widget ban rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)); rule `repo.app_no_material_dropdownbutton` |
| `tool/check_app_no_material_appbar.dart` | Enforce that production UI surface under `app/lib/features/**/*.dart` does not construct the Material `AppBar(` widget — see `SPEC/ui/pixel-art-ui-catalog.md` § Material design ban (#2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). Screen top chrome must resolve through `CtTopBar` (embedded in `CtScreenShell`); the sibling `repo.app_no_material_scaffold` rule covers the `Scaffold` host an `AppBar` typically accompanies. The leading word boundary keeps the separate Material `SliverAppBar(`, any user-defined identifier ending in `AppBar`, and `AppBarTheme(` out of scope. Whole-file scope-only allowlist mirrors the sibling Material-widget ban rule contracts (dev-tooling screens `debug_console_overlay_panel.dart` SYS20001 + `debug_log_viewer_screen.dart` SYS10001, Ct-* catalog widgets live under `app/lib/widgets/` (outside features scan); feature `chrome/` is deleted and barred by `repo.app_no_feature_chrome_imports` (Refs #4035 AC2)); rule `repo.app_no_material_appbar` |

### `repo.logic_dead_files` (colonizethis_logic `lib/src` orphans)

**Goal (GitHub #2201):** Fail CI when a hand-written Dart file under `packages/colonizethis_logic/lib/src/**` is neither reachable from the package `lib/**` import graph nor reachable from any **barrel** entrypoint (`packages/colonizethis_logic/lib/*.dart`) via `export` edges.

**Predicate:** A file under `lib/src` is **dead** when all are true: it is not a `part of` target; it is not imported (directly or transitively from parsed `import`/`export`/`part` directives) by any file under `packages/colonizethis_logic/lib/**`; and it is not on the export-reachability closure rooted at every `lib/*.dart` top-level barrel plus any `lib/src` file already imported by `lib/**`.

**Test imports:** Imports from `packages/colonizethis_logic/test/**` do **not** make a `lib/src` file live.

**Deferred paths:** `lib/src/ai/ai_planner.dart` and `lib/src/ai/sim_game_ai.dart` are temporarily exempt in `tool/check_logic_dead_files.dart` until product wiring or deletion is decided (issue #2201); the checker still lists them in the pass message when they are the only findings.

**Consumer note:** The root barrel may export setup/dossier/world modules that are not referenced by `app/` or `colonizethis_ai/` today; that keeps tests on a single `package:colonizethis_logic/colonizethis_logic.dart` import. Narrowing those exports is optional maintenance, not required for the dead-file rule.

### `repo.domain_package_dead_files` (split domain-package `lib/src` orphans)

**Goal (Refs #3290):** Extend dead-file detection to the eight extracted logic-domain packages (`colonizethis_world`, `colonizethis_combat`, `colonizethis_economy`, `colonizethis_diplomacy`, `colonizethis_setup`, `colonizethis_orders`, `colonizethis_turn`, `colonizethis_ai_contracts`) so an orphaned `lib/src` file in any of them fails CI, the same way `repo.logic_dead_files` protects the thin `colonizethis_logic` core.

**Why workspace-wide (vs. the intra-package `repo.logic_dead_files` model):** After the split, a domain `lib/src` file is frequently consumed by a *different* package — for example `colonizethis_turn` re-exporting `package:colonizethis_world/src/world/...`, the thin `colonizethis_logic` barrel re-exporting domain src through `ai_api.dart` / `order_suggestion_api.dart` / `colonizethis_logic.dart`, or `app/lib` importing a domain src path directly. Intra-package reachability alone would mislabel these public files as dead, so liveness is resolved across the whole workspace.

**Predicate:** `tool/check_domain_package_dead_files.dart` collects every non-test `.dart` file under the consumer roots (`packages/*/lib`, `app/lib`, `ctdev/lib`, `tool`). Every such file that lives **outside** the eight domain `lib/src` trees is an **anchor root**. Reachability follows parsed `import` / `export` / `part` directives (resolving workspace `package:<pkg>/...` URIs to `packages/<pkg>/lib/...` and relative paths) transitively. A domain `lib/src` file is **dead** when no anchor root reaches it through that directive graph.

**Test imports:** Files under any `test/` or `integration_test/` directory are excluded from the consumer roots, so a domain `lib/src` file used only by tests is still treated as dead (mirrors `repo.logic_dead_files`).

**Missing-tree guard:** If any of the eight `packages/colonizethis_<domain>/lib/src` trees is absent, the checker exits `1` (the split is incomplete or a package was misnamed).

**Acceptance criteria:**

- **Given** the post-split domain packages on `dev`, **when** `repo.domain_package_dead_files` scans every `packages/colonizethis_<domain>/lib/src` tree, **then** every `lib/src` file is reachable from at least one non-test anchor root and the rule exits `0`.
- **Given** a `packages/colonizethis_world/lib/src` file that no non-test consumer imports, exports, or includes via `part`, **when** `repo.domain_package_dead_files` runs, **then** the rule lists that file under `packages/colonizethis_world/lib/src` and exits `1`.
- **Given** a domain `lib/src` file consumed only through a cross-package re-export (for example `colonizethis_turn` `export 'package:colonizethis_world/src/world/...'`), **when** `repo.domain_package_dead_files` runs, **then** that file is treated as live and the rule exits `0`.
- **Given** a workspace where one of the eight `packages/colonizethis_<domain>/lib/src` trees is missing, **when** `repo.domain_package_dead_files` runs, **then** the rule reports `Missing domain src tree` and exits `1`.

## Rule IDs and groups

Each rule has a stable `rule_id` (prefix `repo.`). **Groups** (non-exhaustive): `structure`, `architecture`, `identifiers`, `exceptions`, `game_invariants`, `ui_i18n`. Violation text from underlying tools should include **file path and line** (and column when the checker provides it). The orchestrator prints a banner `--- [rule_id] title ---` before each rule so logs stay attributable.

**Dart `runner` rules** listed in the manifest are executed **in-process** by `ct_repo_lint_lib.dart` (calling `runCheck…` entrypoints on the existing `tool/check_*.dart` modules). Unknown `rule_id` values or future scripts without an in-process binding still use `dart run <script>` as a fallback. Standalone `dart run tool/check_*.dart` remains supported via thin `main()` wrappers.

## CI contract

- **Quality workflow (order):** After root `dart pub get`, **`dart run tool/run_workspace_analyze_errors_only.dart`** runs **`flutter pub get`** then **`flutter gen-l10n`** (when **`l10n.yaml`** exists) in each **Flutter** workspace package, then **`dart analyze`** or **`flutter analyze`** for **every** `dart pub workspace list` member **except the workspace host root** (`colonizethis` at repo root — skipped because `dart analyze` there would traverse nested `app/` without package context and false-fail on generated l10n). The step **fails only on analyzer `error` diagnostics** (warnings and infos do not fail), matching `tool/run_quality_gate_tests.sh`. The **repo-root** `colonizethis` package also excludes **`test/**`** in `analysis_options.yaml` (checker harness fixtures). Entrypoint: `tool/run_workspace_analyze_errors_only.dart`; Melos: `melos run workspace_analyze_errors_only`. Then **`tool/verify_order_engine_codegen.sh`**, then **`dart run tool/ct_repo_lint.dart`**. Gates that are **not** bundled repo-lint rules stay separate (e.g. coverage thresholds, `custom_lint`, package tests).
- **App UI string gate:** Rule `repo.app_hardcoded_ui_strings` runs only when `CT_REPO_LINT_INCLUDE_APP` is exactly `true` (workflow sets this from path filters when app/package paths changed).
- **Commodity l10n coverage:** Rule `repo.commodity_l10n_coverage` (`tool/check_commodity_l10n_coverage.dart`) fails when any `CommodityCatalog` id lacks `commodity_<catalogId>` in `app_en.arb` (Refs #3987).
- **`app_tests_cache` job:** Still runs **`flutter analyze`** under `app/` only (errors-only grep) before packing the test cache; redundant with the app slice of the workspace gate but keeps an early signal for app-only paths.

## Test and `integration_test/` static analysis scope (GitHub #2014)

**Goal:** `test/` and `integration_test/` Dart are **first-class** for the same categories of static gates as `lib/` where the toolchain and rule design apply. **Generated code, goldens, fixtures, and similar trees stay excluded** (see [Shared exclusions](#shared-exclusions-testintegration_test-generation-and-fixtures)).

**Phasing:** Land in **at most five mergeable slices** on `dev`. Each merged slice **must keep required CI green**. Do **not** widen **fatal** enforcement under `test/` / `integration_test/` (manifest rules, `ct_repo_lint_scan_contract` collectors, binary AST checkers) unless violations are **fixed in the same PR** **or** an intermediate slice uses a **SPEC-documented** non-fatal / audit / baseline mechanism for that transition (recorded in this doc or the rule’s SPEC with an issue link).

### Failure semantics (do not conflate)

| Mechanism | CI interpretation |
|-----------|---------------------|
| **`dart analyze` / `flutter analyze`** (**when CI uses error-only parsing**) | Fail the job only on **analyzer `error` severity** lines; **warnings and infos do not fail** the gate unless project policy explicitly changes. |
| **`dart run tool/ct_repo_lint.dart`** and manifest-driven `runner` rules | **Binary pass/fail** per rule design (`exit` non-zero on violation); not governed by “analyzer errors only.” |
| **`custom_lint` / `dart run custom_lint`** | Treat as **analyzer diagnostics** (same severity model as `dart analyze` for the plugin); CI should match the wired script’s contract (see [exception-enforcement.md](exception-enforcement.md)). |
| **Standalone AST scripts** | **Binary** on their own thresholds unless a SPEC says otherwise. |

### Shared exclusions (test/integration_test, generation, fixtures)

Across tools that intentionally share skip logic, exclude at minimum:

- Suffixes: `*.g.dart`, `*.freezed.dart`, `*.mocks.dart`, `*.gen.dart` (and any other generated suffixes called out per package).
- Path fragments (fixture / golden trees): see `repoLintFixtureDirPathMarkers` in `tool/ct_repo_lint_scan_contract.dart` (`/test_data/`, `/fixtures/`, `/golden/`, etc.) wherever that helper applies.

`tool/check_long_string_switches.dart` runs as manifest rule `repo.dart_long_string_switches` and uses the shared repo-wide collector in `tool/ct_repo_lint_scan_contract.dart` (`collectRepoLintRepoWideDartFiles` + `repoLintRepoWideDartCollectorShouldSkip`) to preserve broad coverage while centralizing exclusions (`.dart_tool`, `.pub-cache`, `build`, generated suffixes, and tech embed path).

### Split test-family file naming (app)

Large `app/test/**` widget/screen suites that are split into numbered fragments to stay under `repo.dart_file_non_comment_line_size` MUST use the single documented convention **`<family>_part<N>_test.dart`** (for example `order_merge_part1_test.dart` in orders). After wave-15 AC10 (Refs #4352), leftover app `_partN` families were concern-renamed; new numbered shards still must not use the awkward double `_test` suffix `<family>_test_part<N>_test.dart`. Gate `tool/check_app_test_no_partn_double_suffix.dart` (rule `repo.app_test_no_partn_double_suffix`, paired test `test/check_app_test_no_partn_double_suffix_test.dart`) runs as a full-tree `ct_repo_lint` rule alongside the sibling scaffolding / golden / diplomacy / yarn dedup gates (Refs #3730, #4035).

**Stay-split families (Refs #3847, #4352):** The five baseline families below remain split because post-harness combined non-comment LOC exceeds the merge trigger (≤1000 LOC, ≤5 top-level `group(`, ≤3 distinct `app/test/support/` imports). Re-verify before any merge. Wave-15 AC10 concern-renamed leftover `_partN` shards; do not add further numbered shards solely to satisfy the 400-line ratchet.

| Family | Concern files | Stay-split reason (wave-7 re-measure, Refs #4048) |
|--------|-------|-------------------|
| `naval_units_panel` | `roster_and_draft`, `combine_and_mockup` | Combined physical lines ~1939 ≫1000 (wave-11 Slice D densify, Refs #4224; baseline 3217 `naval_units_panel_*` family @ `8d176c93e`, post-densify ~1939 LOC ~40% reduction; combine/locate/mockup pins in `naval_panel_*` siblings outside the concern suites) |
| `ct_region_map_widget` | `fog_and_labels`, `presence_refresh`, `camera_and_view`, `port_and_sea_plates`, `interaction_and_icons` | Combined physical lines ~1824 ≫1000 (local `_pump*` wrappers folded into `pumpCtRegionMapTest` on `ct_region_map_test_support.dart`) |
| `game_map_area_state_logic` | `draft_and_selection`, `prospect_and_explore`, `fleet_harbor` (+ cache/campaign siblings) | Combined physical lines ~1440 ≫1000 (wave-10 Slice E densify, Refs #4183; baseline ~2002) |
| `military_units_panel` | army/display/split siblings | Combined physical lines ~1091 ≫1000 |
| `civilian_units_panel` | `list_and_shortcuts`, `locate_and_cancel`, `pending_work` | Combined physical lines ~1660 ≫1000 |

**Seed-42 demo fixtures (Refs #3847):** `province_overlay_demo_data.dart`, Widgetbook catalog builders, and `debug_init_game.dart` (`getDebugInitGameResult()`) load committed seed-42 JSON via `packages/colonizethis_app_fixtures` (`test_support/seed42_fixture_loader.dart`, `seed42_init_game_result.dart`, `map_view_serialization.dart`) instead of the ~7–11 s procedural generator on VM/desktop (web falls back when fixtures are unavailable).

**Approved app test harness modules (Refs #3847, #3952, #4013, #4021, #4048, #4058, #4117):** Shared widget-test scaffolding MUST live as first-class modules under `app/test/` — not re-declared in individual test files. `app/test/support/` retains committed JSON fixtures only (no Dart harness modules). Canonical modules include `app/test/app_shell_harness.dart` (`buildAppShell`, `buildAppShellMaterialApp` (optional `applyEditorialTheme: false` for bare Flame region-map goldens via `app/test/ct_region_map_test_support.dart` `ctRegionMapTestHarness`, and for e2e helper/smoke bare hosts under `app/test/e2e_*`), `pumpAppShell`), `min_viewport_harness.dart`, `dialogs_320dp_min_viewport_support.dart` (`pumpDialogs320At`, `buildThreeGpDialogueOverlayGame` — required Center-host for governed dialog/overlay `*_320dp_min_viewport_test.dart` pins), `diplomacy_panel_test_support.dart` (`buildDiplomacyPanel`, `buildDiplomacyPanelShell`, `buildCivilianPanel`, `relativePowerSpanColors`, `pumpDiplomacyPanelBuilt`, `PanelBusDialogHost`, `DiplomacyPanelBusDialogHost`, `CivilianPanelBusDialogHost`), `units_panel_test_shared.dart` (`buildUnitsPanelHumanPlayer`, `buildUnitsPanelAdjacentOwProvincesTopology`, `buildUnitsPanelCapitalAdjacentSeaTopology`), `civilian_units_panel_test_support.dart` (`buildCivilianOwUnitsGame`, `buildCivilianExplorerBuilderShortcutGame`, `buildCivilianSingleUnitOwGame`, `buildCivilianPendingProjectionGame`, `buildCivilianWorkingBuilderGame`, `civilianIdleUnit`, `civilianSinglePendingWorkOrder`), `new_game_leader_selection_dialog_test_support.dart` (`pumpNewGameLeaderSelectionDialog`, `ensureTapNewGameLeaderSelectionStart`, `ensureTapNewGameLeaderSelectionCancel`), `combat_ui_specs_test_support.dart` (`combatUiSpecsFrame`, `combatUiSpecsDarkFrame`), `move_dialogs_specs_test_support.dart` (`moveDialogsSpecsFrameWithOpener`), `dialogue_overlays_specs_test_support.dart` (`pumpDialogueOverlaysUntilSettled`, `dialogueOverlaysLibraryUnitSource`, `wrapGameStartIntroOverlay`), `screen_spec_acceptance_test_support.dart` (`buildScreenSpecMainMenu`, `woodPanelButtonFinderFor`, `findGradientSurfaceFor`, `pumpScreenSpecMainMenuAtSize`), `game_screen_test_support.dart` (`buildGameScreenShellOverrides`, `buildGameScreenHost`, `buildGameScreenShellToGameFlow`), `production_panel_test_support.dart` (`buildProductionPanel`, `productionPanelTestFullPlayer`), `widgetbook_test_harness.dart`, `editorial_monocle_dark_token_assertions.dart` (`expectMutedSingleSource`, `expectFgSingleSource`, `expectMutedObfuscated`, `expectEditorialMonocleDarkChrome`), `game_map_area_state_logic_test_support.dart` (`expectedBuildImprovementEnabledFromPipeline`, `expectPortFleetMarkersMatchTownPortDrawables`; scenario fixtures in `app/test/game_map_area_state_logic_test_scenarios.dart` outside the support tree), `military_units_panel_test_support.dart` (`buildMilitaryPanel`, `pumpMilitaryPanel`, `expandFirstArmyExpansion`, `expandAllArmyExpansions`, `pumpArmySplitHarness`, `ArmySplitTestHarness`; scenario Game factories in `app/test/military_units_panel_test_scenarios.dart` outside the support tree), `naval_units_panel_test_support.dart` (`buildNavalPanel`, `pumpNavalPanel`, `wireNavalSplitForWidgetTest`, `wireNavalTransferForWidgetTest`; scenario Game factories in `app/test/naval_units_panel_test_scenarios.dart` and combine/autoclose tables in `app/test/naval_panel_combine_tables.dart` outside the support tree), `technology_panel_test_support.dart` (`buildTechnologyPanel`, `pumpTechnologyPanel`, `pumpTechnologyPanelGolden`), `province_draft_orders_test_support.dart` (`pumpProvinceDraftOrdersOverlay`, `provinceDraftOrdersGame`, `provinceDraftOrdersPlayerView`), `golden_capture_harness.dart`, `yarn_test_fixtures.dart`, and the `panel_fixtures/` barrel (`buildDiplomacyPanelGameWithNoDiscoveredFactions`, `buildDiplomacyPanelGameWithTribeDiscoveredByVisibility`). Trade screen widget-test hosts live in `app/test/trade_screen_test_support.dart` (pump/shell hosts) and `app/test/trade_screen_test_game_builders.dart` (Game factories) outside the support tree. Province overlay editorial-monocle pump shell lives in `app/test/province_overlay_test_harness.dart` outside the support tree. In-lib `ShellPlayerContext.globalObserve` is the canonical global-observe sentinel for `shellPlayerContextProvider` and feature-screen 320 observe pins. AST gates `check_app_test_no_duplicate_scaffolding.dart` (also bans inline `Game(` in `civilian_units_panel_list_and_shortcuts_test.dart` / `civilian_units_panel_locate_and_cancel_test.dart` / `civilian_units_panel_pending_work_test.dart` / `naval_units_panel_roster_and_draft_test.dart` / `naval_units_panel_combine_and_mockup_test.dart` / `military_units_panel_test.dart` / `military_units_panel_army*` / `military_units_panel_display_test.dart` / `civilian_units_panel_row_card_r30_test.dart`, plus naval-suite `_pumpNaval`/`pumpNaval` clones, military-suite `_pumpMilitary`/`pumpMilitary` clones, `Scaffold(body: Center(...))` in governed 320 dp dialog/overlay suites, and inline `MaterialApp` in `production_panel_available_and_allocation_test.dart` / `production_panel_labour_and_chrome_test.dart` / `production_labour_section_test.dart` / `naval_units_panel_roster_and_draft_test.dart` / `naval_units_panel_combine_and_mockup_test.dart`, Refs #4021 / #4035 / #4058 / #4352), `check_app_test_no_duplicate_diplomacy_host.dart`, `check_app_test_no_duplicate_golden_host.dart`, `check_app_test_no_duplicate_yarn_bundle.dart`, `check_app_test_no_duplicate_dark_token_asserts.dart`, and the Widgetbook `_useCase` pattern in the scaffolding gate enforce this.

**App test file size (Refs #4013, #4021, #4305, #4352, #4606, #4642):** `repo.app_test_file_size` caps every `app/test/**/*.dart` file at **350 physical lines**. The shrink-only allowlist in `tool/check_app_test_file_size.dart` is **empty** after wave-15 Slice D densify; adding a new oversized file fails CI until it is split or extracted. Distinguishes from `repo.dart_file_non_comment_line_size` (1000 NCL, unchanged). Wave-15 AC10 concern-renames leftover `_partN` families that already share fixtures (`combat_ui_specs_*`, `dialogs_320dp_min_viewport_*`, `production_panel_*`, `app_event_handler_scope_*`, merged `e2e_helpers_barrel_test.dart`, `dialogue_overlays_specs_*`, `move_dialogs_specs_*`, `screen_spec_acceptance_*`, `new_game_leader_selection_dialog_*`, `game_screen_narrow_*`, `civilian_units_panel_*`, `ct_region_map_widget_*`, `game_map_area_state_logic_*`, `naval_units_panel_*`, `province_panel_draft_orders_military_and_naval_test.dart`) instead of adding further numbered shards.

**App e2e support file size (Refs #4075, #4195, #4344, #4598):** `repo.app_e2e_support_lib_file_size` caps every `packages/colonizethis_app_e2e_support/lib/**/*.dart` file at **250 physical lines**; `repo.app_e2e_support_test_file_size` caps every `packages/colonizethis_app_e2e_support/test/**/*.dart` file at **300 physical lines**. Oversized files must appear on the corresponding shrink-only allowlist; stale allowlist entries (missing path or file now ≤ cap) fail CI. Wave 3 (#4344) ratchets lib **400 → 300** and test **500 → 400** with empty allowlists after de-part / densify. Wave 4 (#4598) ratchets lib **300 → 250** after topic-splitting the remaining over-cap helpers. Wave 4 Slice B (#4598) places shared widget-test `MaterialApp`/`Scaffold` wrap, empty-app, and `showDialog` launcher hosts under `packages/colonizethis_app_e2e_support/test/support/` (`e2e_widget_pump_harness.dart`, `e2e_alert_dialog_pump_harness.dart`, plus next-turn / work-menu / overlay / opener hosts) so pin suites import them instead of re-declaring `_wrap` / `_pumpScaffold` clones. Wave 4 Slice C (#4598) densifies remaining ≥300 test hosts into `test/support/` groups, ratchets the test cap **400 → 300** with an **empty** shrink-only test allowlist, and adds `repo.app_e2e_support_no_duplicate_hosts` so private wrap/pump hosts (`_wrap`, `_pumpScaffold`, `_pumpDialog`, `_pumpEmpty`, `_pumpHost`) cannot return in pin files. Distinct from `repo.app_e2e_helper_location` (helper placement), `repo.app_e2e_support_no_part_directives` (no `part` / `part of` under package `lib/` and `test/`), and from repo-wide NCL 1000.

**App test support LOC (Refs #4021, #4035, #4048, #4058, #4117):** `repo.app_test_support_loc` fails when physical LOC of all Dart under `app/test/support/` exceeds the documented post-densify ceiling (**0**). After wave-9 slice F, harness modules live directly under `app/test/`; `app/test/support/` holds JSON fixtures only. Harness smoke tests live under `app/test/*_test_support_test.dart` and `app/test/*_harness_test.dart` (not counted toward the LOC ratchet). Seed-42 fixture loaders live in `app/test/game_fixture.dart`, `app/test/map_view_fixture.dart`, `app/test/tile_map_fixture.dart`, and `app/test/map_view_serialization.dart`. Lightweight panel `Game` fixtures live under `app/test/panel_fixtures/` (barrel `app/test/panel_test_fixtures.dart`).

**App test support file size (Refs #4048):** `repo.app_test_support_file_size` fails when any `app/test/support/**/*.dart` exceeds **600** physical lines unless listed on the shrink-only allowlist `appTestSupportFileSizeAllowlistForTests`. Stale allowlist entries (missing file, or file now ≤600) also fail.

**Turn lib file size (Refs #4113, #4342, #4583):** `repo.turn_lib_file_size` fails when any non-generated Dart file under `packages/colonizethis_turn/lib/**` exceeds **250** physical lines outside the shrink-only grandfather allowlist (empty after wave 8). Wave 4 landed a 400 ceiling; wave 7 ratchets to 300; wave 8 ratchets to 250 after pipeline-completion and spy-line splits.

**Turn test file size (Refs #4113, #4583):** `repo.turn_test_file_size` fails when any non-support Dart file under `packages/colonizethis_turn/test/**` exceeds **300** physical lines. Files under `test/support/` are excluded (governed by `repo.turn_test_support_loc`).

**Turn test no src imports (Refs #4252):** `repo.turn_test_no_src_imports` fails when any Dart file under `packages/colonizethis_turn/test/**` imports `package:colonizethis_turn/src/...`. Tests must use `package:colonizethis_turn/colonizethis_turn.dart` or `package:colonizethis_turn/colonizethis_turn_testing.dart`.

**Turn integration inline Game ctor (Refs #4252, #4168, #4583):** `repo.turn_test_integration_inline_game_ctor` fails when a file under `packages/colonizethis_turn/test/support/integration/**` constructs `Game(...)` inline. The wave-5 grandfather allowlist is empty after wave 8; integration scenarios must use `turn_*_test_harness.dart` / `turn_resolver_test_harness.dart` builders.

**World lib file size (Refs #4125, #4330, #4515, #4611):** `repo.colonizethis_world_lib_file_size` fails when any non-generated Dart file under `packages/colonizethis_world/lib/**` exceeds **250** physical lines outside the shrink-only grandfather allowlist (empty after wave-8), except `lib/src/game_events.dart` which has a dedicated **400** ceiling so sealed `GameEvent` variants can stay in one Dart library. Multi-file splits would require `part` directives (banned by `repo.world_no_part_directives`) or dropping `sealed` (see `SPEC/program/game-events.md`).

**World test file size (Refs #4125, #4330, #4515, #4611):** `repo.colonizethis_world_test_file_size` fails when any non-support Dart file under `packages/colonizethis_world/test/**` exceeds **250** physical lines outside the shrink-only grandfather allowlist (empty after wave-8 densify). Files under `test/world_test_support/` are excluded (governed by `repo.world_test_support_file_size` and builder-usage gates).

**World test support file size (Refs #4515, #4611):** `repo.world_test_support_file_size` fails when any Dart file under `packages/colonizethis_world/test/world_test_support/` exceeds **250** physical lines. Wave 8 densifies leftover support hosts (including `sea_reachable_test_support.dart`) so the support tree stays under this ceiling.

**World test mirrors lib (Refs #4330):** `repo.world_test_mirrors_lib` fails when any Dart source sits **directly** at `packages/colonizethis_world/test/` (no subdirectory). Tests and helpers must live under mirrored folders such as `test/world/`, `test/trace/`, or `test/world_test_support/`.

**Diplomacy lib file size (Refs #4130, #4341, #4574):** `repo.colonizethis_diplomacy_lib_file_size` fails when any non-generated Dart file under `packages/colonizethis_diplomacy/lib/**` exceeds **250** physical lines outside the shrink-only grandfather allowlist (empty after wave-7).

**Diplomacy test support file size (Refs #4130):** `repo.colonizethis_diplomacy_test_support_file_size` fails when any non-generated Dart file under `packages/colonizethis_diplomacy_test_support/lib/**` exceeds **400** physical lines outside the shrink-only grandfather allowlist (empty after wave-5 slice E densify).

**Turn test support LOC (Refs #4039, #4113, #4168, #4252):** `repo.turn_test_support_loc` fails when physical LOC of all Dart under `packages/colonizethis_turn/test/support/` exceeds the documented post-densify ceiling (**5700** after wave-6 completion). Ratchet downward as densify slices shrink the tree.

**Turn test support region literals (Refs #4168):** `repo.turn_test_support_region_literals` fails when any non-comment line under `packages/colonizethis_turn/test/support/**` contains a bare `'oldWorld'` or `'newWorld'` string literal; use `kRegionOldWorld` / `kRegionNewWorld` from `package:colonizethis_logic/colonizethis_logic.dart`.

**Feature-screen top-bar helper (Refs #3952):** Diplomacy / Production / Technology / Trade screens build Map-back + 18×18 icon + title chrome via `app/lib/widgets/game_feature_screen_top_bar.dart` (`GameFeatureScreenTopBar.build`); screen-specific `topBarKey` / title / icon asset constants remain on each screen.

**Low-risk e2e mirror barrel (Refs #3847):** Standalone `app/test/e2e_*_test.dart` files classified low-risk per issue #3847 (no timing/`pumpUntil`/`Stopwatch`, not AC-pin barrels, ≤2 top-level tests) MUST be covered by `app/test/e2e_low_risk_mirror_barrel_smoke_test.dart` and removed as standalone files. High-risk mirrors stay dedicated.

### Scan contract vs lib-only checkers (GitHub #2014)

**Domain collector** `collectRepoLintDomainDartFiles` in `tool/ct_repo_lint_scan_contract.dart` includes workspace **`packages/*/lib|test|integration_test`**, **`app/lib|test|integration_test`**, **`ctdev/lib|test`**, and **`tool/**`** Dart that passes `repoLintPathIsDomainLibSourceForScan` or `repoLintPathIsDomainTestOrIntegrationTestSourceForScan`: generated suffixes and `repoLintFixtureDirPathMarkers` paths are excluded; **repo-root `test/**`** (checker/tool tests) stays excluded so those files do not run production AST rules. Checkers using it include (non-exhaustive): `check_disallowed_ast_patterns`, `check_custom_exceptions`, `check_function_size`, `check_part_unit_size`, `check_control_flow_nesting_depth`; `check_debug_console_logic_contract_boundary` delegates to the disallowed-AST checker. Per-file guards in `check_disallowed_ast_patterns` use **`repoLintPathShouldSkipAstRuleFile`** (same exclusions except package `test/` is analyzed). `check_repeated_magic_numbers` remains scoped by its own SPEC (`SPEC/program/repeated-magic-numbers.md`) and excludes test trees.

**Identifier-literal helpers** `repoLintIdentifierLiteralShouldSkipFile` and **canonical tile-key** `collectRepoLintCanonicalProvinceTileKeyDartFiles` / `repoLintCanonicalProvinceTileKeyShouldSkipFile` use roots **`app`**, **`packages`**, **`ctdev`**, **`tool`** and include **`lib/`**, **`test/`**, and **`integration_test/`** under those trees (still skipping generated, repo-root `test/`, and fixture markers). **App hardcoded UI strings** remain **`app/lib/**` only** via `collectRepoLintAppLibDartFilesSorted` / `repoLintAppLibHardcodedUiVisitorShouldSkip` (production UI surface).

### CI workflow parity

**Today:** `.github/workflows/quality.yml` is the **only** workflow (as of the #2014 documentation slice) that runs `ct_repo_lint`, domain `custom_lint`, and (in `app_tests_cache`) `flutter analyze`. Any **additional** workflow that runs the same class of checks **must** match the **scope and exclusions** documented here.

**Workspace analyzer:** Implemented — see [CI contract](#ci-contract) (`quality` job, `tool/run_workspace_analyze_errors_only.dart`).

### Acceptance criteria (#2014 documentation)

- Given this section and CONTRIBUTING, when a maintainer widens a fatal repo rule to `test/`, then the change either **co-fixes violations** in the same PR or documents an allowed **audit/baseline** transition in SPEC with tracking reference.
- Given `.github/workflows/quality.yml`, when a contributor adds a new job that runs `ct_repo_lint`, domain `custom_lint`, or package analyze steps, then that job follows the **failure semantics** table above and the **exclusion** rules in this section unless a narrower rule SPEC explicitly overrides.
- Given manifest rule `repo.dart_long_string_switches`, when triaging #2014, then implementers treat long-string switch coverage as **already including tests** unless a deliberate SPEC change narrows scope.

### Phased roadmap (GitHub #2014, ≤5 mergeable slices)

Each slice must leave **`dev` required checks green**. Order matters: do **not** widen fatal repo-lint or scan-contract scope to `test/` / `integration_test/` until violations are fixed in the same PR or a SPEC-approved non-fatal transition exists.

| Slice | Scope | Status on `dev` (authoritative: workflow + tools) |
|-------|--------|-----------------------------------------------------|
| **1** | SPEC, CONTRIBUTING, testing-rule cross-links: scope, exclusions, mergeability, analyzer vs binary semantics, workflow job names | **Documented** — this table + CI contract above; other workflows audited (see below). |
| **2** | **`custom_lint`:** zero analyzer **error**-severity issues in `test/` and `integration_test/` for every package wired by `tool/run_custom_lint_domain_exceptions.sh` (same bar as `lib/`) | **CI enforced** — Quality runs the script after `ct_repo_lint`; fix violations in the same PR as any tightening. |
| **3** | **`dart analyze` / `flutter analyze`:** every Pub workspace package analyzed with test trees; **errors-only** gate | **CI enforced** — `quality` runs `dart run tool/run_workspace_analyze_errors_only.dart` after `dart pub get` (skips the **workspace host root** package; runs **`flutter pub get`** then **`flutter gen-l10n`** for Flutter packages with **`l10n.yaml`**); `app_tests_cache` keeps an early **`flutter analyze`** under `app/` only (redundant for app but preserves cache-job signal). When test-relevant paths change, **`app_build_linux`** runs **`flutter build linux --release --no-pub`** after restoring the same cache artifact (compile gate for the Linux desktop target). Local: `tool/run_quality_gate_tests.sh` includes the same workspace step. |
| **4** | **`ct_repo_lint` + manifest:** `collectRepoLintDomainDartFiles` includes package **`test/`** and **`integration_test/`** (and identifier-literal / canonical tile-key collectors include **`ctdev`** roots); violations fixed in the same change set | **Implemented** — see [Scan contract vs lib-only checkers](#scan-contract-vs-lib-only-checkers-github-2014). |
| **5** | **AST checkers** using the scan contract: per-file skip uses **`repoLintPathShouldSkipAstRuleFile`**; contract tests cover inclusion/exclusion | **Implemented** — `test/ct_repo_lint_scan_contract_test.dart`. |

Slices **4** and **5** may be **one PR** if scan-contract changes and checker fixes ship together, staying within the five-slice budget.

### Workflow audit (parity with #2014)

Only **`.github/workflows/quality.yml`** runs **`dart run tool/run_workspace_analyze_errors_only.dart`**, **`dart run tool/ct_repo_lint.dart`**, or **`bash tool/run_custom_lint_domain_exceptions.sh`**. A repository search of `.github/workflows/*.yml` for those strings finds no other matches as of the slice-1 documentation update. Any **new** workflow that runs the same class of checks must match **scope**, **exclusions**, and the **failure semantics** table in this section.

## PR incremental scans

Rules marked `pr_incremental: true` in the manifest receive `--files <csv>` when `GITHUB_BASE_REF` is set and `git fetch` / `git diff origin/<base>...HEAD -- '*.dart'` succeeds and yields paths—matching the previous inline `quality.yml` behavior. Use `--force-full-scan` to disable incremental arguments locally or in scripts.

Checker CLIs that accept `--files` / `--files=` should use `repoLintSplitRelativeDartPathsArg` in `tool/ct_repo_lint_scan_contract.dart` for splitting. Checkers that **ignore** unknown argv tokens use `repoLintParseIncrementalRelativeDartPathsFromArgs`; checkers that accept **only** `--files` / `--files=` use `repoLintParseStrictIncrementalFilesArgs`, or `repoLintStrictIncrementalFilesArgListOrExit` when they print the shared `ERROR:` stderr lines and exit 2 on parse failure (see `test/ct_repo_lint_scan_contract_test.dart`).

## Adding or changing rules

Do **not** add new top-level `tool/check_*.dart` **entrypoints** for CI without updating this SPEC and the manifest. Prefer:

1. Add a row under `rules:` in `tool/ct_repo_lint_manifest.yaml` with a new stable `rule_id`.
2. Implement or extend logic in a **library** or existing checker module; expose `int runCheck…(String repoRoot, …)` for `ct_repo_lint_lib.dart` to call in-process, keep a thin `main()` that `exit(runCheck…(…))` for `dart run`, and register the manifest row. Add a `switch` arm in `_tryRunDartRuleInProcess` when introducing a new stable `rule_id`.
3. Reuse `collectRepoLintDomainDartFiles` and related helpers from `ct_repo_lint_scan_contract.dart` when scanning the same domain trees (`lib/`, `test/`, `integration_test/` per contract); reuse identifier-literal roots/skip helpers, canonical province tile-key collectors, and app UI lib helpers where they match the checker’s domain (see contract tests).
4. Align wording with `colonizethis_exception_lint` (and similar) when the same policy exists in the analyzer.

## CLI options

`dart run tool/ct_repo_lint.dart --help` — `--list`, `--rule`, `--group`, `--manifest`, `--verbose`, `--force-full-scan`, `--sarif <path>`.

### SARIF (GitHub annotations / code scanning)

- **`--sarif <path>`** or **`--sarif=-`**: write **SARIF 2.1.0** (one result per failed rule); runs all selected rules; checker stdout goes to stderr so JSON stays valid. **`--list` and `--sarif` are mutually exclusive.**
- Optional CI upload via `github/codeql-action/upload-sarif` per GitHub docs.

## Follow-ups (out of scope for initial landing)

- Align **canonical province tile-key** and **app hardcoded UI string** checkers with the same contract patterns where it reduces duplication without changing coverage.
- Optionally lift shared roots into the manifest once all consumers read it.

## Acceptance criteria

- Given the repository root as cwd, when CI runs `dart run tool/ct_repo_lint.dart` with the workflow env, then all manifest rules applicable to that job execute and failures surface with `rule_id` in the orchestrator banner.
- Given a contributor adds a convention, when they follow CONTRIBUTING and this doc, then they register the rule in the manifest rather than adding a new standalone Quality workflow step for the same concern.
- Given `packages/colonizethis_ai/test/planning/domain_planner_orchestrator_*_test.dart` pins import `kGp1OwProvincesBelowQuota` / `kGp1OwProvincesAtQuota` from `test/support/domain_planner_orchestrator_test_support.dart` and do not redeclare `_gp1OwProvincesBelowQuota` / `_gp1OwProvincesAtQuota`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_orchestrator_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3941).
- Given a `domain_planner_orchestrator_*_test.dart` file that redeclares `const List<String> _gp1OwProvincesBelowQuota` or `const List<String> _gp1OwProvincesAtQuota`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_orchestrator_test_shared_fixtures`, then the run fails listing each offending file and exits `1` (Refs #3941).
- Given the shared support file `packages/colonizethis_ai/test/support/domain_planner_orchestrator_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_orchestrator_test_shared_fixtures`, then the rule exits `1` (Refs #3941).
- Given a `domain_planner_orchestrator_*_test.dart` pin that calls `buildOrchestratorExpandMinorWarScenarioGame` or `buildOrchestratorPendingCostTradeScenarioGame` and redeclares `AIWorldSnapshot _expandSnapshot()`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_orchestrator_test_shared_fixtures`, then the run fails and points callers at `buildOrchestratorExpandMinorWarAtWarSnapshot` (Refs #3997).
- Given those same pins call `buildOrchestratorExpandMinorWarAtWarSnapshot` and do not redeclare `_expandSnapshot`, when the rule runs, then it passes for the snapshot-clone check (Refs #3997).
- Given a `domain_planner_orchestrator_*_test.dart` pin that calls `buildOrchestratorGp1TribeNwScenarioGame` and redeclares `AIWorldSnapshot _expandSnapshot()` or `AIWorldSnapshot _colonialSnapshot()`, when the rule runs, then the run fails and points callers at `buildOrchestratorExpandNwTribeTargetSnapshot` / `buildOrchestratorColonialNwTribeTargetSnapshot` (Refs #3997).
- Given a pin that calls `buildOrchestratorExpandGpOnlyBlockerScenarioGame` and redeclares `_expandSnapshot` or `_developSnapshot`, when the rule runs, then the run fails and points callers at `buildOrchestratorExpandGpOnlyBlockerSnapshot` / `buildOrchestratorDevelopGpOnlyBlockerSnapshot` (Refs #3997).
- Given a pin that calls `buildOrchestratorExpandAdjacentMinorScenarioGame` and redeclares `_expandSnapshot` or `_developSnapshot`, when the rule runs, then the run fails and points callers at `buildOrchestratorExpandAdjacentMinorSnapshot` / `buildOrchestratorDevelopAdjacentMinorSnapshot` (Refs #3997).
- Given a pin that calls `buildOrchestratorDevelopGpOwnedNwScenarioGame` and redeclares `_developSnapshot`, when the rule runs, then the run fails and points callers at `buildOrchestratorDevelopNoColonialTargetsSnapshot` (Refs #3997).
- Given a pin that calls `buildOrchestratorColonialLiteDeclareWarScenarioGame` and redeclares `_colonialSnapshot` or `_colonialLiteSnapshot`, when the rule runs, then the run fails and points callers at `buildOrchestratorColonialNwTribeTargetSnapshot` / `buildOrchestratorExpandNwTribeTargetSnapshot` (Refs #3997).
- Given those NW-tribe / GP-blocker / adjacent-minor / DEVELOP GP-owned-NW / COLONIAL-lite declare-war pins call the shared snapshot builders and do not redeclare the matching local helpers, when the rule runs, then it passes for those snapshot-clone checks (Refs #3997).
- Given `diplomatic_candidate_scoring_intervention_tribe_tolerance_test.dart` or `diplomatic_candidate_scoring_personality_colonial_divergence_test.dart` redeclares `const List<String> _gp1OwProvincesAtQuota` or any local `AIWorldSnapshot _colonialSnapshot(` (brace-bodied or arrow wrapper), when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_orchestrator_test_shared_fixtures`, then the run fails and points callers at `kGp1OwProvincesAtQuota` / `buildOrchestratorColonialNwTribeTargetSnapshot` (Refs #3997).
- Given those diplomatic-scoring COLONIAL tribe pins import `kGp1OwProvincesAtQuota` / call `buildOrchestratorColonialNwTribeTargetSnapshot` and do not redeclare the matching local helpers (including thin `=>` wrappers), when the rule runs, then it passes for those snapshot-clone checks (Refs #3997).
- Given `expand_phase_planner_*peace*_test.dart` pins import `ownSnapshot` / shared Game builders from `test/support/expand_phase_peace_test_support.dart` and do not redeclare `_ownSnapshot` / `_criticalGame` / `_distractionGame` / `_zeroRegimentGame` / `_peerGame` / `_ownVsPartnerGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_expand_peace_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3967 / #3997).
- Given an `expand_phase_planner_*peace*_test.dart` file that redeclares `AIWorldSnapshot _ownSnapshot` or `Game _criticalGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_expand_peace_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #3967).
- Given an `expand_phase_planner_*peace*_test.dart` file that redeclares `Game _ownVsPartnerGame`, when the rule runs, then the run fails and points callers at `buildOwnVsPartnerExpandPeaceGame` (Refs #3997).
- Given documented Phase-8 ownSnapshot adopters (`expand_phase_planner_focus_minor_target_*_cases.dart`, `expand_phase_planner_stalled_minor_or_gp_blocker_pivot_test.dart`, `observer_goal_phase_survival_great_power_peace_targets_test.dart`) import `ownSnapshot` and do not redeclare `_ownSnapshot`, when the rule runs, then it passes (Refs #3997).
- Given one of those ownSnapshot adopter files redeclares `AIWorldSnapshot _ownSnapshot`, when the rule runs, then it fails and points callers at `ownSnapshot` (Refs #3997).
- Given `expand_phase_peace_matrix_sole_gp_blocker_guard_cases.dart` or `expand_phase_peace_matrix_sole_gp_blocker_fire_cases.dart` imports `buildOwnVsPartnerExpandPeaceGame` and does not redeclare `_ownVsPartnerGame`, when the rule runs, then it passes (Refs #3997).
- Given one of those sole-GP blocker cases files redeclares `Game _ownVsPartnerGame`, when the rule runs, then it fails and points callers at `buildOwnVsPartnerExpandPeaceGame` (Refs #3997).
- Given Phase-11 expand-peace adopters (`expand_phase_planner_default_start_and_near_quota_peace_test.dart`, stalled-futile / tribe-distraction cases, `expand_phase_planner_default_start_futile_minor_peace_test.dart`, `expand_phase_planner_below_quota_tribe_distraction_peace_test.dart`, `expand_phase_planner_stalled_minor_or_gp_blocker_pivot_test.dart`, `observer_goal_phase_survival_great_power_peace_targets_test.dart`, `expand_phase_peace_matrix_*_cases.dart`) import shared Game builders from `expand_phase_peace_test_support.dart` and do not redeclare `_gameOf` / `_ownGame` / `_tribeDistractionGame` / `_pivotGame` / `_survivalGame` / `_buildGame`, when the rule runs, then it passes (Refs #4239).
- Given a Phase-11 adopter redeclares `Game _gameOf`, `Game _ownGame`, `Game _tribeDistractionGame`, `Game _pivotGame`, `Game _survivalGame`, or `Game _buildGame`, when the rule runs, then it fails and points callers at the matching shared builder in `expand_phase_peace_test_support.dart` (Refs #4239).
- Given the shared support file `packages/colonizethis_ai/test/support/expand_phase_peace_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_expand_peace_test_shared_fixtures`, then the rule exits `1` (Refs #3967).
- Given `packages/colonizethis_ai/test/planning/colonial_phase_planner_acquisition_*_test.dart` pins import shared Game / DiplomacyRelation helpers from `test/support/colonial_acquisition_test_support.dart` and do not redeclare `_acquisitionGame` / `_purchaseLandGame` / `_declareWarGame` / `_bothValidGame` / `_friendly` / `_peaceFriendly` / `_atWar`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_acquisition_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3972).
- Given a `colonial_phase_planner_acquisition_*_test.dart` file that redeclares `Game _acquisitionGame` or `DiplomacyRelation _friendly`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_acquisition_test_shared_fixtures`, then the run fails listing each offending file and exits `1` (Refs #3972).
- Given the shared support file `packages/colonizethis_ai/test/support/colonial_acquisition_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_acquisition_test_shared_fixtures`, then the rule exits `1` (Refs #3972).
- Given every `expand_phase_planner_*peace*_test.dart` under `packages/colonizethis_ai/test/planning/` either has ≤300 physical lines or imports a sibling `*_cases.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_expand_peace_pin_cases_required`, then the rule passes without violations and exits `0` (Refs #3977, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4602 Slice D).
- Given an `expand_phase_planner_*peace*_test.dart` file has more than 300 physical lines and does not import a `*_cases.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_expand_peace_pin_cases_required`, then the run fails listing the offending file and exits `1` (Refs #3977, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4602 Slice D).
- Given `phase_planner_dispatch_test.dart` imports shared dispatch Game/snapshot builders from `colonial_phase_planner_test_support.dart` and does not redeclare `_colonialLiteGame` / `_colonialGame` / `_expandGame` / `_developGame` / `_expandSnapshot` / `_colonialLiteSnapshot` / `_colonialSnapshot` / `_developSnapshot`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_phase_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3977 / #3997).
- Given `phase_planner_dispatch_test.dart` redeclares `Game _colonialLiteGame`, `Game _expandGame`, or `AIWorldSnapshot _expandSnapshot` (or the other gated local helpers), when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_phase_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #3977 / #3997).
- Given the shared support file `packages/colonizethis_ai/test/support/colonial_phase_planner_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_phase_test_shared_fixtures`, then the rule exits `1` (Refs #3977).
- Given `expand_phase_planner_nw_suppression_test.dart` and `develop_phase_planner_nw_suppression_test.dart` import shared NW-suppression Game/snapshot builders from `phase_planner_nw_suppression_test_support.dart` and do not redeclare `_expandGame` / `_expandSnapshot` / `_developGame` / `_developSnapshot`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_phase_planner_nw_suppression_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3997).
- Given `expand_phase_planner_nw_suppression_test.dart` redeclares `Game _expandGame` or `AIWorldSnapshot _expandSnapshot` (or develop pin redeclares `_developGame` / `_developSnapshot`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_phase_planner_nw_suppression_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #3997).
- Given the shared support file `packages/colonizethis_ai/test/support/phase_planner_nw_suppression_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_phase_planner_nw_suppression_test_shared_fixtures`, then the rule exits `1` (Refs #3997).
- Given `cast_iron_labour_gate_test.dart` and `domain_planner_orchestrator_h8_feedstock_civilian_work_test.dart` import shared lock-recovery seller builders from `lock_recovery_seller_test_support.dart` and do not redeclare `_lockRecoverySellerGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_lock_recovery_seller_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3997).
- Given either adopter redeclares `Game _lockRecoverySellerGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_lock_recovery_seller_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #3997).
- Given the shared support file `packages/colonizethis_ai/test/support/lock_recovery_seller_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_lock_recovery_seller_test_shared_fixtures`, then the rule exits `1` (Refs #3997).
- Given the treasury satellite pins (`treasury_planner_forecasting_test.dart`, `treasury_planner_boycott_suppression_test.dart`, `treasury_planner_supplier_castiron_source_test.dart`, `treasury_planner_trade_deal_relation_boost_test.dart`, `treasury_planner_offerable_fabric_test.dart`) import shared Game factories and do not redeclare `_gameWithStockpile` / `_game`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_treasury_satellite_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #4104).
- Given a gated treasury satellite pin redeclares `Game _gameWithStockpile` or `Game _game`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_treasury_satellite_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #4104).
- Given `treasury_planner_main_support.dart` or `treasury_planner_satellite_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_treasury_satellite_test_shared_fixtures`, then the rule exits `1` (Refs #4104).
- Given `recruitment_planner_test.dart` and `recruitment_planner_paper_ledger_test.dart` import `recruitmentPlannerTestGameWith` and do not redeclare `_gameWith`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_recruitment_planner_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #4104).
- Given either recruitment pin redeclares `Game _gameWith`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_recruitment_planner_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #4104).
- Given `recruitment_planner_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_recruitment_planner_test_shared_fixtures`, then the rule exits `1` (Refs #4104).
- Given `phase_planner_diplomacy_ow_bonus_scaling_test.dart` and `phase_planner_diplomacy_declare_war_nw_suppression_test.dart` import shared diplomacy-filter Game builders and do not redeclare `_buildGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_phase_planner_diplomacy_filter_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #4104).
- Given either phase-diplomacy filter pin redeclares `Game _buildGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_phase_planner_diplomacy_filter_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #4104).
- Given `phase_planner_diplomacy_filter_test_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_phase_planner_diplomacy_filter_test_shared_fixtures`, then the rule exits `1` (Refs #4104).
- Given every `*.dart` under `packages/colonizethis_ai/test/support/s7d/` has ≤300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_s7d_support_suite_size`, then the rule passes without violations and exits `0` (Refs #3997, #4079, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice A, #4602 Slice E).
- Given an S7D support module has more than 300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_s7d_support_suite_size`, then the run fails listing the offending file and exits `1` (Refs #3997, #4079, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice A, #4602 Slice E).
- Given every `*.dart` under `packages/colonizethis_ai/test/observer/` has ≤300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_observer_suite_size`, then the rule passes without violations and exits `0` (Refs #4530, #4602).
- Given an observer suite file has more than 300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_observer_suite_size`, then the run fails listing the offending file and exits `1` (Refs #4530, #4602).
- Given every in-scope `*.dart` under `packages/colonizethis_ai/test/support/` (excluding `s7d/`) has ≤300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_support_file_size`, then the rule passes without violations and exits `0` (Refs #4291, #4602).
- Given an in-scope AI test support module has more than 300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_support_file_size`, then the run fails listing the offending file and exits `1` (Refs #4291, #4602).
- Given `colonial_phase_planner_military_test.dart`, `colonial_phase_planner_naval_test.dart`, and `colonial_phase_planner_colonial_lite_naval_test.dart` each either have ≤300 physical lines or import a sibling `*_cases.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_military_naval_pin_cases_required`, then the rule passes without violations and exits `0` (Refs #3977, #3997, #4365 Slice C, #4602 Slice D).
- Given a gated military, naval, or lite-naval contract file has more than 300 physical lines and does not import a `*_cases.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_colonial_military_naval_pin_cases_required`, then the run fails listing the offending file and exits `1` (Refs #3977, #3997, #4365 Slice C, #4602 Slice D).
- Given every `*_cases.dart` under `packages/colonizethis_ai/test/planning/` either has ≤300 physical lines or is registered in `aiPlanningCasesSuiteSizeAllowlist` with an issue Ref, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_planning_cases_suite_size`, then the rule passes without violations and exits `0` (Refs #3997, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4602 Slice D).
- Given a `packages/colonizethis_ai/test/planning/*_cases.dart` file has more than 300 physical lines and is not allowlisted, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_planning_cases_suite_size`, then the run fails listing the offending file and exits `1` (Refs #3997, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4602 Slice D).
- Given every `*.dart` under `packages/colonizethis_ai/test/planning/` and `test/support_test/` has ≤300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_file_size`, then the rule passes without violations and exits `0` (Refs #4602).
- Given a planning or support_test Dart file has more than 300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_file_size`, then the run fails listing the offending file and exits `1` (Refs #4602).
- Given each gated residual fat-pin contract (Phase-8/9 set plus Phase-10 Slice C densify basenames, Phase-11 Slice C densify basenames, and Phase-12 Slice D densify basenames under `planning/` or `support_test/` as listed in `residualFatPinSuiteGatedBasenames`) either has ≤300 physical lines or imports a sibling `*_cases.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_residual_fat_pin_cases_required`, then the rule passes without violations and exits `0` (Refs #3997, #4079, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4602 Slice D).
- Given a gated residual fat-pin contract file has more than 300 physical lines and does not import a `*_cases.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_residual_fat_pin_cases_required`, then the run fails listing the offending file and exits `1` (Refs #3997, #4079, #4104 Slice C, #4239 Slice C, #4291 Slice D, #4310 Slice D, #4602 Slice D).
- Given `packages/colonizethis_ai/test/planning/economy_planner_regiment_build*_test.dart` pins import shared Game factories from `test/planning/economy_planner_regiment_build_input_support.dart` and do not redeclare `_regimentRebuildProductionGame` / `_castIronImprovementInputGame` / `_supplierCastIronSourceGame` / `_castIronFeedstockCoavailabilityGame` / `_castIronStagingNoFabricGateGame` / `_castIronLabourPeasantRecruitFabricStagingGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_economy_regiment_build_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #3972).
- Given an `economy_planner_regiment_build*_test.dart` file that redeclares `Game _regimentRebuildProductionGame` or `Game _castIronImprovementInputGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_economy_regiment_build_test_shared_fixtures`, then the run fails listing each offending file and exits `1` (Refs #3972).
- Given the shared support file `packages/colonizethis_ai/test/planning/economy_planner_regiment_build_input_support.dart` is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_economy_regiment_build_test_shared_fixtures`, then the rule exits `1` (Refs #3972).
- Given `packages/colonizethis_ai/test/planning/` has no `*_partN_test.dart` (N ≥ 2) alongside a sibling `{stem}_support.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_no_matrix_part_shards`, then the rule passes without violations and exits `0` (Refs #3941).
- Given a planning `*_part2_test.dart` of stem `S` and a sibling `S_support.dart` both exist under `packages/colonizethis_ai/test/planning/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_no_matrix_part_shards`, then the run fails listing the part shard and exits `1` (Refs #3941).
- Given a planning `*_part2_test.dart` exists without a sibling `{stem}_support.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_test_no_matrix_part_shards`, then the rule does not flag that mid-migration shard and exits `0` when no other violations exist (Refs #3941).
- Given the [Policy: no violation allowlists](#policy-no-violation-allowlists-repo-lint) section, when `repo.function_size`, `repo.control_flow_nesting_depth`, or `repo.part_unit_size` runs, then that checker does not load keyed waiver data and the matching per-rule SPEC documents universal enforcement only.
- Given the same policy section, when `repo.custom_exceptions` runs, then that checker does not load keyed waiver data and `SPEC/program/exception-enforcement.md` documents enforcement with scope-only skips (generated suffixes, test paths, and the shared domain file collector) only.
- Given the economy cost-check consumers `packages/colonizethis_economy/lib/src/economy/worker_action_cost.dart` and `.../build_cost.dart` both call `checkPreconditionsInOrder` and `.../cost_check.dart` defines that helper, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_cost_check_shared_helper`, then the rule passes without violations and exits `0` (Refs #3517 Cluster 2).
- Given either economy cost-check consumer (`worker_action_cost.dart` or `build_cost.dart`) no longer references `checkPreconditionsInOrder`, or `cost_check.dart` no longer defines the helper, or any of the three files is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_cost_check_shared_helper`, then the run fails listing each offending file and exits `1`, directing the author to delegate the priority-ordered first-failure sweep to the shared `cost_check.dart` helper (Refs #3517 Cluster 2).
- Given `force_feeding_readiness.dart` and `economy_consumption_allocation.dart` both call `allocateMilitaryNavyFood` and that helper is defined, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_force_feeding_shared_helper`, then the rule passes without violations and exits `0` (Refs #4631).
- Given `force_feeding_readiness.dart` calls `consumeMilitaryFood` or inlines `RegimentEconomyCatalog` instead of `allocateMilitaryNavyFood`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_force_feeding_shared_helper`, then the run fails naming that file and exits `1` (Refs #4631).
- Given `resource_extractor.dart`, `development_panel_read_model_scopes.dart`, and `province_extraction_snapshot_builder.dart` call `capitalTileGrainBonusForPlayer` and no other economy `lib/**` file uses non-comment `capitalTileGrainBonusPerTurn` outside the helper, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_grain_bonus_shared_helper`, then the rule passes without violations and exits `0` (Refs #4631).
- Given an economy `lib/**` consumer inlines `capitalTileGrainBonusPerTurn` instead of `capitalTileGrainBonusForPlayer`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_grain_bonus_shared_helper`, then the run fails naming that file and exits `1` (Refs #4631).
- Given every Dart file under `packages/colonizethis_economy/test/**` has ≤250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_file_size`, then the rule passes without violations and exits `0` (Refs #4631).
- Given an economy test Dart file has more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_file_size`, then the run fails naming that file and exits `1` (Refs #4631).
- Given no `*_test_support.dart` basename exists under `packages/colonizethis_economy/test/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_no_local_world_market_support`, then the rule passes without violations and exits `0` (Refs #4631).
- Given a `packages/colonizethis_economy/test/**/*_test_support.dart` file exists, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_no_local_world_market_support`, then the run fails naming that file and exits `1` (Refs #4631).
- Given the world-market consumers `trade_order_validator.dart` and `trade_order_suggester.dart` both import `trade_order_admission.dart` and the module defines `isWorldMarketTradeableCommodity`, `commoditiesWithBidAndOffer`, and `admittedBidCommodityIdsInSubmissionOrder`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_world_market_admission_shared`, then the rule passes without violations and exits `0` (Refs #3615 Cluster 1).
- Given either world-market consumer (`trade_order_validator.dart` or `trade_order_suggester.dart`) no longer imports `trade_order_admission.dart`, or the module no longer defines one of the three shared admission helpers, or any of those files is missing, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_world_market_admission_shared`, then the run fails listing each offending file and exits `1`, directing the author to delegate riches/mutual-exclusion/bid-admission logic to the shared module (Refs #3615 Cluster 1).
- Given no `packages/colonizethis_economy/lib/**` source outside `game_lookup_helpers.dart` inlines `portsByProvinceSeaboard.values.toSet()`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_dedup_port_tile_keys`, then the rule passes without violations and exits `0` (Refs #3615 Cluster 3).
- Given an economy `lib/**` source other than `game_lookup_helpers.dart` inlines `portsByProvinceSeaboard.values.toSet()` on a non-comment line, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_dedup_port_tile_keys`, then the run fails listing each offending `file:line` and exits `1`, directing the author to use `collectPortTileKeys(game)` (Refs #3615 Cluster 3).
- Given no `test('...')` / `testWidgets('...')` description string appears in two or more files under `packages/colonizethis_economy/test/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_duplicate_descriptions`, then the rule passes without violations and exits `0` (Refs #3661).
- Given the same single-line `test('...')` / `testWidgets('...')` description string appears in two or more distinct files under `packages/colonizethis_economy/test/` on non-comment lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_duplicate_descriptions`, then the run fails listing the duplicated description and the files it appears in and exits `1`, directing the author to consolidate the scenario into one file or give genuinely distinct tests distinct descriptions (Refs #3661).
- Given the `colonizethis_orders` package on `dev` where no `lib/**` file imports another colonizethis package's `src/` tree, when `dart run tool/ct_repo_lint.dart` runs rule `repo.orders_no_cross_package_src_imports`, then the rule passes without violations and exits `0` (Refs #3543).
- Given a `colonizethis_orders/lib/**` file that imports `package:colonizethis_world/src/world/sea_reachable_provinces.dart` (or any other `package:colonizethis_<pkg>/src/...` path where `<pkg>` is not `orders`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.orders_no_cross_package_src_imports`, then the run fails listing each offending `file -> import` pair and exits `1`, directing the author to the owning package's public barrel (Refs #3543).
- Given the `colonizethis_world` package on `dev` where no `lib/**` Dart file participates in an `import` / `export` cycle with another file in that tree, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_circular_imports`, then the rule reports the scanned file count, passes without violations, and exits `0` (Refs #3544).
- Given a `packages/colonizethis_world/lib/**` Dart file that imports a second world `lib/**` file which (directly or transitively) imports the first back, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_circular_imports`, then the run fails listing the offending cycle as an ordered file chain and exits `1`, directing the author to break the cycle by extracting the shared symbol into a third file (Refs #3544).
- Given the `packages/colonizethis_world/lib` tree is absent, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_circular_imports`, then the rule reports the missing tree and exits `1`.

- Given the `colonizethis_combat` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.combat_no_part_directives`, then the rule passes without violations and exits `0` (Refs #3983).
- Given a `packages/colonizethis_combat/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.combat_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #3983).
- Given a combat `test/**/*_test.dart` file that contains a bare `for (final scenario in …)` loop, when `dart run tool/ct_repo_lint.dart` runs rule `repo.combat_test_scenario_harness`, then the run fails listing each offending `path:line` and exits `1` (Refs #3983).
- Given the `colonizethis_world` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_part_directives`, then the rule passes without violations and exits `0` (Refs #3968).
- Given a `packages/colonizethis_world/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #3968).
- Given every non-generated Dart file under `packages/colonizethis_world/lib/**` except `lib/src/game_events.dart` has ≤250 physical lines, `game_events.dart` has ≤400 physical lines, and the shrink-only grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_world_lib_file_size`, then the rule passes without violations and exits `0` (Refs #4125, #4330, #4515, #4611).
- Given a non-generated `packages/colonizethis_world/lib/**` Dart file other than `lib/src/game_events.dart` with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_world_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4125, #4330, #4515, #4611).
- Given `packages/colonizethis_world/lib/src/game_events.dart` has more than 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_world_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4515).
- Given every non-support Dart file under `packages/colonizethis_world/test/**` (excluding `test/world_test_support/`) has ≤250 physical lines and the shrink-only grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_world_test_file_size`, then the rule passes without violations and exits `0` (Refs #4125, #4330, #4515, #4611).
- Given a non-support `packages/colonizethis_world/test/**` Dart file with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_world_test_file_size`, then the run fails naming that file and exits `1` (Refs #4125, #4330, #4515, #4611).
- Given every Dart file under `packages/colonizethis_world/test/world_test_support/` has ≤250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_support_file_size`, then the rule passes without violations and exits `0` (Refs #4515, #4611).
- Given a `packages/colonizethis_world/test/world_test_support/` Dart file with more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_support_file_size`, then the run fails naming that file and exits `1` (Refs #4515, #4611).
- Given every non-generated Dart file under `packages/colonizethis_turn/lib/**` has ≤250 physical lines and the shrink-only grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.turn_lib_file_size`, then the rule passes without violations and exits `0` (Refs #4113, #4342, #4583).
- Given a non-generated `packages/colonizethis_turn/lib/**` Dart file with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.turn_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4113, #4342, #4583).
- Given every non-support Dart file under `packages/colonizethis_turn/test/**` (excluding `test/support/`) has ≤300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.turn_test_file_size`, then the rule passes without violations and exits `0` (Refs #4113, #4583).
- Given a non-support `packages/colonizethis_turn/test/**` Dart file with more than 300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.turn_test_file_size`, then the run fails naming that file and exits `1` (Refs #4113, #4583).
- Given `_allowlistedRelativePaths` in `tool/check_turn_test_integration_inline_game_ctor.dart` is empty and no `packages/colonizethis_turn/test/support/integration/**` file constructs `Game(`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.turn_test_integration_inline_game_ctor`, then the rule passes without violations and exits `0` (Refs #4252, #4168, #4583).
- Given a `packages/colonizethis_turn/test/support/integration/**` Dart file that constructs `Game(` inline, when `dart run tool/ct_repo_lint.dart` runs rule `repo.turn_test_integration_inline_game_ctor`, then the run fails naming that file and exits `1` (Refs #4252, #4168, #4583).
- Given every non-generated Dart file under `packages/colonizethis_orders/lib/**` has ≤250 physical lines and the shrink-only grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.orders_lib_source_file_size`, then the rule passes without violations and exits `0` (Refs #4246, #4317, #4508, #4587).
- Given a non-generated `packages/colonizethis_orders/lib/**` Dart file with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.orders_lib_source_file_size`, then the run fails naming that file and exits `1` (Refs #4246, #4317, #4508, #4587).
- Given package `packages/colonizethis_orders/test/**/*.dart` physical LOC is at or below 15800, when `dart run tool/ct_repo_lint.dart` runs rule `repo.orders_test_support_loc`, then the rule passes without violations and exits `0` (Refs #4508).
- Given package `packages/colonizethis_orders/test/**/*.dart` physical LOC exceeds 15800, when `dart run tool/ct_repo_lint.dart` runs rule `repo.orders_test_support_loc`, then the run fails reporting the package test LOC and exits `1` (Refs #4508).
- Given `packages/colonizethis_economy_test_support/lib/**` has no `*_expectations.dart` files, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_support_no_expectations_modules`, then the rule passes without violations and exits `0` (Refs #4410).
- Given a planted `packages/colonizethis_economy_test_support/lib/src/foo_expectations.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_test_support_no_expectations_modules`, then the run fails listing that file and exits `1` (Refs #4410).
- Given the `colonizethis_economy` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_no_part_directives`, then the rule passes without violations and exits `0` (Refs #3979).
- Given a `packages/colonizethis_economy/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.economy_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #3979).
- Given the `colonizethis_models` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_no_part_directives`, then the rule passes without violations and exits `0` (Refs #4068).
- Given a `packages/colonizethis_models/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #4068).
- Given the `colonizethis_ai` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_no_part_directives`, then the rule passes without violations and exits `0` (Refs #4079).
- Given a `packages/colonizethis_ai/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #4079).
- Given the `colonizethis_ai_contracts` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_no_part_directives`, then the rule passes without violations and exits `0` (Refs #4084).
- Given a `packages/colonizethis_ai_contracts/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #4084).
- Given the `colonizethis_setup` package on `dev` where no `lib/**` file uses a `part` / `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.setup_no_part_directives`, then the rule passes without violations and exits `0` (Refs #4086).
- Given a `packages/colonizethis_setup/lib/**` Dart file that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.setup_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #4086).
- Given every non-generated Dart file under `packages/colonizethis_setup/lib/**` is at or below 250 physical lines and the shrink-only grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_lib_file_size`, then the rule passes without violations and exits `0` (Refs #4273, #4349, #4624).
- Given a non-generated `packages/colonizethis_setup/lib/**` Dart file with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4624).
- Given every non-support Dart file under `packages/colonizethis_setup/test/**` (excluding `test/setup/support/`) is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_test_file_size`, then the rule passes and exits `0` (Refs #4624).
- Given a planted `packages/colonizethis_setup/test/**` Dart file outside `test/setup/support/` with 251 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_test_file_size`, then the run fails naming that file and exits `1` (Refs #4624).
- Given every `*.dart` file under `packages/colonizethis_setup/test/setup/support/` has fewer than 250 physical lines and the tree total is at or below 4100, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_test_support_loc`, then the rule passes and exits `0` (Refs #4273, #4349).
- Given a `packages/colonizethis_setup/test/setup/support/**` Dart file with 250 or more physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_test_support_loc`, then the run fails naming that file and exits `1` (Refs #4349).
- Given the support tree physical LOC exceeds 4100 while every file is under 250 lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_setup_test_support_loc`, then the run fails reporting the tree total and exits `1` (Refs #4273, #4349).
- Given `app/lib/**` on `dev` where every `part` / `part of` directive is confined to the shrink-only grandfather allowlist in `tool/app_no_part_directives_grandfather.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_part_directives`, then the rule passes without violations and exits `0` (Refs #4117).
- Given an `app/lib/**` Dart file not on the grandfather allowlist that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #4117).
- Given a stale grandfather allowlist entry (missing file or file no longer containing a `part` directive), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_part_directives`, then the run fails with a stale-entry message and exits `1` (Refs #4117).
- Given `packages/colonizethis_data/lib/**` on `dev` where every `part` / `part of` directive is confined to the shrink-only grandfather allowlist in `tool/data_no_part_directives_grandfather.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_no_part_directives`, then the rule passes without violations and exits `0` (Refs #4121).
- Given a `packages/colonizethis_data/lib/**` Dart file not on the grandfather allowlist that contains `part 'foo.dart';` or `part of 'foo.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_no_part_directives`, then the run fails listing each offending `path:line` and exits `1` (Refs #4121).
- Given a stale grandfather allowlist entry (missing file or file no longer containing a `part` directive), when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_no_part_directives`, then the run fails with a stale-entry message and exits `1` (Refs #4121).
- Given the H8 seller acquisition-target and OW prospect localization / mineral-eligibility pins import `flaggedSellerGame` / `supplierGame` from `packages/colonizethis_ai_contracts/test/support/` and do not redeclare `_flaggedSellerGame` / `_supplierGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_h8_test_shared_fixtures`, then the rule passes without violations and exits `0` (Refs #4084).
- Given a gated H8 seller acquisition-target pin that redeclares `Game _flaggedSellerGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_h8_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #4084).
- Given a gated OW prospect localization or mineral-eligibility pin that redeclares `Game _supplierGame`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_h8_test_shared_fixtures`, then the run fails listing the offending file and exits `1` (Refs #4084).
- Given either shared support file `h8_flagged_seller_game.dart` or `h8_supplier_prospect_game.dart` is missing under `packages/colonizethis_ai_contracts/test/support/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_h8_test_shared_fixtures`, then the rule exits `1` (Refs #4084).
- Given every hand-written file under `packages/colonizethis_ai_contracts/lib/**` is at or below 260 physical lines and the grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_source_file_size`, then the rule passes without violations and exits `0` (Refs #4368).
- Given a hand-written `packages/colonizethis_ai_contracts/lib/**` Dart file with 261 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_source_file_size`, then the run fails naming that file and exits `1` (Refs #4368).
- Given the `packages/colonizethis_ai_contracts/lib` scan root is missing, when `repo.ai_contracts_source_file_size` runs, then the rule exits `1` (Refs #4368).
- Given every `.dart` file under `packages/colonizethis_ai_contracts/test/**` is at or below 280 physical lines and the grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_test_file_size`, then the rule passes without violations and exits `0` (Refs #4368).
- Given a `packages/colonizethis_ai_contracts/test/**` Dart file with 281 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_contracts_test_file_size`, then the run fails naming that file and exits `1` (Refs #4368).
- Given the `packages/colonizethis_ai_contracts/test` scan root is missing, when `repo.ai_contracts_test_file_size` runs, then the rule exits `1` (Refs #4368).
- Given every hand-written file under `packages/colonizethis_ai/lib/src/**` is at or below 250 physical lines and the grandfather allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_source_file_size`, then the rule passes without violations and exits `0` (Refs #4079, #4104, #4239, #4310, #4365, #4602).
- Given a hand-written `packages/colonizethis_ai/lib/src/**` Dart file with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.ai_source_file_size`, then the run fails naming that file and exits `1` (Refs #4079, #4104, #4239, #4310, #4365, #4602).
- Given a grandfather allowlist entry for `repo.ai_source_file_size` that names a missing file or a file now at or below 250 physical lines, when the rule runs, then the run fails reporting a stale grandfather entry and exits `1` (Refs #4079, #4104, #4239, #4310, #4365, #4602).
- Given every `.dart` file under `packages/colonizethis_models/test/**` is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_test_file_size`, then the rule passes without violations and exits `0` (Refs #4068, #4571).
- Given a `packages/colonizethis_models/test/**` Dart file with more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_test_file_size`, then the run fails naming that file and exits `1` (Refs #4068, #4571).
- Given every non-generated `.dart` file under `packages/colonizethis_models/lib/**` is at or below 250 physical lines with an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_lib_physical_file_size`, then the rule passes without violations and exits `0` (Refs #4334, #4571).
- Given a non-generated `packages/colonizethis_models/lib/**` Dart file with more than 250 physical lines and an empty grandfather allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_lib_physical_file_size`, then the run fails naming that file and exits `1` (Refs #4334, #4571).
- Given every models test Dart source lives in a subdirectory under `packages/colonizethis_models/test/` (no loose files at the `test/` root except paths under `test/support/`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_test_mirrors_lib`, then the rule passes and exits `0` (Refs #4334).
- Given a Dart source file directly at `packages/colonizethis_models/test/` root (for example `test/player_test.dart`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.models_test_mirrors_lib`, then the run fails naming that file and exits `1` (Refs #4334).
- Given `topology_identity.dart` defines the four identity helpers and `naval.dart` does not re-declare them as top-level functions, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_topology_identity_home`, then the rule passes and exits `0` (Refs #3968).
- Given `naval.dart` contains a top-level function declaration named `provinceTopologyNodeId`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_topology_identity_home`, then the run fails listing the offending `path:line` and exits `1` (Refs #3968).
- Given each required world support builder (`spyRevealFogGame`, `ownershipTransferVisibilityGame`, `coastalSeaVisibilityGame`, `capitalLossGame`, `gpCapitalLossGame`, `minorCapitalLossGame`, `gpCapitalReassignmentGame`, `factionCapitalReassignmentGame`, `ordersPhaseGame`, `topologyGraph`) has ≥1 call site under `packages/colonizethis_world/test/**` outside `world_test_support/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_support_builders_used`, then the rule passes and exits `0` (Refs #3968 / #4038).
- Given a required world support builder has zero call sites outside `world_test_support/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_support_builders_used`, then the run fails naming the unused builder and exits `1` (Refs #3968 / #4038).
- Given an in-scope fog/capital/connectivity/ownership/player_view/travers world test outside `world_test_support/` that inlines `Game(...)` or `WorldState(...)`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_no_raw_game_worldstate`, then the run fails naming the file and exits `1` (Refs #3978, #4038).
- Given in-scope fog/capital/connectivity/ownership/player_view/travers world tests construct games only via shared builders or `TestFixtures`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_no_raw_game_worldstate`, then the rule passes and exits `0` (Refs #3978, #4038).
- Given the `colonizethis_world` package on `dev` where the free `allProvinces(WorldState)` function delegates to the `WorldState.allProvinces()` extension method, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_duplicate_extension_helper`, then the rule passes without violations and exits `0` (Refs #3710).
- Given a top-level function under `packages/colonizethis_world/lib/**` named `allProvinces` whose body re-implements dual-region traversal (for example `sync* { yield* world.oldWorld.provinces; yield* world.newWorld.provinces; }`) instead of delegating to `world.allProvinces()`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_no_duplicate_extension_helper`, then the run fails listing each offending `path:line` and exits `1`, directing the author to delegate to the `WorldState` extension method (Refs #3710).
- Given a file under `packages/colonizethis_world/test/**` that imports `package:colonizethis_logic/`, `package:colonizethis_turn/`, or `package:colonizethis_orders/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_no_upstream_domain_deps`, then the run fails naming that file and exits `1` (Refs #4515).
- Given `packages/colonizethis_world/pubspec.yaml` lists `colonizethis_logic`, `colonizethis_turn`, or `colonizethis_orders`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_no_upstream_domain_deps`, then the run fails naming the pubspec and exits `1` (Refs #4515).
- Given world tests import only world / models / data / `colonizethis_test` and the world pubspec does not list logic, turn, or orders, when `dart run tool/ct_repo_lint.dart` runs rule `repo.world_test_no_upstream_domain_deps`, then the rule passes and exits `0` (Refs #4515).
- Given the `app/lib` tree on `dev` where only `app/lib/features/game/widgets/shell/shell_player_context.dart` (the declaration site) and `app/lib/features/game/widgets/shell/shell_player_guarded_body.dart` (the canonical helper) reference `shellPanelsNotDefined`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_shell_panel_duplication`, then the rule passes without violations and exits `0` (Refs #3546).
- Given an `app/lib/**` Dart file other than the declaration site and the canonical helper that contains a non-comment line calling `shellPanelsNotDefined(...)`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_shell_panel_duplication`, then the run fails listing each offending `path:line` and exits `1`, directing the author to route the observe-mode guard through `observeNotDefinedSentinel` in `app/lib/features/game/widgets/shell/shell_player_guarded_body.dart` (Refs #3546).
- Given an `app/lib/**` Dart file that only mentions `shellPanelsNotDefined` inside a line comment (`//` or `///`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_shell_panel_duplication`, then the rule does not flag that line (Refs #3546).
- Given the canonical helper `app/lib/features/game/widgets/shell/chrome_anchored_popover.dart` is the only `app/lib` file that constructs `OverlayEntry` together with `DismissIntent` and `dialogScrim`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_chrome_popover_sot`, then the rule passes and exits `0` (Refs #4582).
- Given a temporary `app/lib` Dart file outside that helper that contains `OverlayEntry`, `DismissIntent`, and `dialogScrim`, when `repo.app_chrome_popover_sot` runs, then the checker exits `1` and names that file (Refs #4582).
- Given the `app/lib/widgetbook/` tree on `dev` where every catalog `part` file uses a descriptive domain name (e.g. `catalog_panels.dart`, `catalog_dialogs.dart`, `catalog_primitives.dart`) and no `catalog_part<N>.dart` file exists, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_widgetbook_file_naming`, then the rule passes without violations and exits `0` (Refs #3546).
- Given an `app/lib/widgetbook/` Dart file whose basename matches `catalog_part<N>.dart` (for example `catalog_part1.dart`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_widgetbook_file_naming`, then the run fails listing each offending `path` and exits `1`, directing the author to rename the fragment to a descriptive domain name (Refs #3546).
- Given app game feature code, when `dart run tool/ct_repo_lint.dart` runs rule `repo.no_screen_in_game_widgets`, then no file matching `*_screen.dart` exists under `app/lib/features/game/widgets/**`.
- Given app game panel widgets under `app/lib/features/game/widgets/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_panel_screen_id`, then every public class whose name ends with `Panel` and whose constructor declares a `game` parameter also declares a `static const screenId`, and the run fails listing each violating `path:line` when such a panel omits it; panels without a `game` parameter and sub-panels/slots outside that directory are not flagged.
- Given app game widget Dart files under `app/lib/features/game/widgets/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.game_widgets_file_size`, then each scanned file has at most 250 physical lines, the run fails listing each violating path when any file exceeds 250, and the checker does not load keyed waiver data.
- Given every hand-written Dart file under `app/lib/core/services/**` is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_core_services_file_size`, then the rule passes without violations and exits `0` (Refs #4450, #4582, #4606).
- Given every hand-written Dart file under `app/lib/features/game/flame/**` is at or below 250 physical lines and the shrink-only grandfather is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_flame_lib_physical_file_size`, then the rule passes without violations and exits `0` (Refs #4352, #4582, #4606).
- Given every hand-written Dart file under `app/lib/features/game/screens/**` is at or below 250 physical lines and the shrink-only grandfather is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_features_screens_lib_physical_file_size`, then the rule passes without violations and exits `0` (Refs #4352, #4582, #4606).
- Given every hand-written Dart file under `app/lib/features/game/turn_resolution/**` is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_turn_resolution_file_size`, then the rule passes without violations and exits `0` (Refs #4512, #4582, #4606).
- Given a temporary workspace whose only matching file is `app/lib/core/services/game_service/too_long.dart` with 251 physical lines, when the System runs `runCheckAppCoreServicesFileSize`, then the checker exits non-zero and names that file and `251 physical lines > 250` (Refs #4450, #4582, #4606).
- Given a governed `app/test/province_*shortcut_host_*_test.dart` file (excluding `province_shortcut_host_emit_test_support_test.dart`) that re-declares `MapTopology(` as a top-level field, when the System runs `runCheckAppTestNoDuplicateShortcutFixtures`, then the checker exits non-zero and names that file (Refs #4450 AC9).
- Given only `app/test/province_shortcut_host_emit_fixtures.dart` and `app/test/province_shortcut_host_emit_test_support.dart` declare `MapTopology(` / `TileMapResult(` fields, when the System runs `runCheckAppTestNoDuplicateShortcutFixtures`, then the checker exits `0` and does not name those support modules or `province_shortcut_host_emit_test_support_test.dart` (Refs #4450 AC9).
- Given every `app/test/**/*.dart` file is at or below 350 physical lines and `appTestFileSizeAllowlistForTests` is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_test_file_size`, then the rule passes without violations and exits `0` (Refs #4352, #4606, #4642).
- Given every hand-written Dart file under `app/lib/features/shell/**` is at or below 250 physical lines and the shrink-only grandfather is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_features_shell_lib_physical_file_size`, then the rule passes without violations and exits `0` (Refs #4606).
- Given a temporary workspace whose only `app/test/**` Dart file has 401 physical lines and is not allowlisted, when the System runs `runCheckAppTestFileSize`, then the checker exits non-zero and names that file as exceeding **400** (Refs #4352).
- Given `app/test/` after wave-15 AC10, when a reviewer lists the `combat_ui_specs`, `e2e_helpers_barrel`, `app_event_handler_scope`, `dialogs_320dp_min_viewport`, `production_panel`, `dialogue_overlays_specs`, `move_dialogs_specs`, `screen_spec_acceptance`, `new_game_leader_selection_dialog`, `game_screen_narrow`, `civilian_units_panel`, `ct_region_map_widget`, `game_map_area_state_logic`, `naval_units_panel`, and `province_panel_draft_orders` suites, then those families use concern-named files plus shared fixtures (`combat_ui_specs_test_support.dart`, `dialogs_320dp_min_viewport_support.dart`, `production_panel_widget_helpers.dart`, `app_event_handler_scope_fixtures.dart`, `e2e_helpers_barrel_test_support.dart`, `dialogue_overlays_specs_test_support.dart`, `move_dialogs_specs_test_support.dart`, `screen_spec_acceptance_test_support.dart`, `new_game_leader_selection_dialog_test_support.dart`, `game_screen_test_support.dart`, `civilian_units_panel_test_support.dart`, `ct_region_map_test_support.dart`, `game_map_area_state_logic_test_support.dart`, `naval_units_panel_test_support.dart`, `province_draft_orders_test_support.dart`) and do not add further `*_partN_test.dart` shards solely to satisfy the 400-line ratchet (Refs #4352).
- Given chrome button sources under `app/lib/widgets/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.chrome_button_base`, then every `State<X>` whose `X` is a public class name ending in `TextButton` declares `with CtHoverButtonStateMixin`, and the run fails listing each violating `path:line` when such a state omits the canonical mixin (Refs #3594).
- Given in-game split-dialog sources under `app/lib/features/game/widgets/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.split_dialog_base`, then every public class whose name starts with `Split` and ends with `Dialog` (other than the `SplitEntityDialog` base) declares `extends SplitEntityDialog`, and the run fails listing each violating `path:line` when such a dialog omits the canonical base (Refs #3594).
- Given in-game train-at-capital dialog sources under `app/lib/features/game/widgets/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.train_dialog_base`, then every public class whose name starts with `Train` and ends with `Dialog` (other than the `TrainDialogBase` base) declares `extends TrainDialogBase`, and the run fails listing each violating `path:line` when such a dialog omits the canonical base (Refs #3594).
- Given Flame asset-image sources under `app/lib/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_asset_image_decode_dedup`, then `ui.decodeImageFromList(...)` is invoked only in `app/lib/features/game/flame/caches/asset_image_cache.dart` and every public `*IconCache` class declared in a `*_icon_cache.dart` file declares `extends AssetImageCache`, and the run fails listing each violating `path:line` when a file outside the helper re-inlines the decode idiom or an icon cache omits the canonical base (Refs #3699).
- Given the commodity-cost train dialog sources `app/lib/features/game/widgets/train/train_military_dialog.dart` and `train_naval_dialog.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_train_dialog_commodity_cost_shared`, then each file declares a state class `extends CommodityCostTrainDialogState` and re-declares none of `_totalTreasuryCost` / `_totalPeasantCost` / `_totalCommodityCosts` / `_remainingTreasury` / `_remainingPeasants` / `_remainingCommodity` / `_deficitHint` / `canAffordIncrement` nor any private `_*ResourceBar` / `_*Row` widget, and the run fails listing each violating `path:line` when a dialog omits the canonical base or re-duplicates the cost machinery (Refs #3686).
- Given repository Dart files excluding generated suffixes (`.g.dart`, `.freezed.dart`, `.mocks.dart`, `.gen.dart`) and generated app l10n paths under `app/lib/l10n/gen/` matching the checker’s prefix list, when `dart run tool/ct_repo_lint.dart` runs rule `repo.dart_file_non_comment_line_size`, then each remaining scanned file has at most 1000 non-comment lines and the run fails while listing every violating file when any file is strictly greater than 1000.
- Given the [Policy: no violation allowlists](#policy-no-violation-allowlists-repo-lint) section, when `repo.repeated_magic_numbers` runs, then that checker does not load keyed waiver data; a decoy keyed-waiver-shaped YAML file under `tool/` must not suppress fail-level repeated-literal violations for in-scope domain sources (see `SPEC/program/repeated-magic-numbers.md` and `test/check_repeated_magic_numbers_test.dart`).
- Given `packages/colonizethis_debug_console/lib/**` imports only `colonizethis_logic` URIs in the rule's closed `allowed_imports` contract set, when `dart run tool/ct_repo_lint.dart` runs rule `repo.debug_console_logic_contract_boundary`, then the rule passes without violations.
- Given any debug-console file imports `package:colonizethis_logic/src/**` or another `package:colonizethis_logic/...` URI outside that closed contract set, when repo lint runs rule `repo.debug_console_logic_contract_boundary`, then the run fails and reports file path, line, and disallowed import context in checker output.
- Given `app/lib/core/services/app_event_handler/app_event_handler_scope.dart` has no direct imports from `package:colonizethis_app/features/game/logic/**`, when repo lint runs rule `repo.app_event_handler_scope_logic_boundary`, then the rule passes without violations.
- Given `app/lib/core/services/app_event_handler/app_event_handler_scope.dart` directly imports any `package:colonizethis_app/features/game/logic/**` path, when repo lint runs rule `repo.app_event_handler_scope_logic_boundary`, then the run fails and reports file path and line number.
- Given app feature Dart files under `app/lib/features/**`, when repo lint runs rule `repo.subscription_tracker`, then no code line declares a raw `List<StreamSubscription<...>>` or inferred `<StreamSubscription<...>>[]` list, and the run fails with file path and line number when any such list is present.
- Given any `.github/workflows/*.{yml,yaml}` file contains a step with `uses: subosito/flutter-action@...`, when repo lint runs rule `repo.flutter_action_pins`, then the step includes `with.flutter-version` as a valid semantic version and that value is greater than or equal to `3.41.9`; the rule fails when the pin is missing, invalid, or below the minimum.
- Given the #2073 workspace outdated audit targets (repo root plus every workspace member whose `pubspec.yaml` declares `sdk: flutter`), when repo lint runs rule `repo.workspace_outdated_resolvable`, then each package row reports `current` equal to `resolvable` and the run fails listing package root/name/version deltas when any row is below `Resolvable`.
- Given the #2073 workspace outdated audit targets (repo root plus every workspace member whose `pubspec.yaml` declares `sdk: flutter`), when repo lint runs rule `repo.workspace_outdated_latest_direct`, then every `kind: direct` or `kind: dev` row where `resolvable == latest` reports `current == latest`, and the run fails listing package root/name/version deltas when any direct row is below Latest despite Latest already being resolvable.
- Given `packages/colonizethis_world/lib/src/**` Dart sources excluding `world/province_lookup.dart`, `world/province_lookup_extension.dart`, `world/province_lookup_indexes.dart`, and `world/unit_lookup.dart`, when repo lint runs rule `repo.logic_dual_region_province_field_access`, then at most 0 physical lines contain `oldWorld.provinces`, `newWorld.provinces`, `oldWorld.units`, `newWorld.units`, or manual `if (regionId == kRegionOldWorld)` / `else if (regionId == kRegionOldWorld)` branching, and the checker does not use keyed waiver tables (whole-file exclusion of canonical helper files only).
- Given `app/lib/**/*.dart` Dart sources excluding generated suffixes (`*.g.dart`, `*.freezed.dart`, `*.mocks.dart`, `*.gen.dart`), generated app l10n paths under `packages/colonizethis_app_l10n/lib/l10n/gen/`, the Widgetbook entrypoint `app/lib/widgetbook.dart` and catalog stories under `widgetbook_host/lib/catalogs/`, the E2E expected-line snapshots under `packages/colonizethis_app_fixtures/lib/test_support/`, test paths, and any path containing a fixture / golden marker (`/test_data/`, `/fixtures/`, `/golden/`, …), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_duplicate_helpers`, then each tracked canonical helper symbol from #2180 (`eraRoman` and `techCategoryLabelL10n` in `app/lib/features/game/widgets/technology/tech_ui_helpers.dart`; `commodityDisplayName` in `app/lib/widgets/commodity_display_name.dart`; the `trainDialog*` and `*TrainDialog*` set in `app/lib/features/game/widgets/train/train_unit_dialog_helper.dart`) is declared as a top-level function only in its canonical file, no removed helper (`_eraRoman`, `_categoryLabel`, `_categoryLabelL10n`, `_commodityDisplayName`, or the deleted public wrapper `unitsPanelRegionLabel` replaced by `regionDisplayLabel`, #3279) appears as a top-level function or class method anywhere in the scanned tree, the run fails listing every offending file path and start line when any tracked symbol redefinition or removed-helper reappearance is found, and the checker does not load keyed waiver tables (whole-file exclusion of generated/l10n/widgetbook/test_support/fixture paths only per this section).
- Given `app/test/**/*.dart` Dart sources, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_test_no_debug_init`, then every file that invokes `getDebugInitGameResult()` is listed in the checker's in-script allowlist, and the run fails listing each violating `path:line` when a non-allowlisted file calls the helper; files that mention the symbol only in comments are not flagged (Refs #3656).
- Given the checker's in-script allowlist for rule `repo.app_test_no_debug_init`, when `dart run tool/ct_repo_lint.dart` runs and an allowlisted entry's file is missing or no longer invokes `getDebugInitGameResult()` (AST method-invocation match, so a comment-only mention does not keep the entry alive), then the run fails listing each stale entry and instructs the author to remove it, enforcing the allowlist's shrink-only invariant (Refs #3656).
- Given `app/lib/**/*.dart` Dart sources excluding generated suffixes (`*.g.dart`, `*.freezed.dart`, `*.mocks.dart`, `*.gen.dart`) and `_test.dart` / `/test/` paths, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_region_string_literals`, then no scanned line outside `//` / `///` comments contains a string literal whose entire content equals `oldWorld` or `newWorld`, and the run passes with zero violations (Refs #3658, SPEC/program/logic-dual-region-province-access.md).
- Given an `app/lib/**/*.dart` source (outside the documented exclusions) with a line such as `regionId == 'newWorld'` outside any comment, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_region_string_literals`, then the run fails listing the offending `path:line` and directs the author to use `kRegionOldWorld` / `kRegionNewWorld`; prefixed/composite ids (`'oldWorld|p1'`), interpolations (`'$kRegionOldWorld|'`), and identifiers (`region_oldWorld`) are not flagged (Refs #3658).
- Given `SPEC/ui/screen-registry.md` contains a `## Registry` table where every row whose `Status` column is `active` has an Implementation cell that is a single backticked path of the form `` `app/lib/...dart` `` and that file exists on disk, when `dart run tool/ct_repo_lint.dart` runs rule `repo.screen_registry_active_paths`, then the rule passes without violations (Refs #2801).
- Given any `active` row in `SPEC/ui/screen-registry.md` has an Implementation cell equal to `TBD`, `—`, an empty value, a path not under `app/lib/`, a non-`.dart` value, or a backticked path that does not resolve to an existing file under the repo root, when `dart run tool/ct_repo_lint.dart` runs rule `repo.screen_registry_active_paths`, then the run fails listing each offending screen ID, registry line number, and remediation hint (Refs #2801). `draft` rows are exempt from this rule.
- Given `app/lib/features/**/*.dart` and `app/lib/widgets/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, the canonical Flame-renderer / palette-data / dev-tooling file allowlist documented in this rule's table row, and the `app/lib/widgets/` canvas-compositing allowlist `app/lib/widgets/main_menu.dart` + its split-out part file `app/lib/widgets/main_menu_buttons.dart` where the literal is an alpha / blend operand rather than a theme palette reference), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_editorial_monocle_colors`, then no scanned line outside `//` comments contains a banned Material color literal `Colors.{black,white,red,redAccent,green,greenAccent,grey,gray}` (with any opacity / grade suffix) or a `const Color(0x...)` hex literal, and the run fails listing every offending `file:line: literal` when any line does (Refs #2914 §S4 + §G1). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` and `app/lib/widgets/**/*.dart` files (excluding generated suffixes, `_test.dart`, and paths under `test/`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_textstyle_fontsize_fallback`, then for every `textTheme.<slot> ?? const TextStyle(fontSize: N)` fallback (tolerating a line break between the slot read and the fallback) whose `<slot>` appears in the canonical M3 dark slot→size table (`headlineMedium`=28, `headlineSmall`=24, `titleLarge`=22, `titleMedium`=16, `titleSmall`=14, `bodyLarge`=16, `bodyMedium`=14, `bodySmall`=12, `labelLarge`=14, `labelMedium`=12, `labelSmall`=11) the literal `N` equals that slot's canonical size, fallbacks whose slot-token line begins with `//` or whose slot is outside the table are not flagged, the run fails listing every offending `file:line:` with the expected size when any in-table fallback differs, and the checker uses only generated-suffix / test-path scope skips and loads no keyed waiver data (Refs #2914 §S7).
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_iconbutton`, then no scanned line outside `//` comments constructs a Material `IconButton(` (including the named constructors `IconButton.outlined(`, `IconButton.filled(`, `IconButton.filledTonal(`), and the run fails listing every offending `file:line: IconButton(` when any line does (Refs #2914 Phase 2 §G2). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_alertdialog`, then no scanned line outside `//` comments constructs a Material `AlertDialog(` (including the named constructor `AlertDialog.adaptive(`), and the run fails listing every offending `file:line: AlertDialog(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended beyond IconButton). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_textbutton`, then no scanned line outside `//` comments constructs a Material `TextButton(` (including the named constructors `TextButton.icon(` and `TextButton.tonalIcon(`; the static `TextButton.styleFrom(` `ButtonStyle` factory is not flagged), the catalog widget `CtDangerTextButton(` and any other identifier with `TextButton` as a non-word-boundary suffix are not flagged, and the run fails listing every offending `file:line: TextButton(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_scaffold`, then no scanned line outside `//` comments and `///` dartdoc constructs a Material `Scaffold(` (the leading word boundary excludes `ScaffoldMessenger(`, `ScaffoldState(`, and user-defined identifiers ending in `Scaffold`; static member accesses such as `Scaffold.of(context)` and `Scaffold.maybeOf(context)` are also not flagged because the regex requires an opening parenthesis immediately after the `Scaffold` token), and the run fails listing every offending `file:line: Scaffold(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_chip`, then no scanned line outside `//` comments and `///` dartdoc constructs the un-prefixed Material `Chip(` widget (the negative lookbehind `(?<![A-Za-z0-9_])` excludes prefixed chip families `FilterChip(`, `ChoiceChip(`, `ActionChip(`, `InputChip(`, `RawChip(`, `MaterialChip(` so they remain in scope only for their dedicated sibling rules, and excludes user-defined identifiers ending in `Chip` such as `_LegendChip(`, `MyChip(`, and the Ct-* catalog widget `CtChoiceChip(`; static member accesses such as `Chip.styleFrom` are not flagged because the regex requires an opening parenthesis immediately after the `Chip` token), and the run fails listing every offending `file:line: Chip(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile to cover the base Material `Chip`). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_listtile`, then no scanned line outside `//` comments and `///` dartdoc constructs the bare Material `ListTile(` widget (the leading word boundary excludes the sibling list-tile families `SwitchListTile(`, `CheckboxListTile(`, and `RadioListTile(`, plus any user-defined identifier ending in `ListTile` such as `MyListTile(`, because their `ListTile` token is preceded by a word character), and the run fails listing every offending `file:line: ListTile(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile / Chip). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_button`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material raised / outlined button families `ElevatedButton(`, `FilledButton(`, or `OutlinedButton(` (including the named constructors `ElevatedButton.icon(`, `FilledButton.icon(`, `FilledButton.tonal(`, `FilledButton.tonalIcon(`, and `OutlinedButton.icon(`; the static `*.styleFrom(` `ButtonStyle` factories and theme types such as `ElevatedButtonTheme(` / `OutlinedButtonThemeData(` are not flagged because the regex requires an optional named-constructor then an opening parenthesis immediately after the button token), the leading word boundary keeps any identifier ending in a banned button name as a non-word-boundary suffix out of scope, the sibling `repo.app_no_material_textbutton` rule's `TextButton` is not handled here, and the run fails listing every offending `file:line:` token when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended beyond IconButton / AlertDialog / TextButton / Scaffold / FilterChip / SwitchListTile / Chip / ListTile). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_card`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material `Card(` widget (including the named constructors `Card.filled(` and `Card.outlined(`; the leading word boundary excludes user-defined identifiers ending in `Card` such as `ResourceCard(` and theme types such as `CardTheme(` because the `Theme` token prevents the `\s*\(` tail from matching), and the run fails listing every offending `file:line: Card(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_dialog`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material `Dialog(` widget (including the named constructor `Dialog.fullscreen(`; the leading word boundary excludes the separately-banned `AlertDialog(`, `SimpleDialog(`, the `showDialog(` / `showGeneralDialog(` plumbing helpers, the catalog widget `CtDialogShell(`, and types such as `DialogRoute(` / `DialogTheme(` whose trailing token prevents the `\s*\(` tail from matching), and the run fails listing every offending `file:line: Dialog(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_choicechip`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material `ChoiceChip(` widget (including the named constructor `ChoiceChip.elevated(`; the leading word boundary excludes the catalog widget `CtChoiceChip(` and any user-defined identifier ending in `ChoiceChip`), and the run fails listing every offending `file:line: ChoiceChip(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_slider`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material `Slider(` widget (including the named constructor `Slider.adaptive(`; the leading word boundary excludes the catalog widget `CtSlider(`, the separate Material `RangeSlider(`, any user-defined identifier ending in `Slider`, and `SliderTheme(`), and the run fails listing every offending `file:line: Slider(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_dropdownbutton`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material `DropdownButton(` widget, including the generic-typed form `DropdownButton<T>(` (the matcher accepts an optional `<...>` type-argument segment before the parenthesis; the leading word boundary plus the immediate generic / `\s*\(` requirement excludes the sibling Material types `DropdownButtonFormField(` and `DropdownButtonHideUnderline(`, and any user-defined identifier ending in `DropdownButton`), and the run fails listing every offending `file:line: DropdownButton(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `app/lib/features/**/*.dart` files (excluding generated suffixes, `_test.dart`, paths under `test/`, and the dev-tooling allowlist `app/lib/features/debug_log/debug_log_viewer_screen.dart` SYS10001 + `app/lib/features/game/flame/overlays/debug_console_overlay_panel.dart` SYS20001 documented in this rule's table row), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_no_material_appbar`, then no scanned line outside `//` comments and `///` dartdoc constructs the Material `AppBar(` widget (the leading word boundary excludes the separate Material `SliverAppBar(`, any user-defined identifier ending in `AppBar`, and `AppBarTheme(`), and the run fails listing every offending `file:line: AppBar(` when any line does (Refs #2914 Phase 2 §G2 — Material widget bans extended to the remaining ban-list widgets). The checker uses only whole-file scope exclusions; it does not load keyed waiver data.
- Given `packages/colonizethis_logic/lib/**/*.dart` files (excluding generated suffixes `*.g.dart` / `*.freezed.dart` / `*.mocks.dart`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.logic_no_map_deps`, then no scanned line outside `//` / `*` comment lines is an `import`/`export` directive matching `package:colonizethis_map/`, and the run exits `0` (Refs #3574 slice 6). Test code under `packages/colonizethis_logic/test/**` is out of scope, so the two map-importing integration tests remain valid.
- Given a `packages/colonizethis_logic/lib` file that adds an `import 'package:colonizethis_map/colonizethis_map.dart';` (or any `package:colonizethis_map/src/...` import/export), when `dart run tool/ct_repo_lint.dart` runs rule `repo.logic_no_map_deps`, then the run fails listing each offending `path:line` and exits `1`, directing the author to keep map a dev-only dependency. A same-prefix package such as `package:colonizethis_mapper/...` does not match and is not flagged.
- Given `packages/colonizethis_map/lib/**/*.dart` files excluding the canonical helper file `packages/colonizethis_map/lib/src/tile_map_grid.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_grid_ops_central`, then no scanned line outside `//` / `*` comment lines references the removed `copyTileMapGrid(` helper or contains an inline row-major deep copy matching `.map((<ident>) => <ident>.toList()).toList()`, and the run exits `0` (Refs #3459 AC5). The checker uses only the whole-file canonical-helper scope exclusion; it does not load keyed waiver data.
- Given a `packages/colonizethis_map/lib/src` file (other than `tile_map_grid.dart`) that calls `copyTileMapGrid(grid)` or writes an inline `grid.map((row) => row.toList()).toList()` deep copy, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_grid_ops_central`, then the run fails listing each offending `path:line` and exits `1`, directing the author to `TileMapGrid.copy(...)`.
- Given `packages/colonizethis_map/lib/**/*.dart` files excluding the whole-file exemptions (`src/tile_map_grid.dart`, `src/gen/tile_map_grid_graph.dart`, `src/gen/tile_map_manhattan_distance_transform.dart`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_grid_cell_iteration_central`, then no scanned `for (<decl> y …)` header outside `//` / `*` comment lines is immediately followed (skipping blank/comment lines) by a `for (<decl> x …)` header unless an inline `ct-lint-allow: nested-grid-walk` marker is present on that header or the comment line directly above it, and the run exits `0` (Refs #3574). The view-builder, visualizer, and generator-pass cell walks route through `TileMapGrid.forEachIndex(...)` / `TileMapGrid.forEachCell(...)`.
- Given a `packages/colonizethis_map/lib/src` file (not whole-file exempt) that adds a hand-rolled nested `for (var y …) { for (var x …) }` tile walk without a `ct-lint-allow: nested-grid-walk` marker, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_grid_cell_iteration_central`, then the run fails listing each offending `path:line` and exits `1`, directing the author to `TileMapGrid.forEachIndex(...)` / `TileMapGrid.forEachCell(...)` or a sanctioned marker.
- Given `packages/colonizethis_map/lib/src/gen/map_gen_stage.dart` and the generator service files under `lib/src/gen/` (`tile_map_generator_land_seeds.dart`, `tile_map_generator_lakes_provinces.dart`, `tile_map_gen_continent_join_pass.dart`, `tile_map_gen_terrain_jitter_pass.dart`, `tile_map_gen_sea_zone_subdivide_pass.dart`, `tile_map_generator_terrain_assign.dart`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_stage_protocol`, then the contract file declares `abstract interface class MapGenStage` and each service class declares `implements MapGenStage` (or the uniform `MapGenPass`), and the run exits `0` (Refs #3459 AC5, #3588).
- Given a generator service file listed above whose class declaration omits `implements MapGenStage`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_stage_protocol`, then the run fails listing the offending path and exits `1`.
- Given fewer than 4 bound generator service families declare `implements MapGenPass`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_stage_protocol`, then the run fails reporting that at least 4 families must adopt the uniform `MapGenPass` entry point and exits `1` (Refs #3588).
- Given the generation-layer files under `packages/colonizethis_map/lib/src/gen/**` and view-layer files under `lib/src/view/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_no_new_partfiles`, then no file declares a `part` or `part of` directive (empty allowlist after #4022), and the run exits `0` (Refs #3588, #4022).
- Given a generation-layer or view-layer file that declares a `part` or `part of` directive, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_no_new_partfiles`, then the run fails listing the offending `path:line:column` and exits `1` (Refs #3588, #4022).
- Given the generation-layer files under `packages/colonizethis_map/lib/src/gen/**`, view-layer files under `lib/src/view/**`, and render-layer files under `lib/src/render/**`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_lib_file_size`, then every scanned `.dart` file has at most 500 non-comment lines and the run exits `0` (Refs #3588, #3846).
- Given a generation-layer, view-layer, or render-layer `.dart` file with strictly more than 500 non-comment lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_lib_file_size`, then the run fails listing the offending path with its non-comment line count and exits `1` (Refs #3588, #3846).
- Given the generation-layer, view-layer, and render-layer files under the same scan roots as `repo.map_lib_file_size`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_lib_file_size_headroom`, then every scanned `.dart` file has at most 250 non-comment lines (empty grandfather allowlist after #4561) and the run exits `0` (Refs #4561).
- Given a generation-layer, view-layer, or render-layer `.dart` file with strictly more than 250 non-comment lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_lib_file_size_headroom`, then the run fails listing the offending path with its non-comment line count and exits `1` (Refs #4561).
- Given every `.dart` file under `packages/colonizethis_map/test/**` is at or below 250 physical lines (empty grandfather allowlist after #4561), when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_test_file_size`, then the rule passes and exits `0` (Refs #4561).
- Given a `packages/colonizethis_map/test/**` Dart file with strictly more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_test_file_size`, then the run fails naming that file and exits `1` (Refs #4561).
- Given every hand-written `.dart` file under `packages/colonizethis_data/lib/src/**` is at or below 250 physical lines (excluding `*.gen.dart`), when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_lib_file_size`, then the rule passes and exits `0` (Refs #4072, #4292, #4412, #4626).
- Given a hand-written `packages/colonizethis_data/lib/src/**` Dart file with strictly more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4072, #4292, #4412, #4626).
- Given every `.dart` file under `packages/colonizethis_data/test/**` is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_test_file_size`, then the rule passes and exits `0` (Refs #4121, #4292, #4412, #4626).
- Given a `packages/colonizethis_data/test/**` Dart file with strictly more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_test_file_size`, then the run fails naming that file and exits `1` (Refs #4121, #4292, #4412, #4626).
- Given every `.dart` file under `packages/colonizethis_save/lib/src/**` is at or below 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.save_source_file_size`, then the rule passes and exits `0` (Refs #4077).
- Given a `packages/colonizethis_save/lib/src/**` Dart file with more than 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.save_source_file_size`, then the run fails naming that file and exits `1` (Refs #4077).
- Given every `.dart` file under `packages/colonizethis_save/test/**` is at or below 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.save_test_file_size`, then the rule passes and exits `0` (Refs #4077).
- Given a `packages/colonizethis_save/test/**` Dart file with more than 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.save_test_file_size`, then the run fails naming that file and exits `1` (Refs #4077).
- Given every `.dart` file under `packages/colonizethis_test/lib/**` is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_test_lib_file_size`, then the rule passes and exits `0` (Refs #4547).
- Given a `packages/colonizethis_test/lib/**` Dart file with more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_test_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4547).
- Given every `.dart` file under `packages/colonizethis_test/test/**` is at or below 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_test_test_file_size`, then the rule passes and exits `0` (Refs #4547).
- Given a `packages/colonizethis_test/test/**` Dart file with more than 250 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.colonizethis_test_test_file_size`, then the run fails naming that file and exits `1` (Refs #4547).
- Given every non-generated `.dart` file under `packages/colonizethis_debug_console/lib/**` is at or below 270 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.debug_console_lib_file_size`, then the rule passes and exits `0` (Refs #4433).
- Given a `packages/colonizethis_debug_console/lib/**` Dart file with more than 270 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.debug_console_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4433).
- Given every `.dart` file under `packages/colonizethis_debug_console/test/**` is at or below 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.debug_console_test_file_size`, then the rule passes and exits `0` (Refs #4433).
- Given a `packages/colonizethis_debug_console/test/**` Dart file with more than 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.debug_console_test_file_size`, then the run fails naming that file and exits `1` (Refs #4433).
- Given every non-generated `.dart` file under `packages/colonizethis_app_ui_chrome/lib/**` is at or below 300 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_ui_chrome_lib_file_size`, then the rule passes and exits `0` (Refs #4480).
- Given every non-generated `.dart` file under `packages/colonizethis_app_e2e_support/lib/**` is at or below 250 physical lines and the shrink-only allowlist is empty, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_e2e_support_lib_file_size`, then the rule passes and exits `0` (Refs #4075, #4195, #4344, #4598).
- Given a `packages/colonizethis_app_e2e_support/lib/**` Dart file with more than 250 physical lines and an empty allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_e2e_support_lib_file_size`, then the run fails naming that file and exits `1` (Refs #4075, #4195, #4344, #4598).
- Given every `.dart` file under `packages/colonizethis_app_e2e_support/test/**` is at or below 300 physical lines except the shrink-only allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_e2e_support_test_file_size`, then the rule passes and exits `0` (Refs #4075, #4195, #4344, #4598).
- Given a `packages/colonizethis_app_e2e_support/test/**` Dart file with more than 300 physical lines and an empty allowlist, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_e2e_support_test_file_size`, then the run fails naming that file and exits `1` (Refs #4075, #4195, #4344, #4598).
- Given a pin suite under `packages/colonizethis_app_e2e_support/test/*.dart` (not `test/support/`) that declares `_wrap`, `_pumpScaffold`, `_pumpHost`, or `_pumpAndResolveRoot`, or constructs `MaterialApp(` inline, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_e2e_support_no_duplicate_hosts`, then the run fails naming that file and exits `1` (Refs #4598).
- Given wrap/pump helpers only under `packages/colonizethis_app_e2e_support/test/support/` and pin suites that import them (no inline `MaterialApp(` in pin files), when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_e2e_support_no_duplicate_hosts`, then the rule passes and exits `0` (Refs #4598).
- Given every `.dart` file under `packages/colonizethis_app_ui_chrome/test/**` is at or below 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_ui_chrome_test_file_size`, then the rule passes and exits `0` (Refs #4480).
- Given a `packages/colonizethis_app_ui_chrome/test/**` Dart file with more than 400 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.app_ui_chrome_test_file_size`, then the run fails naming that file and exits `1` (Refs #4480).
- Given every `const int` / `const double` `k*` in `packages/colonizethis_data/lib/src/ai_victory_config*.dart` appears as a `victoryConfigIntParam` / `victoryConfigDoubleParam` name across `ai_parameter_victory_config_params*.dart` with no orphan param names, when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_victory_config_registry_parity`, then the rule passes and exits `0` (Refs #4072).
- Given an `ai_victory_config*.dart` scalar `k*` without a matching param name string (or a param name without a matching scalar const), when `dart run tool/ct_repo_lint.dart` runs rule `repo.data_victory_config_registry_parity`, then the run fails listing each mismatch and exits `1` (Refs #4072).
- Given more than 4 files matching `packages/colonizethis_map/test/init_game_map_view_builder_*_test.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_test_view_builder_file_count`, then the run fails listing each matching path and exits `1` (Refs #3846).
- Given the map view-builder marker suites after #4022 AC5, when counting `init_game_map_view_builder_*_test.dart` files, then the count remains ≤4 and ownership/capital/port/warp marker coverage lives in `init_game_map_view_builder_markers_test.dart` while town/civilian/army-stack marker coverage lives in `init_game_map_view_builder_town_civilian_markers_test.dart` (visibility/presence coverage is folded into `init_game_map_view_builder_region_cells_test.dart`) (Refs #4022, #4384).
- Given `packages/colonizethis_map/test/tile_map_visualization_*`, `test/game_world_state_map_visualizer_test.dart`, or `test/multi_region_map_rendering_test.dart` with an unallowlisted inline `\bTopologyNode\(`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_test_shared_topology_fixtures`, then the run fails listing each offending path and exits `1` (Refs #3846).
- Given an in-scope map generator suite (`tile_map_generator_*`, `tile_map_generation_*`, `tile_map_pass4_*`, `tile_map_forest_*`, `tile_map_terrain_*`) with an unallowlisted inline `\bTileMapGenerator\s*\(`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_test_run_generation_harness`, then the run fails listing each offending path and exits `1` (Refs #4022).
- Given a scoped colour/capital/region-input/format/port map test with an inline `\bGame\(`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_test_minimal_game_shared`, then the run fails listing each offending path and exits `1` (Refs #4022).
- Given the public barrel `packages/colonizethis_map/lib/colonizethis_map.dart` and its transitive `export` closure, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_public_barrel_surface`, then no `export` directive (outside `//` / `*` comment lines) in that closure re-exports the internal-only modules `src/gen/grid_voronoi.dart`, `src/gen/topology_inference.dart`, `src/gen/tile_map_grid_graph.dart`, or any `*_test_api.dart` module, and the run exits `0` (Refs #3459 AC4, #4561).
- Given a module in the barrel transitive `export` closure that adds back `export 'src/gen/grid_voronoi.dart';` (or another forbidden internal / test-only module), when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_public_barrel_surface`, then the run fails listing each offending `path:line` and exits `1`, directing the author to import the module from `src/` in same-package tests instead.
- Given `packages/colonizethis_map/lib/**/*.dart` files excluding the canonical helper file `packages/colonizethis_map/lib/src/region_data_access.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_region_data_access_central`, then no scanned line outside `//` / `*` comment lines contains an inline region-data branch matching `.(oldWorld|newWorld).(provinces|units)`, and the run exits `0` (Refs #3459 AC3). The checker uses only the whole-file canonical-helper scope exclusion; it does not load keyed waiver data.
- Given a `packages/colonizethis_map/lib/src` file (other than `region_data_access.dart`) that reads `game.worldState.oldWorld.provinces` (or `.newWorld.units`, or the sibling `.oldWorld.units` / `.newWorld.provinces`) inline, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_region_data_access_central`, then the run fails listing each offending `path:line` and exits `1`, directing the author to `regionDataForMapRegionId(world, regionId)`.
- Given `packages/colonizethis_map/lib/**/*.dart` files excluding the canonical dispatch file `packages/colonizethis_map/lib/src/map_region_dispatch.dart`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_region_dispatch_central`, then no scanned line outside `//` / `*` comment lines contains an `==` / `!=` comparison against `kRegionOldWorld` or `kRegionNewWorld`, and the run exits `0` (Refs #3574). Lines using the constants only as map keys, list elements, named arguments, or string-interpolation operands are not flagged.
- Given `packages/colonizethis_map/lib/**/*.dart` files outside the render-layer directory `packages/colonizethis_map/lib/src/render/`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_no_image_import`, then no scanned line outside `//` / `*` comment lines is an `import`/`export` directive matching `package:image` (the trailing `/` or quote excludes same-prefix packages such as `package:image_picker/`), and the run exits `0` (Refs #3574 — generation code free of `package:image`). Files under `lib/src/render/` keep their PNG-encoding `package:image` imports.
- Given a `packages/colonizethis_map/lib/src` generation or view-model file (outside `lib/src/render/`) that adds `import 'package:image/image.dart';`, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_gen_no_image_import`, then the run fails listing each offending `path:line` and exits `1`, directing the author to keep generation/view code image-free by moving rendering work into the render layer (`lib/src/render/`).
- Given a `packages/colonizethis_map/lib/src` file (other than `map_region_dispatch.dart`) that adds an inline `regionId == kRegionOldWorld` (or `kRegionNewWorld`) comparison, when `dart run tool/ct_repo_lint.dart` runs rule `repo.map_region_dispatch_central`, then the run fails listing each offending `path:line` and exits `1`, directing the author to `selectByMapRegionId(...)` / `selectByMapRegionIdOrNull(...)`.
- Given a temporary `app/lib` file containing `for (final p in game.players) { if (p.id == x) return p; }`, when the System runs `runCheckAppLibPlayerByIdLookup`, then the checker exits non-zero and names that file (Refs #4534).
- Given a temporary `app/lib` file containing only `p.capitalProvinceId ==` walks or `{for (final p in game.players) p.id: p.isHuman}`, when the System runs `runCheckAppLibPlayerByIdLookup`, then the checker exits `0` (Refs #4534).
- Given a temporary `app/lib` file containing `.players.where((p) => p.id == x)`, when the System runs `runCheckAppLibPlayerByIdLookup`, then the checker exits non-zero (Refs #2575, #4534).
- Given a temporary `app/lib` file that concatenates `...worldState.oldWorld.units, ...worldState.newWorld.units` or walks both lists for `unit.id ==`, when the System runs `runCheckAppLibUnitLookupSot`, then the checker exits non-zero (Refs #4534).
- Given `civilianUnitsInRegionForOwners(worldState.oldWorld.units, …)` (single-region argument) as the only `*.units` use, when the System runs `runCheckAppLibUnitLookupSot`, then the checker exits `0` (Refs #4534).
- Given a `packages/colonizethis_world/lib/src` file other than `unit_lookup.dart` that contains a non-comment `allUnitsFromWorld(` call, when the System runs `runCheckWorldLibUnitLookupSot`, then the checker exits non-zero (Refs #4611).
- Given only `packages/colonizethis_combat/lib/src` calling `allUnitsFromWorld(`, when the System runs `runCheckWorldLibUnitLookupSot`, then the checker exits `0` (Refs #4611).
- Given the current `packages/colonizethis_world/lib/src` tree, when the System runs `runCheckWorldLibUnitLookupSot`, then the checker exits `0` (Refs #4611).
- Given every Dart file under `packages/colonizethis_combat_test_support/lib/**` is at or below 200 physical lines, when `dart run tool/ct_repo_lint.dart` runs rule `repo.combat_test_support_file_size`, then the rule exits `0` (Refs #4633).
- Given a file under that tree with more than 200 physical lines, when the same rule runs, then it exits `1` and names the file (Refs #4633).
- Given aggregate physical Dart LOC under `packages/colonizethis_combat_test_support/lib` is at or below 6997, when rule `repo.combat_test_support_loc` runs, then it exits `0` (Refs #4633).
- Given a planted `import 'package:colonizethis_logic/colonizethis_logic.dart';` under `packages/colonizethis_combat_test_support/lib/`, when rule `repo.combat_test_support_no_logic_barrel` runs, then it exits `1` and names that file; when the tree has no such import, it exits `0` (Refs #4633).
