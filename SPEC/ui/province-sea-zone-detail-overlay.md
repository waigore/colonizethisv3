# Province and Sea Zone Detail Overlay

**Screen ID:** `MAP20001` — stable; do not reassign.
**SPEC/ui** — Detail overlay for map tile selection. Implementation: `app/lib/features/game/widgets/province_overlay/province_sea_zone_detail_overlay.dart`.
**Widgetbook:** `Province Overlay` → `app/lib/widgetbook/catalog.dart`. Integrates with [map-widget.md](map-widget.md). Identity: [world-model-identity.md](../game/world-model-identity.md).

**Mockup:** [mockups/MAP20001-province-sea-zone-detail.html](mockups/MAP20001-province-sea-zone-detail.html)
---

## Widget contract

`ProvinceSeaZoneDetailOverlay` — presentational; parents pass `displayId`, `selectedTileKey`, `draftOrders`, and game/view data. No direct `AppEventBus` in overlay (provider-based map↔panel contract).

---

## Trigger conditions

- **Map tile tap:** `mapProvincePanelProvider` sets `selectedTileKey` and `overlayOpen`.
- **Close:** Overlay close control sets `overlayOpen` false.

---

## Architecture and wiring (Riverpod-only)

- The **region map** (Flame stack / `CtRegionMap`) and the **province/sea zone detail UI** (side panel or narrow bottom slot) MUST **not** import or reference each other. No passing panel callbacks into the map widget for panel orchestration, and no map types inside the overlay widget file.
- Shared UI state lives in **`mapProvincePanelProvider`** (Riverpod `StateNotifier`): `overlayOpen`, `selectedTileKey` (orange selection / panel content), `secondaryHighlightTileKey` (optional locate/list cursor on the map), and **`secondaryHighlightTileKeys`** (multi-tile secondary outlines for Extraction/Available commodity hover; Refs #4002). The **map embedding layer** (e.g. `GameMapCanvasStack`) reads/writes this provider and passes **plain data + callbacks** into `CtRegionMap` (`selectedTileKey`, `secondaryHighlightTileKey`, `secondaryHighlightTileKeys`, `onMapTileTappedForDetail`). The **panel hosts** (`GameMapProvinceDetailSidePanel`, [`GameMapNarrowDetailOverlaySlot`](game-map-narrow-detail-overlay-slot.md)) are `ConsumerWidget`s that read the same provider and build `ProvinceSeaZoneDetailOverlay` with `displayId` derived from `selectedTileKey`.
- **Draft orders preview:** Panel hosts also pass **`draftOrders`** (`Orders`, default empty) into `ProvinceSeaZoneDetailOverlay`, sourced from **`currentOrdersProvider`** (same in-memory orders snapshot the player is editing). The overlay uses this only for **read-only preview** strings (civilian work-order targets, pending land/naval lines). It does **not** import Riverpod; parents remain the bridge.
- Other features may use `AppEventBus`; this overlay’s map↔panel contract is the provider. No `Ref`/`BuildContext` chains across unrelated panels (TDD app UI wiring).

---

## Purpose

When the user **taps/clicks a map tile** (not hover), the shell shows detail for the **province or sea zone** containing that tile. **Hover** may move the hover selector and province glow on the map but does **not** change panel content. The overlay is toggleable (close control; a further tile tap can reopen/update per shell rules).

---

## Interaction

- **Open / update:** User taps/clicks a tile → provider records `selectedTileKey`, sets `overlayOpen` → panel shows that province/sea zone; **Tile** section uses the same tile key.
- **Close:** User uses the overlay close control → `overlayOpen` false; `selectedTileKey` may remain for a later reopen. Map **orange selection** follows provider: implementation may clear or keep selection when closed; tile tap while closed should be able to **reopen** with that tile selected.
- **Panel motion:** Panel hosts animate open/close with `ProvinceDetailPanelSlideTransition` (**200 ms**): wide [`GameMapProvinceDetailSidePanel`](../../app/lib/features/game/flame/overlays/game_map_province_detail_side_panel.dart) slides from the **end** (right); narrow [`GameMapNarrowDetailOverlaySlot`](game-map-narrow-detail-overlay-slot.md) slides from the **bottom** (`Offset(0, 1)`). Enter uses `Curves.easeOut`; exit uses `Curves.easeIn`. Content updates while open (tile/province switch) do not replay the enter animation — only `overlayOpen` false→true toggles it.
- **Switch province/tile:** Tap another tile → new `selectedTileKey` → panel updates (including province-scoped sections for the new tile's province).
- **Hover:** Pointer hover updates **only** map hover visuals (and optional `onProvinceHovered` / tooltips). It does **not** update `selectedTileKey` or the Tile section.
- **Touch / mobile:** There is **no** "tap-as-hover" for **panel** content. Only **tap** commits selection for the overlay.
- **Data context:** `RegionMapViewData` and human player for cell visibility and prospecting. **[PlayerView](../program/player-view.md)** (`buildPlayerView` from `GameMapArea` with combined topology) gates foreign civilian lines and the Tile section's civilian count (`foreignCivilianVisibleToPlayer` in colonizethis_logic).
- **Port harbor sea cell:** When `selectedTileKey` is the drawable **sea** cell for a port (`TownMarkerView` `portIconX`/`portIconY`), the overlay **display id** resolves to the **owning land province** (`regionId|localProvinceId`), not sea-zone-only context. Map tile section still uses the selected sea tile key. GitHub [#1761](https://github.com/waigore/colonizethisv3/issues/1761); [town-port-icons.md](town-port-icons.md), [map-widget.md](map-widget.md).

---

## Map cursors (with map widget)

- **Selection (orange outline):** The tile in `selectedTileKey` (when set). Must match the Tile section and political `displayId` (`regionId|provinceId` from tile key).
- **Secondary highlight:** Optional outline for list/locate (`secondaryHighlightTileKey`); distinct from orange selection.

---

## Layout / wireframe

**Narrow** means viewport width &lt; shell breakpoint (e.g. 600 logical px). Let `H` = `MediaQuery` screen height, `third = 0.33 * H`. Let layout max height from parent be `parentMax` (from `LayoutBuilder` / parent constraints).

| Case | Effective max content height |
|------|-------------------------------|
| Not narrow (wide shell) | Use parent’s bounded height (full side column). |
| Narrow, **full-width** map (max width ≈ screen width) | `min(parentMax, third)` so the overlay never exceeds one-third of screen height. |
| Narrow, **side rail** (panel width clearly less than screen width, e.g. fixed 320px) | `parentMax` (full height of the rail), **not** capped to `third`. |
| Narrow, parent **already** constrains height to ≤ `third` (e.g. bottom slot `SizedBox(height: third)`) | Use that height exactly (do not shrink further). |
| Narrow, **unbounded** vertical constraint | `third`. |

**Scrolling:** On narrow layouts with tabs, each tab’s body must be **scrollable** so content never overflows a short panel (e.g. `SingleChildScrollView` around tab page content).

- **Desktop / larger shell:** Overlay in a **side panel** (e.g. right); single **scrollable** column of sections when not using tabs.
- **Mobile / narrow full-width:** **Bottom** area with **tabs**; tab strip + scrollable pages; obey height table above.

**Tab order (labels):** Political, Tile, Economic, Military, Civilian, Naval (Political before Tile). Sea zones: Political, Naval only where applicable.

Political hierarchy: `Name` → `Owner` → **Sight** → optional `No consulate with {owner}` + wrapping `CtActionTextButton` → `Region` → `Capital` → Town development/gist → optional Upgrade town. The Consulate control wraps at 320–360 dp and never creates another tab. **Sight** uses the selected tile’s `CellViewData.visibility` and the same three phrases as the MAP10001 hover readout (Fully visible / Fogged — terrain only / Unknown — no intel yet).

---

## Style / implementation

Non-Material, pixel-art friendly: `CtPanel`, `CtTabStrip`, explicit text styles (UXD 02). Overlay widget receives **`displayId`**, **`selectedTileKey`**, and **`playerView`** from parents; it does **not** import `mapProvincePanelProvider`.

### Dark-theme chrome (header + close control)

The overlay frame uses the editorial-monocle dark tokens from [`pixel-art-ui-catalog.md`](pixel-art-ui-catalog.md) § Editorial-monocle palette and matches the typographic convention shared with `CtTopBar` / combat dialogs:

- **Overlay title** (`Province` or `Sea zone` label rendered above the tab strip / section column) resolves to **`EditorialMonoclePalette.accent`** with **`letterSpacing: 0.05`** so the heading reads as the brass accent line found across other dark surfaces.
- **Close control** (the `×` glyph in the upper-right) borders with **`EditorialMonoclePalette.accentDim`** (1 px) and renders the glyph in **`EditorialMonoclePalette.muted`**; the tap target keeps key `overlay_close` and continues to invoke `onClose`.
- No hex literals or raw Material colour scheme lookups (e.g. `colorScheme.outline`) are introduced by the header chrome; all colours resolve from the `EditorialMonoclePalette` tokens above. Section body styling is owned by S4/S5/S6 slices and is not in this contract.

### Dark-theme section labels (S4 — sections layer)

The six province / two sea-zone section bodies are rendered by a shared `_buildSection` helper that emits one header band per section (`Political`, `Tile`, `Economic`, `Military`, `Civilian`, `Naval`). Under `AppThemes.editorialMonocle` that header band MUST resolve to the canonical `CtSectionLabel` widget from [`pixel-art-ui-catalog.md`](pixel-art-ui-catalog.md) § Pixel-art component catalog (`CtSectionLabel` entry):

- **Section header widget:** the helper renders the section title via `CtSectionLabel` (Refs #2859 R9), so each header inherits the upper-cased small-caps text, `EditorialMonoclePalette.muted` text colour, font weight `500`, `letterSpacing: 1.0`, and the 1 px `EditorialMonoclePalette.accentDim` bottom border defined for the catalog widget. No raw `Text(..., style: TextStyle(fontWeight: FontWeight.bold))` is allowed as the section header.
- **Wide-layout obfuscated fallback:** when the province or sea zone is fully unrevealed and the wide-layout `sections` column lists each section inline, the same `CtSectionLabel` widget MUST head each section (`Political`, `Tile`, `Economic`, `Military`, `Civilian`, `Naval` for the province case; `Political`, `Naval` for the sea-zone case). The narrow-layout tabs continue to read their labels from `CtTabStrip`, so the obfuscated tab-view body MAY omit the inline `CtSectionLabel` (the tab label is the header in that context).
- **Material defaults forbidden:** the section header MUST NOT consume `Theme.of(context).textTheme.titleSmall`/`labelMedium`/`titleMedium` with `FontWeight.bold` overrides for the header text, MUST NOT mount a Material `ListSubheader`, and MUST NOT introduce hex literals or raw `colorScheme.outline` for the header underline. All colours resolve from `EditorialMonoclePalette` via `CtSectionLabel`.

### Dark-theme Tile section body tokens (S5 — Tile body)

The Tile section body (live-data rows, `Road / railroad` caption lines and the `Prospected` / `Improvement` inline shortcut icons) MUST resolve every text and icon colour from `EditorialMonoclePalette` so the dark theme owns the body just as it does the header chrome and section labels. Material defaults (`Theme.of(context).colorScheme.onSurface`, `Theme.of(context).disabledColor`) are forbidden on this surface.

- **Live-data body rows:** every bare Tile row that renders exact world-state values for a revealed selected tile MUST set `TextStyle.color` to `EditorialMonoclePalette.fg` directly so the canonical body foreground colour shared with the Political "Name" / "Owner" rows, the Military owner sub-header (S7), the Economic improved-row label (S6), the Civilian own-unit row label (S8), and the Naval fleet-summary roster line also owns the Tile section's live-data body. The following rows are in scope:
  - coordinates (`provinceOverlay_tileCoordinates(x, y)` → `Coordinates: (X, Y)`),
  - terrain (`provinceOverlay_tileTerrain(terrainStr)` → `Terrain: <id>`),
  - civilian-units count (`provinceOverlay_tileCivilianUnits(count)` → `Civilian units (province): <n>`),
  - prospected state (`provinceOverlay_tileProspected(prospectedLabel)` → `Prospected: yes` / `Prospected: no` / `Prospected: —`); the `prospectedLabel` value resolves through `AppLocalizations` — `provinceOverlay_tileProspectedYes` → `yes` when the tile is prospectable and prospected by the human player, `provinceOverlay_tileProspectedNo` → `no` when prospectable but not yet prospected, and the literal em-dash `—` symbol when the tile's terrain is not prospectable; no hardcoded `yes` / `no` string literals,
  - improvement label (`provinceOverlay_tileImprovement(improvementLine)` → `Improvement: <label>` / `Improvement: —`); the improvement-type name embedded in `<label>` resolves through `AppLocalizations` (the `provinceOverlay_improvement*` keys: `provinceOverlay_improvementFarm` → `Farm` for grain, `provinceOverlay_improvementRanch` → `Ranch` for meat/horses, `provinceOverlay_improvementPasture` → `Pasture` for wool, `provinceOverlay_improvementLumberCamp` → `Lumber camp` for timber, `provinceOverlay_improvementPlantation` → `Plantation` for sugar cane/tobacco/cotton/spices, `provinceOverlay_improvementFurPost` → `Fur post` for furs, `provinceOverlay_improvementMine` → `Mine` for minerals, and `provinceOverlay_improvementGeneric` → `Improvement` as the no-resource fallback), with the level suffix `L<n>` appended; no hardcoded improvement-name literals,
  - the **primary** road / railroad numeric line on land tiles (`roadRailTransportLevelPrimaryLine(l10n, roadLevel)` → `provinceOverlay_tileRoadTransportLevel(level)` → `Road / railroad: transport level N` for `N` in `{0, 1, 2, 4}` or any other stored integer),
  - the sea-tile (or no-land-transport) row (`provinceOverlay_tileRoadNone` → `Road / railroad: —`),
  - the **Resource row prefix** (`provinceOverlay_tileResourcePrefix` literal `Resource: `) rendered as the first child of the `_buildTileResourceLabelRow` `Row`,
  - the **Resource row no-resource fallback** (the `Text(resourceLabel)` rendered as the second child of the `_buildTileResourceLabelRow` `Row` when no visible commodity exists — typically the `—` placeholder per the SPEC § Province overlay content `Tile` row),
  - the **Resource row commodity-id label** (the internal `Text(label ?? commodityDisplayName(appL10n(context), commodityId))` rendered by the shared `ResourceLabelInline` widget mounted as the second child of the `_buildTileResourceLabelRow` `Row` when a visible commodity exists). The Tile call site MUST forward an `EditorialMonoclePalette.fg`-coloured `TextStyle` through `ResourceLabelInline.labelStyle` so the commodity-id label resolves to the canonical body foreground without `DefaultTextStyle` fall-through. The widget keeps `labelStyle` optional so other consumers of `ResourceLabelInline` (Economic improved / improvable rows, production-panel chips) retain their existing token contract until they are migrated by separate slices.

  None of these rows MAY be authored as bare `Text(...)` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through.
- **Road / railroad caption lines:** the supplementary GDD label (the localized `provinceOverlay_tileRoadLabelNone` → `none`, `provinceOverlay_tileRoadLabelPrimitiveRoad` → `primitive road`, `provinceOverlay_tileRoadLabelImprovedRoad` → `improved road`, `provinceOverlay_tileRoadLabelPortOrRailroad` → `port or railroad`, and `provinceOverlay_tileRoadLabelNonStandard` → `non-standard transport level` for unexpected levels) and the level-1 railroad gloss (the localized `provinceOverlay_tileRoadRailGloss`) render with `color: EditorialMonoclePalette.muted` so the caption reads as the brass-muted under-line shared with other small caption text in the dark theme. The primary numeric line above the caption is **not** a caption (it is a live-data row) and resolves to `EditorialMonoclePalette.fg` per the live-data rule.
- **Inline shortcut icons — disabled state:** when an `Explore`, `Prospect`, `Build improvement`, or **`Build road`** inline action renders disabled, its `Icon.color` MUST resolve to `EditorialMonoclePalette.muted` at the canonical Tile inline-action disabled alpha (`kProvinceOverlayTileInlineActionDisabledAlpha = 0.65`). The enabled state continues to inherit the active `IconTheme` so the brass `--fg` colour reads through the dark theme without a per-call override.
- **Material defaults forbidden:** the Tile section body MUST NOT (a) author any of the live-data `Text(...)` rows above without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for either the live-data rows or the road caption text, (c) consume `Theme.of(context).colorScheme.onSurface`, `colorScheme.outline`, `Theme.of(context).disabledColor`, or any other Material colour-scheme lookup for the road caption text or the disabled inline-action icon colours, or (d) introduce hex literals for either token. All colours resolve from `EditorialMonoclePalette` tokens above. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for the live-data rows; the regression guard pins the explicit token instead (matching the Political body pattern, the Military owner sub-header pattern in S7, the Economic improved-row pattern in S6, the Civilian own-unit pattern in S8, and the Naval fleet-summary pattern).

### Dark-theme tab strip (narrow shell)

The narrow-shell tab strip (`CtTabStrip`) hosting the six province / two sea-zone tabs uses the same editorial-monocle dark tokens. The contract is defined in full in [`pixel-art-ui-catalog.md`](pixel-art-ui-catalog.md) § Pixel-art component catalog (`CtTabStrip` entry) and summarised here for cross-reference:

- **Selected tab:** background painted in `EditorialMonoclePalette.accentDim` at `0.25` alpha; 1 px `EditorialMonoclePalette.accent` border; label text in `EditorialMonoclePalette.accentBright`.
- **Unselected tabs:** background painted in `EditorialMonoclePalette.surface` at `0.5` alpha; 1 px `EditorialMonoclePalette.accentDim` border; label text in `EditorialMonoclePalette.muted`.
- **Material defaults forbidden:** the tab strip MUST NOT consume `Theme.of(context).colorScheme.primary`, `colorScheme.outline`, or `colorScheme.surface` to colour either state, and MUST NOT mount a Material `TabBar` / `TabBarView` chrome.

### Dark-theme Military section body tokens (S7 — Military body)

The Military section groups in-province military units by owner (human player first, then foreign owners sorted by display name) and optionally appends pending land `MoveOrder` preview lines from `draftOrders`. Under `AppThemes.editorialMonocle` the section body MUST resolve every text colour from `EditorialMonoclePalette` so the dark theme owns this surface end-to-end (alongside the header / section-label / tab-strip / Tile-body slices):

- **Owner sub-header** (the player or minor-nation display name rendered above the per-owner type-count lines, e.g. `"France"`) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg` and keeps `fontWeight: FontWeight.w600`. The owner sub-header MUST NOT be authored as a `const TextStyle` without an `EditorialMonoclePalette`-sourced colour (i.e. relying on `DefaultTextStyle` fall-through is forbidden so the contract survives changes to ambient `bodyMedium`).
- **Pending land MoveOrder preview lines** (appended beneath the in-province unit listing when `draftOrders` contains `MoveOrder` entries that concern the selected province) resolve their `TextStyle.color` to `EditorialMonoclePalette.muted` so preview lines visually trail the live in-province roster.
- **Material defaults forbidden:** the Military section body MUST NOT (a) author the owner sub-header as a `const TextStyle` with `color: null` so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for the owner sub-header or pending preview lines, or (c) introduce hex literals for either token. The owner sub-header MUST set `color: EditorialMonoclePalette.fg` and the pending preview lines MUST set `color: EditorialMonoclePalette.muted` directly. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for the owner sub-header; the regression guard pins the explicit token instead.

### Dark-theme Naval section body tokens (S7 — Naval body)

The Naval section lists fleets in-port (province context) or in-zone (sea-zone context) and, in **province** context only, appends pending naval preview lines from `draftOrders` (`NavalMoveOrder` and `NavalMissionOrder` entries for fleets in port at the selected province — see § Province overlay content `Naval`). Under `AppThemes.editorialMonocle` the section body MUST keep its dark-theme contract aligned with the Military body slice above:

- **Fleet-summary roster lines** (the in-port / in-zone `Text(...)` rows rendered from the `provinceOverlay_fleetSummary(owner, fleetLabel, shipParts)` localized string) resolve their `TextStyle.color` to `EditorialMonoclePalette.fg` so the live fleet roster reads in the canonical body foreground colour shared with the Military owner sub-header (S7) and the Economic improved-row label (S6). The fleet-summary line MUST NOT be authored as a bare `Text(...)` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through.
- **Pending naval preview lines** (the `Padding`-wrapped `Text(line)` rows rendered beneath the in-port fleet roster from the `provincePanelPendingNavalLines` helper) resolve their `TextStyle.color` to `EditorialMonoclePalette.muted` so preview lines visually trail the live in-port fleet roster, mirroring the Military `MoveOrder` preview rule.
- **Material defaults forbidden:** the Naval section body MUST NOT (a) author the fleet-summary roster line or the pending preview lines as a bare `Text(...)` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for either line, or (c) consume `Theme.of(context).colorScheme.onSurface` for the pending preview lines. The fleet-summary roster line MUST set `color: EditorialMonoclePalette.fg` and the pending preview lines MUST set `color: EditorialMonoclePalette.muted` directly. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for the fleet-summary roster line; the regression guard pins the explicit token instead (matching the Military owner sub-header pattern in S7 and the Economic improved-row pattern in S6).

### Dark-theme Economic section body tokens (S6 — Economic body)

The Economic section groups player-visible discovered resources and, under each resource id, lists improved tiles first followed by improvable (terrain-eligible, not yet improved) tiles for that resource (see § Province overlay content `Economic` for the resource-grouping and prospection gates). Under `AppThemes.editorialMonocle` the row-label text on every Economic row MUST resolve its colour from `EditorialMonoclePalette` so the dark theme owns the body surface alongside the Tile, Military, and Naval slices:

- **Improved-resource row label** (the `Expanded(child: Text(...))` rendered to the right of the `ResourceLabelInline` icon for tiles that already carry an improvement — content is the localized `province_economic_resourceRow(terrain, resourceName, withImprovementSuffix)` string) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg` so the realized-yield row reads in the canonical body foreground colour shared with the Military owner sub-header. The label MUST NOT be authored as a bare `Text(...)` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through.
- **Improvable-resource row label** (the `Expanded(child: Text(...))` rendered to the right of the `ResourceLabelInline` icon for terrain-eligible tiles that have not yet been improved — content is the localized `province_economic_resourceRow(terrain, resourceName, improvableSuffix)` string) resolves its `TextStyle.color` to `EditorialMonoclePalette.muted` so the latent-yield row visually trails the improved rows it follows under the same resource heading, mirroring the Military / Naval pending-preview muted treatment.
- **Improved-resource commodity-id label** (the internal `Text(label ?? commodityDisplayName(appL10n(context), commodityId))` rendered by the shared `ResourceLabelInline` widget mounted as the `Row`'s leading child for an improved tile row) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg`. The Economic improved-row call site MUST forward an `EditorialMonoclePalette.fg`-coloured `TextStyle` through `ResourceLabelInline.labelStyle` so the commodity-id label resolves to the canonical body foreground without `DefaultTextStyle` fall-through (mirroring the Tile Resource row commodity-id label pattern in § Dark-theme Tile section body tokens (S5 — Tile body)).
- **Improvable-resource commodity-id label** (the internal `Text(label ?? commodityDisplayName(appL10n(context), commodityId))` rendered by the shared `ResourceLabelInline` widget mounted as the `Row`'s leading child for an improvable, terrain-eligible tile row) resolves its `TextStyle.color` to `EditorialMonoclePalette.muted` so the commodity-id reads in the same muted token as the improvable row label that follows it. The Economic improvable-row call site MUST forward an `EditorialMonoclePalette.muted`-coloured `TextStyle` through `ResourceLabelInline.labelStyle` so the commodity-id label resolves to the canonical latent-yield muted token without `DefaultTextStyle` fall-through.
- **Material defaults forbidden:** the Economic row labels MUST NOT (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for either row label or for either commodity-id label rendered by `ResourceLabelInline`, or (c) introduce hex literals for either token. The improved-row label and the improved-row `ResourceLabelInline.labelStyle` MUST set `color: EditorialMonoclePalette.fg`; the improvable-row label and the improvable-row `ResourceLabelInline.labelStyle` MUST set `color: EditorialMonoclePalette.muted` directly. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for the improved-row label; the regression guard pins the explicit token instead (matching the Military owner sub-header pattern in S7).

### Dark-theme Civilian section body tokens (S8 — Civilian body)

The Civilian section lists in-province civilian units after `foreignCivilianVisibleToPlayer` gating (see § Province overlay content `Civilian` and SPEC/program/orders.md for the work-order target rendering). Under `AppThemes.editorialMonocle` each Civilian row MUST resolve its colour from `EditorialMonoclePalette` so the human player's own units stay the visual anchor and foreign units visually trail them, mirroring the Military owner-sub-header (S7) / Economic improved-vs-improvable (S6) split:

- **Own-unit row label** (the `Text(...)` rendered for `u.ownerId == humanPlayerId` — whether the unit shows a pending work-order target line via `provinceOverlay_unitTarget(type, targetLabel)` or an idle status line via `provinceOverlay_unitTarget(type, unitStatusLabel)`) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg` so the human player's own civilians paint in the canonical body foreground colour shared with the Military owner sub-header and the Economic improved row label.
- **Foreign-unit row label** (the `Text(...)` rendered for `u.ownerId != humanPlayerId` — content is the localized `provinceOverlay_foreignUnitStatus(owner, type, statusLabel)` string, only emitted when `foreignCivilianVisibleToPlayer` allows the unit to be shown) resolves its `TextStyle.color` to `EditorialMonoclePalette.muted` so foreign civilians visually trail the human player's own roster, mirroring the Military / Naval pending-preview muted treatment.
- **Material defaults forbidden:** the Civilian row labels MUST NOT (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for either row label, or (c) introduce hex literals for either token. The own-unit row label MUST set `color: EditorialMonoclePalette.fg` and the foreign-unit row label MUST set `color: EditorialMonoclePalette.muted` directly. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for the own-unit row label; the regression guard pins the explicit token instead (matching the Military owner sub-header pattern in S7 and the Economic improved-row pattern in S6).

### Dark-theme empty-state body tokens (S9 — empty `—` placeholders)

When the Economic, Military, Civilian, or Naval section body has no content to render — no improved/improvable resources on the player's intel view for Economic, no in-province military with no pending land `MoveOrder` preview for Military, no `foreignCivilianVisibleToPlayer`-eligible civilians for Civilian, or no in-port / in-zone fleets with no pending naval preview for Naval — the section emits a single em-dash placeholder body `Text('—')`. Under `AppThemes.editorialMonocle` every such placeholder MUST resolve from `EditorialMonoclePalette` so the empty surface stays in sync with the live-data and pending-preview slices on the same section:

- **Empty-state `—` placeholder colour:** every `Text('—')` widget rendered as the standalone empty body for the Economic / Military / Civilian / Naval sections resolves its `TextStyle.color` to `EditorialMonoclePalette.muted` so absence-of-content rows visually demote relative to the canonical body foreground (`EditorialMonoclePalette.fg`) used by the live-data rows on the same sections (Economic improved row, Military owner sub-header, Civilian own-unit, Naval fleet-summary). The placeholder MUST NOT be authored as a `const Text('—')` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through.
- **Shared helper:** the four sites MUST consume a single shared helper (e.g. `_emptyBodyDashText()`) that returns `Text('—', style: TextStyle(color: EditorialMonoclePalette.muted))`, so every empty-state surface stays in sync with the existing Tile-disabled / pending-preview / obfuscated muted token contracts.
- **Material defaults forbidden:** the empty-state `—` placeholder MUST NOT (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`), (c) consume `Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted` under any non-`editorialMonocle` theme), or (d) introduce hex literals for the token. The placeholder text MUST set `color: EditorialMonoclePalette.muted` directly via the shared helper.

### Dark-theme Tile section placeholder body tokens (S5 follow-up — Tile empty / unparsed)

Unlike the four intel-gated sections owned by the S9 empty-state slice, the **Tile** section renders two distinct placeholder bodies that are not driven by intel gating: the **no-selection guidance prompt** (`provinceOverlay_clickTileForDetails`, e.g. `Click a tile to see details.`) rendered when no `selectedTileKey` is set, and the **degenerate em-dash placeholder** (`Text('—')`) rendered when a `selectedTileKey` is set but cannot be parsed into in-region tile coordinates (a defensive fall-through path). Both are placeholder copy — not live world-state data — so under `AppThemes.editorialMonocle` each MUST resolve its colour from `EditorialMonoclePalette.muted`, visually demoting relative to the canonical body foreground (`EditorialMonoclePalette.fg`) used by the Tile live-data rows (coordinates / terrain / civilian-units / Prospected / Improvement / road / Resource):

- **No-selection guidance prompt:** the `provinceOverlay_clickTileForDetails` `Text(...)` rendered as the Tile section body when `selectedTileKey == null` resolves its `TextStyle.color` to `EditorialMonoclePalette.muted`. It MUST NOT be authored as a bare `Text(...)` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through.
- **Degenerate em-dash placeholder:** the Tile-section `Text('—')` rendered when the selected tile key fails coordinate parsing MUST consume the same shared `_emptyBodyDashText()` helper as the S9 empty-state sections (returns `Text('—', style: TextStyle(color: EditorialMonoclePalette.muted))`), so every em-dash placeholder surface in the overlay stays in sync with a single muted token source. It MUST NOT be authored as a `const Text('—')` with `style: null`.
- **Material defaults forbidden:** neither Tile placeholder MUST (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`), (c) consume `Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted` under any non-`editorialMonocle` theme), or (d) introduce hex literals for the token. Both placeholders MUST set `color: EditorialMonoclePalette.muted` directly (the guidance prompt inline, the em-dash via the shared `_emptyBodyDashText()` helper).

### Dark-theme obfuscated `???` body tokens

The overlay substitutes the localized **`provinceOverlay_unknown`** placeholder (`???`) for every obfuscated body — fully-unrevealed province/sea-zone bodies (`_provinceContent` and `_seaZoneContent` `isFullyUnrevealed` branches), partially-revealed Tile rows (the seven `provinceOverlay_tile*Unknown` placeholders rendered when `CellViewData.visibility == TileVisibility.unrevealed`), and the intel-gated Economic / Military / Civilian / Naval body fallbacks (`showsFullIntel == false`). Each placeholder corresponds to information the human player MUST NOT see; under `AppThemes.editorialMonocle` every such `???` `Text(...)` MUST resolve its colour from `EditorialMonoclePalette` so the obfuscation reads as the canonical hidden-information muted token (mirroring the Tile / Military / Naval / Economic / Civilian / Political body-token slices on this issue):

- **Obfuscated body text colour:** every `Text(...)` widget that renders `provinceOverlay_unknown` or one of the seven `provinceOverlay_tile*Unknown` strings (`provinceOverlay_tileCoordinatesUnknown`, `provinceOverlay_tileTerrainUnknown`, `provinceOverlay_tileResourceUnknown`, `provinceOverlay_tileProspectedUnknown`, `provinceOverlay_tileImprovementUnknown`, `provinceOverlay_tileRoadUnknown`, `provinceOverlay_tileCivilianUnitsUnknown`) resolves its `TextStyle.color` to `EditorialMonoclePalette.muted` so obfuscated rows visually demote relative to the canonical body foreground (`EditorialMonoclePalette.fg`) used by the always-exact Political rows (per § Dark-theme Political section body tokens) and the live-data section bodies (Economic improved-row, Military owner sub-header, Naval fleet summary, Civilian own-unit). The `???` body MUST NOT be authored as a bare `Text(...)` with `style: null` so the rendered colour resolves through `DefaultTextStyle` fall-through.
- **Material defaults forbidden:** the obfuscated `???` body MUST NOT (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for the `???` placeholder, (c) consume `Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`), or (d) introduce hex literals for the obfuscation token. The placeholder text MUST set `color: EditorialMonoclePalette.muted` directly via a shared helper so every obfuscated surface stays in sync.

### Dark-theme Political section body tokens

The Political section renders the always-visible province identity rows (`Name: {province}`, `Owner: {ownerName}`, `Region: {region}`, and `Capital: {Yes|No}` — see § Province overlay content `Political / Economic / Naval`). Unlike the gated Economic / Military / Civilian / Naval bodies, Political content is shown regardless of fog (ownership, region, and capital intel are always exact per [fog-and-exploration.md](../game/fog-and-exploration.md)). Under `AppThemes.editorialMonocle` the section body MUST resolve every text colour from `EditorialMonoclePalette` so the dark theme owns this surface end-to-end (alongside the Tile / Economic / Military / Civilian / Naval slices):

- **"Name" row** (the localized `provinceOverlay_name(name)` string above the owner row) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg` so the row reads in the canonical body foreground colour shared with the Military owner sub-header (S7), the Economic improved-row label (S6), the Civilian own-unit row label (S8), and the Naval fleet-summary roster line.
- **"Owner" row** (the localized `provinceOverlay_owner(ownerName)` string beneath the name row) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg` so both Political rows render at the same visual weight (ownership is always-exact intel and never visually demoted to the `muted` token).
- **"Region" row** (the localized `provinceOverlay_region(region)` string) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg`; region identity is always-exact intel and renders at the same visual weight as the Name / Owner rows.
- **"Capital" row** (the localized `provinceOverlay_capitalYes` / `provinceOverlay_capitalNo` string) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg`; capital status is always-exact intel and renders at the same visual weight as the Name / Owner rows.
- **Material defaults forbidden:** the Political rows MUST NOT (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for either row, or (c) introduce hex literals for either token. Both rows MUST set `color: EditorialMonoclePalette.fg` directly. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for either row; the regression guard pins the explicit token instead (matching the Military owner sub-header pattern in S7, the Economic improved-row pattern in S6, and the Civilian own-unit pattern in S8).

### Dark-theme sea-zone Political display-name body token

In sea-zone context the Political section body renders a single row — the localized `provinceOverlay_seaZone(seaName)` string (e.g. `Sea zone: North Atlantic`), driven by `seaZoneDisplayName(...)` from world state per § Sea zone fog rules. Like the province Political rows above, this row is **always shown** when the sea zone is at least partially revealed (the fully-unrevealed case is owned by the existing § Dark-theme obfuscated `???` body tokens slice). Under `AppThemes.editorialMonocle` the same dark-theme contract that owns the province Political body MUST own this row so the overlay surface stays consistent across province / sea-zone contexts:

- **"Sea zone" display-name row** (the localized `provinceOverlay_seaZone(seaName)` `Text(...)` widget rendered inside the Political section when the sea zone has at least one non-`TileVisibility.unrevealed` sea cell) resolves its `TextStyle.color` to `EditorialMonoclePalette.fg`, mirroring the always-exact province "Name" / "Owner" rows and the other live-data body foreground rows (Military owner sub-header S7, Economic improved-row label S6, Civilian own-unit row label S8, Naval fleet-summary roster line).
- **Material defaults forbidden:** the sea-zone "Sea zone" display-name row MUST NOT (a) author the `Text(...)` widget without a `style:` argument so the rendered colour resolves through `DefaultTextStyle` fall-through, (b) fall back to the bare `Brightness.dark` Material `bodyMedium` colour (`Colors.white`) for the row, or (c) introduce hex literals for the token. The row MUST set `color: EditorialMonoclePalette.fg` directly. Note: under `editorialMonocle` the dark `ThemeData.colorScheme.onSurface` is itself wired to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` assertion would be self-contradictory for the row; the regression guard pins the explicit token instead (matching the province Political body pattern above and the Military / Economic / Civilian / Naval body-token patterns).

---

## Province overlay content

**Tile:** From **`selectedTileKey`** only. Empty: “Click a tile to see details.” Else: coordinates, terrain, **town/capital designation** (see below), **resource** with **commodity icon beside the localized commodity display name** (`commodity_<id>` / `commodityDisplayName`; never raw camelCase catalog ids when a catalog entry exists) (— if none; same rule as [pixel-art-ui-catalog.md](pixel-art-ui-catalog.md) commodity labels), **Prospected** (prospectable & not prospected → no; not prospectable → —), improvement, roads/rail, **civilian count** (fog-aware: same rules as Civilian — `foreignCivilianVisibleToPlayer`; enemy Spies never). `???` when `CellViewData.visibility` is unrevealed or province is fully unrevealed.

**Tile town / capital designation (province context only; revealed land tiles only):** Between the **Terrain** row and the **Resource** row, the Tile section MAY render a single contextual designation line. This line is shown **only** for a revealed selected tile (`CellViewData.visibility != TileVisibility.unrevealed`) in **province** context — never in sea-zone context and never inside the obfuscated `???` block. Priority (highest first):

- **Capital tile:** When `selectedTileKey` equals any faction's `CapitalTile.toTileKey()` (player, minor nation, or tribe), render the localized `provinceOverlay_tileCapitalOf(provinceName, factionName)` line → `{provinceName}, the capital of {factionName}`. `{provinceName}` resolves the same as the Political **Name** row (`Province.displayName`, falling back to the province id). `{factionName}` resolves the capital-holder faction's display name the same family as the Political **Owner** name (`Player.displayName`; `MinorNation`/`Tribe` `displayName` falling back to id). When a tile is both a capital tile and the province town tile, only the capital line is shown.
- **Town tile (not a capital):** When `selectedTileKey` equals the containing province's `Province.townTileKey` and the tile is **not** a capital tile, render the localized `provinceOverlay_tileTownOf(provinceName)` line → `The town of {provinceName}`.
- **Otherwise:** Omit the designation line entirely (no `Town: No`, no em-dash placeholder).

Every designation string resolves through `AppLocalizations` parameterized keys (no hardcoded English literals in Dart). Under `AppThemes.editorialMonocle` the line is a live-data body row and resolves its `TextStyle.color` to `EditorialMonoclePalette.fg` via the shared `_fgBodyStyle()` helper.

**Tile `Prospected` inline actions (province context only):** The `Prospected` row can show inline action icons in this order: **`Explore with explorer`** then **`Prospect with explorer`**. Actions are shown only when Tile details are visible (not `???`) and only in province context (never sea-zone). Overlay render/read paths must use stable cached world/player state only (no order-suggestion/order-engine recomputation in UI rebuilds).

- **Explore icon gate:** Show only when all are true:
  - selected tile belongs to a **partially revealed province** (for the viewing human player, that province has at least one tile that is not `TileVisibility.unrevealed` and at least one tile that is `TileVisibility.unrevealed`),
  - a dedicated per-turn cached explore-eligible tile set contains at least one tile in that province,
  - and the selected tile details are not obfuscated.
- **Explore icon behavior:** If the human player has no Explorer units, keep icon visible but disabled (grayscale, no tap). On tap, open Civilian Units panel in explorer-only shortcut mode targeting `explore` for the exact selected tile key. Assign bypasses chooser and commits pending `WorkOrder(target: explore, targetTileKey: <exact selected tile key>)`.
- **Explore target semantics:** `explore` remains province-level. The selected full tile key is the canonical province-level assignment key per [orders.md](../program/orders.md) and must not be rewritten to a synthetic anchor.
- **Work-target selection cache policy:** Civilian cache-first targets and cache lifecycle match [order-suggestions.md](../program/order-suggestions.md) § Per-player work-target selection cache, § Cache-first selection (app shell), and § Runtime stale-tile filter for cache-first protected targets. For **this overlay’s** province Tile section, **`explore`**, **`build_improvement`**, and **`build_road`** inline actions consult the shell’s **`PerPlayerWorkTargetSelectionCache`** (logic implementation) for the gates described below; **`Prospect with explorer`** uses stable world/player mineral and prospection state only (not selection-cache tile membership) for its icon gates. Overlay rebuild churn must not replace those cache-backed gates with live order-suggestion recomputation. Cache refresh is boundary-based: initialize on active game load/start and refresh on turn-resolution completion.
- **Prospect icon gate:** Show only when tile is mineral-eligible and not already prospected by the human player, with the same visibility/obfuscation and province-only gating. Mineral eligibility matches **`isMineralEligibleTile`** in colonizethis_logic (known terrain resources such as wool on hills are not mineral-eligible).
- **Prospect icon behavior:** If the human player has no Explorer units, keep icon visible but disabled; disabled state uses grayscale styling and no tap action.
- **Consulate gate — Explore / Prospect (Refs [diplomacy.md](../game/diplomacy.md) R4 / issue #3753 R4b):** When the selected tile's province is owned by a **Minor or Tribe** with which the viewing human player holds **no Consulate (or higher) overture**, both the `Explore with explorer` and `Prospect with explorer` inline actions render **visible but disabled** (not hidden) so the gate is discoverable. The disabled action's tooltip/semantics label is **`Establish a consulate before exploring or prospecting`** (`provinceOverlay_tileConsulateRequiredForExploreTooltip`) instead of the default `Explore with explorer` / `Prospect with explorer` hint. The gate condition mirrors the order-engine submission validator via the shared `explorerConsulateGateBlocksMinorTribeProvince` predicate (single source of truth), so the disabled UI never offers an action that would be rejected at submission. The consulate tooltip applies **only** when the action is disabled **and** the disablement is caused by the Consulate gate; other disable reasons (e.g. no Explorer units) keep the default tooltip. Own/unowned and Great-Power-owned provinces are never gated.
- Explorers with pending work still count as Explorer units for icon availability and panel filtering.
- **Build-improvement icon gate:** The `Improvement` row can show inline **`Build improvement`** only in province context when Tile details are visible (not `???`) and the selected tile is improvable (`resource exists` and `current improvement level < extractionCapForResourceForUnlocked(player tech, resource)`).
- **Build-improvement icon behavior:** Visibility is trait-only (improvable) and independent from assignability. Enabled/disabled must use full assign-time validity from order suggestions/validators for `build_improvement` (including affordability and reservations for the current draft). If no eligible Builder is assignable, keep icon visible but disabled (grayscale, no tap). On enabled tap, open Civilian Units panel in Builder-only shortcut mode targeting the exact selected tile key for direct `build_improvement` assignment.
- **Build-improvement mineral-discovery edge case (accepted):** For prospect-required mineral tiles, icon visibility still follows authoritative improvable trait from world state. Therefore an unprospected mineral tile may show `Build improvement` as **visible but disabled**; enablement remains false until `getValidWorkOrderTileKeysWithVisibility` includes the tile after prospection and other rules pass.
- **Build-improvement enablement (testing branch A):** Shortcut **`enabled`** is defined as **pipeline contract A** in [order-suggestions.md](../program/order-suggestions.md) § Province Tile `Build improvement` shortcut enablement (`getValidWorkOrderTileKeysWithVisibility` per Builder). App tests and goldens anchor to that SPEC wording.
- **Build-improvement cost preview (Refs #4262):** When the shortcut is **enabled** and `previewWorkOrderAffordAtTile` reports a non-empty material cost for the selected tile (after other pending work in the draft), the icon tooltip uses `provinceOverlay_tileBuildImprovementTooltipWithCost` with a one-line commodity summary (`formatWorkOrderMaterialCostSummary`). When the shortcut is **disabled solely** for materials/treasury (Builder units exist, tile otherwise assignable, but projected stockpile/treasury cannot cover cost after earlier pending work), the tooltip uses `provinceOverlay_tileBuildImprovementDisabledMaterialsTooltip` with the primary shortfall line from `workOrderAffordStatusLine` (`workOrderAfford_shortMaterial` / `workOrderAfford_shortTreasury`). Other disable reasons (no Builder, consulate gate, invalid tile, mineral not prospected, etc.) keep their existing tooltip keys unchanged.
- **Build-road icon gate:** The Road / railroad transport-level row can show inline **`Build road`** only in province context when Tile details are visible (not `???`), the selected tile is land, and a further `build_road` step is still conceivable under product rules (transport level 0, or level 1 when **Road Construction** is unlocked). Hide the icon when transport is already at the `build_road` maximum (level 2, or level 1 without Road Construction) or is port/railroad (level 4).
- **Build-road icon behavior:** Visibility follows conceivable road-step trait only. **Enabled** uses the same cache-first **`build_road`** membership as other protected work targets (`PerPlayerWorkTargetSelectionCache`). If no Engineer can legally assign `build_road` this turn, keep the icon visible but disabled. On enabled tap, open Civilian Units panel in Engineer-only shortcut mode targeting the exact selected tile key for direct `build_road` assignment. Disabled tooltip uses plain-language refusal keys (`provinceOverlay_tileBuildRoadDisabledNoEngineerTooltip`, `provinceOverlay_tileBuildRoadDisabledTooltip`) unless materials/treasury shortfall is the primary reason (see cost preview below).
- **Build-road cost preview (Refs #4262):** When the shortcut is **enabled** and `previewWorkOrderAffordAtTile` reports a non-empty material cost for the selected tile (after other pending work in the draft), the icon tooltip uses `provinceOverlay_tileBuildRoadTooltipWithCost` with a one-line commodity summary (`formatWorkOrderMaterialCostSummary`). When the shortcut is **disabled solely** for materials/treasury (Engineer units exist, tile otherwise assignable, but projected stockpile/treasury cannot cover cost after earlier pending work), the tooltip uses `provinceOverlay_tileBuildRoadDisabledMaterialsTooltip` with the primary shortfall line from `workOrderAffordStatusLine`. Other disable reasons (no Engineer, invalid tile, max transport, etc.) keep their existing tooltip keys unchanged.
- **Build-port icon gate (Refs #4332):** The Road / railroad transport row can show inline **`Build port`** only in province context when Tile details are visible (not `???`), the selected tile is human-owned land cardinally adjacent to a sea zone whose seaboard does not yet have a port for that province, and the tile is not already a registered port tile. Hide the icon when inland / sea / fog / seaboard already ported / foreign-owned.
- **Build-port icon behavior:** Visibility follows conceivable port-step trait only. **Enabled** uses cache-first **`build_port`** membership (`PerPlayerWorkTargetSelectionCache`). If no Engineer can legally assign `build_port` this turn, keep the icon visible but disabled. On enabled tap, open Civilian Units panel in Engineer-only shortcut mode targeting the exact selected tile key for direct `build_port` assignment (`buildPortShortcutTargetTileKey`). Disabled tooltip uses plain-language refusal keys (`provinceOverlay_tileBuildPortDisabledNoEngineerTooltip`, `provinceOverlay_tileBuildPortDisabledTooltip`) unless materials/treasury shortfall is the primary reason (see cost preview below).
- **Build-port cost preview (Refs #4262 / #4332):** When the shortcut is **enabled** and `previewWorkOrderAffordAtTile` reports a non-empty material cost, the icon tooltip uses `provinceOverlay_tileBuildPortTooltipWithCost`. When **disabled solely** for materials/treasury (Engineer units exist, tile otherwise assignable, but projected stockpile/treasury cannot cover cost after earlier pending work), the tooltip uses `provinceOverlay_tileBuildPortDisabledMaterialsTooltip` with the primary shortfall line from `workOrderAffordStatusLine`.
- **Build-railroad icon gate (Refs #4383):** The Road / railroad row can show inline **`Build railroad`** only in province context when Tile details are visible (not `???`), the selected tile is human-owned revealed land, and stored transport level is **1** or **2**. Hide on sea, fog/`???`, foreign-owned land, transport **0** (road first), or transport **4** (already port or railroad). Observe / `canMutateViaUi == false` hides the control.
- **Build-railroad icon behavior:** Visibility follows the conceivable rail-step trait only. **Enabled** uses cache-first **`build_rail`** membership (`PerPlayerWorkTargetSelectionCache`) plus at least one idle human Rail Builder who can legally assign this turn (terrain-matched rail tech and materials). On enabled tap, emit `OpenCivilianUnitsPanelEvent(railBuilderOnly: true, buildRailShortcutTargetTileKey: <exact selected tile key>)`. Click-time drift is a silent no-op. Disabled tooltip uses `provinceOverlay_tileBuildRailroadDisabledNoRailBuilderTooltip` / `provinceOverlay_tileBuildRailroadDisabledTooltip` unless materials/treasury shortfall is the primary reason.
- **Build-railroad cost preview (Refs #4262 / #4383):** When **enabled** and `previewWorkOrderAffordAtTile` reports a non-empty material cost (2 lumber + 2 steel), the tooltip uses `provinceOverlay_tileBuildRailroadTooltipWithCost`. When **disabled solely** for materials/treasury (Rail Builder units exist), the tooltip uses `provinceOverlay_tileBuildRailroadDisabledMaterialsTooltip` with `workOrderAffordStatusLine`.
- **Port status line (Refs #4332 / #4369):** On human-owned revealed land tiles, plain-language `Port: None` (`provinceOverlay_tilePortStatusNone`) or `Port: Present` (`provinceOverlay_tilePortStatusPresent`) from `portsByProvinceSeaboard` appears in **Tile details** (on request), not on the default Tile surface — no raw seaboard ids.
- **Build-fort icon gate (Refs #4280):** The **Military** section can show inline **`Build fort`** only in province context when Military intel allows full body content, Tile details are visible (not `???`), the selected tile is the province **town tile**, and a further `build_fort` step is still conceivable (`fortLevel < 3` with required fort technologies for the next step). Hide the icon when the town tile is not selected, `fortLevel` is already 3, or the next fort step is blocked by missing **Mine Engineering** / **Modern Forts** tech.
- **Build-fort icon behavior:** Visibility follows conceivable fort-step trait on the town tile only. **Enabled** uses cache-first **`build_fort`** membership (`PerPlayerWorkTargetSelectionCache`). If no Engineer can legally assign `build_fort` this turn, keep the icon visible but disabled. On enabled tap, open Civilian Units panel in Engineer-only shortcut mode targeting the exact selected town tile key for direct `build_fort` assignment (`buildFortShortcutTargetTileKey`). Disabled tooltip uses plain-language refusal keys (`provinceOverlay_tileBuildFortDisabledNoEngineerTooltip`, `provinceOverlay_tileBuildFortDisabledTooltip`) unless materials/treasury shortfall is the primary reason (see cost preview below).
- **Build-fort cost preview (Refs #4262 / #4280):** When the shortcut is **enabled** and `previewWorkOrderAffordAtTile` reports a non-empty material cost for the next fort step, the icon tooltip uses `provinceOverlay_tileBuildFortTooltipWithCost`. When **disabled solely** for materials/treasury (Engineer units exist, town tile otherwise assignable, but projected stockpile/treasury cannot cover cost after earlier pending work), the tooltip uses `provinceOverlay_tileBuildFortDisabledMaterialsTooltip` with the primary shortfall line from `workOrderAffordStatusLine`.
- **Purchase-land icon gate (Refs #4274):** The **Resource** row can show inline **`Purchase land`** only in province context when Tile details are visible (not `???`), the selected tile is land with a player-visible resource, and the province owner is a **Minor or Tribe** (not human, GP, or unowned). Hide on own land, GP land, resource-less tiles, sea, or obfuscated tiles.
- **Purchase-land icon behavior:** **Enabled** uses cache-first **`purchase_land`** membership (pipeline contract A; same as Build improvement). On enabled tap, open Civilian Units panel in Merchant-only shortcut mode targeting the exact selected tile key for direct `purchase_land` assignment. Disabled tooltips use embassy-specific copy (`provinceOverlay_tilePurchaseLandDisabledEmbassyTooltip`) — not the Explore/Prospect Consulate string. Enabled cost tooltips use `provinceOverlay_tilePurchaseLandTooltipWithCost` from `previewWorkOrderAffordAtTile` treasury projection.

**Road / railroad (Tile; Refs #4369):** On **land** tiles, the **default** Tile surface shows only the **numeric transport level** (stored road/rail level: **0**, **1**, **2**, or **4** per [extraction-and-improvements.md](../game/extraction-and-improvements.md) § Transport Level), e.g. `Road / railroad: transport level N` (`provinceOverlay_tileRoadTransportLevel(level)`), plus Build road / Build port / Build railroad icons when gated. The GDD caption (`provinceOverlay_tileRoadLabel*` → `none` / `primitive road` / `improved road` / `port or railroad`), the level-**1** railroad gloss (`provinceOverlay_tileRoadRailGloss`), and port status live in **Tile details** (on request). **Sea** tiles: a single default line `Road / railroad: —` (`provinceOverlay_tileRoadNone`) with no Tile-details cluster. Every road / railroad Tile line resolves through `AppLocalizations` (no hardcoded literals).

**Tile details (on request; Refs #4369):** For revealed **land** tiles in province context, teaching lines that are not required to commit Explore / Prospect / Improve / Road / Port open via:

- **Primary:** tap/click the transport / connectivity **text** cluster (numeric transport line and, when present, the default stranded line). Do **not** steal taps from Build road / Build port / Build railroad / other Tile shortcut icons.
- **Fallback:** named **Tile details** text control (`provinceOverlay_tileDetailsAction`) adjacent to that cluster (stable for tests, keyboard/a11y, Widgetbook).
- **Surface:** dismissible `CtDialogShell` helper (`provinceOverlay_tileDetailsTitle`); Close / outside tap / Esc; no auto-open; no new Tile tab.
- **Helper content (when applicable):** road GDD caption + level-1 gloss; `Port: None`/`Present` for human-owned land; capital-link Connected / Not connected (and path-cap wording when connected); `Extraction from this tile: E of F` **only when E > 0 and F > 0**.

**Capital link and per-tile extraction (Tile; province context; human-owned land only; Refs #4149 / #4369):** Hosts still pass `tileConnectivity` once per panel build (no overlay build recompute). **Default surface:**

- **Stranded exception only:** when preview is present, the tile is **not** capital-connected, and **F > 0** (`showExtractionRow`), show one compact line `provinceOverlay_tileCapitalLinkNotConnected` (`Capital link: Not connected — will not extract`). Do not hide this behind Tile details.
- Omit Connected capital-link and all `E of F` rows from the default surface (including connected tiles and `0 of F`).

**Tile details** lists capital-link (Connected or Not connected) when `tileConnectivity` is present, and lists `E of F` only when **E > 0** and **F > 0**. Suppress details extraction when **E == 0** (including stranded improved tiles) so details do not duplicate the stranded exception with `0 of F`.

Omit connectivity preview entirely when: sea-zone overlay context, tile unrevealed (`???` block), selected **sea** tile, foreign-owned province, or map/connectivity preview unavailable.

**Military:** In-province military units (`Unit.locationProvinceId`); group by **owner**, then **type counts** per owner. **Regiment type ids** use **l10n** keys (`province_regiment_*`); unknown catalog ids fall back to the raw id. Append **pending land military** preview lines from **`draftOrders`**: draft **`MoveOrder`** entries for armies/regiments whose move concerns this province (e.g. move toward destination). When Military intel allows full body content, show a **fort posture** line using invasion plain-language labels (`moveArmy_fortOpenField`, `moveArmy_fortWoodSiege`, `moveArmy_fortStoneSiege`, `moveArmy_fortModernSiege`) derived from `Province.fortLevel` — no raw `fortLevel` or `build_fort` strings. On the town tile only, an inline **`Build fort`** icon may appear per the Build-fort gate above. **Move / Invade (Refs #4350):** When Military intel allows full body (not `???`) and not sea-zone, show **`Move`** (`provinceOverlay_moveArmyAction`) on human-owned provinces that host a human non-Home field army, or show **Move** disabled when only the Home Army is present (capital / split-field copy). Show **`Invade`** (`provinceOverlay_invadeArmyAction`) on foreign GP/Minor/Tribe provinces when a cheap conceivable predicate holds: same region as ≥1 human non-Home field army **and** land-adjacency (`neighborProvinceIdsInRegion`) to a province hosting such an army. Hide Invade for wilderness/unowned, sea-zone, and obfuscated Military. **Enablement** reads turn-boundary `PerPlayerArmyMovePickerCache` only (no live `armyMovePickerDestinations` on overlay rebuild). Enabled Move/Invade opens `DLG20001` via overlay-local `showDialog` / `showOverlayArmyMoveFlow` (multi-army → `DLG20002` then dialog); Invade passes `initialDestinationProvinceId` = this province. Observe / `canMutateViaUi == false` hides controls. If there are **no** in-province military units but there **are** pending lines for this province, still show the Military section with fort posture (when intel allows) and those lines.

**Civilian:** Own units — for each unit, if a matching **`WorkOrder`** exists in `draftOrders.workOrdersByPlayerId[humanPlayerId]` (same `unitId`), show a localized **work-order target** line; otherwise show localized **unit status** (not raw enum/id). Format: **`{type}: {targetOrStatus}`** (e.g. `Explorer: Prospect`, `Builder: Idle`). **Do not** show internal `Unit.id` strings (e.g. `gp1_explorer_1`) in player-visible copy; duplicate lines when multiple units share type and status are allowed. Other players — only if `foreignCivilianVisibleToPlayer` allows (tile visibility ≠ unknown; not enemy Spy); show **`{owner} — {type}: {status}`** with the same **no raw `unit.id`** rule.

**Political / Economic / Naval:** Political always uses authoritative province ownership and identity from world state and remains visible regardless of fog; ownership, region, capital, and **town development level** intel are always exact for all players (see [fog-and-exploration.md](../game/fog-and-exploration.md)). The Political body renders, in order: **Name**, **Owner**, **Sight** (`provinceOverlay_sight` — Fully visible / Fogged — terrain only / Unknown — no intel yet from the selected tile’s `CellViewData.visibility`; omit the row when no selected tile cell is available), **Region**, **Capital**, **Town development** (`provinceOverlay_townDevelopmentOfMax` — `N of 4` from `Province.townDevelopmentLevel`), a one-line **manufacturing gist** (`provinceOverlay_townDevelopmentGist*`), and—when a further upgrade is legal for the human owner—an inline **Upgrade town** control (`provinceOverlay_upgradeTownAction`). **Upgrade town** visibility: human-owned province, `townDevelopmentLevel < 4`, non-empty `townTileKey`. **Enabled** when cache-first `upgrade_town` membership exists for the province town tile and assign-time affordability passes (`previewWorkOrderAffordAtTile`); **disabled** with plain-language tooltips for missing Builder, `national_bureaucracy` tech gate, materials shortfall, or other validator refusal (parity with Build improvement / fort shortcuts). **Enabled** tap opens Civilian Units (`UNIT10001`) in **Builder-only** shortcut mode targeting `Province.townTileKey` for direct `upgrade_town` assign (`upgradeTownShortcutTargetTileKey`). At max level (`4`), show max gist only; hide the control. **Port status** is not shown in the Political body; it appears in Tile details (on request) for human-owned revealed land (Refs #4332 / #4369). **Economic**, **Military**, **Civilian**, and **Naval** section bodies are gated by province intel: show full content only when at least one of the following is true: (a) the province is human-owned (`Province.ownerId == humanPlayerId`), (b) every land-province tile key for that province is `VisibilityLevel.fullyVisible` in `PlayerView`, (c) human has an own Spy in that foreign province, or (d) human has an active Spy fog-decay timer `spyRevealTurnsByPlayer[humanPlayerId][prefixedProvinceId] > 0` for that foreign province. When none apply, keep those section headers but show body `???` (no resource rows, military/naval counts, civilian lines). **Economic** full-content mode groups rows by **confirmed player-visible discoveries** only (commodity icon + visible name when known). A tile is eligible only when it is **prospected by the viewing human player** and has an **actual player-visible discovered resource**. Under each resource: rows for **improved** tiles (with improvement label resolved through the `provinceOverlay_improvement*` keys above — no hardcoded improvement-name literals), then **improvable** terrain rows (suffix such as “improvable”). **Do not** include terrain-only prospect rows. **Do not** show tile coordinates on economic rows; **hover** on a row still sets **`secondaryHighlightTileKey`** (map outline). After resource rows (when present), append a **Town production** subsection (`provinceOverlay_townProductionHeading`) listing each projected town-manufacturing bonus commodity for the displayed province: `ResourceIcon` + `+{quantity}` per commodity, sorted by commodity id. Data comes from `previewTownManufacturingBonusByProvince` (panel hosts pass the province slice). When the preview map is empty for that province, show a muted em-dash placeholder under the heading. Hidden under the same intel gate as other Economic body content (`???` when intel fails). Other economic rules follow game specs and commodity label rules (see [pixel-art-ui-catalog.md](pixel-art-ui-catalog.md)). **Naval:** Ship **type ids** use **l10n** (`province_ship_*`); unknown ids fall back to raw id. Fleets in port as before. Append **pending naval** preview lines from **`draftOrders`** only in **province** context: **`NavalMoveOrder`** / **`NavalMissionOrder`** for fleets **in port** in that province. **Sea-zone** overlay does **not** show port-scoped naval pending lines (pass **no** port province id for that helper).

When `explorerConsulateGateBlocksMinorTribeProvince` is true for the human and owner, insert only `No consulate with {owner}` plus **Establish Consulate** directly under Owner. Probe `OvertureStage.tradeConsulate` through `enumerateDiplomaticPanelActionsForTarget`; rejected probes stay visible/disabled with `rejectionReason` in tooltip and semantics, plus inline reason on narrow. A matching pending Establish Overture/Consulate changes the label to **Cancel**. Hide for own, unclaimed, GP-owned, Consulate+ provinces, or unavailable topology; never add other diplomacy actions. Hosts emit the existing confirm/append/remove events; the overlay remains presentational.

**Sea zone:** Political + Naval (fleets in zone). **Player-view / fog parity:** if **every** sea tile in that zone in `RegionMapViewData` is `TileVisibility.unrevealed` for the human player, Political and Naval mirror fully unrevealed provinces (`???`); **do not** show canonical `seaZoneDisplayNameById` text until at least one water tile in the zone is not unrevealed (see [fog-and-exploration.md](../game/fog-and-exploration.md)). Otherwise, Political uses the sea-zone display name from world state (keyed by prefixed sea-zone id), not raw ids; if missing, fallback to id is allowed only as a defensive legacy path. Revealed sea Political also includes the **Sight** row for the selected tile (same three phrases as land).

---

## Behavior

### Incoming (what shows this UI)

| Source | Condition | Result |
|--------|-----------|--------|
| Map tile tap | User taps committed selection | `overlayOpen` + overlay content for `selectedTileKey`. |
| Hover | Pointer over map | Does not change overlay content. |

### User actions → outcomes

| Control / gesture | When enabled | Emits / calls | Side effects |
|-------------------|--------------|---------------|--------------|
| Close | Overlay open | `overlayOpen = false` | Scrim may remain per provider rules. |
| Tile shortcuts (Explore / Prospect / Build improvement / Build road) | Province intel + unit/work gates | Opens civilian panel shortcuts / work orders | Per Interaction section. |
| Tile transport/connectivity text cluster or **Tile details** | Revealed land tile (province) | Opens dismissible Tile details helper | Shows caption/gloss/port/capital-link/(E of F when E>0); no order staging. |
| Political **Upgrade town** shortcut | Human-owned province, level &lt; 4, Builder + tech/afford gates | `OpenCivilianUnitsPanelEvent(builderOnly: true, upgradeTownShortcutTargetTileKey: <townTileKey>)` | Opens Builder-only civilian panel for direct `upgrade_town` assign. |
| Political **Establish Consulate** | Missing Consulate gate applies and canonical Consulate probe accepts | `ConfirmDialogEvent`; confirm emits `AppendDiplomaticOrderRequestedEvent` with Establish Overture / Trade Consulate | Dismiss emits nothing; overlay stays open. |
| Political **Cancel** Consulate | Matching Consulate overture pending | `RemoveDiplomaticOrderRequestedEvent` for Establish Overture + owner | Removes that pending target order; overlay stays open. |
| Military **Build fort** shortcut | Town tile + Military intel + fort-step gates | Opens Engineer-only civilian panel shortcut | Per Interaction section. |
| Military **Move** / **Invade** | Own field army / conceivable foreign target; cache enablement | Overlay-local `showDialog` → `DLG20001` (optional `DLG20002`) | Stages via existing `ArmyMoveRequestedEvent` on confirm. |
| Tile **Build port** shortcut | Human-owned coastal tile + port-step gates | `OpenCivilianUnitsPanelEvent(engineerOnly: true, buildPortShortcutTargetTileKey: …)` | Opens Engineer-only civilian panel for direct `build_port` assign. |
| Tile **Build railroad** shortcut | Human-owned revealed land, transport 1 or 2 | `OpenCivilianUnitsPanelEvent(railBuilderOnly: true, buildRailShortcutTargetTileKey: …)` | Opens Rail-Builder-only civilian panel for direct `build_rail` assign. |
| Economic row hover | Intel allows | Sets `secondaryHighlightTileKey` | Map secondary outline. |

---

## States and variants

| Variant | Trigger | Render difference |
|---------|---------|-------------------|
| Province context | Land tile selected | Full section set per intel gating. |
| Sea zone context | Sea tile selected | Political + Naval; no port-scoped naval pending. |
| Obfuscated | Fog / intel fails | Section bodies `???`. |
| Build road — hidden | Max transport / port-railroad / sea / `???` | Road / railroad row has no inline `Build road` icon. |
| Build road — visible disabled | Conceivable road step but no legal Engineer assign | Inline `Build road` icon on Road / railroad row; disabled styling + refusal tooltip. |
| Build road — visible enabled | Conceivable road step and cache-first valid assign | Inline `Build road` icon enabled; tap opens Engineer-only panel shortcut. |
| Build fort — hidden | Non-town tile / max fort / missing tech / sea / `???` | Military section has no inline `Build fort` icon. |
| Build fort — visible disabled | Conceivable fort step on town tile but no legal Engineer assign | Inline `Build fort` icon on Military fort row; disabled styling + refusal tooltip. |
| Build fort — visible enabled | Town tile + conceivable fort step and cache-first valid assign | Inline `Build fort` icon enabled; tap opens Engineer-only panel shortcut. |
| Move — hidden | Foreign / no human army / sea / `???` | Military section has no Move control. |
| Move — visible disabled | Home Army only, or stationed field armies with empty cache destinations | Move control disabled; capital/split or cannot-move tooltip. |
| Move — visible enabled | ≥1 non-Home field army stationed with cache destinations | Move enabled; tap opens `DLG20001` (or `DLG20002` then dialog). |
| Invade — hidden | Not conceivable / wilderness / sea / `???` / own province | Military section has no Invade control. |
| Invade — visible disabled | Conceivable but province absent from cache reachability | Invade disabled; cannot-reach tooltip from cache. |
| Invade — visible enabled | ≥1 field army has this province in army-move picker cache | Invade enabled; opens `DLG20001` with `initialDestinationProvinceId`. |
| Build port — hidden | Inland / foreign / fog / seaboard already ported / sea / `???` | Road / railroad row has no inline `Build port` icon. |
| Build port — visible disabled | Conceivable port step but no legal Engineer assign | Inline `Build port` icon on transport row; disabled styling + refusal tooltip. |
| Build port — visible enabled | Conceivable port step and cache-first valid assign | Inline `Build port` icon enabled; tap opens Engineer-only panel shortcut. |
| Build railroad — hidden | Transport 0 or 4 / sea / fog / foreign / `???` | Road / railroad row has no inline `Build railroad` icon. |
| Build railroad — visible disabled | Conceivable rail step but no legal Rail Builder assign | Inline `Build railroad` icon; disabled styling + refusal tooltip. |
| Build railroad — visible enabled | Conceivable rail step and cache-first valid assign | Inline `Build railroad` icon enabled; tap opens Rail-Builder-only panel shortcut. |
| Establish Consulate — enabled | Minor/Tribe gate + accepted Consulate probe | Enabled control under Owner. |
| Establish Consulate — disabled | Gate applies but validator rejects | Visible disabled; reason in tooltip/semantics and inline on narrow. |
| Establish Consulate — pending | Matching Consulate overture pending | Control reads **Cancel**. |
| Establish Consulate — hidden | Gate does not apply | Political layout has no diplomacy control. |

---

## Components

- `ProvinceSeaZoneDetailOverlay`, `GameMapProvinceDetailSidePanel`, `GameMapNarrowDetailOverlaySlot`.
- `mapProvincePanelProvider` bridge — no cross-import with map widget.

---

## Widgetbook

Folder: **Province Overlay**. Map stories use provider overrides; Flame map does not import the overlay. Use cases: **Standalone — province**, **Standalone — town production preview** (Economic **Town production** rows with sample `lumber` + `fabric` bonus quantities; Refs #3872), **Standalone — tile stranded capital-link default (Refs #4369)** (default stranded exception + Tile details affordance; teaching lines on request), **Standalone — tile Build road enabled**, **Standalone — tile Build road disabled**, **Standalone — tile Build road hidden** (MAP20001 Road / railroad inline-action variants; Refs #4260), **Standalone — tile Build fort enabled**, **Standalone — tile Build fort disabled**, **Standalone — tile Build fort hidden** (MAP20001 Military fort-row inline-action variants; Refs #4280), **Standalone — tile Build port enabled**, **Standalone — tile Build port disabled**, **Standalone — tile Build port hidden** (MAP20001 Road / transport Build port variants; Refs #4332), **Standalone — tile Build railroad enabled**, **Standalone — tile Build railroad disabled**, **Standalone — tile Build railroad hidden** (MAP20001 Road / railroad Build railroad variants; Refs #4383), **Standalone — tile Purchase land enabled**, **Standalone — tile Purchase land disabled**, **Standalone — tile Purchase land hidden** (MAP20001 Resource-row Purchase land shortcut variants; Refs #4274), **Standalone — Political Upgrade town enabled**, **Standalone — Political Upgrade town disabled**, **Standalone — Political Upgrade town hidden** (MAP20001 Political Town development shortcut variants; Refs #4316), **Standalone — Military Move enabled**, **Standalone — Military Move disabled**, **Standalone — Military Move hidden**, **Standalone — Military Invade enabled**, **Standalone — Military Invade disabled**, **Standalone — Military Invade hidden** (MAP20001 Military Move/Invade variants; Refs #4350), **Standalone — Political Establish Consulate enabled**, **Standalone — Political Establish Consulate disabled**, **Standalone — Political Establish Consulate pending**, **Standalone — Political Establish Consulate hidden** (MAP20001 Political Establish Consulate variants; Refs #4346), **Standalone — Political Sight fully visible**, **Standalone — Political Sight fogged**, **Standalone — Political Sight unknown** (MAP20001 Political Sight row; Refs #4406), **Standalone — extraction & available** (partial-bracket Extraction + capital grain bonus annotation + Available wrap; Refs #4064), **Standalone — sea zone**, **Standalone (mobile)**, **With map — province selected**, **With map — sea zone selected**. Map Widget folder adds **Fort glyphs L1–L3** (Refs #4280; [town-port-icons.md](town-port-icons.md)).


The **Standalone (mobile)** use case wraps the overlay in `mobileViewport(context, …)` so reviewers can verify the `< 600 dp` narrow body (`MainAxisSize.min`, height capped at ~33 % of viewport per `SPEC/ui/in-game-shell-narrow.md` § Province/sea zone detail overlay) without resizing the host window. The **Standalone (mobile)** use case must be pinned by `app/test/widgetbook_province_overlay_mobile_viewport_test.dart` (Refs #2870 R22 / S9) so its removal or rename surfaces in CI before reviewers lose the narrow-viewport review surface.

---

## Acceptance criteria

- **Dark-theme overlay title:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the header builds the `Province` or `Sea zone` title text, then the UI layer applies `EditorialMonoclePalette.accent` as the title color and `letterSpacing: 0.05` on the resolved `TextStyle`.
- **Dark-theme close control border:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the close control (`overlay_close` key) builds, then the UI layer renders a 1 px border colored `EditorialMonoclePalette.accentDim` around the `×` glyph.
- **Dark-theme close glyph color:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the close glyph renders, then the UI layer paints the `×` text in `EditorialMonoclePalette.muted`.
- **Dark-theme chrome avoids Material defaults:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the header or close control builds, then the UI layer does not call `Theme.of(context).colorScheme.outline` or use a `const TextStyle` without a `EditorialMonoclePalette`-sourced color for the title and close glyph.
- **Dark-theme section headers use `CtSectionLabel`:** Given the overlay mounts under `AppThemes.editorialMonocle` and the wide-layout `sections` column renders (viewport width >= shell breakpoint), when each of the section bodies (`Political`, `Tile`, `Economic`, `Military`, `Civilian`, `Naval`) builds, then the UI layer renders the section title via a `CtSectionLabel` widget (not a raw `Text(..., style: TextStyle(fontWeight: FontWeight.bold))`) so the header inherits the canonical small-caps style and 1 px `EditorialMonoclePalette.accentDim` bottom border defined for `CtSectionLabel` in `SPEC/ui/pixel-art-ui-catalog.md`.
- **Dark-theme section headers in fully-unrevealed wide layout:** Given the selected province (or sea zone) is fully unrevealed for the human player and the wide-layout `sections` column renders, when the obfuscated section list builds, then the UI layer renders the header for every listed section (`Political`, `Tile`, `Economic`, `Military`, `Civilian`, `Naval` for province context; `Political`, `Naval` for sea-zone context) using a `CtSectionLabel` widget rather than a raw `Text(..., style: TextStyle(fontWeight: FontWeight.bold))` heading.
- **Dark-theme section header — Material fallback regression guard:** Given the overlay mounts under any `ThemeData`, when any section header in the wide-layout sections column renders, then the UI layer does not mount a Material `ListSubheader` widget for that header and does not paint the underline using `Theme.of(context).colorScheme.outline` (the `CtSectionLabel`-owned `EditorialMonoclePalette.accentDim` underline is the single source).
- **Dark-theme tab strip — selected tab palette:** Given the narrow-shell overlay mounts under `AppThemes.editorialMonocle` with the default selected tab (`index 0`), when the `CtTabStrip` builds, then the UI layer paints that tab's container with a background colour resolved from `EditorialMonoclePalette.accentDim` at `0.25` alpha, a 1 px border colour resolved from `EditorialMonoclePalette.accent`, and a label `TextStyle` whose `color` resolves from `EditorialMonoclePalette.accentBright`.
- **Dark-theme tab strip — unselected tab palette:** Given the narrow-shell overlay mounts under `AppThemes.editorialMonocle` with at least two tabs and the default selected tab (`index 0`), when the `CtTabStrip` builds the non-selected tabs, then the UI layer paints each non-selected tab's container with a background colour resolved from `EditorialMonoclePalette.surface` at `0.5` alpha, a 1 px border colour resolved from `EditorialMonoclePalette.accentDim`, and a label `TextStyle` whose `color` resolves from `EditorialMonoclePalette.muted`.
- **Dark-theme tab strip — Material fallback regression guard:** Given the narrow-shell overlay mounts under any `ThemeData`, when the `CtTabStrip` builds, then the UI layer does not consume `Theme.of(context).colorScheme.primary`, `colorScheme.outline`, or `colorScheme.surface` for either the selected or unselected tab background, border, or label colour (the dark palette is the single source).
- **Dark-theme tab strip — selection swap:** Given the narrow-shell overlay is mounted under `AppThemes.editorialMonocle` and the user taps a non-selected tab, when the tab strip rebuilds after the selection change, then the previously selected tab paints the unselected palette per the unselected-tab AC above and the newly selected tab paints the selected palette per the selected-tab AC above.
- **Dark-theme Military owner sub-header colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Military section passes intel gating and contains at least one in-province military unit, when the Military section renders the per-owner sub-header text (the owner display name above the type-count lines), then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg` and retains `FontWeight.w600`.
- **Dark-theme Military pending preview line colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Military section passes intel gating and `draftOrders` contains at least one pending land `MoveOrder` concerning that province, when the Military section appends the pending preview line beneath the in-province roster, then the UI layer resolves the preview line's rendered `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme Military body — Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Military section renders the owner sub-header text, then the UI layer asserts (a) `style.color != null` (the owner sub-header must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the owner sub-header's correct value.
- **Dark-theme Military body — pending preview line Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Military section renders a pending land `MoveOrder` preview line, then the UI layer asserts (a) `style.color != null`, (b) `style.color != Colors.white`, (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`), and (d) `style.color == EditorialMonoclePalette.muted`.
- **Dark-theme Military regiment-type-count line colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Military section passes intel gating and contains at least one in-province military unit, when the Military section renders the per-regiment-type indented count `Text(...)` line beneath the owner sub-header (formatted via the localized `provinceOverlay_indentedCount(label, count)` string, e.g. `  Pikemen: 1`), then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Military body — regiment-type-count line Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Military section renders a per-regiment-type indented count line, then the UI layer asserts (a) `style.color != null` (the type-count line must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the type-count line's correct value (matching the S6 Economic improved-row, S7 Military owner sub-header, S8 Civilian own-unit, and Naval fleet-summary patterns).
- **Dark-theme Naval pending preview line colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Naval section passes intel gating, the human player owns at least one fleet in port at that province, and `draftOrders` contains at least one pending `NavalMoveOrder` for that fleet, when the Naval section appends the pending preview line beneath the in-port fleet roster, then the UI layer resolves the preview line's rendered `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme Naval body — pending preview line Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Naval section renders a pending in-port `NavalMoveOrder` preview line, then the UI layer asserts (a) `style.color != null` (the pending preview line must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback), (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`), and (d) `style.color == EditorialMonoclePalette.muted` (the explicit single source).
- **Dark-theme Naval fleet-summary roster line colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Naval section passes intel gating and contains at least one in-port fleet whose owner is rendered via the localized `provinceOverlay_fleetSummary(owner, fleetLabel, shipParts)` string, when the Naval section renders that fleet-summary `Text(...)` row, then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Naval body — fleet-summary Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Naval section renders a fleet-summary roster line, then the UI layer asserts (a) `style.color != null` (the fleet-summary line must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the fleet-summary line's correct value (matching the S6 Economic improved-row, S7 Military owner sub-header, and S8 Civilian own-unit patterns).
- **Dark-theme Economic improved-row label colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Economic section passes intel gating, the resource group renders at least one improved tile row, when the `Expanded(child: Text(...))` row label to the right of the `ResourceLabelInline` icon renders, then the UI layer resolves the row label's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Economic improvable-row label colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Economic section passes intel gating, the resource group renders at least one improvable (terrain-eligible, not yet improved) tile row, when the `Expanded(child: Text(...))` row label to the right of the `ResourceLabelInline` icon renders, then the UI layer resolves the row label's `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme Economic body — improved-row Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Economic section renders an improved-resource row label, then the UI layer asserts (a) `style.color != null` (the row label must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the improved-row label's correct value.
- **Dark-theme Economic body — improvable-row Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Economic section renders an improvable-resource row label, then the UI layer asserts (a) `style.color != null` (the row label must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback), (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`), and (d) `style.color == EditorialMonoclePalette.muted` (the explicit single source).
- **Dark-theme Economic improved-row commodity-id label colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Economic section passes intel gating and the resource group renders at least one improved tile row, when the leading `ResourceLabelInline` widget mounted on that improved row renders its internal commodity-id `Text(label ?? commodityDisplayName(appL10n(context), commodityId))`, then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg` because the Economic improved-row call site forwards `labelStyle` from an `EditorialMonoclePalette.fg`-coloured `TextStyle` through `ResourceLabelInline.labelStyle`.
- **Dark-theme Economic improvable-row commodity-id label colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Economic section passes intel gating and the resource group renders at least one improvable (terrain-eligible, not yet improved) tile row, when the leading `ResourceLabelInline` widget mounted on that improvable row renders its internal commodity-id `Text(label ?? commodityDisplayName(appL10n(context), commodityId))`, then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.muted` because the Economic improvable-row call site forwards `labelStyle` from an `EditorialMonoclePalette.muted`-coloured `TextStyle` through `ResourceLabelInline.labelStyle`.
- **Dark-theme Economic body — improved-row commodity-id label Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Economic section renders the leading `ResourceLabelInline` widget on an improved tile row, then the UI layer asserts for the rendered `ResourceLabelInline.labelStyle` (a) `labelStyle != null` (the widget must declare its own `labelStyle` so the internal `Text(...)` does not fall through `DefaultTextStyle`), (b) `labelStyle.color != null`, (c) `labelStyle.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (d) `labelStyle.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `labelStyle.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the improved-row commodity-id label's correct value (matching the existing Tile Resource row commodity-id label regression guard pattern).
- **Dark-theme Economic body — improvable-row commodity-id label Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Economic section renders the leading `ResourceLabelInline` widget on an improvable tile row, then the UI layer asserts for the rendered `ResourceLabelInline.labelStyle` (a) `labelStyle != null`, (b) `labelStyle.color != null`, (c) `labelStyle.color != Colors.white` (the bare dark Material `bodyMedium` fallback), (d) `labelStyle.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`), and (e) `labelStyle.color == EditorialMonoclePalette.muted` (the explicit single source).
- **Dark-theme Civilian own-unit row label colour (S8):** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Civilian section passes intel gating and lists at least one own civilian unit owned by the human player, when that own-unit `Text(...)` line renders (whether the unit shows the pending work-order target line or the fallback idle status line), then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Civilian foreign-unit row label colour (S8):** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Civilian section passes intel gating, the section lists at least one foreign civilian unit, the unit passes `foreignCivilianVisibleToPlayer`, when that foreign-unit `Text(...)` line renders, then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme Civilian body — own-unit Material fallback regression guard (S8):** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Civilian section renders an own-unit row label, then the UI layer asserts (a) `style.color != null` (the row label must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the own-unit row label's correct value (matching the S6 Economic improved-row and S7 Military owner sub-header patterns).
- **Dark-theme Civilian body — foreign-unit Material fallback regression guard (S8):** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Civilian section renders a foreign-unit row label, then the UI layer asserts (a) `style.color != null`, (b) `style.color != Colors.white`, (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`), and (d) `style.color == EditorialMonoclePalette.muted`.
- **Dark-theme obfuscated `???` body colour — fully-unrevealed province sections:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose `isFullyUnrevealed` branch fires (no `RegionMapViewData` cell carries the selected local province id with a non-`TileVisibility.unrevealed` visibility), when the wide-layout `sections` column renders each of the six obfuscated body `Text(...)` widgets (`Political`, `Tile`, `Economic`, `Military`, `Civilian`, `Naval` placeholders), then the UI layer resolves every rendered `???` `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme obfuscated `???` body colour — fully-unrevealed sea-zone sections:** Given the overlay mounts under `AppThemes.editorialMonocle` for a sea zone whose `isSeaZoneFullyUnrevealed` branch fires (every sea cell in the selected zone has `TileVisibility.unrevealed` for the human player), when the wide-layout `sections` column renders the two obfuscated body `Text(...)` widgets (`Political`, `Naval` placeholders), then the UI layer resolves both rendered `???` `TextStyle.color` to `EditorialMonoclePalette.muted` (the canonical sea-zone display name remains suppressed per the existing § Sea zone fog rule).
- **Dark-theme obfuscated `???` body colour — partially-revealed Tile section:** Given the overlay mounts under `AppThemes.editorialMonocle` with a `selectedTileKey` whose `CellViewData.visibility == TileVisibility.unrevealed` inside a partially-revealed province, when the Tile section body renders the seven obfuscated rows (`provinceOverlay_tileCoordinatesUnknown`, `provinceOverlay_tileTerrainUnknown`, `provinceOverlay_tileResourceUnknown`, `provinceOverlay_tileProspectedUnknown`, `provinceOverlay_tileImprovementUnknown`, `provinceOverlay_tileRoadUnknown`, `provinceOverlay_tileCivilianUnitsUnknown`), then the UI layer resolves every rendered placeholder `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme obfuscated `???` body colour — intel-gated section fallbacks:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose intel gating fails (`provincePanelShowsFullTileDerivedIntel == false`), when the Economic / Military / Civilian / Naval section bodies render the `???` fallback `Text(...)` widget (instead of full-content bodies), then the UI layer resolves every rendered fallback `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme obfuscated body — Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when any obfuscated `???` body `Text(...)` renders (fully-unrevealed sections, partially-revealed Tile rows, or intel-gated body fallbacks), then the UI layer asserts (a) `style.color != null` (the obfuscated body must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted`, and itself wired to `EditorialMonoclePalette.fg` under `editorialMonocle` so the obfuscated body would otherwise visually equal the always-exact Political rows), and (d) `style.color == EditorialMonoclePalette.muted` (the explicit single source).
- **Dark-theme Political "Name" row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Political section renders (always — regardless of fog gating, per the always-exact ownership intel rule in [fog-and-exploration.md](../game/fog-and-exploration.md)), when the Political section renders the localized `provinceOverlay_name(name)` row, then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Political "Owner" row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Political section renders, when the Political section renders the localized `provinceOverlay_owner(ownerName)` row, then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Political body — Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Political section renders either the "Name" or the "Owner" row, then the UI layer asserts for each rendered row (a) `style.color != null` (the row must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for either Political row's correct value (matching the S6 Economic improved-row, S7 Military owner sub-header, S8 Civilian own-unit, and Naval fleet-summary patterns).
- **Dark-theme sea-zone Political display-name row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` for a sea zone whose Political section renders (the sea zone has at least one sea cell whose `TileVisibility != TileVisibility.unrevealed`, so the `isSeaZoneFullyUnrevealed` branch does not fire and the obfuscated `???` fallback does not apply), when the Political section renders the localized `provinceOverlay_seaZone(seaName)` row (`Sea zone: {name}`), then the UI layer resolves the rendered `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme sea-zone Political body — Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle` for a sea zone whose Political section renders the live `provinceOverlay_seaZone(seaName)` display-name row, when the row builds, then the UI layer asserts (a) `style.color != null` (the row must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the row's correct value (matching the province Political "Name" / "Owner" row pattern and the Military / Economic / Civilian / Naval body-token patterns).
- **Dark-theme empty-state `—` placeholder colour — Economic section (S9):** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Economic section passes intel gating but emits no improved or improvable rows (no player-visible discovered resources in the province), when the Economic section renders its standalone em-dash `Text('—')` body, then the UI layer resolves the rendered placeholder `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme empty-state `—` placeholder colour — Military section (S9):** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Military section passes intel gating but has zero in-province military units and `draftOrders` contains no pending land `MoveOrder` entries concerning that province, when the Military section renders its standalone em-dash `Text('—')` body, then the UI layer resolves the rendered placeholder `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme empty-state `—` placeholder colour — Civilian section (S9):** Given the overlay mounts under `AppThemes.editorialMonocle` for a province whose Civilian section passes intel gating but no civilian unit passes `foreignCivilianVisibleToPlayer` (own units absent and foreign units excluded), when the Civilian section renders its standalone em-dash `Text('—')` body, then the UI layer resolves the rendered placeholder `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme empty-state `—` placeholder colour — Naval section (S9):** Given the overlay mounts under `AppThemes.editorialMonocle` for a sea-zone displayId whose Naval section finds no in-zone fleets (and the sea-zone context emits no port-scoped pending naval preview lines), when the Naval section renders its standalone em-dash `Text('—')` body, then the UI layer resolves the rendered placeholder `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme empty-state body — Material fallback regression guard (S9):** Given the overlay mounts under `AppThemes.editorialMonocle`, when any of the Economic / Military / Civilian / Naval sections renders its standalone empty-state em-dash `Text('—')` body, then the UI layer asserts (a) `style.color != null` (the placeholder must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted` under any non-`editorialMonocle` theme), and (d) `style.color == EditorialMonoclePalette.muted` (the explicit single source).
- **Dark-theme Tile placeholder — no-selection guidance prompt colour (S5 follow-up):** Given the overlay mounts under `AppThemes.editorialMonocle` with `selectedTileKey == null`, when the Tile section renders the `provinceOverlay_clickTileForDetails` guidance prompt body, then the UI layer resolves that prompt's `TextStyle.color` to `EditorialMonoclePalette.muted`.
- **Dark-theme Tile placeholder — degenerate em-dash colour (S5 follow-up):** Given the overlay mounts under `AppThemes.editorialMonocle` with a `selectedTileKey` that cannot be parsed into in-region tile coordinates, when the Tile section renders its degenerate `Text('—')` placeholder body, then the UI layer resolves that placeholder's `TextStyle.color` to `EditorialMonoclePalette.muted` via the shared `_emptyBodyDashText()` helper.
- **Dark-theme Tile placeholder — Material fallback regression guard (S5 follow-up):** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Tile section renders either the `provinceOverlay_clickTileForDetails` guidance prompt or the degenerate `Text('—')` placeholder, then the UI layer asserts for each rendered placeholder (a) `style.color != null` (the placeholder must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), (c) `style.color != Theme.of(context).colorScheme.onSurface` (the dark Material `bodyMedium` proxy — distinct from `EditorialMonoclePalette.muted` under any non-`editorialMonocle` theme), and (d) `style.color == EditorialMonoclePalette.muted` (the explicit single source).
- **Close control tap fires onClose:** Given the overlay is mounted and an `onClose` callback is supplied, when the user taps the widget with key `overlay_close`, then the system invokes `onClose` exactly once per tap and the `overlay_close` key remains addressable across header restyles.
- **Dark-theme Tile live-data — coordinates row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected (the Tile section's coordinates row is rendered, not the `???` placeholder), when the Tile section renders the localized `provinceOverlay_tileCoordinates(x, y)` row (e.g. `Coordinates: (3, 5)`), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — terrain row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected, when the Tile section renders the localized `provinceOverlay_tileTerrain(terrainStr)` row (e.g. `Terrain: plain`), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — civilian-units row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected, when the Tile section renders the localized `provinceOverlay_tileCivilianUnits(count)` row (e.g. `Civilian units (province): 0`), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected, when the Tile section renders any of the three live-data body rows above (coordinates, terrain, civilian units), then the UI layer asserts (a) `style?.color != null` (each row must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the live-data rows' correct value (matching the S6 Economic improved-row, S7 Military owner sub-header, S8 Civilian own-unit, Naval fleet-summary, and Political body patterns).
- **Dark-theme Tile live-data — Prospected row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected, when the Tile section renders the localized `provinceOverlay_tileProspected(prospectedLabel)` row (e.g. `Prospected: no`, `Prospected: yes`, or `Prospected: —`), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — Improvement row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected, when the Tile section renders the localized `provinceOverlay_tileImprovement(improvementLine)` row (e.g. `Improvement: —` for unimproved tiles or `Improvement: Farm L1` for an improved grain tile), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — Road / railroad primary numeric line colour (land tiles):** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected whose stored transport level is any integer (notably `0`, `1`, `2`, or `4`), when the Tile section renders the primary numeric line `roadRailTransportLevelPrimaryLine(roadLevel)` (e.g. `Road / railroad: transport level 2`), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`. The supplementary GDD caption underneath continues to resolve to `EditorialMonoclePalette.muted` per the existing caption AC.
- **Dark-theme Tile live-data — sea-tile no-road row colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed **sea** tile selected (no land transport level applies), when the Tile section renders the localized `provinceOverlay_tileRoadNone` row (`Road / railroad: —`), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — Prospected / Improvement / road primary / sea-tile no-road Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Tile section renders any of the Prospected, Improvement, road primary numeric (land), or sea-tile no-road rows, then the UI layer asserts for each rendered row (a) `style?.color != null` (the row must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the rows' correct value (matching the existing live-data Tile-row pattern and the S6 Economic / S7 Military / S8 Civilian / Naval / Political body-token patterns).
- **Dark-theme Tile live-data — Resource row prefix colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected (the Tile section's Resource row is rendered, not the `???` placeholder), when the Tile section renders the localized `provinceOverlay_tileResourcePrefix` literal (`Resource: `) as the first child of the `_buildTileResourceLabelRow` `Row`, then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — Resource row no-resource fallback colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected whose visible resource is `null` (no commodity exists or none is player-visible — typically rendered as the localized `—` placeholder), when the Tile section renders the second child of the `_buildTileResourceLabelRow` `Row` (the `Text(resourceLabel)` no-resource fallback, not the `ResourceLabelInline(commodityId: resourceVisible)` icon branch), then the UI layer resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Dark-theme Tile live-data — Resource row Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Tile section renders the Resource row prefix and (where the `null`-visible-resource branch fires) the no-resource fallback `Text(...)`, then the UI layer asserts for each rendered `Text(...)` (a) `style?.color != null` (the row must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the row's correct value (matching the existing live-data Tile-row pattern and the S6 Economic / S7 Military / S8 Civilian / Naval / Political body-token patterns).
- **Dark-theme Tile live-data — Resource row commodity-id label colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a revealed land tile selected whose visible resource is non-`null` (e.g. `grain`), when the Tile section renders the visible-commodity branch of the `_buildTileResourceLabelRow` `Row` (the `ResourceLabelInline(commodityId: resourceVisible, labelStyle: <fg style>)` widget mounted as the second child), then the UI layer resolves the internal commodity-id `Text(label ?? commodityDisplayName(appL10n(context), commodityId))` `TextStyle.color` to `EditorialMonoclePalette.fg` because the Tile call site forwards `labelStyle` from the shared body-style helper.
- **Dark-theme Tile live-data — Resource row commodity-id label Material fallback regression guard:** Given the overlay mounts under `AppThemes.editorialMonocle`, when the Tile section renders the visible-commodity branch of the Resource row, then the UI layer asserts for the rendered `ResourceLabelInline` internal commodity-id `Text(...)` (a) `style?.color != null` (the label must declare its own colour, not fall through `DefaultTextStyle`), (b) `style.color != Colors.white` (the bare dark Material `bodyMedium` fallback before the `editorialMonocle` overlay), and (c) `style.color == EditorialMonoclePalette.fg` (the explicit single source). Note: `style.color != Theme.of(context).colorScheme.onSurface` is intentionally NOT asserted because under `editorialMonocle` the dark theme wires `colorScheme.onSurface` to `EditorialMonoclePalette.fg`, so an `isNot(onSurface)` guard would tautologically fail for the label's correct value (matching the prefix and no-resource fallback regression guard above, and the S5 / S6 / S7 / S8 / Naval / Political body-token patterns). The `ResourceLabelInline.labelStyle` parameter remains optional so other consumers (Economic improved / improvable rows, production-panel chips) preserve their existing token contract until separate slices migrate them.
- **Tile town designation line:** Given province context and a revealed land `selectedTileKey` equal to that province's `Province.townTileKey` and not equal to any faction's `CapitalTile.toTileKey()`, when the Tile section renders, then the UI layer shows the localized `provinceOverlay_tileTownOf(provinceName)` line (`The town of {provinceName}`) immediately after the terrain row and before the resource row, with `TextStyle.color` resolved to `EditorialMonoclePalette.fg`.
- **Tile capital designation line:** Given province context and a revealed land `selectedTileKey` equal to a faction's `CapitalTile.toTileKey()`, when the Tile section renders, then the UI layer shows the localized `provinceOverlay_tileCapitalOf(provinceName, factionName)` line (`{provinceName}, the capital of {factionName}`) after the terrain row and before the resource row; when the tile is both a capital and the province town, only the capital line renders (no town line).
- **Tile ordinary land tile — no designation:** Given province context and a revealed land tile that is neither the province town nor any faction capital tile, when the Tile section renders, then the UI layer omits the designation line entirely (no `Town: No`, no em-dash placeholder between Terrain and Resource).
- **Tile designation suppressed for sea / unrevealed:** Given sea-zone context or a selected tile whose `CellViewData.visibility == TileVisibility.unrevealed`, when the Tile section renders, then the UI layer does not render a town/capital designation line (the obfuscated `???` block is unchanged).
- **Tile designation uses localized keys:** Given the town or capital designation line is shown, when the rendered string is inspected, then all user-visible text resolves through `AppLocalizations` parameterized keys (`provinceOverlay_tileTownOf` / `provinceOverlay_tileCapitalOf`) with no hardcoded English literals in Dart.
- **Dark-theme Tile road caption colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a land tile selected whose stored transport level is in `{0, 1, 2, 4}` (or any other integer value), when the Tile section renders the supplementary GDD line (`none`, `primitive road`, `improved road`, `port or railroad`, or `non-standard transport level`), then the UI layer paints the supplementary line's resolved `TextStyle.color` from `EditorialMonoclePalette.muted` and does not derive that colour from `Theme.of(context).colorScheme.onSurface` or any other Material colour-scheme lookup.
- **Dark-theme Tile road caption — level-1 rail gloss colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with a land tile whose stored transport level is `1`, when the Tile section renders the third line clarifying that railroads are level 4 (`kRoadRailPrimitiveVersusRailGloss`), then the UI layer paints that gloss line's resolved `TextStyle.color` from `EditorialMonoclePalette.muted`.
- **Dark-theme Tile inline action — disabled `Explore` icon colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with `showExploreActionIcon` set to `true` and `exploreActionEnabled` set to `false`, when the `Explore with explorer` `IconButton` builds its leading `Icon`, then the UI layer paints `Icon.color` as `EditorialMonoclePalette.muted` at alpha `kProvinceOverlayTileInlineActionDisabledAlpha` (`0.65`) and does not derive the disabled colour from `Theme.of(context).disabledColor`.
- **Dark-theme Tile inline action — disabled `Prospect` icon colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with `showProspectActionIcon` set to `true` and `prospectActionEnabled` set to `false`, when the `Prospect with explorer` `IconButton` builds its leading `Icon`, then the UI layer paints `Icon.color` as `EditorialMonoclePalette.muted` at alpha `kProvinceOverlayTileInlineActionDisabledAlpha` (`0.65`) and does not derive the disabled colour from `Theme.of(context).disabledColor`.
- **Dark-theme Tile inline action — disabled `Build improvement` icon colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with `showBuildImprovementActionIcon` set to `true` and `buildImprovementActionEnabled` set to `false`, when the `Build improvement` `IconButton` builds its leading `Icon`, then the UI layer paints `Icon.color` as `EditorialMonoclePalette.muted` at alpha `kProvinceOverlayTileInlineActionDisabledAlpha` (`0.65`) and does not derive the disabled colour from `Theme.of(context).disabledColor`.
- **Dark-theme Tile inline action — disabled `Build road` icon colour:** Given the overlay mounts under `AppThemes.editorialMonocle` with `showBuildRoadActionIcon` set to `true` and `buildRoadActionEnabled` set to `false`, when the `Build road` `IconButton` builds its leading `Icon`, then the UI layer paints `Icon.color` as `EditorialMonoclePalette.muted` at alpha `kProvinceOverlayTileInlineActionDisabledAlpha` (`0.65`) and does not derive the disabled colour from `Theme.of(context).disabledColor`.
- **Dark-theme Tile body — Material defaults regression guard:** Given the overlay mounts under any `ThemeData`, when the Tile section body builds the road caption lines or the disabled `Explore` / `Prospect` / `Build improvement` / `Build road` inline action icons, then the UI layer does not call `Theme.of(context).colorScheme.onSurface`, `Theme.of(context).colorScheme.outline`, or `Theme.of(context).disabledColor` to colour those elements (the `EditorialMonoclePalette` tokens above are the single source).

- **Consulate gate — disabled `Prospect` tooltip (Refs #3753 R4b):** Given the overlay shows the Tile section for a province owned by a Minor/Tribe with which the human player holds no Consulate (or higher) overture, and `showProspectActionIcon` is `true` with `prospectActionEnabled` `false`, when the Tile section renders the `Prospect with explorer` inline action, then the UI layer sets that action's tooltip to `Establish a consulate before exploring or prospecting` (`provinceOverlay_tileConsulateRequiredForExploreTooltip`) and not to `Prospect with explorer`.
- **Consulate gate — disabled `Explore` tooltip (Refs #3753 R4b):** Given the overlay shows the Tile section for a province owned by a Minor/Tribe with which the human player holds no Consulate (or higher) overture, and `showExploreActionIcon` is `true` with `exploreActionEnabled` `false`, when the Tile section renders the `Explore with explorer` inline action, then the UI layer sets that action's tooltip to `Establish a consulate before exploring or prospecting` and not to `Explore with explorer`.
- **Consulate gate — message is gate-specific (negative, Refs #3753 R4b):** Given the overlay shows the Tile section for a province that is the human player's own (so the Consulate gate does not apply), and `showProspectActionIcon` is `true` with `prospectActionEnabled` `false`, when the Tile section renders the `Prospect with explorer` inline action, then the UI layer keeps the default `Prospect with explorer` tooltip and does not show `Establish a consulate before exploring or prospecting`.
- **Political Consulate visibility:** Given a Minor/Tribe-owned province and the human holds no Consulate+, when Political builds, then **Establish Consulate** is the only diplomacy action; given own/unclaimed/GP/Consulate+ ownership, it is absent.
- **Political Consulate validation:** Given the Consulate probe rejects, when Political builds, then the control is disabled and exposes the canonical rejection reason in tooltip/semantics and inline on narrow.
- **Political Consulate confirm:** Given the probe accepts, when the player taps and confirms, then the host appends Establish Overture / Trade Consulate for that owner; dismiss appends nothing and MAP20001 stays open.
- **Political Consulate pending:** Given that exact order is pending, when Political builds and the player taps **Cancel**, then the host removes the pending Establish Overture for that owner.
- **Narrow Consulate guidance:** Given the gate disables Explore/Prospect below 600 dp, when Tile renders, then its tooltip names Political **Establish Consulate**; Political at 320 dp wraps without overflow.
- **Consulate gate — prospect disabled when no Consulate (Refs #3753 R4b):** Given a mineral-eligible, unprospected, revealed tile in a Minor/Tribe province with at least one human Explorer unit but no Consulate (or higher) overture with the owner, when `GameMapAreaProvinceActionStates.prospect` computes the inline state, then the state is `showIcon = true` and `enabled = false`. Given the human holds a Consulate (or higher) with the owner, the state is `showIcon = true` and `enabled = true`.
- **Map/panel decoupling:** Given the implementation files for the Flame `CtRegionMap` widget and `ProvinceSeaZoneDetailOverlay`, when those files are inspected for imports, then neither imports nor references the other; the map embedding layer reads and writes `mapProvincePanelProvider` and passes plain data plus callbacks (`selectedTileKey`, `secondaryHighlightTileKey`, `onMapTileTappedForDetail`) into the map widget's constructor.
- **Tap opens/updates the overlay:** Given the overlay is closed or showing a different tile, when the user taps or clicks a map tile, then `mapProvincePanelProvider` records the tapped tile in `selectedTileKey`, sets `overlayOpen` to true, and the overlay's Tile section renders content derived from that exact `selectedTileKey`.
- **Hover never updates selection:** Given the user's pointer is hovering over a map tile without a tap or click, when the pointer moves, then the UI layer does not change `selectedTileKey`, does not toggle `overlayOpen`, and does not change the overlay's Tile section content (hover may still update map-only hover visuals).
- **Narrow full-width, uncapped parent:** Given a narrow viewport (width < shell breakpoint, e.g. 600 logical px) where the parent does not already constrain the overlay's height to at most `0.33 × MediaQuery.size.height`, when the overlay layout builds, then the UI layer caps the effective max content height at `min(parentMax, 0.33 × H)`.
- **Narrow full-width, parent already capping:** Given a narrow viewport where the parent already constrains the overlay height to exactly `0.33 × H` (e.g. a bottom slot wrapped in `SizedBox(height: third)`), when the overlay layout builds, then the UI layer uses that constrained height exactly without further shrinking and wraps each tab body in a scrollable container so content never overflows.
- **Narrow side rail:** Given a narrow viewport where the overlay is hosted in a fixed-width side rail (panel width clearly less than screen width, e.g. 320 dp), when the overlay layout builds, then the UI layer uses the full rail height (`parentMax`) without applying the `0.33 × H` cap.
- **Wide viewport side panel:** Given a viewport width ≥ shell breakpoint, when the overlay mounts, then the UI layer renders the overlay in a side panel using the parent's bounded height with a single scrollable column of sections and without a tab strip.
- **Economic row hover sets secondary highlight:** Given the overlay is open and the Economic section is rendering at least one resource row, when the user hovers over an Economic resource row, then the UI layer writes that row's tile key to `secondaryHighlightTileKey` and the map renders a non-orange outline (distinct from the orange `selectedTileKey` outline) on that tile.
- **Overlay close behavior:** Given the overlay is open, when the user activates the overlay's close control, then `mapProvincePanelProvider.overlayOpen` becomes false while `selectedTileKey` retention is implementation-defined per the Interaction § Close rules (may remain or clear).
- **Reopen via tile tap after close:** Given the overlay is closed and `selectedTileKey` is set to a valid tile, when the user taps a tile (the same one or another), then the overlay may reopen with `overlayOpen` set to true and `selectedTileKey` updated to the newly tapped tile.
- **Civilian line from draft work order:** Given `draftOrders.workOrdersByPlayerId[humanPlayerId]` contains a `WorkOrder` whose `unitId` matches an own civilian unit, when the Civilian section renders that unit, then the UI layer renders the line as `{localized type}: {localized target}` derived from the first matching draft `WorkOrder` (e.g. `Explorer: Prospect`) and the line text does not contain the raw `unit.id` substring.
- **Civilian line falls back to unit status:** Given an own civilian unit with no matching draft `WorkOrder` in `draftOrders.workOrdersByPlayerId[humanPlayerId]`, when the Civilian section renders that unit, then the UI layer renders the line as `{localized type}: {localized status}` using the unit's current status (e.g. `Builder: Idle`) with no raw `unit.id` substring.
- **Military labels are localized:** Given the Military section renders in-province units, when ship and regiment type labels are emitted, then the UI layer uses the localized type ids (`province_regiment_*` / `province_ship_*`) and falls back to the raw catalog id only when the localization key is missing.
- **Pending land MoveOrder preview lines:** Given province intel gating allows full content for the selected province and `draftOrders` contains pending `MoveOrder` entries for armies or regiments concerning that province, when the Military section renders, then the UI layer appends a localized pending land-movement preview line for each such order beneath the in-province unit listing.
- **Pending in-port naval preview lines (province context):** Given the overlay is in **province** context, province intel gating allows full content, and `draftOrders` contains pending `NavalMoveOrder` or `NavalMissionOrder` entries for fleets that are in-port in the selected province, when the Naval section renders, then the UI layer appends a localized pending naval preview line for each such order.
- **Sea-zone overlay omits port-scoped naval lines:** Given the overlay is in **sea-zone** context, when the Naval section renders pending lines, then the UI layer does not append any port-scoped pending naval lines (no port province id is forwarded to the helper) regardless of `draftOrders` content.
- Given the Civilian section lists an own Explorer with pending `prospect` work, when the overlay renders, then the line reads `Explorer: Prospect` (localized type and target) and does **not** contain the internal `unit.id` substring.
- Given the Civilian section lists a visible foreign civilian, when the overlay renders, then the line uses owner display name, type, and localized status only (e.g. `France — Explorer: Idle`) with **no** internal `unit.id` in parentheses.
- **Economic rows bucket by visible resource:** Given the Economic section renders in full-content mode for the selected province, when rows are emitted, then the UI layer groups rows by the player-visible commodity discovery (commodity icon + localized commodity display name) for each prospected tile that has an actual player-visible discovered resource.
- **Localized commodity names (Tile + Economic):** Given locale `en` and a visible commodity id `sugarCane` (and likewise `castIron`, `refinedSugar`, `furHats`), when the Tile Resource row or Economic `ResourceLabelInline` / `province_economic_resourceRow` renders, then the UI layer shows the localized ARB display name (e.g. `Sugar cane`), not the raw camelCase catalog id. Refs #3987.
- **Economic improved before improvable order:** Given a commodity bucket in the Economic section, when rows render under that bucket, then the UI layer lists **improved** tiles first (with the improvement label) and then **improvable** terrain rows (suffixed e.g. `improvable`), with no terrain-only prospect-required rows interleaved.
- **Economic excludes unprospected and no-resource tiles:** Given a tile in the selected province that is either unprospected by the human player or prospected with no player-visible discovered resource, when the Economic section renders, then the UI layer excludes that tile from every commodity bucket.
- **Economic rows omit coordinates:** Given the Economic section renders any row, when the row text is emitted, then the UI layer does not include tile coordinates in the row text, while hover behavior continues to update `secondaryHighlightTileKey` per the Economic-row-hover AC.
- **Economic Town production preview rows:** Given the overlay mounts for a province whose Economic section passes intel gating and `townProductionBonusByCommodity` contains at least one positive quantity (e.g. `lumber: 1`, `fabric: 1`), when the Economic section renders, then the UI layer shows the `provinceOverlay_townProductionHeading` subsection with a `ResourceIcon` and localized `provinceOverlay_townProductionQuantity` (`+{quantity}`) row per commodity id, sorted by commodity id. Refs #3872.
- **Economic Extraction and Available condensed lines:** Given Economic full intel (including observe/omniscient), when the Economic section renders, then **Extraction** and **Available** subsections appear above Town production per [province-economic-extraction-available.md](province-economic-extraction-available.md) (Refs #4064). Extraction is a post-resolution **projection** (capital grain bonus distinctly labeled); Available counts current improvable tiles. Projection rules: [province-extraction-snapshot.md](../program/province-extraction-snapshot.md).
- **Economic Town production empty preview:** Given the overlay mounts for a province whose Economic section passes intel gating and `townProductionBonusByCommodity` is empty, when the Economic section renders the Town production subsection, then the UI layer shows the heading and a muted em-dash placeholder beneath it. Refs #3872.
- **Unrevealed obfuscation:** Given the selected tile's `CellViewData.visibility` is `unrevealed` or the entire selected province is fully unrevealed for the human player in `PlayerView`, when the overlay renders the Tile section and any province-scoped section, then the UI layer substitutes `???` for all obfuscated fields per the fog-of-war obfuscation rules in `SPEC/game/fog-and-exploration.md`.
- **Province intel gating obfuscates section bodies:** Given a non-fully-unrevealed province where none of the province intel conditions hold (province not human-owned, not all land tiles fully visible, no own Spy present, and no active Spy fog-decay timer entry), when the overlay renders, then the UI layer renders the bodies of the **Economic**, **Military**, **Civilian**, and **Naval** sections as `???` while keeping each section's header visible.
- **Province intel gating hides pending draft lines:** Given the same province intel gating failure condition, when the overlay renders, then the UI layer hides all pending land `MoveOrder` and naval `NavalMoveOrder` / `NavalMissionOrder` draft preview lines for that province.
- **Province intel gating preserves Political and Tile sections:** Given the same province intel gating failure condition, when the overlay renders, then the UI layer continues to render the **Political** section per its own authoritative-ownership rule and the **Tile** section per the selected-tile visibility rule (intel gating does not suppress those sections).
- **Political renders Region row:** Given a province overlay (any fog/intel state where the Political body is not the fully-unrevealed `???` placeholder), when the Political section renders, then the UI layer renders a `Region` row whose text is the localized `provinceOverlay_region` for the province's `regionId` (the `Old World` / `New World` label via `region_oldWorld` / `region_newWorld`, falling back to the raw `regionId` for any other region) and resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Political renders Capital row — capital province:** Given a province whose id equals the `capitalProvinceId` of any faction (player, minor nation, or tribe), when the Political section renders, then the UI layer renders a `Capital` row reading the localized `provinceOverlay_capitalYes` (`Capital: Yes`) and resolves that row's `TextStyle.color` to `EditorialMonoclePalette.fg`.
- **Political renders Capital row — non-capital / unclaimed province:** Given a province whose id does not equal the `capitalProvinceId` of any faction (including an unclaimed province with no owner), when the Political section renders, then the UI layer renders a `Capital` row reading the localized `provinceOverlay_capitalNo` (`Capital: No`).
- **Political renders Town development row:** Given any province overlay where the Political body is not the fully-unrevealed `???` placeholder, when the Political section renders, then the UI layer renders a `Town development` row reading `provinceOverlay_townDevelopment` with the province's true `townDevelopmentLevel` integer **1–4** (always exact, including fogged provinces). Refs #3870.
- **Political body omits port status:** Given any province overlay, when the Political section renders, then the UI layer does not emit a port-status row in the Political body.
- **Fully unrevealed sea zone obfuscation:** Given the overlay is in sea-zone context and every sea tile in the selected sea zone has `TileVisibility.unrevealed` for the human player in `RegionMapViewData`, when the overlay renders, then the UI layer renders the Political and Naval section bodies as `???` and does not surface the world-state `seaZoneDisplayNameById` text for that zone.
- **Sea-zone partial reveal uses world-state display name:** Given the overlay is in sea-zone context and at least one sea tile in the selected sea zone is not `TileVisibility.unrevealed` for the human player, when the Political section header renders, then the UI layer uses `seaZoneDisplayNameById` keyed by the selected prefixed sea-zone id as the header text, falling back to the raw id only as a defensive path when the display-name lookup is missing.
- Given a province Tile section with visible tile details and a selected tile that is mineral-eligible and not already prospected by the human player, when the overlay renders, then the UI layer shows an inline icon next to `Prospected` with tooltip/accessibility label `Prospect with explorer`.
- Given the Tile section is unrevealed/obfuscated (`???`) or the overlay is in sea-zone context, when the overlay renders, then the UI layer does not show the inline `Prospect with explorer` icon.
- Given the human player has zero Explorer units, when the province Tile section renders the inline `Prospect with explorer` icon, then the UI layer renders it disabled, grayscale, and non-clickable.
- Given the selected tile is already prospected by the human player, when the province Tile section renders, then the UI layer does not show the inline `Prospect with explorer` icon.
- Given map scrolling, panning, or unrelated overlay rebuilds occur while selection stays on the same tile, when the overlay re-renders, then the UI layer computes the prospect icon state from stable world/player tile state and does not invoke order-suggestion or order-engine validation helpers for this state.
- Given province context Tile section with visible tile details and a partially revealed province, when the dedicated per-turn explore eligibility cache contains at least one tile in that province, then the UI layer shows inline `Explore with explorer` before `Prospect with explorer`.
- Given province context Tile section with visible tile details and explore icon conditions true but the human player has zero Explorer units, when the overlay renders, then the UI layer keeps `Explore with explorer` visible but disabled, grayscale, and non-clickable.
- Given tile details are obfuscated (`unknown` or `unrevealed`) or the selected context is sea-zone, when the overlay renders, then the UI layer does not show `Explore with explorer`.
- Given user taps `Explore with explorer` and click-time state remains valid, when the Civilian Units panel explorer shortcut assign is triggered, then the UI layer commits pending `WorkOrder(target: explore, targetTileKey: <exact selected tile key>)` and does not enter generic work-target selection mode.
- Given click-time state drift invalidates `Explore with explorer` assignment, when the user taps the icon, then the UI layer performs a silent no-op and commits no pending work order.
- Given map scrolling/panning/rebuild churn with unchanged selected tile and unchanged turn snapshot, when the overlay re-renders, then **`explore`**, **`build_improvement`**, and **`build_road`** Tile inline action states that are driven by the shell’s **`PerPlayerWorkTargetSelectionCache`** read from that cache and do not perform live target-set recomputation for those gates.
- Given turn resolution advances to the next turn snapshot, when the UI refreshes overlay-related caches, then **`PerPlayerWorkTargetSelectionCache`** is refreshed for subsequent overlay and shell decisions that depend on it (including **`explore`**, **`build_improvement`**, and **`build_road`** Tile inline actions per the cache policy above).
- Given province Tile details are visible and selected tile has a resource with current improvement level below the player extraction tech cap for that resource, when the overlay renders, then the UI layer shows inline `Build improvement` on the `Improvement` row.
- Given selected tile has no resource or its current improvement level is at/above the player extraction tech cap, when the overlay renders, then the UI layer does not show `Build improvement`.
- Given the selected context is sea-zone or tile details are obfuscated (`???`), when the overlay renders, then the UI layer does not show `Build improvement`.
- Given `Build improvement` is visible and no Builder can validly assign `build_improvement` this turn (including affordability/reservation constraints), when the overlay renders, then the UI layer keeps the icon visible but disabled, grayscale, and non-clickable.
- Given selected tile is a prospect-required mineral and the tile is not yet prospected by the human player, when the tile is otherwise improvable by trait, then the UI layer may keep `Build improvement` visible but disabled until prospection and all assign-time rules pass.
- Given user taps enabled `Build improvement` and click-time state remains valid, when the Civilian Units panel opens, then it opens in Builder-only shortcut mode targeting the exact selected tile key for direct `WorkOrder(target: build_improvement, targetTileKey: <exact selected tile key>)`.
- Given click-time state drift invalidates `Build improvement`, when user taps the icon, then the UI layer performs a silent no-op and commits no pending work order.
- Given `Build improvement` is enabled for the selected tile and `previewWorkOrderAffordAtTile` reports a non-empty material cost after other pending work, when the UI layer renders the inline action tooltip, then the tooltip includes required commodity quantities via `provinceOverlay_tileBuildImprovementTooltipWithCost` without opening Production (`GAME20001`).
- Given `Build improvement` is visible and disabled solely because projected stockpile or treasury cannot cover the tile cost after earlier pending work (and at least one Builder unit exists), when the UI layer renders the inline action tooltip, then the tooltip primary reason is a plain shortfall line from `workOrderAffordStatusLine` wrapped by `provinceOverlay_tileBuildImprovementDisabledMaterialsTooltip`.
- Given province Tile details are visible and the selected land tile can still take a legal `build_road` step and at least one human Engineer can assign that step this turn, when the overlay renders, then the UI layer shows inline `Build road` on the Road / railroad row and it is enabled.
- Given the selected land tile is already at absolute max road/rail transport under product rules so no further `build_road` applies, when the overlay renders, then the UI layer does not show `Build road`.
- Given a road step is still conceivable for the tile but no Engineer can legally assign `build_road` this turn, when the overlay renders, then the UI layer keeps `Build road` visible but disabled with a plain-language primary refusal in tooltip/semantics (not raw `build_road`).
- Given the selected context is sea-zone or tile details are obfuscated (`???`), when the overlay renders, then the UI layer does not show `Build road`.
- Given user taps enabled `Build road` and click-time state remains valid, when the Civilian Units panel opens, then it opens in Engineer-only shortcut mode targeting the exact selected tile key for direct `WorkOrder(target: build_road, targetTileKey: <exact selected tile key>)`.
- Given more than one human Engineer exists, when the user taps enabled `Build road`, then the UI layer opens the Civilian Units panel so the player can choose which Engineer to Assign (no silent auto-commit without panel choice).
- Given click-time state drift invalidates `Build road`, when user taps the icon or Assign on the shortcut panel, then the UI layer performs a silent no-op and commits no pending work order.
- Given `Build road` is enabled for the selected tile and `previewWorkOrderAffordAtTile` reports a non-empty material cost after other pending work, when the UI layer renders the inline action tooltip, then the tooltip includes required commodity quantities via `provinceOverlay_tileBuildRoadTooltipWithCost` without opening Production (`GAME20001`).
- Given `Build road` is visible and disabled solely because projected stockpile or treasury cannot cover the tile cost after earlier pending work (and at least one Engineer unit exists), when the UI layer renders the inline action tooltip, then the tooltip primary reason is a plain shortfall line from `workOrderAffordStatusLine` wrapped by `provinceOverlay_tileBuildRoadDisabledMaterialsTooltip`.
- Given Military intel allows full body and the selected tile is the province town tile with a conceivable `build_fort` step and at least one human Engineer can assign that step this turn, when the overlay renders, then the UI layer shows inline `Build fort` on the Military fort posture row and it is enabled.
- Given the selected tile is not the province town tile or `fortLevel` is already 3 (or the next step is tech-blocked), when the overlay renders, then the UI layer does not show `Build fort`.
- Given a fort step is still conceivable on the town tile but no Engineer can legally assign `build_fort` this turn, when the overlay renders, then the UI layer keeps `Build fort` visible but disabled with a plain-language primary refusal in tooltip/semantics (not raw `build_fort`).
- Given the selected context is sea-zone or tile details are obfuscated (`???`), when the overlay renders, then the UI layer does not show `Build fort`.
- Given user taps enabled `Build fort` and click-time state remains valid, when the Civilian Units panel opens, then it opens in Engineer-only shortcut mode targeting the exact selected town tile key for direct `WorkOrder(target: build_fort, targetTileKey: <exact selected tile key>)`.
- Given `Build fort` is enabled for the town tile and `previewWorkOrderAffordAtTile` reports a non-empty material cost after other pending work, when the UI layer renders the inline action tooltip, then the tooltip includes required commodity quantities via `provinceOverlay_tileBuildFortTooltipWithCost` without opening Production (`GAME20001`).
- Given `Build fort` is visible and disabled solely because projected stockpile or treasury cannot cover the next fort-step cost after earlier pending work (and at least one Engineer unit exists), when the UI layer renders the inline action tooltip, then the tooltip primary reason is a plain shortfall line from `workOrderAffordStatusLine` wrapped by `provinceOverlay_tileBuildFortDisabledMaterialsTooltip`.
- Given Military intel allows full body and `fortLevel` is known, when `MAP20001` opens, then the Military section shows plain Open field / Wood / Stone / Modern fort copy via `moveArmy_fort*` labels (no raw `fortLevel` integer).
- Given a human-owned province contains exactly one non-Home field army, when the player taps **Move** on `MAP20001` Military, then the UI layer opens `DLG20001` for that army without opening `UNIT20001`.
- Given a foreign province `P` is in the army-move picker cache for exactly one field army, when the player taps **Invade**, then `DLG20001` opens with `initialDestinationProvinceId` = `P`.
- Given two or more field armies can legally invade `P`, when the player taps **Invade**, then `DLG20002` lists those armies and only the chosen army’s dialog opens.
- Given the only army in an owned province is the Home Army, when Military renders, then Move is absent or disabled with plain capital / split-field copy.
- Given Invade is visible under the cheap conceivable predicate but no army has `P` in the cache snapshot, when Military renders, then Invade is disabled with a cannot-reach reason and tapping stages no order.
- Given observe / `canMutateViaUi == false`, when Military renders, then Move and Invade controls are not shown (or taps stage no draft).
- Given overlay rebuilds while the map is panned, when visibility and enablement are computed, then visibility uses only the cheap predicate and enablement reads `PerPlayerArmyMovePickerCache` without live `armyMovePickerDestinations`.
- Given `MAP20001` Military Move/Invade enabled, disabled, and hidden variants under `AppThemes.editorialMonocle`, when the host golden suite in `app/test/province_overlay_move_invade_goldens_test.dart` captures each keyed `RepaintBoundary`, then each `matchesGoldenFile` baseline under `app/test/goldens/province_overlay_move_*.png` / `province_overlay_invade_*.png` matches the committed PNG (Refs #4350).
- Given `MAP20001` Political Establish Consulate enabled, disabled, pending, hidden, and 320 dp layouts under `AppThemes.editorialMonocle`, when the System runs the host golden suite in `app/test/province_establish_consulate_shortcut_host_goldens_test.dart` and captures each keyed `RepaintBoundary`, then the UI layer's `matchesGoldenFile` baselines under `app/test/goldens/province_establish_consulate_*.png` match the committed PNGs (Refs #4346).

### Localization of improvement names and unclaimed owner

- Given the Tile or Economic section renders an improved tile whose discovered resource is `grain`, when the improvement label builds, then the UI layer resolves the improvement-type name from `AppLocalizations.provinceOverlay_improvementFarm` (`Farm`) and renders no hardcoded `Farm` string literal.
- Given the Tile or Economic section renders an improved tile whose discovered resource is a mineral requiring prospection (e.g. `iron`, `gold`), when the improvement label builds, then the UI layer resolves the improvement-type name from `AppLocalizations.provinceOverlay_improvementMine` (`Mine`).
- Given the Tile section renders an improved tile with no discovered resource, when the improvement label builds, then the UI layer resolves the improvement-type name from `AppLocalizations.provinceOverlay_improvementGeneric` (`Improvement`).
- Given the Political section renders a province whose `Province.ownerId` is null or empty, when the Owner row builds, then the UI layer resolves the owner display name from `AppLocalizations.provinceOverlay_ownerUnclaimed` (`Unclaimed`) and renders no hardcoded `Unclaimed` string literal.
- Given `MAP20001` Political is visible for a selected land or sea tile with a resolvable `CellViewData`, when the section renders, then the UI layer includes a Sight row using the same three phrases as the MAP10001 hover readout for that tile’s `CellViewData.visibility` (no raw `TileVisibility` enum text) (Refs #4406).
- Given a 320 dp-wide viewport, when the Political Sight row renders, then the UI layer does not overflow horizontally.
- Given Widgetbook / host goldens for Political Sight variants, when the suite runs, then committed PNGs match (`app/test/province_overlay_political_sight_goldens_test.dart`).
- Given the Tile section renders a revealed land tile whose terrain is prospectable and already prospected by the human player, when the `Prospected` row builds, then the UI layer resolves the row value from `AppLocalizations.provinceOverlay_tileProspectedYes` (`yes`) and renders no hardcoded `yes` string literal.
- Given the Tile section renders a revealed land tile whose terrain is prospectable but not yet prospected by the human player, when the `Prospected` row builds, then the UI layer resolves the row value from `AppLocalizations.provinceOverlay_tileProspectedNo` (`no`) and renders no hardcoded `no` string literal.
- Given the Tile section renders a revealed tile whose terrain is not prospectable, when the `Prospected` row builds, then the UI layer renders the literal em-dash `—` symbol and consults neither `provinceOverlay_tileProspectedYes` nor `provinceOverlay_tileProspectedNo`.

### Tile capital-link and per-tile extraction (Refs #4149 / #4369)

- Given the human player owns a revealed selected land tile that is not capital-connected and F ≥ 1, when the Tile section renders on the default surface, then the UI layer shows `provinceOverlay_tileCapitalLinkNotConnected` and does **not** show `provinceOverlay_tileExtractionFromTile`.
- Given the human player owns a revealed selected land tile that is capital-connected (any E/F), when the Tile section renders on the default surface, then the UI layer omits capital-link and `E of F` rows.
- Given Tile details opens for a capital-connected tile with E > 0 and F > 0, when the helper renders, then the UI layer shows capital-link Connected (or path-cap variant) and `provinceOverlay_tileExtractionFromTile(E, F)`.
- Given Tile details opens for a not-connected tile with F ≥ 1 (E = 0), when the helper renders, then the UI layer may show capital-link Not connected and does **not** show `Extraction from this tile: 0 of F`.
- Given sea-zone overlay context or obfuscated Tile (`???`), when the overlay renders, then the UI layer omits capital-link, extraction, and Tile-details teaching rows.
- Given an owned land tile with F = 0, when the Tile section renders on the default surface, then the UI layer omits the stranded exception line; Tile details may still list capital-link without an extraction row.
- Given overlay rebuild on the same selection, when the Tile section re-renders, then capital-link and E-of-F values are read from host-passed `tileConnectivity` preview data (no connectivity recompute inside the overlay widget build).

### Tile details disclosure (Refs #4369)

- Given an owned revealed inland tile that is capital-connected with F = 0, when Tile renders, then the default surface does **not** show `Port: None`/`Present`, road GDD caption, railroad gloss, `Capital link: Connected`, or `Extraction from this tile`.
- Given the same tile, when the player taps the transport/connectivity text cluster or the named **Tile details** control, then the helper lists applicable teaching lines (port status, captions, connected capital-link; `E of F` only when F > 0 and E > 0).
- Given Build road / Build port / Build railroad / Explore / Prospect / Build improvement / Purchase land gates, when Tile renders, then those shortcuts remain on the default surface and opening Tile details does not require tapping those icons.

### Build railroad shortcut (Refs #4383)

- Given a revealed human-owned land tile with transport level 1 or 2, when `MAP20001` Tile renders, then the Road / railroad row shows an inline **Build railroad** control.
- Given transport level 0, 4, sea, fog/`???`, or foreign ownership, when Tile renders, then **Build railroad** is hidden.
- Given cache-first `build_rail` membership and an idle assignable Rail Builder, when the player taps enabled **Build railroad**, then the UI opens `UNIT10001` in Rail-Builder-only shortcut mode and stages `WorkOrder(target: build_rail)` on the exact selected tile key.
- Given no Rail Builder, missing terrain rail tech, or lumber/steel shortfall after other pending work, when the control is visible, then it is disabled with a plain-language tooltip and does not emit a panel-open event.
- Given the shortcut is enabled, when the tooltip is shown, then it includes the lumber + steel cost summary from `previewWorkOrderAffordAtTile`.
- Given a 320–360 dp viewport, when Tile details opens, then the helper is dismissible, does not overflow the overlay slot, and does not add a new tab.
- Given `MAP20001` Tile details helper open (caption + port + capital-link + `E of F`) and 320 dp overlay-open layouts under `AppThemes.editorialMonocle`, when the host golden suite in `app/test/province_overlay_tile_details_goldens_test.dart` captures each keyed surface, then each `matchesGoldenFile` baseline under `app/test/goldens/province_overlay_tile_details_dialog*.png` matches the committed PNG (Refs #4369).

---

## Integration

- **Map widget:** [map-widget.md](map-widget.md) — `onMapTileTappedForDetail`, `selectedTileKey`, `secondaryHighlightTileKey`.
- **Provider:** `mapProvincePanelProvider` in app; see TDD for app state if split.
- **PlayerView:** `GameMapArea` builds with `buildPlayerView` + combined topology and passes through `GameMapCanvasStack` → `GameMapProvinceDetailSidePanel` / `GameMapNarrowDetailOverlaySlot` into `ProvinceSeaZoneDetailOverlay`.
- **Ships in port:** colonizethis_logic helpers as before.
- **Other `seaZoneDisplayName` call sites (audit):** Naval/military panels and fleet dialogs label zones for **own-fleet / own-port** flows and topology-adjacent move targets; they do not receive `RegionMapViewData` today. Map **detail overlay** was the surface leaking preset names without any revealed water in the zone. If [fog-and-exploration.md](../game/fog-and-exploration.md) later requires name obfuscation for adjacent-move or unit-panel labels, extend those widgets with the same visibility predicate and SPEC updates.
