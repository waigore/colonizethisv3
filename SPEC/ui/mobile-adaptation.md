# Mobile adaptation

**SPEC/ui** — Requirements for Flutter screens on small and narrow viewports. Applies to all screen-level widgets in the catalog (e.g. CtMainMenu, CtGameSetup). Authority: UXD 03; derives from GDD/TDD.

---

## Design requirement

When designing screens for the app, **mobile must be considered from the start.** Layouts, wireframes, and component choices should account for narrow viewports (~320 dp and up) and short viewports. It may be **necessary to create a mobile-only mockup in Widgetbook** (e.g. a use case that only makes sense or is primarily intended for the mobile viewport) in addition to, or instead of, a single responsive story. When in doubt, add a dedicated mobile viewport use case so that mobile layout and behavior can be reviewed in isolation.

---

## Scope

- **Target:** Phones and narrow/short viewports (min ~320 dp width; height may be short).
- **Screens:** Every screen designed in Widgetbook must behave correctly on mobile: no overflow, usable touch targets, readable layout. This spec defines the rules; individual screen specs (main-menu, game-setup, etc.) implement them.

---

## Rules

### 1. Touch targets

- Interactive elements (buttons, list tiles, dropdowns) must have **at least 44 dp** in the smallest dimension (already required in UXD 03). Buttons in Main Menu and Game Setup use 48 dp height; keep that or larger.

### 2. Content width and centering

- Content columns use a **max width of 400 dp** so that on tablets/desktop the layout doesn’t stretch. On narrow viewports (&lt; 400 dp) the content uses full width (minus padding). Content is **centered** (e.g. `Center` + `ConstrainedBox(maxWidth: 400)`).

### 3. Scroll on overflow

- **Full-screen screens** (Main Menu, Game Setup, etc.) must **scroll** when content overflows the viewport. Use `SingleChildScrollView` (or equivalent) so that on short viewports the user can scroll to reach all actions. No unreachable content.

### 4. Narrow viewport layout (breakpoints)

This rule covers four normalised Flutter dp breakpoints, each matching a per-screen mockup `@media` rule:

- **`< 600 dp`** — in-game shell narrow adaptations.
- **`< 500 dp`** — Game Setup stacked rows.
- **`≤ 500 dp`** — Diplomacy faction-row stacked layout (action buttons wrap below info, left-aligned). Source: [diplomacy-panel.md](diplomacy-panel.md) § Responsive layout; `SPEC/ui/mockups/GAME30001-diplomacy-panel.html` `@media (max-width: 500px)`.
- **`≤ 430 dp`** — Main Menu extra-tight padding and letter-spacing.

#### Game Setup (`< 500 dp`)

- Each player-slot row uses a **stacked layout**: slot label on one line, nation dropdown full width on the next, leader dropdown full width below. Above the breakpoint, one row: label | nation | leader. Source: [game-setup.md](game-setup.md) and `SPEC/ui/mockups/SHEL20001-game-setup.html` `@media (max-width:499px)`.
- The same stacked rule applies to **`NewGameLeaderSelectionDialog`** (DLG10001) — the shell-mounted dialog version of Game Setup that runs in the production app — so both Game Setup surfaces honour the 500 dp breakpoint with the same slot-row body switching. Source: [new-game-leader-selection-dialog.md](new-game-leader-selection-dialog.md) § Layout / wireframe and § Acceptance Criteria.

#### Main Menu (`≤ 430 dp`)

- Below `430 dp` viewport width the menu container compacts and button labels reduce letter-spacing per `SPEC/ui/mockups/SHEL10002-main-menu.html` `@media (max-width: 430px)`:
  - Menu container padding compacts to `24 px 12 px` (mockup `.menu-container` override).
  - Wood-panel `CtNinePatchButton` labels reduce `letter-spacing` from `0.08em` to `0.04em` (mockup `.menu-btn` override).
- Above `430 dp` the vertical list of buttons keeps its default padding and letter-spacing; only scroll is required (see §3).
- Source: [main-menu.md](main-menu.md) § Responsive rules; `SPEC/ui/mockups/SHEL10002-main-menu.html`.

#### In-game shell (`< 600 dp`)

- Below `600 dp` the in-game shell adopts the narrow chrome defined in [in-game-shell-narrow.md](in-game-shell-narrow.md) and [empire-buttons.md](empire-buttons.md): the top bar carries only the hamburger and the turn counter, the **Debug log** lives in the hamburger side menu, and empire actions stay on the map left rail (rendered at the narrow measurements below).
- The following measurements are normative for the narrow chrome. They mirror `SPEC/ui/mockups/GAME10001-game-screen.html` `@media (max-width:600px)` and the layout sections of [in-game-shell-narrow.md](in-game-shell-narrow.md):

| Element             | Narrow (`< 600 dp`)                                            | Default (`≥ 600 dp`)                                | Source                                                                                                                  |
|---------------------|----------------------------------------------------------------|-----------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------|
| Minimap panel       | 90 × 70 dp                                                     | `clamp(100, 15vw, 160) × clamp(80, 12vw, 120)` dp   | `GAME10001-game-screen.html` `.minimap-panel @media`; [empire-overview.md](empire-overview.md) § Region minimap          |
| Left-rail buttons   | 26 × 26 dp                                                     | 36 × 36 dp                                          | `GAME10001-game-screen.html` `.empire-btn @media`; [empire-buttons.md](empire-buttons.md) § Display                       |
| Corner controls     | 24 × 24 dp                                                     | 32 × 32 dp                                          | `GAME10001-game-screen.html` `.corner-btn @media`; [empire-overview.md](empire-overview.md) § Corner controls            |
| Players bar         | Hidden (not present in widget tree)                            | Visible (per-player chip strip)                     | `GAME10001-game-screen.html` `.players-bar @media`                                                                       |
| Province / sea detail | Full-width bottom sheet, height ~33 vh, accent-dim top border | Right side panel, width 320 dp                       | `GAME10001-game-screen.html` `.province-panel @media`; [in-game-shell-narrow.md](in-game-shell-narrow.md) § Province/sea zone detail overlay |
| Victory overlay (OVL20001) | Laurel `24` px, title `titleMedium`, body `bodyMedium`, action buttons stacked in a vertical `Column` (full-width) | Laurel `28` px, title `headlineSmall`, body `bodyLarge`, actions in a `Wrap` row (12 dp spacing) | [victory-overlay.md](victory-overlay.md) § Narrow viewport; `OVL20001-game-victory-overlay.html` `.victory-actions { flex-wrap:wrap }` + `clamp()` lower bounds |

- The wide-layout widgets themselves (left rail, corner controls, minimap, players bar, province panel, victory overlay) are introduced by their per-screen alignment issues; this section codifies the **narrow measurements** so the cross-cutting adaptation work has a single authoritative source.

### 5. Safe area

- Use `SafeArea` (or equivalent) so that content and buttons are not obscured by system UI (notches, status bar, navigation bar).

### 6. Widgetbook verification

- For each screen in Widgetbook, provide at least one **mobile viewport** use case: render the widget inside a constrained viewport (e.g. 360×640 dp) so that mobile layout and scroll can be verified without resizing the window. Document in the widget catalog or story name (e.g. "Default (mobile)").
- **Mobile-only mockups:** When a screen's mobile layout differs meaningfully from desktop (e.g. stacked vs row, different navigation), consider adding a **mobile-only** Widgetbook use case that is designed and named for the mobile viewport (e.g. "Mobile layout" or "Narrow — stacked slots"). This makes mobile the primary frame for that story and ensures the mobile design is reviewed explicitly.

### 7. Minimum-viewport pin (`kMinViewportWidth = 320`)

The minimum supported viewport width is **`kMinViewportWidth = 320` dp** (from `app/lib/config/constants.dart`). The minimum touch-target size is **`kMinTouchTargetSize = 44` dp** (matching §1 and UXD 03). Both constants are normative for screen tests that pin the AC below.

#### Acceptance criteria

- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **CtMainMenu** (`plain` and `pixelArt` variants, all `MainMenuState` values) is rendered with the running theme, then `WidgetTester.takeException()` returns `null`, no `RenderFlex` overflow exception is thrown by the framework, and every visible `CtNinePatchButton` reports a rendered height ≥ `kMinTouchTargetSize`.
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **CtGameSetup** (any `GameSetupVariant` × `GameSetupState`) is rendered with the running theme, then `WidgetTester.takeException()` returns `null`, no `RenderFlex` overflow exception is thrown, and the six player-slot rows render in the stacked layout (label / nation dropdown / leader dropdown) defined by §4 Game Setup.
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **NewGameLeaderSelectionDialog** (DLG10001) is rendered with `baseConfig = GameSetupConfig.defaultConfig`, `naming = defaultNamingConfig`, and the default per-GP leader-variant map, then `WidgetTester.takeException()` returns `null`, every one of the six slot rows mounts the stacked column body keyed `ValueKey<String>('newGameLeaderDialogSlotPickersColumn')` (so `find.byKey(...).evaluate().length == 6`) and no slot mounts the wide side-by-side row body keyed `ValueKey<String>('newGameLeaderDialogSlotPickersRow')` (per [new-game-leader-selection-dialog.md](new-game-leader-selection-dialog.md) § Narrow-viewport slot pickers stacking and §4 Game Setup, because `320 < kGameSetupNarrowBreakpoint = 500`). The dialog title `New game — Setup`, the slot labels `Player 1 (You)` through `Player 6 (AI)`, and the trailing `Cancel` and `Start` `CtNinePatchButton` labels all render within the ~288 dp `CtDialogShell` content column (`maxWidth: 480` + outer `Dialog.insetPadding: 16` × 2 dominates whenever the viewport is narrower than `maxWidth`), and every rendered `CtNinePatchButton` reports a rendered height `>= kMinTouchTargetSize` (44 dp) per §1.
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **ProductionPanel** is rendered with the full or partial production-panel test fixtures, then `WidgetTester.takeException()` returns `null` and both `Available` and `Allocation` labels render (the narrow `_ProductionPanelNarrowLayout` path selected at `MediaQuery.sizeOf(context).width < kNarrowBreakpoint`, per [production-panel.md](production-panel.md) § Layout).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **ProductionScreen** (`UiScreenIds.productionScreen`) is rendered against the `getDebugInitGameResult()` fixture under the default (non-observe) `shellPlayerContextProvider` path with `panelTopologyOverride: const MapTopology()`, then `WidgetTester.takeException()` returns `null`, exactly one dark `CtTopBar` keyed `ProductionScreen.topBarKey` is mounted carrying the literal title `Production` and back label `Map` with a descendant `CtBackButton` chevron, and both `Available` and `Allocation` section labels from the narrow `_ProductionPanelNarrowLayout` render inside the `CtGameFeatureScreenShell` body (the dark `CtTopBar` chrome — back chevron + `Map` label + 18 × 18 px production icon + `Production` title — plus the `ProductionPanel` body must lay out within the 320 dp column without horizontal overflow per [production-panel.md](production-panel.md) § Top bar and § Layout).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **ProductionScreen** (`UiScreenIds.productionScreen`) is rendered against the same fixture under a `shellPlayerContextProvider` override whose `showPlayerChrome` is `false` (global observe), then `WidgetTester.takeException()` returns `null`, the dark `CtTopBar` keyed `ProductionScreen.topBarKey` still renders, and the `ObserveModeNotDefinedPanel` sentinel with literal title `Production` renders in place of the `ProductionPanel` body (per [production-panel.md](production-panel.md) § States and variants the observe override only swaps the body — the chrome must keep its 320 dp overflow contract).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DiplomacyPanel** is rendered with the debug-init-game fixture, then `WidgetTester.takeException()` returns `null`, the first discovered faction row's body widget is a `Column` (narrow variant at `≤ kDiplomacyRowNarrowMaxWidth`), and the bottom mode-bar filter chips wrap onto a second run instead of overflowing horizontally (per [diplomacy-panel.md](diplomacy-panel.md) § Mode bar).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TechnologyPanel** is rendered inside the same `SingleChildScrollView` + 16 dp padding host as `TechnologyScreen`’s Slots tab (`_SlotsBody`), with the debug-init-game fixture, then `WidgetTester.takeException()` returns `null`, three `ResearchSlotCard` widgets and one `LockedResearchSlotCard` (slot 4) render, and the `RESEARCHED TECHS` section heading is visible (slot cards and researched grid scroll vertically within the host per [technology-panel.md](technology-panel.md) § Body).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **CivilianUnitsPanel** is rendered with the debug-init-game fixture and the first seeded player as the human, then `WidgetTester.takeException()` returns `null` and the `Civilian Units` title from [civilian-units-panel.md](civilian-units-panel.md) renders (the shared `UnitsPanelShell` chrome — `max-width: 400` `ConstrainedBox` + 8 dp padding + `ListView` body + per-unit `UnitsEntityActionRow` with `iconOnlyBreakpoint = 280` — must fit within the 304 dp content column without overflowing).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **MilitaryUnitsPanel** is rendered with the debug-init-game fixture (`topology = const MapTopology()`, `draftOrders = const Orders()`) and the first seeded player as the human, then `WidgetTester.takeException()` returns `null` and the `Military Units` title from [military-units-panel.md](military-units-panel.md) renders (the shared `UnitsPanelShell` chrome plus Army `ExpansionTile` rows and per-army `UnitsEntityActionRow` must fit within the 304 dp content column without overflowing).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **NavalUnitsPanel** is rendered with the debug-init-game fixture (`topology = result.combinedTopology` from `getDebugInitGameResult()`), the first seeded player as the human, and the brass nine-patch button asset (`assets/images/ui_button_nine_patch.png`) pre-warmed into the Flame image cache, then `WidgetTester.takeException()` returns `null` and the `Naval Units` title from [naval-units-panel.md](naval-units-panel.md) renders (the shared `UnitsPanelShell` chrome plus header Combine + select-all checkbox, Fleet `ExpansionTile` rows, and per-fleet `UnitsEntityActionRow` must fit within the 304 dp content column without overflowing, with `_panelConstraints` resolving to `UnitsPanelShell.defaultPanelConstraints` because `MediaQuery.sizeOf(context).width < 1280` per the naval panel `_desktopViewportThreshold`).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **ProvinceSeaZoneDetailOverlay** is rendered against the `province_overlay_demo_data` fixture (`displayId = sampleProvinceIdForOverlay`, `selectedTileKey = sampleTileKeyForProvinceOverlay`) inside a bottom-anchored `SizedBox(height: viewport.height * 0.33)` host that mirrors `GameMapNarrowDetailOverlaySlot`, then `WidgetTester.takeException()` returns `null`, the `Province` header label renders, and the narrow `CtTabStrip` body exposes the `Tile` tab label (the six-tab Tile / Political / Economic / Military / Civilian / Naval narrow layout per [province-sea-zone-detail-overlay.md](province-sea-zone-detail-overlay.md) § Tabs and [in-game-shell-narrow.md](in-game-shell-narrow.md) § Province/sea zone detail overlay must fit within the 320 dp column without horizontal overflow).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TurnNewsDialog** is rendered against a two-GP `Game` fixture (`gp1 = England`, `gp2 = France`) and an empty `TurnNewsDigest(resolvedTurnNumber: 1, lines: [])` with `newTurnNumber = 2`, then `WidgetTester.takeException()` returns `null`, the localized `Turn 2` title renders, the empty-state copy `No major events last turn.` renders, and the trailing `Close` `CtNinePatchButton` renders (the [CtDialogShell](pixel-art-ui-catalog.md) chrome — `Dialog` with `insetPadding: 16` plus the inner `ConstrainedBox(maxWidth: 480)` — must wrap the title, muted empty-state line, and right-aligned `Close` action within the ~288 dp content width without horizontal overflow per [turn-news-dialog.md](turn-news-dialog.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TurnNewsDialog** is rendered with a populated `TurnNewsDigest` containing three `TurnNewsLine` entries (one `TurnNewsDiplomacyLine.war`, one `TurnNewsDiplomacyLine.peace`, one `TurnNewsOvertureAdvancedLine` advancing to `OvertureStage.nap`), then `WidgetTester.takeException()` returns `null`, each digest line text wraps within the dialog body without horizontal overflow (the `ConstrainedBox(maxHeight: 320)` shrink-wrapped `ListView` rows render with `bodyMedium`-coloured text), and the trailing `Close` `CtNinePatchButton` remains right-aligned (per [turn-news-dialog.md](turn-news-dialog.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **GameMapOptionsDialog** is rendered with an `initialState = MapViewState(showProvinceOverlay: true, showProvinceOwnershipTint: true, showProvinceNamesLayer: false)` and a no-op `onChanged`, then `WidgetTester.takeException()` returns `null`, the localized `Map display options` title renders, all three `CtToggleSwitch` rows (overlay / ownership / names) render with `Expanded` label + 12 dp gap + switch within the ~288 dp content width without horizontal overflow, and the trailing `Close` `CtNinePatchButton` is right-aligned (the [CtDialogShell](pixel-art-ui-catalog.md) chrome wraps the three rows + Close action per [empire-overview.md](empire-overview.md) § Map display options).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TurnResolutionProcessingDialog** is rendered with `phaseText = 'Resolving turn 3...'`, then `WidgetTester.takeException()` returns `null`, the localized processing title renders, the `CtLoadingIndicator` row body wraps the `phaseText` `Expanded` widget within the ~288 dp content width without horizontal overflow, and the dialog refuses to pop via the system back button (the `PopScope(canPop: false)` chrome must keep the modal mounted while the worker isolate runs per [turn-resolution.md](../program/turn-resolution.md)).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **CombatModeChoiceDialog** is rendered with `provinceName = 'Lisbon'` and `isCapitalSiege = false`, then `WidgetTester.takeException()` returns `null`, the localized title containing `Lisbon` renders, and both `Auto-Resolve` and `Quick Battle` `CtNinePatchButton` labels render within the ~288 dp content width without horizontal overflow (the end-aligned action row per [combat-mode-choice-dialog.md](combat-mode-choice-dialog.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **CombatModeChoiceDialog** is rendered with `provinceName = 'Madrid'` and `isCapitalSiege = true`, then `WidgetTester.takeException()` returns `null`, the localized title containing `Madrid` renders, the `Auto-Resolve` label is absent, and the `Quick Battle` `CtNinePatchButton` label renders within the ~288 dp content width without horizontal overflow (capital-siege forced Quick Battle path per [combat-mode-choice-dialog.md](combat-mode-choice-dialog.md)).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **NextTurnConfirmationDialog** is rendered with `currentTurn = 7`, then `WidgetTester.takeException()` returns `null`, the localized `End turn?` title renders, the body line containing `Turn 7 will end` renders, and both `No` and `Yes` `CtNinePatchButton` labels render within the ~288 dp content width without horizontal overflow (the end-aligned action row per [next-turn-confirmation.md](next-turn-confirmation.md) § UI/UX).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **QuickBattleResultDialog** is rendered with `result.winner = QuickBattleWinner.attacker`, `provinceFlips = true`, `attackerName = 'Castile'`, and `defenderName = 'England'`, then `WidgetTester.takeException()` returns `null`, winner text containing both faction names renders, the province-captured banner renders, both casualty rows render, and the trailing `OK` `CtNinePatchButton` is right-aligned within the ~288 dp content width without horizontal overflow (per [quick-battle-result-dialog.md](quick-battle-result-dialog.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **SplitArmyDialog** is rendered with a non-home Army containing one `peasant_levy` regiment and `isHomeArmy: false`, then `WidgetTester.takeException()` returns `null`, the `Split Army` title renders, the `CtTransferList` left-column title `Army <id>` and right-column title `New Army` both render, and the `Confirm Split` action label renders (the [CtTransferList](military-units-army-management.md) narrow stack at `constraints.maxWidth < kCtTransferListSideBySideMinWidth` must lay out the two side panels vertically and the Cancel / Confirm `Wrap` action row within the ~288 dp `CtDialogShell` content column without horizontal overflow, per [military-units-army-management.md](military-units-army-management.md) § Split Army Dialog).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **SplitFleetDialog** is rendered with a non-home Fleet containing two ship instances and `isHomeFleet: false`, then `WidgetTester.takeException()` returns `null`, the `Split Fleet` title renders, the `CtTransferList` right-column title `New Fleet` renders, and the `Confirm Split` action label renders (the [CtTransferList](naval-units-fleet-management.md) narrow stack must lay out source + new fleet panels vertically with the Cancel / Confirm `Wrap` action row within the ~288 dp `CtDialogShell` content column without horizontal overflow, per [naval-units-fleet-management.md](naval-units-fleet-management.md) § Split Fleet dialog).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TransferToHomeFleetDialog** is rendered with a source `Fleet` (1 carrack) and a same-region `Home Fleet`, then `WidgetTester.takeException()` returns `null`, the localized `Transfer Ships to Home Fleet` title renders, the `CtTransferList` right-column title `Home Fleet` renders, and the localized `Transfer` action label renders (the [CtTransferList](transfer-to-home-fleet-dialog.md) narrow stack must lay out source + home fleet panels vertically with the Cancel / Transfer `Wrap` action row within the ~288 dp `CtDialogShell` content column without horizontal overflow, per [transfer-to-home-fleet-dialog.md](transfer-to-home-fleet-dialog.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **MoveArmyDialog** is rendered with a non-Home army (`isHomeArmy: false`) and a topology offering exactly one player-owned and one rival-owned adjacent province (so both `YOUR PROVINCES` and `INVASION TARGETS` `CtSectionLabel`-headed groups render), then `WidgetTester.takeException()` returns `null`, the `Move army — Army <id>` title renders, both section labels render, and both `Cancel` and `Confirm` `CtNinePatchButton` labels render (the [CtDialogShell](pixel-art-ui-catalog.md) chrome wraps the title row, the two destination groups, and the trailing `Wrap`-based action row within the ~288 dp content column without horizontal overflow per [move-army-dialog.md](move-army-dialog.md) § Layout / wireframe — at narrow widths the per-invasion-row `declare war on <faction>` trigger label stacks below the destination name inside the same outlined row container and the bottom Cancel/Confirm action `Wrap` runs onto a second line when the two buttons cannot fit side-by-side).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **MoveFleetDialog** is rendered with a non-Home fleet (one `carrack` ship in sea zone `sea_origin`) and a topology offering exactly one adjacent sea-zone destination (so the `SEA ZONES` `CtSectionLabel`-headed group renders), then `WidgetTester.takeException()` returns `null`, the `Move fleet — Fleet <id>` title renders, the `SEA ZONES` section label renders, and both `Cancel` and `Confirm` `CtNinePatchButton` labels render (the [CtDialogShell](pixel-art-ui-catalog.md) chrome wraps the title row, the destination group with its trailing locate icon, and the trailing `Wrap`-based action row within the ~288 dp content column without horizontal overflow per [move-fleet-dialog.md](move-fleet-dialog.md) § Layout / wireframe — the bottom Cancel/Confirm action `Wrap` runs onto a second line when the two buttons cannot fit side-by-side).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **GrantOrSubsidyDialog** (DIPL20001) is rendered in grant mode (`isSubsidy: false`) against a two-GP `Game` fixture with the human GP's `treasury == 5 * grantAidAmountStep` (so both stepper buttons are enabled and the optional below-minimum hint stays unmounted), then `WidgetTester.takeException()` returns `null`, the localized `Grant aid` title renders (keyed `grantOrSubsidyDialogTitle`), the localized `Treasury: …  Step: …` body row renders (keyed `grantOrSubsidyDialogTreasury`), the 1 dp `_ThinDivider` (keyed `grantOrSubsidyDialogThinDivider`) renders, the keyed amount label `grantOrSubsidyDialogAmount` and both stepper buttons keyed `diplo_amount_minus` and `diplo_amount_plus` mount inside the centered stepper `Row`, and the trailing right-aligned `Cancel` + `Submit` `CtNinePatchButton` labels render (the [CtDialogShell](pixel-art-ui-catalog.md) chrome — `maxWidth: 480` clamped by outer `Dialog.insetPadding: 16` × 2 — must wrap the title, treasury / step body, thin divider, bespoke `−` / `+` stepper, and trailing action row within the ~288 dp content column without horizontal overflow per [grant-or-subsidy-dialog.md](grant-or-subsidy-dialog.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **GrantOrSubsidyDialog** (DIPL20001) is rendered in subsidy mode (`isSubsidy: true`) against the same fixture, then `WidgetTester.takeException()` returns `null`, the localized `Set subsidy` title renders (the title slot flips to `diplomacy_setSubsidy`), the localized treasury body row renders, the keyed below-minimum warning `grantOrSubsidyDialogWarning` is absent (treasury comfortably exceeds the smaller `setSubsidyAmountStep == 100`), and both `Cancel` and `Submit` `CtNinePatchButton` labels render within the ~288 dp content column without horizontal overflow per [grant-or-subsidy-dialog.md](grant-or-subsidy-dialog.md) § States and variants.
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **GrantOrSubsidyDialog** (DIPL20001) is rendered in grant mode (`isSubsidy: false`) against a two-GP `Game` fixture with the human GP's `treasury == grantAidAmountStep - 1` (so `_maxAffordable() < step` and `canAdjust == false`), then `WidgetTester.takeException()` returns `null`, the keyed below-minimum warning `grantOrSubsidyDialogWarning` mounts (the optional `_BelowMinimumWarning` row from [grant-or-subsidy-dialog.md](grant-or-subsidy-dialog.md) § States and variants), the trailing right-aligned `Cancel` + `Submit` `CtNinePatchButton` labels still render, and the optional warning + 16 dp gap + action row continues to fit within the ~288 dp content column without horizontal overflow.
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TrainCiviliansDialog** is rendered against the `getDebugInitGameResult()` fixture with the seeded human player id and an empty `Orders()`, then `WidgetTester.takeException()` returns `null`, the localized `Train Civilians` title renders, and the trailing right-aligned `Reset` `CtNinePatchButton` label renders (the [CtDialogShell](pixel-art-ui-catalog.md) chrome wraps the `TrainDialogHeader` accent title + 32 dp close `×`, the `Wrap`-based `TrainDialogResourceBar` (`Treasury` + `Paper` lines), the per-civilian-unit rows with name + cost header + `+`/`−` stepper, and the trailing right-aligned `Reset` action within the ~288 dp `CtDialogShell` content column per [train-civilians-dialog.md](train-civilians-dialog.md) § Layout).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TrainMilitaryDialog** is rendered against the `getDebugInitGameResult()` fixture with the seeded human player id and an empty `Orders()`, then `WidgetTester.takeException()` returns `null`, the localized `Train Military` title renders, and the trailing right-aligned `Reset` `CtNinePatchButton` label renders (the [CtDialogShell](pixel-art-ui-catalog.md) chrome wraps the `TrainDialogHeader`, the `Wrap`-based military resource bar (`Treasury` + `Peasants` + six commodity chips: `fabric`, `castIron`, `lumber`, `horses`, `steel`, `bronze`), the per-regiment rows with name + cost header + `+`/`−` stepper, and the trailing right-aligned `Reset` action within the ~288 dp `CtDialogShell` content column per [train-military-dialog.md](train-military-dialog.md) § Layout — the military resource `Wrap` must flow onto extra runs without overflowing horizontally).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TradeScreen** is rendered against the `getDebugInitGameResult()` fixture under the default (non-observe) `shellPlayerContextProvider` path, then `WidgetTester.takeException()` returns `null`, the dark `CtTopBar` keyed `TradeScreen.topBarKey` renders with literal title `Trade` and back label `Map`, and the scaffold placeholder body keyed `TradeScreen.placeholderBodyKey` renders within the 320 dp column without horizontal overflow (the 36 dp `CtTopBar` chrome — back chevron + `Map` label + 18 × 18 px trade icon + `Trade` title — plus the `CtPanel` placeholder body must lay out within the 320 dp column per [trade-screen.md](trade-screen.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **TradeScreen** is rendered against the `getDebugInitGameResult()` fixture under a `shellPlayerContextProvider` override whose `showPlayerChrome` is `false` (global observe), then `WidgetTester.takeException()` returns `null`, the dark `CtTopBar` keyed `TradeScreen.topBarKey` still renders, the `ObserveModeNotDefinedPanel` sentinel with literal title `Trade` renders, and the scaffold placeholder body keyed `TradeScreen.placeholderBodyKey` is absent (per [trade-screen.md](trade-screen.md) § States and variants the observe override only swaps the body — the chrome must keep its 320 dp overflow contract).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **CallToArmsDialogueOverlay** (OVL40001) is rendered with a three-GP `Game` fixture (`gp_player` / `gp_portugal` / `gp_spain`) and one or two `CallToArmsPending` entries targeting `gp_player`, then `WidgetTester.takeException()` returns `null`, the localized `Call to arms` title renders, every pending row mounts both `Join` and `Refuse` `CtNinePatchButton` labels (the end-aligned per-row `Wrap` flows onto a second run rather than overflowing horizontally), and the trailing `Submit` `CtNinePatchButton` renders (the [CtFullScreenDialogueShell](pixel-art-ui-catalog.md) scrim + centered [CtDialogShell](pixel-art-ui-catalog.md) chrome must wrap the title, intro line, single `CtBrassDivider`, per-call rows, and submit action within the ~288 dp content column without horizontal overflow per [call-to-arms-dialogue-overlay.md](call-to-arms-dialogue-overlay.md) § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **VictoryOverlay** (OVL20001) is rendered above a `Stack` host against the `getDebugInitGameResult()` fixture with a synthesized `VictoryState(type: VictoryType.military, winnerPlayerId: <first seeded player id>, turnNumber: 7)` and a fresh `AppEventBus.create()`, then `WidgetTester.takeException()` returns `null`, the upper-cased `MILITARY VICTORY` title renders, the localized winner sentence containing `wins on turn 7` renders, both `Return to main menu` and `View final state` `CtNinePatchButton` labels render, exactly two `CtNinePatchButton` instances mount, and exactly one `CtBrassDivider` mounts (the full-screen `EditorialMonoclePalette.dialogScrim` wash + centered `VictoryPanel` narrow column wireframe — laurel row, title, `CtBrassDivider`, body sentence, stacked vertical `Column` of two `CtNinePatchButton`s per [victory-overlay.md](victory-overlay.md) § Narrow viewport — must wrap within the panel's narrow content column without horizontal overflow at the minimum viewport).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DiplomacyScreen** (GAME30001) is rendered against the `getDebugInitGameResult()` fixture under the default (non-observe) `shellPlayerContextProvider` path, then `WidgetTester.takeException()` returns `null`, exactly one `CtTopBar` keyed `DiplomacyScreen.topBarKey` is mounted carrying the literal title `Diplomacy` and back label `Map` with a descendant `CtBackButton` chevron, and the `Great Powers` faction-section heading from [diplomacy-panel.md](diplomacy-panel.md) renders inside the `CtGameFeatureScreenShell` body (the dark `CtTopBar` chrome — back chevron + `Map` label + 18 × 18 px diplomacy icon + `Diplomacy` title — plus the `DiplomacyPanel` body must lay out within the 320 dp column without horizontal overflow per [diplomacy-panel.md](diplomacy-panel.md) § Top bar and § Layout / wireframe).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DiplomacyScreen** (GAME30001) is rendered against the `getDebugInitGameResult()` fixture under a `shellPlayerContextProvider` override whose `showPlayerChrome` is `false` (global observe), then `WidgetTester.takeException()` returns `null`, the dark `CtTopBar` keyed `DiplomacyScreen.topBarKey` still renders, and the `ObserveModeNotDefinedPanel` sentinel with literal title `Diplomacy` renders in place of the `DiplomacyPanel` body (per [diplomacy-panel.md](diplomacy-panel.md) § States and variants the observe override only swaps the body — the chrome must keep its 320 dp overflow contract).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DiplomacyDetailScreen** (GAME30002) is rendered against a minimal `Game` fixture (one human GP + one rival GP with a `RelationState.atWar` relation, one `DiplomaticEventType.declareWar` history event, and one `DossierEvidenceEntry` for the pair) with `kind = FactionKind.greatPower`, then `WidgetTester.takeException()` returns `null`, exactly one `CtTopBar` is mounted carrying the target faction `displayName` as its title with a descendant `CtBackButton` chevron, and the `CURRENT RELATION`, `DIPLOMATIC HISTORY`, and `DOSSIER` `_DetailCard` titles plus the seeded history sentence (`declared war`) and dossier description (`evidence-1`) all render within the `Center` / `ConstrainedBox(maxWidth: 600)` / `ListView` body without horizontal overflow per [diplomacy-detail-screen.md](diplomacy-detail-screen.md) § Layout / wireframe (the 600 dp content cap collapses to the 320 dp viewport, so the three cards must lay out inside a `~292 dp` content column after the 14 dp horizontal `ListView` padding).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DiplomacyDetailScreen** (GAME30002) is rendered against the same minimal fixture with `kind = FactionKind.minor`, an empty `diplomaticHistoryEvents` list, and an empty `dossierEvidenceEntries` list, then `WidgetTester.takeException()` returns `null`, exactly one `CtTopBar` is mounted, the `CURRENT RELATION` and `DIPLOMATIC HISTORY` `_DetailCard` titles render, and the `DOSSIER` `_DetailCard` title is absent from the widget tree per [diplomacy-detail-screen.md](diplomacy-detail-screen.md) § States and variants (non-GP renders no dossier section — negative AC for the GP-only branch at the minimum viewport).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **QuickBattleScreen** (CMPT20001) is rendered with `interactive: false` against a minimal two-faction `QuickBattleInput` (one CENTER + FRONT group per side, `maxRounds = 3`) and a no-op `onComplete`, then after the first `setState`-driven pump `WidgetTester.takeException()` returns `null`, the localized `quickBattle_battleResult(...)` winner sentence and both `quickBattle_casualties` rows render, and the trailing `Continue` `CtNinePatchButton` label renders within the ~288 dp `CtDialogShell` content column (the result view from [quick-battle-screen.md](quick-battle-screen.md) § Result phase auto-runs through `resolveQuickBattle` on `initState` and must lay out the winner title, optional captured banner, casualty rows, and trailing right-aligned Continue action without horizontal overflow).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **QuickBattleScreen** (CMPT20001) is rendered with `interactive: true` against the same minimal `QuickBattleInput` (one CENTER + FRONT group per side, `maxRounds = 3`) and a no-op `onComplete`, then `WidgetTester.takeException()` returns `null`, the round-counter title (`Quick Battle — Round 1 / 3`) and the `Command Points: 3` action-selector header both render, every `QuickBattleActionSelector` `Wrap` child (`Volley Fire`, `Defend`, `Maneuver`, `Fall Back`, `Assault`) is mounted, and the `Resolve (Auto)` button is absent (the round phase from [quick-battle-screen.md](quick-battle-screen.md) § Round phase must lay out the round-counter `Text`, the `QuickBattleDeploymentView` `CtPanel` + `Wrap` of lane/line rows, and the action `Wrap` within the ~288 dp `CtDialogShell` content column without horizontal overflow — the `CtNinePatchButton` action chips MUST flow onto extra runs rather than overflowing the row).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **ProductionCommodityBreakdownDialog** (PROD20001) is rendered against the `getDebugInitGameResult()` fixture with the seeded human player id and an empty `Orders()` (opened through `showDialog` with `barrierColor: EditorialMonoclePalette.dialogScrim`), then `WidgetTester.takeException()` returns `null`, the localized `Commodity breakdown` title from `production_breakdown_title` renders, the trailing `Close` `CtNinePatchButton` from `common_close` renders, exactly one `DataTable` mounts with column count equal to `1 + EconomyPreviewStockpilePhase.values.length + 1` (commodity + per-phase + total) inside a horizontal-axis `SingleChildScrollView` wrapped by a visible `Scrollbar`, and at least one of the upper-cased section header labels (`FOOD` / `RAW MATERIALS` / `MANUFACTURED`) renders inside the table body (the [CtDialogShell](pixel-art-ui-catalog.md) chrome — `maxWidth: 720` clamped by outer `Dialog.insetPadding: 16` × 2 — must wrap the title, the wide `DataTable` reachable via the horizontal scroll affordance from the dialog's Wide-table state, and the trailing right-aligned `Close` action within the ~288 dp content width without horizontal overflow per [production-commodity-breakdown-dialog.md](production-commodity-breakdown-dialog.md) § Layout / wireframe and § States and variants).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DebugLogViewerScreen** (SYS10001) is rendered against a freshly reset `SessionLogBuffer` (no buffered entries), then `WidgetTester.takeException()` returns `null`, the literal `Debug log` `AppBar` title from [debug-log-viewer.md](../program/debug-log-viewer.md) § 4 renders, the trailing `Icons.close` action button renders, the default-selected `app` package `FilterChip` mounts inside the horizontal-scroll filter row, and each of the default-selected `info` / `warning` / `error` level `FilterChip`s mounts inside the same row per [debug-log-viewer.md](../program/debug-log-viewer.md) § 3 Filters defaults (the Material `Scaffold` chrome — `AppBar` above a horizontal `SingleChildScrollView`-hosted filter row + 1 dp `Divider` + expanding `ListView.builder` body — must lay out within the 320 dp column without horizontal overflow because the side menu's `Debug log` entry is the primary reach for the viewer on mobile and narrow viewports per [debug-log-viewer.md](../program/debug-log-viewer.md) § 4 Entry points).
- Given the viewport width is exactly `kMinViewportWidth` (320 dp) and the height is at least 640 dp, when **DebugLogViewerScreen** (SYS10001) is rendered against a `SessionLogBuffer` populated with two `Level.warning` lines tagged with the `app` prefix, then `WidgetTester.takeException()` returns `null`, the `Debug log` `AppBar` title renders, and both seeded warning messages render in the `ListView` body (each line wrapped by the per-entry tinted `Container` chrome from [debug-log-viewer.md](../program/debug-log-viewer.md) § 5a Visual chrome). The default-selected `app` package + `warning` level filters from [debug-log-viewer.md](../program/debug-log-viewer.md) § 3 surface both seeded lines so the per-row `BoxDecoration`-tinted layout exercises within the 320 dp column without horizontal overflow.

#### Pinning tests

The above ACs are pinned by:

- `app/test/mobile_320dp_min_viewport_test.dart` — Main Menu + Game Setup + NewGameLeaderSelectionDialog (DLG10001) shell dialog (Refs #2870 S7 / S8 / S10). The NewGameLeaderSelectionDialog group pins the same 500 dp `CtGameSetup` narrow-stacking rule applied to the shell-mounted dialog version: at 320 dp every slot mounts the keyed stacked column body (`newGameLeaderDialogSlotPickersColumn`) and the wide row body (`newGameLeaderDialogSlotPickersRow`) is absent; a 1024×768 negative control flips the contract so a regression in the breakpoint comparator direction would surface.
- `app/test/panels_320dp_min_viewport_test.dart` — ProductionPanel, DiplomacyPanel, TechnologyPanel, and ProvinceSeaZoneDetailOverlay (Refs #2870 S10). The ProvinceSeaZoneDetailOverlay pin hosts the overlay inside a bottom-anchored `SizedBox(height: viewport.height * 0.33)` to mirror `GameMapNarrowDetailOverlaySlot`'s 33 vh slot so the narrow `CtTabStrip` body lays out under the same constraints it would in the running game.
- `app/test/unit_panels_320dp_min_viewport_test.dart` — CivilianUnitsPanel, MilitaryUnitsPanel, and NavalUnitsPanel (Refs #2870 S10). The Naval pin pre-warms the brass nine-patch asset into the Flame image cache (mirroring the helper used by the DiplomacyPanel group in `panels_320dp_min_viewport_test.dart`) so the `FleetExpansionTile` `CtNinePatchButton` chrome lays out at its true declared height instead of falling back to a `SizedBox.shrink()` silhouette.
- `app/test/dialogs_320dp_min_viewport_test.dart` — GameParametersDialog, ExitConfirmDialog, TurnNewsDialog, GameMapOptionsDialog, TurnResolutionProcessingDialog, CombatModeChoiceDialog, NextTurnConfirmationDialog, QuickBattleResultDialog, SplitArmyDialog, SplitFleetDialog, TransferToHomeFleetDialog, PauseMenuPanel, and CallToArmsDialogueOverlay (Refs #2870 S8/S10). The CallToArmsDialogueOverlay group pins both the single-pending-call and two-pending-calls paths so the per-row `Column(Text + Wrap(Join + Refuse))` stack from [call-to-arms-dialogue-overlay.md](call-to-arms-dialogue-overlay.md) § Layout / wireframe lays out within the ~288 dp [CtDialogShell](pixel-art-ui-catalog.md) content column without horizontal overflow at the minimum viewport. The three `CtTransferList`-hosted dialogs (Split Army / Split Fleet / Transfer to Home Fleet) also exercise the [CtTransferList](pixel-art-ui-catalog.md) narrow-stack contract: at `constraints.maxWidth < kCtTransferListSideBySideMinWidth` (`360` dp) the two side panels stack vertically and the trailing Cancel / Confirm action row flows through a `Wrap` so the Cinzel engraved-label `CtNinePatchButton` pair never overflows the ~288 dp `CtDialogShell` content column horizontally.
- `app/test/train_dialogs_320dp_min_viewport_test.dart` — TrainCiviliansDialog and TrainMilitaryDialog (Refs #2870 S8/S10). Both dialogs render their chrome via [CtDialogShell](pixel-art-ui-catalog.md) and the shared `TrainDialogHeader` / `TrainDialogSectionDivider` / `TrainDialogResourceBar` chrome from `train_dialog_chrome.dart`; the military pin further exercises the larger `Wrap`-based resource bar (six commodity chips plus treasury + peasants chips) so the additional runs wrap inside the ~288 dp content column rather than overflowing horizontally.
- `app/test/move_dialogs_320dp_min_viewport_test.dart` — MoveArmyDialog and MoveFleetDialog (Refs #2870 S8/S10).
- `app/test/grant_or_subsidy_dialog_320dp_min_viewport_test.dart` — GrantOrSubsidyDialog (DIPL20001) (Refs #2870 S8/S10). Three positive cases plus a wide regression sentinel exercise the two modes (`Grant aid` / `Set subsidy`) plus the below-minimum branch where `_maxAffordable() < step` and the optional `_BelowMinimumWarning` row mounts above the trailing Cancel + Submit `Row`. The pin proves the bespoke `−` / `+` stepper from [grant-or-subsidy-dialog.md](grant-or-subsidy-dialog.md) § Layout / wireframe (keyed `diplo_amount_minus` and `diplo_amount_plus`), the keyed amount label `grantOrSubsidyDialogAmount`, the keyed `_ThinDivider` `grantOrSubsidyDialogThinDivider`, and the trailing right-aligned action row all fit inside the ~288 dp [CtDialogShell](pixel-art-ui-catalog.md) content column without horizontal overflow. Both dialogs render their chrome via [CtDialogShell](pixel-art-ui-catalog.md) plus the shared `CtSectionLabel` + outlined-radio-row pattern from [move-army-dialog.md](move-army-dialog.md) § Layout and [move-fleet-dialog.md](move-fleet-dialog.md) § Layout; the army pin further exercises the per-invasion-row `declare war on <faction>` trigger label stacked below the destination name (the row's inner `Row` puts the radio dot on the left and an `Expanded(Column(...))` carrying the title + trigger label on the right so the dialog body never depends on the trigger text fitting on the same physical line at narrow widths), and both pins exercise the trailing `Wrap`-based Cancel / Confirm action row so the two `CtNinePatchButton`s flow onto a second run rather than overflowing the ~288 dp `CtDialogShell` content column horizontally.
- `app/test/trade_screen_320dp_min_viewport_test.dart` — TradeScreen (default scaffold body + global-observe `ObserveModeNotDefinedPanel` body) (Refs #2870 S10, #2993). The pin overrides `currentGameProvider` with the `getDebugInitGameResult()` fixture and (for the observe variant) `shellPlayerContextProvider` with a `showPlayerChrome: false` context so both `TradeScreen` body branches from [trade-screen.md](trade-screen.md) § States and variants are exercised against the same 320 dp `CtTopBar` chrome.
- `app/test/diplomacy_detail_screen_320dp_min_viewport_test.dart` — DiplomacyDetailScreen (GP variant with history + dossier, minor-nation variant without dossier, wide regression sentinel) (Refs #2870 S10). The pin uses an inline minimal `Game` fixture (one human GP + one rival GP) and a seeded `DiplomaticEvent` + `DossierEvidenceEntry` so the populated `_LeftBorderTile` body and dossier evidence sentence both lay out at the 320 dp viewport. The non-GP variant exercises the `kind != FactionKind.greatPower` branch from [diplomacy-detail-screen.md](diplomacy-detail-screen.md) § States and variants so the absent `DOSSIER` card serves as the negative AC at the minimum viewport.
- `app/test/diplomacy_screen_320dp_min_viewport_test.dart` — DiplomacyScreen (GAME30001) default `DiplomacyPanel` body + global-observe `ObserveModeNotDefinedPanel` body + wide regression sentinel (Refs #2870 S10).
- `app/test/production_screen_320dp_min_viewport_test.dart` — ProductionScreen (`UiScreenIds.productionScreen`) default `ProductionPanel` narrow body (`Available` + `Allocation` stacked) + global-observe `ObserveModeNotDefinedPanel` body + wide regression sentinel (Refs #2870 S10). The pin overrides `currentGameProvider` with the `getDebugInitGameResult()` fixture and `panelTopologyOverride: const MapTopology()` so the shell renders without touching Hive / `GameService`, and (for the observe variant) `shellPlayerContextProvider` with a `showPlayerChrome: false` context so both `ProductionScreen` body branches from [production-panel.md](production-panel.md) § States and variants are exercised against the same dark `CtTopBar` chrome (back chevron + `Map` label + production icon + `Production` title) at the minimum viewport. The pin overrides `currentGameProvider` with the `getDebugInitGameResult()` fixture and (for the observe variant) `shellPlayerContextProvider` with a `showPlayerChrome: false` context so both `DiplomacyScreen` body branches from [diplomacy-panel.md](diplomacy-panel.md) § States and variants are exercised against the same dark `CtTopBar` chrome (back chevron + `Map` label + diplomacy icon + `Diplomacy` title) at the minimum viewport.
- `app/test/quick_battle_screen_320dp_min_viewport_test.dart` — QuickBattleScreen (CMPT20001) (Refs #2870 S10). Two positive cases plus a wide regression sentinel: the non-interactive `interactive: false` mount auto-runs through `resolveQuickBattle` in `initState` and pins the result view (`Battle Result: …` winner sentence, both casualty rows, trailing `Continue`); the interactive `interactive: true` mount pins the round phase (round-counter `Quick Battle — Round 1 / 3`, `Command Points: 3` header, all five `Wrap` action `CtNinePatchButton` labels, and the absence of the `Resolve (Auto)` fallback). Both positive cases assert no `RenderFlex` overflow exception escapes at the 320 dp `CtDialogShell` content column (`maxWidth: 400` collapses to ~288 dp once the outer `Dialog.insetPadding: 16` is applied); the wide regression sentinel pumps the non-interactive variant at 1024 × 768 dp so the contract upstream of `QuickBattleScreen` itself stays exercised.
- `app/test/production_commodity_breakdown_dialog_320dp_min_viewport_test.dart` — ProductionCommodityBreakdownDialog (PROD20001) (Refs #2870 S8/S10). Three positive cases plus a wide regression sentinel: the first pins the chrome (title + trailing `Close` `CtNinePatchButton`) at 320 dp; the second pins the wide-table body shape (`1 + EconomyPreviewStockpilePhase.values.length + 1` columns inside a horizontal `Scrollbar` + `SingleChildScrollView`) so the SPEC's Wide-table state escape hatch from [production-commodity-breakdown-dialog.md](production-commodity-breakdown-dialog.md) § States and variants stays exercised at the minimum viewport; the third pins at least one of the upper-cased section header labels (`FOOD` / `RAW MATERIALS` / `MANUFACTURED`) so the body sections from the SPEC layout / wireframe actually mount under the narrow column. The wide regression sentinel pumps the same fixture at 1024 × 768 dp so a regression in the host overflow contract upstream of `ProductionCommodityBreakdownDialog` itself would still surface. The TurnNewsDialog group pins both the empty-state path (title + muted empty-copy + trailing `Close`) and a populated digest path (three `TurnNewsLine` entries inside the `ConstrainedBox(maxHeight: 320)` `ListView` body) so the universal turn-start modal contract from [turn-news-dialog.md](turn-news-dialog.md) is exercised at the minimum viewport. The GameMapOptionsDialog group pins all three `CtToggleSwitch` rows + `Close` action under the shared [CtDialogShell](pixel-art-ui-catalog.md) chrome so the in-game map display options dialog from [empire-overview.md](empire-overview.md) § Map display options stays within the ~288 dp content column. The TurnResolutionProcessingDialog group pins the title + `CtLoadingIndicator` row body so the worker-isolate "processing turn" modal from [turn-resolution.md](../program/turn-resolution.md) stays within the ~288 dp content column without wrapping the spinner / phase text off the visible canvas. The CombatModeChoiceDialog group pins both the regular-province path (`Auto-Resolve` + `Quick Battle`) and the capital-siege path (Quick Battle only) so the combat-mode picker from [combat-mode-choice-dialog.md](combat-mode-choice-dialog.md) stays within the ~288 dp content column. The NextTurnConfirmationDialog group pins the DLG60001 title + body + `No` / `Yes` action row from [next-turn-confirmation.md](next-turn-confirmation.md). The QuickBattleResultDialog group pins the attacker-wins + `provinceFlips` path (winner text, captured banner, casualty rows, trailing `OK`) from [quick-battle-result-dialog.md](quick-battle-result-dialog.md).
- `app/test/game_screen_320dp_min_viewport_test.dart` — live `GameScreen` composite at `kMinViewportWidth` (320 dp): narrow `GameTopBar` (hamburger + Next-turn only), left-rail empire buttons, and players-bar hidden (Req 6) (Refs #2870 S10).
- `app/test/victory_overlay_320dp_min_viewport_test.dart` — VictoryOverlay (OVL20001) (Refs #2870 S8/S10). Two positive cases plus a wide regression sentinel exercise the canonical happy path: the first mounts `VictoryOverlay` inside a `Stack` (mirroring the real `GameScreen` host plumbing) at 320 × 640 dp; the second mounts `VictoryPanel` directly (no scrim) at the same viewport so the panel-only overflow contract pins independently of the outer `Positioned.fill` scrim `Container`. Both positive cases assert no `RenderFlex` overflow exception escapes, the upper-cased `MILITARY VICTORY` title + winner sentence + both `CtNinePatchButton` action labels render, exactly two `CtNinePatchButton` instances mount, and exactly one `CtBrassDivider` mounts (per [victory-overlay.md](victory-overlay.md) § Layout / wireframe and § Narrow viewport). The wide regression sentinel pumps the overlay at 1024 × 768 dp so a regression in the host overflow contract upstream of `VictoryOverlay` itself would still surface and the wide-layout `Wrap` action row + `headlineSmall` title slot from § Narrow viewport stay exercised in the same pin file.
- `app/test/game_top_bar_test.dart` — unit pins for narrow vs wide `GameTopBar` row chrome at `kMinViewportWidth` and `kNarrowBreakpoint` (Refs #2870 S10).
- `app/test/widgetbook_game_top_bar_mobile_viewport_test.dart` — Widgetbook `Game Top Bar` → `Mobile viewport — narrow bar (< 600 dp)` story inside the 360 × 640 dp frame (Refs #2870 R22 / S9).
- `app/test/debug_log_viewer_320dp_min_viewport_test.dart` — DebugLogViewerScreen (SYS10001) empty-buffer + populated-buffer paths (Refs #2870 S10). The empty-buffer group pins the Material `Scaffold` chrome (`AppBar` `Debug log` title + trailing `Icons.close`) plus the default-selected `app` package `FilterChip` and the default-selected `info` / `warning` / `error` level `FilterChip`s mounted inside the horizontal-scroll filter row per [debug-log-viewer.md](../program/debug-log-viewer.md) § 3 Filters defaults. The populated-buffer group seeds two `Level.warning` entries tagged with the `app` prefix so the per-entry tinted `Container` chrome from [debug-log-viewer.md](../program/debug-log-viewer.md) § 5a Visual chrome exercises inside the 320 dp `ListView` body without overflowing horizontally. A wide regression sentinel at 1024 × 768 dp pumps the same fixture so the host overflow contract upstream of `DebugLogViewerScreen` stays exercised, mirroring the dual-viewport pattern used by every other `*_320dp_min_viewport_test.dart` file.

---

## Summary table

| Requirement        | Main Menu          | Game Setup        | Diplomacy panel       | In-game shell        |
|--------------------|--------------------|-------------------|-----------------------|----------------------|
| Max width 400 dp   | ✓                  | ✓                 | —                     | —                    |
| Scroll on overflow | ✓                  | ✓                 | —                     | —                    |
| Safe area          | ✓                  | ✓                 | ✓                     | ✓                    |
| 44 dp touch        | ✓ (48 dp)          | ✓ (48 dp)         | ✓                     | ✓                    |
| Narrow breakpoint  | ✓ tight ≤ 430 dp   | ✓ stacked < 500 dp | ✓ row stacked ≤ 500 dp | ✓ side menu < 600 dp |

---

## References

- Main menu: [main-menu.md](main-menu.md) § Responsive rules — layout, scroll, ≤ 430 dp tight-layout rule.
- Game Setup: [game-setup.md](game-setup.md) — layout, narrow breakpoint, scroll.
- Diplomacy panel: [diplomacy-panel.md](diplomacy-panel.md) § Responsive layout — faction-row wrapping at ≤ 500 dp.
- In-game shell (narrow): [in-game-shell-narrow.md](in-game-shell-narrow.md) — side menu, top bar, province/sea detail overlay; [empire-buttons.md](empire-buttons.md) — empire buttons; [empire-overview.md](empire-overview.md) — minimap, corner controls.
- Per-screen mockups (visual source of truth for `@media` rules normalised above): `SPEC/ui/mockups/SHEL10002-main-menu.html`, `SPEC/ui/mockups/SHEL20001-game-setup.html`, `SPEC/ui/mockups/GAME30001-diplomacy-panel.html`, `SPEC/ui/mockups/GAME10001-game-screen.html`.
- UXD 03: acceptance criteria and touch targets.
