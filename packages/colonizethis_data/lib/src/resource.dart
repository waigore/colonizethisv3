/// Map resource (raw commodity on a tile). SPEC/game/tile-map-and-generation, GDD 04b.
/// Minimal set for Phase 1; extend per GDD 04 economy list later.
enum Resource {
  grain,
  timber,
  iron,
}
