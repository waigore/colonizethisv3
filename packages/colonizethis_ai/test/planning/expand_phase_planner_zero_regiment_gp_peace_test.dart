// Thin contract for EXPAND-peace pin suite (Refs #3977 Phase 6).
// Case bodies live in `expand_phase_planner_zero_regiment_gp_peace_cases.dart`; this file only registers them so
// the `*_test.dart` surface stays under review pressure.

import 'expand_phase_planner_zero_regiment_gp_peace_cases.dart';

void main() {
  registerExpandPhasePlannerZeroRegimentGpPeaceCases();
}
