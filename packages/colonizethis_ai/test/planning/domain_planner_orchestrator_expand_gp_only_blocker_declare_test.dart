// Pins the EXPAND-phase forced GP-only invadable frontier blocker
// `declareWar` contract at the `runDomainPlanners` integration boundary
// (Refs #2509).
// Case bodies: `domain_planner_orchestrator_expand_gp_only_blocker_declare_cases.dart`.

import 'domain_planner_orchestrator_expand_gp_only_blocker_declare_cases.dart';

void main() {
  registerDomainPlannerOrchestratorExpandGpOnlyBlockerDeclareCases();
}
