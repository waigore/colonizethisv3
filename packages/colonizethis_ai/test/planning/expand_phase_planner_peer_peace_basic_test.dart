// Thin contract for EXPAND-peace pin suite (Refs #3977 Phase 6).
// Case bodies live in `expand_phase_planner_peer_peace_basic_cases.dart`; this file only registers them so
// the `*_test.dart` surface stays under review pressure.

import 'expand_phase_planner_peer_peace_basic_cases.dart';

void main() {
  registerExpandPhasePlannerPeerPeaceBasicCases();
}
