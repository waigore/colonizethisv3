accumulates on existing stockpile
accumulates onto an existing entry
adds extracted quantities to stockpile
applies per-player extraction to player stockpiles
bronze limiting input min(8,2)=2 → floor(2/4)=0
can remove merchant ships when interception triggers and RNG hits
canAffordWorkMaterialCost is false when any commodity is short
capital grain bonus adds equal effective and full
capital tile grain bonus is unconditional on connectivity
cargo cap limits delivered overseas
collects the seaboard port tile keys as a set
combined limits aggregate to a single effective (full) pair
converts multiple riches and sums treasury delta
converts spices to treasury at base price
cotton fabric requires cotton_weaving tech
counts improvable tiles including partially improved below cap
creates a new entry starting from zero
custom priorityOrder is respected
decrements running treasury tally after a priced fill
deductWorkMaterialCost reduces quantities
deduplicates seaboards that map to the same tile key
delegates canAffordBuildOrder to build_cost canAffordBuild
disconnected improved tile contributes 0 (N)
does not boost gathering tech
does not filter zero or negative deltas (caller guards)
empty extracted returns same stockpile
empty extractedByPlayerId returns game unchanged
empty input yields empty index
empty precondition list passes (returns null)
empty stockpile yields zero delta
empty when no improvable tiles
empty world produces an empty index
enemy blockade fleet sets the blockade flag
enemy with privateering reduces cargo more (strictly less kept)
enemy without privateering reduces cargo by the baseline
every Resource.name resolves to a catalog commodity id
every riches commodity is not tradeable
excludes unprospected mineral tiles
falls back to game.worldState when index misses
fed peasants contribute labour without luxury
fed-but-unluxuried trained worker contributes no labour
first attribution per tileKey wins on duplicates
FirstRightProfit equality works on identical (rate, treasury)
FirstRightProfit.zero has zero rate and zero treasury
fleetsById index matches default linear home-fleet lookup
floor(7/4)*1 = 1 lumber at level 2
GP town-connected timber yields lumber bonus in bonusByFactionId
handles equal ids (degenerate self-pair) deterministically
honours canonical priority: earlier failure wins over later
ignores negative values in extracted
indexes provinces across both regions by prefixed id
is deterministic for a fixed seed
is monotonically non-decreasing across 0..100
kEmbassyOverseasProfitKickbackMultiplier is 0.10 (#3753 R8.3)
kFirstRightMaxProfitRate is 1.0 (#3753 R8.2 — full share, no cap)
kFirstRightRelationScoreMax is 100
level 2 → 1, level 4 → 2, others → 0
level 3 grants zero bonus
level 4 with 4 timber → 2 lumber (replacement multiplier)
lumber from timber is eligible
mapped commodity ids are unique per resource (no collisions)
maps commodity id via resource.name consistently
match SPEC values
matches resolveRichesToTreasury treasuryDelta
matches the inline fold idiom it replaces
military upkeep consumes food before workers
minor town-connected timber accumulates delivered raw extraction
negative: out-of-bounds improvement keys do not throw during snapshot build
no enemy patrol/blockade fleets yields zero intercept score
no food leaves workers on strike (zero labour)
no ports produces an empty set
non-riches commodity is tradeable
non-riches in stockpile are unchanged
player merchant ships counted; escorts feed escort strength
players with no extraction keep existing stockpile
preserves first-seen insertion order across keys
previewTownManufacturingBonusByProvince matches live bonusByProvinceId when connectivity resolves
previewTownManufacturingBonusByProvince returns empty without tile maps
priority order: food before raw materials
privateering enemy scales intercept score above the baseline
privateering trade-raid result is deterministic for a fixed seed
recipe excluded when any input is manufactured
reduces cargo when enemy has patrol fleet
resolves province from provincesByFullId index
returns 0 for an empty iterable
returns 0 for no maps and for empty maps
returns 0 when home fleet has only warship types (cargoHold 0)
returns 0 when no home fleet exists
returns 0 when no overseas totals or zero holds
returns as-is when overseasDelivered is empty
returns canonical key regardless of argument order
returns empty when overseas is empty
returns full delivered when at war but no interceptor fleet
returns full delivered when no enemies at war
returns full home fleet when tile maps are empty
returns null for invalid or out-of-range keys
returns null for silver riches regardless of stored prices
returns null when every check passes
returns null when province row is missing
returns resource context for valid tile key
returns the first failing reason in list order
returns zero when stockpile has no riches
richesCashMultiplier scales treasury delta
road-rule path with no binding constraints has no brackets
short-circuits: no later check runs once one fails
skips decrement on missing-price free-fill path
steel from iron and coal is eligible (all raw inputs)
sums cargoHold from home-fleet ship types
sums every value across nested maps
sums positive and negative values
sums tier multipliers (1/4/6/8) over idle counts
sums units committed by allocateOverseasToStockpile
the mapping is total over all Resource values
tile extraction contribution is null for disconnected tile
toString is informative for trace logs
town development cap yields effective < full
town-rule-only + non-port: townDevelopmentLevel does NOT cap yield
town-rule-only + port: townDevelopmentLevel DOES cap yield
townManufacturingBonusToAutoOffers emits priority-1 offers for minors
trained worker needs both food and luxury to count
transport limit yields effective < full with brackets
treasury equals filledQuantity * pricePerUnit * profitRate
zero idle counts contribute no labour
zero quantities do not change stockpile
zero riches yields zero delta and unchanged stockpile
