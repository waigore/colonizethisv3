name: init_game
description: Game creation tool (map gen, province/capital assignment, init). SPEC/program/init-game-tool.md.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  colonizethis_data:
  colonizethis_logic:
  colonizethis_models:
  colonizethis_save:

dev_dependencies:
  test: ^1.24.0

executables:
  init_game: bin/init_game.dart
