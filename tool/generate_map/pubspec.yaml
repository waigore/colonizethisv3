name: generate_map
description: End-to-end map generation (topology + tile map + PNG). SPEC/program/map-data.md.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.11.5 <4.0.0'

dependencies:
  colonizethis_data:
  colonizethis_logger:
  colonizethis_map:
  colonizethis_models:

dev_dependencies:
  colonizethis_exception_lint:
    path: ../../packages/colonizethis_exception_lint
  colonizethis_test:
  custom_lint: ^0.8.1
  path: ^1.9.0
  test: ^1.24.0

executables:
  generate_map: bin/generate_map.dart

